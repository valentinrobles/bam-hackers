declare const protobufPackage = "lf.a2a.v1";
/** Older protoc compilers don't understand edition yet. */
/** Defines the possible lifecycle states of a `Task`. */
declare enum TaskState {
    /** TASK_STATE_UNSPECIFIED - The task is in an unknown or indeterminate state. */
    TASK_STATE_UNSPECIFIED = 0,
    /** TASK_STATE_SUBMITTED - Indicates that a task has been successfully submitted and acknowledged. */
    TASK_STATE_SUBMITTED = 1,
    /** TASK_STATE_WORKING - Indicates that a task is actively being processed by the agent. */
    TASK_STATE_WORKING = 2,
    /** TASK_STATE_COMPLETED - Indicates that a task has finished successfully. This is a terminal state. */
    TASK_STATE_COMPLETED = 3,
    /** TASK_STATE_FAILED - Indicates that a task has finished with an error. This is a terminal state. */
    TASK_STATE_FAILED = 4,
    /** TASK_STATE_CANCELED - Indicates that a task was canceled before completion. This is a terminal state. */
    TASK_STATE_CANCELED = 5,
    /** TASK_STATE_INPUT_REQUIRED - Indicates that the agent requires additional user input to proceed. This is an interrupted state. */
    TASK_STATE_INPUT_REQUIRED = 6,
    /**
     * TASK_STATE_REJECTED - Indicates that the agent has decided to not perform the task.
     * This may be done during initial task creation or later once an agent
     * has determined it can't or won't proceed. This is a terminal state.
     */
    TASK_STATE_REJECTED = 7,
    /** TASK_STATE_AUTH_REQUIRED - Indicates that authentication is required to proceed. This is an interrupted state. */
    TASK_STATE_AUTH_REQUIRED = 8,
    UNRECOGNIZED = -1
}
declare function taskStateFromJSON(object: any): TaskState;
declare function taskStateToJSON(object: TaskState): string;
/** Defines the sender of a message in A2A protocol communication. */
declare enum Role {
    /** ROLE_UNSPECIFIED - The role is unspecified. */
    ROLE_UNSPECIFIED = 0,
    /** ROLE_USER - The message is from the client to the server. */
    ROLE_USER = 1,
    /** ROLE_AGENT - The message is from the server to the client. */
    ROLE_AGENT = 2,
    UNRECOGNIZED = -1
}
declare function roleFromJSON(object: any): Role;
declare function roleToJSON(object: Role): string;
/** Configuration of a send message request. */
interface SendMessageConfiguration {
    /**
     * A list of media types the client is prepared to accept for response parts.
     * Agents SHOULD use this to tailor their output.
     */
    acceptedOutputModes: string[];
    /**
     * Configuration for the agent to send push notifications for task updates.
     * Task id should be empty when sending this configuration in a `SendMessage` request.
     */
    taskPushNotificationConfig: TaskPushNotificationConfig | undefined;
    /**
     * The maximum number of most recent messages from the task's history to retrieve in
     * the response. An unset value means the client does not impose any limit. A
     * value of zero is a request to not include any messages. The server MUST NOT
     * return more messages than the provided value, but MAY apply a lower limit.
     */
    historyLength?: number | undefined;
    /**
     * If `true`, the operation returns immediately after creating the task,
     * even if processing is still in progress.
     * If `false` (default), the operation MUST wait until the task reaches a
     * terminal (`COMPLETED`, `FAILED`, `CANCELED`, `REJECTED`) or interrupted
     * (`INPUT_REQUIRED`, `AUTH_REQUIRED`) state before returning.
     */
    returnImmediately: boolean;
}
declare const SendMessageConfiguration: MessageFns<SendMessageConfiguration>;
/**
 * `Task` is the core unit of action for A2A. It has a current status
 * and when results are created for the task they are stored in the
 * artifact. If there are multiple turns for a task, these are stored in
 * history.
 */
interface Task {
    /**
     * Unique identifier (e.g. UUID) for the task, generated by the server for a
     * new task.
     */
    id: string;
    /**
     * Unique identifier (e.g. UUID) for the contextual collection of interactions
     * (tasks and messages).
     */
    contextId: string;
    /** The current status of a `Task`, including `state` and a `message`. */
    status: TaskStatus | undefined;
    /** A set of output artifacts for a `Task`. */
    artifacts: Artifact[];
    /**
     * protolint:disable REPEATED_FIELD_NAMES_PLURALIZED
     * The history of interactions from a `Task`.
     */
    history: Message[];
    /**
     * protolint:enable REPEATED_FIELD_NAMES_PLURALIZED
     * A key/value object to store custom metadata about a task.
     */
    metadata: {
        [key: string]: any;
    } | undefined;
}
declare const Task: MessageFns<Task>;
/** A container for the status of a task */
interface TaskStatus {
    /** The current state of this task. */
    state: TaskState;
    /** A message associated with the status. */
    message: Message | undefined;
    /**
     * ISO 8601 Timestamp when the status was recorded.
     * Example: "2023-10-27T10:00:00Z"
     */
    timestamp: string | undefined;
}
declare const TaskStatus: MessageFns<TaskStatus>;
/**
 * `Part` represents a container for a section of communication content.
 * Parts can be purely textual, some sort of file (image, video, etc) or
 * a structured data blob (i.e. JSON).
 */
interface Part {
    content?: //
    /** The string content of the `text` part. */
    {
        $case: "text";
        value: string;
    } | //
    /** The `raw` byte content of a file. In JSON serialization, this is encoded as a base64 string. */
    {
        $case: "raw";
        value: Buffer;
    } | //
    /** A `url` pointing to the file's content. */
    {
        $case: "url";
        value: string;
    } | //
    /** Arbitrary structured `data` as a JSON value (object, array, string, number, boolean, or null). */
    {
        $case: "data";
        value: any | undefined;
    } | undefined;
    /** Optional. metadata associated with this part. */
    metadata: {
        [key: string]: any;
    } | undefined;
    /** An optional `filename` for the file (e.g., "document.pdf"). */
    filename: string;
    /**
     * The `media_type` (MIME type) of the part content (e.g., "text/plain", "application/json", "image/png").
     * This field is available for all part types.
     */
    mediaType: string;
}
declare const Part: MessageFns<Part>;
/**
 * `Message` is one unit of communication between client and server. It can be
 * associated with a context and/or a task. For server messages, `context_id` must
 * be provided, and `task_id` only if a task was created. For client messages, both
 * fields are optional, with the caveat that if both are provided, they have to
 * match (the `context_id` has to be the one that is set on the task). If only
 * `task_id` is provided, the server will infer `context_id` from it.
 */
interface Message {
    /** The unique identifier (e.g. UUID) of the message. This is created by the message creator. */
    messageId: string;
    /** Optional. The context id of the message. If set, the message will be associated with the given context. */
    contextId: string;
    /** Optional. The task id of the message. If set, the message will be associated with the given task. */
    taskId: string;
    /** Identifies the sender of the message. */
    role: Role;
    /** Parts is the container of the message content. */
    parts: Part[];
    /** Optional. Any metadata to provide along with the message. */
    metadata: {
        [key: string]: any;
    } | undefined;
    /** The URIs of extensions that are present or contributed to this Message. */
    extensions: string[];
    /** A list of task IDs that this message references for additional context. */
    referenceTaskIds: string[];
}
declare const Message: MessageFns<Message>;
/** Artifacts represent task outputs. */
interface Artifact {
    /** Unique identifier (e.g. UUID) for the artifact. It must be unique within a task. */
    artifactId: string;
    /** A human readable name for the artifact. */
    name: string;
    /** Optional. A human readable description of the artifact. */
    description: string;
    /** The content of the artifact. Must contain at least one part. */
    parts: Part[];
    /** Optional. Metadata included with the artifact. */
    metadata: {
        [key: string]: any;
    } | undefined;
    /** The URIs of extensions that are present or contributed to this Artifact. */
    extensions: string[];
}
declare const Artifact: MessageFns<Artifact>;
/** An event sent by the agent to notify the client of a change in a task's status. */
interface TaskStatusUpdateEvent {
    /** The ID of the task that has changed. */
    taskId: string;
    /** The ID of the context that the task belongs to. */
    contextId: string;
    /** The new status of the task. */
    status: TaskStatus | undefined;
    /** Optional. Metadata associated with the task update. */
    metadata: {
        [key: string]: any;
    } | undefined;
}
declare const TaskStatusUpdateEvent: MessageFns<TaskStatusUpdateEvent>;
/** A task delta where an artifact has been generated. */
interface TaskArtifactUpdateEvent {
    /** The ID of the task for this artifact. */
    taskId: string;
    /** The ID of the context that this task belongs to. */
    contextId: string;
    /** The artifact that was generated or updated. */
    artifact: Artifact | undefined;
    /**
     * If true, the content of this artifact should be appended to a previously
     * sent artifact with the same ID.
     */
    append: boolean;
    /** If true, this is the final chunk of the artifact. */
    lastChunk: boolean;
    /** Optional. Metadata associated with the artifact update. */
    metadata: {
        [key: string]: any;
    } | undefined;
}
declare const TaskArtifactUpdateEvent: MessageFns<TaskArtifactUpdateEvent>;
/** Defines authentication details, used for push notifications. */
interface AuthenticationInfo {
    /**
     * HTTP Authentication Scheme from the [IANA registry](https://www.iana.org/assignments/http-authschemes/).
     * Examples: `Bearer`, `Basic`, `Digest`.
     * Scheme names are case-insensitive per [RFC 9110 Section 11.1](https://www.rfc-editor.org/rfc/rfc9110#section-11.1).
     */
    scheme: string;
    /** Push Notification credentials. Format depends on the scheme (e.g., token for Bearer). */
    credentials: string;
}
declare const AuthenticationInfo: MessageFns<AuthenticationInfo>;
/**
 * Declares a combination of a target URL, transport and protocol version for interacting with the agent.
 * This allows agents to expose the same functionality over multiple protocol binding mechanisms.
 */
interface AgentInterface {
    /**
     * The URL where this interface is available. Must be a valid absolute HTTPS URL in production.
     * Example: "https://api.example.com/a2a/v1", "https://grpc.example.com/a2a"
     */
    url: string;
    /**
     * The protocol binding supported at this URL. This is an open form string, to be
     * easily extended for other protocol bindings. The core ones officially
     * supported are `JSONRPC`, `GRPC` and `HTTP+JSON`.
     */
    protocolBinding: string;
    /** Tenant ID to be used in the request when calling the agent. */
    tenant: string;
    /**
     * The version of the A2A protocol this interface exposes.
     * Use the latest supported minor version per major version.
     * Examples: "0.3", "1.0"
     */
    protocolVersion: string;
}
declare const AgentInterface: MessageFns<AgentInterface>;
/**
 * A self-describing manifest for an agent. It provides essential
 * metadata including the agent's identity, capabilities, skills, supported
 * communication methods, and security requirements.
 * Next ID: 20
 */
interface AgentCard {
    /**
     * A human readable name for the agent.
     * Example: "Recipe Agent"
     */
    name: string;
    /**
     * A human-readable description of the agent, assisting users and other agents
     * in understanding its purpose.
     * Example: "Agent that helps users with recipes and cooking."
     */
    description: string;
    /** Ordered list of supported interfaces. The first entry is preferred. */
    supportedInterfaces: AgentInterface[];
    /** The service provider of the agent. */
    provider: AgentProvider | undefined;
    /**
     * The version of the agent.
     * Example: "1.0.0"
     */
    version: string;
    /** A URL providing additional documentation about the agent. */
    documentationUrl?: string | undefined;
    /** A2A Capability set supported by the agent. */
    capabilities: AgentCapabilities | undefined;
    /** The security scheme details used for authenticating with this agent. */
    securitySchemes: {
        [key: string]: SecurityScheme;
    };
    /** Security requirements for contacting the agent. */
    securityRequirements: SecurityRequirement[];
    /**
     * protolint:enable REPEATED_FIELD_NAMES_PLURALIZED
     * The set of interaction modes that the agent supports across all skills.
     * This can be overridden per skill. Defined as media types.
     */
    defaultInputModes: string[];
    /** The media types supported as outputs from this agent. */
    defaultOutputModes: string[];
    /**
     * Skills represent the abilities of an agent.
     * It is largely a descriptive concept but represents a more focused set of behaviors that the
     * agent is likely to succeed at.
     */
    skills: AgentSkill[];
    /** JSON Web Signatures computed for this `AgentCard`. */
    signatures: AgentCardSignature[];
    /** Optional. A URL to an icon for the agent. */
    iconUrl?: string | undefined;
}
declare const AgentCard: MessageFns<AgentCard>;
interface AgentCard_SecuritySchemesEntry {
    key: string;
    value: SecurityScheme | undefined;
}
declare const AgentCard_SecuritySchemesEntry: MessageFns<AgentCard_SecuritySchemesEntry>;
/** Represents the service provider of an agent. */
interface AgentProvider {
    /**
     * A URL for the agent provider's website or relevant documentation.
     * Example: "https://ai.google.dev"
     */
    url: string;
    /**
     * The name of the agent provider's organization.
     * Example: "Google"
     */
    organization: string;
}
declare const AgentProvider: MessageFns<AgentProvider>;
/** Defines optional capabilities supported by an agent. */
interface AgentCapabilities {
    /** Indicates if the agent supports streaming responses. */
    streaming?: boolean | undefined;
    /** Indicates if the agent supports sending push notifications for asynchronous task updates. */
    pushNotifications?: boolean | undefined;
    /** A list of protocol extensions supported by the agent. */
    extensions: AgentExtension[];
    /** Indicates if the agent supports providing an extended agent card when authenticated. */
    extendedAgentCard?: boolean | undefined;
}
declare const AgentCapabilities: MessageFns<AgentCapabilities>;
/** A declaration of a protocol extension supported by an Agent. */
interface AgentExtension {
    /** The unique URI identifying the extension. */
    uri: string;
    /** A human-readable description of how this agent uses the extension. */
    description: string;
    /** If true, the client must understand and comply with the extension's requirements. */
    required: boolean;
    /** Optional. Extension-specific configuration parameters. */
    params: {
        [key: string]: any;
    } | undefined;
}
declare const AgentExtension: MessageFns<AgentExtension>;
/** Represents a distinct capability or function that an agent can perform. */
interface AgentSkill {
    /** A unique identifier for the agent's skill. */
    id: string;
    /** A human-readable name for the skill. */
    name: string;
    /** A detailed description of the skill. */
    description: string;
    /** A set of keywords describing the skill's capabilities. */
    tags: string[];
    /** Example prompts or scenarios that this skill can handle. */
    examples: string[];
    /** The set of supported input media types for this skill, overriding the agent's defaults. */
    inputModes: string[];
    /** The set of supported output media types for this skill, overriding the agent's defaults. */
    outputModes: string[];
    /** Security schemes necessary for this skill. */
    securityRequirements: SecurityRequirement[];
}
declare const AgentSkill: MessageFns<AgentSkill>;
/**
 * AgentCardSignature represents a JWS signature of an AgentCard.
 * This follows the JSON format of an RFC 7515 JSON Web Signature (JWS).
 */
interface AgentCardSignature {
    /**
     * (-- api-linter: core::0140::reserved-words=disabled
     *     aip.dev/not-precedent: Backwards compatibility --)
     * Required. The protected JWS header for the signature. This is always a
     * base64url-encoded JSON object.
     */
    protected: string;
    /** Required. The computed signature, base64url-encoded. */
    signature: string;
    /** The unprotected JWS header values. */
    header: {
        [key: string]: any;
    } | undefined;
}
declare const AgentCardSignature: MessageFns<AgentCardSignature>;
/** A container associating a push notification configuration with a specific task. */
interface TaskPushNotificationConfig {
    /** Optional. Tenant ID. */
    tenant: string;
    /**
     * The push notification configuration details.
     * A unique identifier (e.g. UUID) for this push notification configuration.
     */
    id: string;
    /** The ID of the task this configuration is associated with. */
    taskId: string;
    /** The URL where the notification should be sent. */
    url: string;
    /** A token unique for this task or session. */
    token: string;
    /** Authentication information required to send the notification. */
    authentication: AuthenticationInfo | undefined;
}
declare const TaskPushNotificationConfig: MessageFns<TaskPushNotificationConfig>;
/**
 * protolint:disable REPEATED_FIELD_NAMES_PLURALIZED
 * A list of strings.
 */
interface StringList {
    /** The individual string values. */
    list: string[];
}
declare const StringList: MessageFns<StringList>;
/** Defines the security requirements for an agent. */
interface SecurityRequirement {
    /** A map of security schemes to the required scopes. */
    schemes: {
        [key: string]: StringList;
    };
}
declare const SecurityRequirement: MessageFns<SecurityRequirement>;
interface SecurityRequirement_SchemesEntry {
    key: string;
    value: StringList | undefined;
}
declare const SecurityRequirement_SchemesEntry: MessageFns<SecurityRequirement_SchemesEntry>;
/**
 * Defines a security scheme that can be used to secure an agent's endpoints.
 * This is a discriminated union type based on the OpenAPI 3.2 Security Scheme Object.
 * See: https://spec.openapis.org/oas/v3.2.0.html#security-scheme-object
 */
interface SecurityScheme {
    scheme?: //
    /** API key-based authentication. */
    {
        $case: "apiKeySecurityScheme";
        value: APIKeySecurityScheme;
    } | //
    /** HTTP authentication (Basic, Bearer, etc.). */
    {
        $case: "httpAuthSecurityScheme";
        value: HTTPAuthSecurityScheme;
    } | //
    /** OAuth 2.0 authentication. */
    {
        $case: "oauth2SecurityScheme";
        value: OAuth2SecurityScheme;
    } | //
    /** OpenID Connect authentication. */
    {
        $case: "openIdConnectSecurityScheme";
        value: OpenIdConnectSecurityScheme;
    } | //
    /** Mutual TLS authentication. */
    {
        $case: "mtlsSecurityScheme";
        value: MutualTlsSecurityScheme;
    } | undefined;
}
declare const SecurityScheme: MessageFns<SecurityScheme>;
/** Defines a security scheme using an API key. */
interface APIKeySecurityScheme {
    /** An optional description for the security scheme. */
    description: string;
    /** The location of the API key. Valid values are "query", "header", or "cookie". */
    location: string;
    /** The name of the header, query, or cookie parameter to be used. */
    name: string;
}
declare const APIKeySecurityScheme: MessageFns<APIKeySecurityScheme>;
/** Defines a security scheme using HTTP authentication. */
interface HTTPAuthSecurityScheme {
    /** An optional description for the security scheme. */
    description: string;
    /**
     * The name of the HTTP Authentication scheme to be used in the Authorization header,
     * as defined in RFC7235 (e.g., "Bearer").
     * This value should be registered in the IANA Authentication Scheme registry.
     */
    scheme: string;
    /**
     * A hint to the client to identify how the bearer token is formatted (e.g., "JWT").
     * Primarily for documentation purposes.
     */
    bearerFormat: string;
}
declare const HTTPAuthSecurityScheme: MessageFns<HTTPAuthSecurityScheme>;
/** Defines a security scheme using OAuth 2.0. */
interface OAuth2SecurityScheme {
    /** An optional description for the security scheme. */
    description: string;
    /** An object containing configuration information for the supported OAuth 2.0 flows. */
    flows: OAuthFlows | undefined;
    /**
     * URL to the OAuth2 authorization server metadata [RFC 8414](https://datatracker.ietf.org/doc/html/rfc8414).
     * TLS is required.
     */
    oauth2MetadataUrl: string;
}
declare const OAuth2SecurityScheme: MessageFns<OAuth2SecurityScheme>;
/** Defines a security scheme using OpenID Connect. */
interface OpenIdConnectSecurityScheme {
    /** An optional description for the security scheme. */
    description: string;
    /** The [OpenID Connect Discovery URL](https://openid.net/specs/openid-connect-discovery-1_0.html) for the OIDC provider's metadata. */
    openIdConnectUrl: string;
}
declare const OpenIdConnectSecurityScheme: MessageFns<OpenIdConnectSecurityScheme>;
/** Defines a security scheme using mTLS authentication. */
interface MutualTlsSecurityScheme {
    /** An optional description for the security scheme. */
    description: string;
}
declare const MutualTlsSecurityScheme: MessageFns<MutualTlsSecurityScheme>;
/** Defines the configuration for the supported OAuth 2.0 flows. */
interface OAuthFlows {
    flow?: //
    /** Configuration for the OAuth Authorization Code flow. */
    {
        $case: "authorizationCode";
        value: AuthorizationCodeOAuthFlow;
    } | //
    /** Configuration for the OAuth Client Credentials flow. */
    {
        $case: "clientCredentials";
        value: ClientCredentialsOAuthFlow;
    } | //
    /** Deprecated: Use Authorization Code + PKCE instead. */
    {
        $case: "implicit";
        value: ImplicitOAuthFlow;
    } | //
    /** Deprecated: Use Authorization Code + PKCE or Device Code. */
    {
        $case: "password";
        value: PasswordOAuthFlow;
    } | //
    /** Configuration for the OAuth Device Code flow. */
    {
        $case: "deviceCode";
        value: DeviceCodeOAuthFlow;
    } | undefined;
}
declare const OAuthFlows: MessageFns<OAuthFlows>;
/** Defines configuration details for the OAuth 2.0 Authorization Code flow. */
interface AuthorizationCodeOAuthFlow {
    /** The authorization URL to be used for this flow. */
    authorizationUrl: string;
    /** The token URL to be used for this flow. */
    tokenUrl: string;
    /** The URL to be used for obtaining refresh tokens. */
    refreshUrl: string;
    /** The available scopes for the OAuth2 security scheme. */
    scopes: {
        [key: string]: string;
    };
    /**
     * Indicates if PKCE (RFC 7636) is required for this flow.
     * PKCE should always be used for public clients and is recommended for all clients.
     */
    pkceRequired: boolean;
}
declare const AuthorizationCodeOAuthFlow: MessageFns<AuthorizationCodeOAuthFlow>;
interface AuthorizationCodeOAuthFlow_ScopesEntry {
    key: string;
    value: string;
}
declare const AuthorizationCodeOAuthFlow_ScopesEntry: MessageFns<AuthorizationCodeOAuthFlow_ScopesEntry>;
/** Defines configuration details for the OAuth 2.0 Client Credentials flow. */
interface ClientCredentialsOAuthFlow {
    /** The token URL to be used for this flow. */
    tokenUrl: string;
    /** The URL to be used for obtaining refresh tokens. */
    refreshUrl: string;
    /** The available scopes for the OAuth2 security scheme. */
    scopes: {
        [key: string]: string;
    };
}
declare const ClientCredentialsOAuthFlow: MessageFns<ClientCredentialsOAuthFlow>;
interface ClientCredentialsOAuthFlow_ScopesEntry {
    key: string;
    value: string;
}
declare const ClientCredentialsOAuthFlow_ScopesEntry: MessageFns<ClientCredentialsOAuthFlow_ScopesEntry>;
/** Deprecated: Use Authorization Code + PKCE instead. */
interface ImplicitOAuthFlow {
    /**
     * The authorization URL to be used for this flow. This MUST be in the
     * form of a URL. The OAuth2 standard requires the use of TLS
     */
    authorizationUrl: string;
    /**
     * The URL to be used for obtaining refresh tokens. This MUST be in the
     * form of a URL. The OAuth2 standard requires the use of TLS.
     */
    refreshUrl: string;
    /**
     * The available scopes for the OAuth2 security scheme. A map between the
     * scope name and a short description for it. The map MAY be empty.
     */
    scopes: {
        [key: string]: string;
    };
}
declare const ImplicitOAuthFlow: MessageFns<ImplicitOAuthFlow>;
interface ImplicitOAuthFlow_ScopesEntry {
    key: string;
    value: string;
}
declare const ImplicitOAuthFlow_ScopesEntry: MessageFns<ImplicitOAuthFlow_ScopesEntry>;
/** Deprecated: Use Authorization Code + PKCE or Device Code. */
interface PasswordOAuthFlow {
    /**
     * The token URL to be used for this flow. This MUST be in the form of a URL.
     * The OAuth2 standard requires the use of TLS.
     */
    tokenUrl: string;
    /**
     * The URL to be used for obtaining refresh tokens. This MUST be in the
     * form of a URL. The OAuth2 standard requires the use of TLS.
     */
    refreshUrl: string;
    /**
     * The available scopes for the OAuth2 security scheme. A map between the
     * scope name and a short description for it. The map MAY be empty.
     */
    scopes: {
        [key: string]: string;
    };
}
declare const PasswordOAuthFlow: MessageFns<PasswordOAuthFlow>;
interface PasswordOAuthFlow_ScopesEntry {
    key: string;
    value: string;
}
declare const PasswordOAuthFlow_ScopesEntry: MessageFns<PasswordOAuthFlow_ScopesEntry>;
/**
 * Defines configuration details for the OAuth 2.0 Device Code flow (RFC 8628).
 * This flow is designed for input-constrained devices such as IoT devices,
 * and CLI tools where the user authenticates on a separate device.
 */
interface DeviceCodeOAuthFlow {
    /** The device authorization endpoint URL. */
    deviceAuthorizationUrl: string;
    /** The token URL to be used for this flow. */
    tokenUrl: string;
    /** The URL to be used for obtaining refresh tokens. */
    refreshUrl: string;
    /** The available scopes for the OAuth2 security scheme. */
    scopes: {
        [key: string]: string;
    };
}
declare const DeviceCodeOAuthFlow: MessageFns<DeviceCodeOAuthFlow>;
interface DeviceCodeOAuthFlow_ScopesEntry {
    key: string;
    value: string;
}
declare const DeviceCodeOAuthFlow_ScopesEntry: MessageFns<DeviceCodeOAuthFlow_ScopesEntry>;
/** Represents a request for the `SendMessage` method. */
interface SendMessageRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
    /** The message to send to the agent. */
    message: Message | undefined;
    /** Configuration for the send request. */
    configuration: SendMessageConfiguration | undefined;
    /** A flexible key-value map for passing additional context or parameters. */
    metadata: {
        [key: string]: any;
    } | undefined;
}
declare const SendMessageRequest: MessageFns<SendMessageRequest>;
/** Represents a request for the `GetTask` method. */
interface GetTaskRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
    /** The resource ID of the task to retrieve. */
    id: string;
    /**
     * The maximum number of most recent messages from the task's history to retrieve. An
     * unset value means the client does not impose any limit. A value of zero is
     * a request to not include any messages. The server MUST NOT return more
     * messages than the provided value, but MAY apply a lower limit.
     */
    historyLength?: number | undefined;
}
declare const GetTaskRequest: MessageFns<GetTaskRequest>;
/** Parameters for listing tasks with optional filtering criteria. */
interface ListTasksRequest {
    /** Tenant ID, provided as a path parameter. */
    tenant: string;
    /** Filter tasks by context ID to get tasks from a specific conversation or session. */
    contextId: string;
    /** Filter tasks by their current status state. */
    status: TaskState;
    /**
     * The maximum number of tasks to return. The service may return fewer than this value.
     * If unspecified, at most 50 tasks will be returned.
     * The minimum value is 1.
     * The maximum value is 100.
     */
    pageSize?: number | undefined;
    /**
     * A page token, received from a previous `ListTasks` call.
     * `ListTasksResponse.next_page_token`.
     * Provide this to retrieve the subsequent page.
     */
    pageToken: string;
    /** The maximum number of messages to include in each task's history. */
    historyLength?: number | undefined;
    /**
     * Filter tasks which have a status updated after the provided timestamp in ISO 8601 format (e.g., "2023-10-27T10:00:00Z").
     * Only tasks with a status timestamp time greater than or equal to this value will be returned.
     */
    statusTimestampAfter: string | undefined;
    /**
     * Whether to include artifacts in the returned tasks.
     * Defaults to false to reduce payload size.
     */
    includeArtifacts?: boolean | undefined;
}
declare const ListTasksRequest: MessageFns<ListTasksRequest>;
/** Result object for `ListTasks` method containing an array of tasks and pagination information. */
interface ListTasksResponse {
    /** Array of tasks matching the specified criteria. */
    tasks: Task[];
    /** A token to retrieve the next page of results, or empty if there are no more results in the list. */
    nextPageToken: string;
    /** The page size used for this response. */
    pageSize: number;
    /** Total number of tasks available (before pagination). */
    totalSize: number;
}
declare const ListTasksResponse: MessageFns<ListTasksResponse>;
/** Represents a request for the `CancelTask` method. */
interface CancelTaskRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
    /** The resource ID of the task to cancel. */
    id: string;
    /** A flexible key-value map for passing additional context or parameters. */
    metadata: {
        [key: string]: any;
    } | undefined;
}
declare const CancelTaskRequest: MessageFns<CancelTaskRequest>;
/** Represents a request for the `GetTaskPushNotificationConfig` method. */
interface GetTaskPushNotificationConfigRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
    /** The parent task resource ID. */
    taskId: string;
    /** The resource ID of the configuration to retrieve. */
    id: string;
}
declare const GetTaskPushNotificationConfigRequest: MessageFns<GetTaskPushNotificationConfigRequest>;
/** Represents a request for the `DeleteTaskPushNotificationConfig` method. */
interface DeleteTaskPushNotificationConfigRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
    /** The parent task resource ID. */
    taskId: string;
    /** The resource ID of the configuration to delete. */
    id: string;
}
declare const DeleteTaskPushNotificationConfigRequest: MessageFns<DeleteTaskPushNotificationConfigRequest>;
/** Represents a request for the `SubscribeToTask` method. */
interface SubscribeToTaskRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
    /** The resource ID of the task to subscribe to. */
    id: string;
}
declare const SubscribeToTaskRequest: MessageFns<SubscribeToTaskRequest>;
/** Represents a request for the `ListTaskPushNotificationConfigs` method. */
interface ListTaskPushNotificationConfigsRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
    /** The parent task resource ID. */
    taskId: string;
    /** The maximum number of configurations to return. */
    pageSize: number;
    /** A page token received from a previous `ListTaskPushNotificationConfigsRequest` call. */
    pageToken: string;
}
declare const ListTaskPushNotificationConfigsRequest: MessageFns<ListTaskPushNotificationConfigsRequest>;
/** Represents a request for the `GetExtendedAgentCard` method. */
interface GetExtendedAgentCardRequest {
    /** Optional. Tenant ID, provided as a path parameter. */
    tenant: string;
}
declare const GetExtendedAgentCardRequest: MessageFns<GetExtendedAgentCardRequest>;
/** Represents the response for the `SendMessage` method. */
interface SendMessageResponse {
    /** The payload of the response. */
    payload?: //
    /** The task created or updated by the message. */
    {
        $case: "task";
        value: Task;
    } | //
    /** A message from the agent. */
    {
        $case: "message";
        value: Message;
    } | undefined;
}
declare const SendMessageResponse: MessageFns<SendMessageResponse>;
/** A wrapper object used in streaming operations to encapsulate different types of response data. */
interface StreamResponse {
    /** The payload of the stream response. */
    payload?: //
    /** A Task object containing the current state of the task. */
    {
        $case: "task";
        value: Task;
    } | //
    /** A Message object containing a message from the agent. */
    {
        $case: "message";
        value: Message;
    } | //
    /** An event indicating a task status update. */
    {
        $case: "statusUpdate";
        value: TaskStatusUpdateEvent;
    } | //
    /** An event indicating a task artifact update. */
    {
        $case: "artifactUpdate";
        value: TaskArtifactUpdateEvent;
    } | undefined;
}
declare const StreamResponse: MessageFns<StreamResponse>;
/**
 * Represents a successful response for the `ListTaskPushNotificationConfigs`
 * method.
 */
interface ListTaskPushNotificationConfigsResponse {
    /** The list of push notification configurations. */
    configs: TaskPushNotificationConfig[];
    /** A token to retrieve the next page of results, or empty if there are no more results in the list. */
    nextPageToken: string;
}
declare const ListTaskPushNotificationConfigsResponse: MessageFns<ListTaskPushNotificationConfigsResponse>;
interface MessageFns<T> {
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
}

export { TaskStatus as $, AgentCard as A, DeviceCodeOAuthFlow_ScopesEntry as B, CancelTaskRequest as C, DeleteTaskPushNotificationConfigRequest as D, ImplicitOAuthFlow_ScopesEntry as E, type MessageFns as F, GetExtendedAgentCardRequest as G, HTTPAuthSecurityScheme as H, ImplicitOAuthFlow as I, MutualTlsSecurityScheme as J, OAuthFlows as K, ListTasksRequest as L, Message as M, OpenIdConnectSecurityScheme as N, OAuth2SecurityScheme as O, Part as P, PasswordOAuthFlow as Q, PasswordOAuthFlow_ScopesEntry as R, SendMessageRequest as S, Task as T, Role as U, SecurityRequirement as V, SecurityRequirement_SchemesEntry as W, SecurityScheme as X, SendMessageConfiguration as Y, StringList as Z, TaskState as _, StreamResponse as a, protobufPackage as a0, roleFromJSON as a1, roleToJSON as a2, taskStateFromJSON as a3, taskStateToJSON as a4, GetTaskRequest as b, ListTasksResponse as c, TaskPushNotificationConfig as d, GetTaskPushNotificationConfigRequest as e, ListTaskPushNotificationConfigsRequest as f, ListTaskPushNotificationConfigsResponse as g, SubscribeToTaskRequest as h, TaskStatusUpdateEvent as i, TaskArtifactUpdateEvent as j, AgentInterface as k, SendMessageResponse as l, APIKeySecurityScheme as m, AgentCapabilities as n, AgentCardSignature as o, AgentCard_SecuritySchemesEntry as p, AgentExtension as q, AgentProvider as r, AgentSkill as s, Artifact as t, AuthenticationInfo as u, AuthorizationCodeOAuthFlow as v, AuthorizationCodeOAuthFlow_ScopesEntry as w, ClientCredentialsOAuthFlow as x, ClientCredentialsOAuthFlow_ScopesEntry as y, DeviceCodeOAuthFlow as z };
