/**
 * Transport-agnostic A2A error hierarchy shared by client and server.
 *
 * Every SDK error extends {@link A2AError}. Transport-specific subclasses
 * (in `./rest`, `./grpc`, `./json_rpc`) mix in a transport-context
 * interface; catch sites narrow via `instanceof <SemanticError>` and
 * `isRestError` / `isGrpcError` / `isJsonRpcError` type guards.
 *
 * Only the spec-defined fields (§3.3.2 "A2A-Specific Errors") live
 * here: `name`, `reason` (`ErrorInfo.reason`), and a default message.
 * Per-transport code/status mappings (§5.4) live in the corresponding
 * transport files.
 */
/** Domain for `google.rpc.ErrorInfo.domain`. */
declare const A2A_ERROR_DOMAIN = "a2a-protocol.org";
/** `@type`/`typeUrl` for `google.rpc.ErrorInfo` in ProtoJSON `Any`. */
declare const ERROR_INFO_TYPE = "type.googleapis.com/google.rpc.ErrorInfo";
/** A structured detail object included in error responses. */
interface ErrorDetail {
    '@type': string;
    [key: string]: unknown;
}
/** `google.rpc.ErrorInfo` as it travels on any A2A wire. */
interface A2AErrorInfo extends ErrorDetail {
    '@type': typeof ERROR_INFO_TYPE;
    reason: string;
    domain: typeof A2A_ERROR_DOMAIN;
    metadata?: Record<string, string>;
}
/** Options accepted by every `A2AError` constructor. */
interface A2AErrorOptions {
    /** Human-readable message. If omitted, the per-class default is used. */
    message?: string;
    /** Original error / rejection reason (see `Error.cause`). */
    cause?: unknown;
    /** Free-form ErrorInfo.metadata carried on the wire when possible. */
    metadata?: Record<string, string>;
}
/**
 * Base class for every SDK error and the concrete fallback used when
 * no semantic subclass matches (e.g. an unknown wire code). Carries
 * the spec-aligned `reason`, structured `metadata`, and a stable
 * `error.name` of `'A2AError'`. Transport subclasses in `./rest`,
 * `./grpc`, `./json_rpc` add wire context via the
 * {@link import('./rest.js').RestA2AError} / {@link import('./grpc/index.js').GrpcA2AError}
 * / {@link import('./json_rpc.js').JsonRpcA2AError} interfaces.
 *
 * Accepts either a bare message string or an options object.
 */
declare class A2AError extends Error {
    /** UPPER_SNAKE_CASE reason from `google.rpc.ErrorInfo` (§10.6 / §11.6). */
    readonly reason: string;
    /** Optional `google.rpc.ErrorInfo.metadata`. */
    readonly metadata?: Record<string, string>;
    constructor(options?: A2AErrorOptions | string);
    /** Builds `google.rpc.ErrorInfo` from this error. */
    toErrorInfo(): A2AErrorInfo;
}
declare const TaskNotFoundError: new (options?: A2AErrorOptions | string) => A2AError;
type TaskNotFoundError = InstanceType<typeof TaskNotFoundError>;
declare const TaskNotCancelableError: new (options?: A2AErrorOptions | string) => A2AError;
type TaskNotCancelableError = InstanceType<typeof TaskNotCancelableError>;
declare const PushNotificationNotSupportedError: new (options?: A2AErrorOptions | string) => A2AError;
type PushNotificationNotSupportedError = InstanceType<typeof PushNotificationNotSupportedError>;
declare const UnsupportedOperationError: new (options?: A2AErrorOptions | string) => A2AError;
type UnsupportedOperationError = InstanceType<typeof UnsupportedOperationError>;
declare const ContentTypeNotSupportedError: new (options?: A2AErrorOptions | string) => A2AError;
type ContentTypeNotSupportedError = InstanceType<typeof ContentTypeNotSupportedError>;
declare const InvalidAgentResponseError: new (options?: A2AErrorOptions | string) => A2AError;
type InvalidAgentResponseError = InstanceType<typeof InvalidAgentResponseError>;
declare const ExtendedAgentCardNotConfiguredError: new (options?: A2AErrorOptions | string) => A2AError;
type ExtendedAgentCardNotConfiguredError = InstanceType<typeof ExtendedAgentCardNotConfiguredError>;
declare const ExtensionSupportRequiredError: new (options?: A2AErrorOptions | string) => A2AError;
type ExtensionSupportRequiredError = InstanceType<typeof ExtensionSupportRequiredError>;
declare const VersionNotSupportedError: new (options?: A2AErrorOptions | string) => A2AError;
type VersionNotSupportedError = InstanceType<typeof VersionNotSupportedError>;
declare const RequestMalformedError: new (options?: A2AErrorOptions | string) => A2AError;
type RequestMalformedError = InstanceType<typeof RequestMalformedError>;
/**
 * Coerces an arbitrary rejected-promise reason into a printable message.
 * Promise rejections are not required to be `Error` instances.
 */
declare function extractErrorMessage(err: unknown): string;

export { A2AError as A, ContentTypeNotSupportedError as C, type ErrorDetail as E, InvalidAgentResponseError as I, PushNotificationNotSupportedError as P, RequestMalformedError as R, TaskNotCancelableError as T, UnsupportedOperationError as U, VersionNotSupportedError as V, type A2AErrorOptions as a, type A2AErrorInfo as b, A2A_ERROR_DOMAIN as c, ERROR_INFO_TYPE as d, ExtendedAgentCardNotConfiguredError as e, ExtensionSupportRequiredError as f, TaskNotFoundError as g, extractErrorMessage as h };
