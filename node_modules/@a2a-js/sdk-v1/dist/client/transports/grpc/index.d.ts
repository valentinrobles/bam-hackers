import * as grpc from '@grpc/grpc-js';
import { A as AgentCard } from '../../../a2a-4AAMnZHp.js';
import { a as TransportFactory, T as Transport } from '../../../multitransport-client-CCrSt7Jj.js';
import '../../../signature-CufuY-ga.js';
import 'jose';
import '../../../index.js';
import '../../../constants-PdvgZRkF.js';
import '../../../extensions-DcJm6niD.js';

declare class GrpcTransportFactoryOptions {
    grpcChannelCredentials?: grpc.ChannelCredentials;
    grpcCallOptions?: Partial<grpc.CallOptions>;
    /**
     * Enables the v0.3 protocol compatibility layer. When enabled, the
     * factory inspects the matched `AgentInterface.protocolVersion`; if
     * it falls in `[0.3, 1.0)`, the v0.3 `LegacyGrpcTransport` is
     * instantiated instead of v1.0.
     *
     * Default: omitted (disabled).
     */
    legacyCompat?: {
        enabled: boolean;
    };
}
/**
 * Factory producing a gRPC `Transport`. With
 * `legacyCompat: { enabled: true }` it dispatches between the v1.0 and
 * v0.3 transports based on `AgentInterface.protocolVersion`.
 */
declare class GrpcTransportFactory implements TransportFactory {
    private readonly options?;
    constructor(options?: GrpcTransportFactoryOptions | undefined);
    get protocolName(): string;
    create(url: string, agentCard: AgentCard): Promise<Transport>;
}

export { GrpcTransportFactory };
