import { k as AgentInterface } from '../../a2a-4AAMnZHp.js';
export { A as A2A_LEGACY_PROTOCOL_VERSION } from '../../constants-PdvgZRkF.js';

/**
 * Compat-layer constants and method-name mappings for the legacy A2A v0.3
 * protocol.
 */
/**
 * The legacy A2A protocol version this compat layer targets. Re-exported
 * from core so it's reachable via the compat import path.
 */

/**
 * The HTTP extensions header used by v0.3. v1.0 dropped the `X-` prefix
 * (see `HTTP_EXTENSION_HEADER` in `src/constants.ts`).
 */
declare const LEGACY_HTTP_EXTENSION_HEADER = "X-A2A-Extensions";
/**
 * The content type used by v0.3 JSON-RPC and REST transports. v1.0 uses
 * `application/a2a+json` for REST/push payloads instead.
 */
declare const LEGACY_JSON_CONTENT_TYPE = "application/json";
declare const LEGACY_METHOD_MESSAGE_SEND = "message/send";
declare const LEGACY_METHOD_MESSAGE_STREAM = "message/stream";
declare const LEGACY_METHOD_TASKS_GET = "tasks/get";
declare const LEGACY_METHOD_TASKS_CANCEL = "tasks/cancel";
declare const LEGACY_METHOD_TASKS_RESUBSCRIBE = "tasks/resubscribe";
declare const LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_SET = "tasks/pushNotificationConfig/set";
declare const LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_GET = "tasks/pushNotificationConfig/get";
declare const LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_LIST = "tasks/pushNotificationConfig/list";
declare const LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_DELETE = "tasks/pushNotificationConfig/delete";
declare const LEGACY_METHOD_GET_AUTHENTICATED_EXTENDED_CARD = "agent/getAuthenticatedExtendedCard";
/** Translate a v0.3 JSON-RPC method name to its v1.0 PascalCase equivalent. */
declare function legacyJsonRpcToV1Method(method: string): string;
/**
 * Returns `true` if `method` is a known v0.3 JSON-RPC method name. Used
 * by the Express JSON-RPC handler to route between the v1.0 and v0.3
 * dispatchers.
 */
declare function isLegacyJsonRpcMethod(method: unknown): boolean;
/**
 * Returns `true` if `method` is a known v1.0 JSON-RPC (PascalCase)
 * method name. Includes both {@link V1_TO_LEGACY_JSONRPC} and the
 * v1.0-only methods in {@link V1_METHODS_WITHOUT_LEGACY_EQUIVALENT}.
 */
declare function isV1JsonRpcMethod(method: unknown): boolean;
/**
 * Translate a v1.0 method name to its v0.3 JSON-RPC equivalent. Throws
 * `A2AError.unsupportedOperation` for v1.0 names with no v0.3 equivalent
 * (e.g. `ListTasks`); throws `A2AError.invalidRequest` for unknown names.
 */
declare function v1MethodToLegacyJsonRpc(method: string): string;
/** Translate a v0.3 JSON-RPC method name to its v0.3 gRPC equivalent. */
declare function legacyJsonRpcToLegacyGrpcMethod(method: string): string;
/** Translate a v0.3 gRPC method name to its v0.3 JSON-RPC equivalent. */
declare function legacyGrpcToLegacyJsonRpcMethod(method: string): string;
/** Translate a v0.3 gRPC method name to its v1.0 PascalCase equivalent. */
declare function legacyGrpcToV1Method(method: string): string;
/**
 * Translate a v1.0 method name to its v0.3 gRPC equivalent. Throws
 * `A2AError.unsupportedOperation` for v1.0 names with no v0.3 equivalent
 * (e.g. `ListTasks`); throws `A2AError.invalidRequest` for unknown names.
 */
declare function v1MethodToLegacyGrpc(method: string): string;

/**
 * `AgentCard` translators. The big shape differences:
 *  - Endpoints: v0.3 has `url` + `preferredTransport` + an
 *    `additionalInterfaces[]` sidecar; v1.0 has one
 *    `supportedInterfaces[]` with per-entry version.
 *  - `protocolVersion`: card-level in v0.3, per-interface in v1.0.
 *  - Extended-card flag: card-level in v0.3, on `capabilities` in v1.0.
 *  - `AgentSkill.security`: plain `{ [k]: string[] }[]` in v0.3, wrapped
 *    `SecurityRequirement[]` in v1.0.
 *
 * v1.0 → v0.3 filters interfaces to the legacy range and throws
 * `VersionNotSupportedError` if none remain.
 */

/**
 * Returns `interfaces` augmented with a v0.3 mirror entry for each
 * binding in `bindings` that does not already have one. Idempotent: a
 * binding that already declares a legacy-range interface is left
 * untouched. Mirror entries copy `url`/`tenant` from the first v1.0
 * entry for the binding and stamp `protocolVersion: '0.3'`.
 *
 * @example
 * ```ts
 * supportedInterfaces: duplicateInterfacesForLegacy(
 *   [
 *     { url: '/jsonrpc', protocolBinding: 'JSONRPC',   tenant: '', protocolVersion: '1.0' },
 *     { url: '/rest',    protocolBinding: 'HTTP+JSON', tenant: '', protocolVersion: '1.0' },
 *     { url: '/grpc',    protocolBinding: 'GRPC',      tenant: '', protocolVersion: '1.0' },
 *   ],
 *   ['JSONRPC', 'HTTP+JSON'],
 * )
 * ```
 */
declare function duplicateInterfacesForLegacy(interfaces: AgentInterface[], bindings: string[]): AgentInterface[];

export { LEGACY_HTTP_EXTENSION_HEADER, LEGACY_JSON_CONTENT_TYPE, LEGACY_METHOD_GET_AUTHENTICATED_EXTENDED_CARD, LEGACY_METHOD_MESSAGE_SEND, LEGACY_METHOD_MESSAGE_STREAM, LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_DELETE, LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_GET, LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_LIST, LEGACY_METHOD_PUSH_NOTIFICATION_CONFIG_SET, LEGACY_METHOD_TASKS_CANCEL, LEGACY_METHOD_TASKS_GET, LEGACY_METHOD_TASKS_RESUBSCRIBE, duplicateInterfacesForLegacy, isLegacyJsonRpcMethod, isV1JsonRpcMethod, legacyGrpcToLegacyJsonRpcMethod, legacyGrpcToV1Method, legacyJsonRpcToLegacyGrpcMethod, legacyJsonRpcToV1Method, v1MethodToLegacyGrpc, v1MethodToLegacyJsonRpc };
