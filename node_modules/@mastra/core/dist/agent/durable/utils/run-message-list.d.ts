import type { Mastra } from '../../../mastra/index.js';
import { MessageList } from '../../message-list/index.js';
/** The id generator does not survive the serialize/deserialize round-trip a durable run makes on every step. */
export declare function createRunMessageList({ mastra, threadId, resourceId, }: {
    mastra?: Mastra;
    threadId?: string;
    resourceId?: string;
}): MessageList;
//# sourceMappingURL=run-message-list.d.ts.map