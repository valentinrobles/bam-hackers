import type { Mastra } from '../../mastra/index.js';
import type { ExperimentConfig, ExperimentSummary } from './types.js';
/** Error code recorded on an item whose `beforeEach` hook threw. */
export declare const EXPERIMENT_ITEM_BEFORE_EACH_FAILED = "EXPERIMENT_ITEM_BEFORE_EACH_FAILED";
export type { DataItem, ExperimentConfig, ExperimentHookArgs, ExperimentHookItem, ExperimentItemHookArgs, ExperimentItemResultHookArgs, ExperimentRunResultHookArgs, ExperimentSummary, ItemWithScores, ItemResult, ScorerResult, StartExperimentConfig, } from './types.js';
export type { ExperimentEvent, ExperimentEventObserver, ExperimentItemCompletedEvent, ExperimentJsonValue, ExperimentRunFinishedEvent, ExperimentRunStartedEvent, } from './events.js';
export { executeTarget, type Target, type ExecutionResult } from './executor.js';
export { EXPERIMENT_ITEM_SCORER_NOT_FOUND, resolveScorers, runScorersForItem } from './scorer.js';
export { ToolMockMatcher, TOOL_MOCK_MISMATCH, TOOL_MOCK_EXHAUSTED, TOOL_MOCK_NOT_DECLARED, type ItemToolMock, type ToolMockMatchArgs, type ToolMockReport, type ToolMockResolution, type ToolMockFailureCode, type UnmockedToolPolicy, } from './tool-mocks.js';
export * from './analytics/index.js';
export { executeExperimentItem, type ExecuteExperimentItemArgs, type ExecuteExperimentItemOutput, type ExperimentItemInput, } from './item.js';
/**
 * Run a dataset experiment against a target with optional scoring.
 *
 * Executes all items in the dataset concurrently (up to maxConcurrency) against
 * the specified target (agent or workflow). Optionally applies scorers to each
 * result and persists both results and scores to storage.
 *
 * @param mastra - Mastra instance for storage and target resolution
 * @param config - Experiment configuration
 * @returns ExperimentSummary with results and scores
 *
 * @example
 * ```typescript
 * const summary = await runExperiment(mastra, {
 *   datasetId: 'my-dataset',
 *   targetType: 'agent',
 *   targetId: 'my-agent',
 *   scorers: [accuracyScorer, latencyScorer],
 *   maxConcurrency: 10,
 * });
 * console.log(`${summary.succeededCount}/${summary.totalItems} succeeded`);
 * ```
 */
export declare function runExperiment(mastra: Mastra, config: ExperimentConfig): Promise<ExperimentSummary>;
export { resolveTarget } from './resolve-target.js';
//# sourceMappingURL=index.d.ts.map