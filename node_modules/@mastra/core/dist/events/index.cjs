Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_rolldown_runtime = require("../rolldown-runtime-uwYp4b74.cjs");
const require_event_emitter = require("../event-emitter-B85_hXV9.cjs");
const require_topics = require("../topics-CbFO7xAb.cjs");
const require_file = require("../file-Bav4lJQ9.cjs");
const require_output_helpers = require("../output-helpers-BxsqfJ0U.cjs");
let crypto = require("crypto");
let path = require("path");
let fs_promises = require("fs/promises");
let net = require("net");
net = require_rolldown_runtime.__toESM(net, 1);
//#region src/events/codec/error.ts
const MAX_CAUSE_DEPTH = 5;
/**
* Serializes an Error instance to a plain SerializedError object without
* mutating the original. Unlike `getErrorFromUnknown(...).toJSON()`, this does
* not attach a non-enumerable `toJSON` to the live Error.
*/
function serializeError(err, depth = 0) {
	const json = {
		name: err.name || "Error",
		message: err.message
	};
	if (err.stack !== void 0) json.stack = err.stack;
	if (err.cause !== void 0) if (err.cause instanceof Error && depth < MAX_CAUSE_DEPTH) json.cause = serializeError(err.cause, depth + 1);
	else json.cause = err.cause;
	for (const key in err) {
		if (!Object.prototype.hasOwnProperty.call(err, key)) continue;
		if (key === "message" || key === "name" || key === "stack" || key === "cause") continue;
		json[key] = err[key];
	}
	return json;
}
/**
* Rehydrates a SerializedError into a vanilla Error instance. We never
* instantiate user-controlled prototypes — name is preserved as a string field.
*/
function rehydrateError(s) {
	const cause = s.cause !== void 0 ? s.cause && typeof s.cause === "object" && "message" in s.cause && "name" in s.cause ? rehydrateError(s.cause) : s.cause : void 0;
	const err = cause !== void 0 ? new Error(s.message, { cause }) : new Error(s.message);
	if (s.name) err.name = s.name;
	if (s.stack !== void 0) err.stack = s.stack;
	for (const key in s) {
		if (!Object.prototype.hasOwnProperty.call(s, key)) continue;
		if (key === "message" || key === "name" || key === "stack" || key === "cause") continue;
		err[key] = s[key];
	}
	return err;
}
//#endregion
//#region src/events/codec/registry.ts
const classRegistry = /* @__PURE__ */ new Map();
function registerClass(name, codec) {
	classRegistry.set(name, codec);
}
function getClassCodec(name) {
	return classRegistry.get(name);
}
//#endregion
//#region src/events/codec/registrations.ts
/**
* Built-in class codecs.
*
* IMPORTANT: This module must be loaded for `instanceof` to survive
* serialization across the unix-socket pubsub. The `BUILTIN_CODECS_REGISTERED`
* constant exists solely so consumers can import it as a *named* import (see
* `codec.ts`). A `import './registrations'` side-effect import would be
* tree-shaken out of dist consumers because `packages/core/package.json` has
* `"sideEffects": false`. Named imports are kept by every bundler.
*
* `DefaultStepResult` flows through workflow output schemas typed as
* `z.any()`, so it crosses the unix-socket pubsub on the evented engine.
* Without a class registration, consumers that rely on
* `instanceof DefaultStepResult` (e.g. the in-memory storage path) would
* receive plain data. The generic `TOOLS` type parameter is erased at
* runtime, so we register the constructor name once.
*/
const BUILTIN_CODECS_REGISTERED = (() => {
	registerClass("DefaultGeneratedFile", {
		toData: (f) => ({
			data: f.base64,
			mediaType: f.mediaType
		}),
		fromData: (d) => new require_file.DefaultGeneratedFile({
			data: d.data,
			mediaType: d.mediaType
		})
	});
	registerClass("DefaultGeneratedFileWithType", {
		toData: (f) => ({
			data: f.base64,
			mediaType: f.mediaType
		}),
		fromData: (d) => new require_file.DefaultGeneratedFileWithType({
			data: d.data,
			mediaType: d.mediaType
		})
	});
	registerClass("DefaultStepResult", {
		toData: (s) => ({
			content: s.content,
			finishReason: s.finishReason,
			usage: s.usage,
			warnings: s.warnings,
			request: s.request,
			response: s.response,
			providerMetadata: s.providerMetadata,
			tripwire: s.tripwire
		}),
		fromData: (d) => new require_output_helpers.DefaultStepResult(d)
	});
	return true;
})();
//#endregion
//#region src/events/codec/tags.ts
/**
* Discriminator key for codec-tagged envelopes. Long, namespaced, and unlikely
* to collide with user data. Plain objects that happen to carry this key but
* do not match an envelope shape are preserved as-is by the decoder.
*
* **Reservation contract:** `__m_codec__` is reserved on the wire. Do not use
* it as a property name on user objects that cross the pubsub boundary — if
* the surrounding shape also happens to match an envelope (e.g.
* `{ __m_codec__: 'Date', v: '...' }`), the decoder will reconstruct it as
* the tagged type. The conservative shape check in `isEnvelope` keeps the
* blast radius narrow, and `toJSON()` is skipped for objects already carrying
* this key, but the safest path is to avoid the namespace entirely.
*/
const CODEC_TAG = "__m_codec__";
/**
* Returns true when `value` looks like a codec envelope. The check is
* conservative — an object with the tag key but an unknown tag value, or a
* shape that does not match any envelope variant, is treated as user data.
*/
function isEnvelope(value) {
	const tag = value[CODEC_TAG];
	if (typeof tag !== "string") return false;
	switch (tag) {
		case "Undefined": return true;
		case "Date":
		case "BigInt":
		case "URL": return typeof value.v === "string";
		case "RegExp": {
			const v = value.v;
			if (!v || typeof v.source !== "string" || typeof v.flags !== "string") return false;
			if (v.source.length > 1024) return false;
			return /^[dgimsuvy]*$/.test(v.flags) && new Set(v.flags).size === v.flags.length;
		}
		case "Map":
		case "Set": return Array.isArray(value.v);
		case "Error": return typeof value.v === "object" && value.v !== null;
		case "Class": return typeof value.n === "string";
		default: return false;
	}
}
//#endregion
//#region src/events/codec/codec.ts
/**
* Encode a value into a JSON-safe shape. Non-JSON-safe types (Date, Error,
* Map, Set, RegExp, URL, BigInt, undefined, registered classes) are wrapped
* in tagged envelopes that the decoder can reconstruct.
*
* Functions and symbols are dropped (parity with JSON.stringify). Cycles are
* replaced with null at the second visit. NaN/Infinity become null. Honors
* user `toJSON()` methods on plain objects.
*/
function encode(value) {
	if (!BUILTIN_CODECS_REGISTERED) throw new Error("Built-in codec registrations failed to load");
	return walk(value, /* @__PURE__ */ new WeakSet());
}
function walk(v, seen) {
	if (v === void 0) return { [CODEC_TAG]: "Undefined" };
	if (v === null) return null;
	const t = typeof v;
	if (t === "string" || t === "boolean") return v;
	if (t === "number") return Number.isFinite(v) ? v : null;
	if (t === "bigint") return {
		[CODEC_TAG]: "BigInt",
		v: v.toString()
	};
	if (t === "function" || t === "symbol") return void 0;
	if (v instanceof Date) return {
		[CODEC_TAG]: "Date",
		v: v.toISOString()
	};
	if (v instanceof RegExp) return {
		[CODEC_TAG]: "RegExp",
		v: {
			source: v.source,
			flags: v.flags
		}
	};
	if (v instanceof URL) return {
		[CODEC_TAG]: "URL",
		v: v.toString()
	};
	if (v instanceof Error) return {
		[CODEC_TAG]: "Error",
		v: serializeError(v)
	};
	if (v instanceof Map) {
		if (seen.has(v)) return null;
		seen.add(v);
		const entries = [];
		for (const [k, val] of v.entries()) entries.push([walk(k, seen), walk(val, seen)]);
		return {
			[CODEC_TAG]: "Map",
			v: entries
		};
	}
	if (v instanceof Set) {
		if (seen.has(v)) return null;
		seen.add(v);
		const values = [];
		for (const x of v) values.push(walk(x, seen));
		return {
			[CODEC_TAG]: "Set",
			v: values
		};
	}
	if (Array.isArray(v)) {
		if (seen.has(v)) return null;
		seen.add(v);
		return v.map((x) => walk(x, seen));
	}
	if (t === "object") {
		if (seen.has(v)) return null;
		seen.add(v);
		const maybeToJSON = v.toJSON;
		if (typeof maybeToJSON === "function") return walk(maybeToJSON.call(v), seen);
		const ctorName = v.constructor?.name;
		if (ctorName && ctorName !== "Object") {
			const reg = getClassCodec(ctorName);
			if (reg) return {
				[CODEC_TAG]: "Class",
				n: ctorName,
				v: walk(reg.toData(v), seen)
			};
		}
		const out = {};
		for (const k of Object.keys(v)) {
			if (k === "__proto__") continue;
			const raw = v[k];
			if (raw === void 0) {
				out[k] = { [CODEC_TAG]: "Undefined" };
				continue;
			}
			const encoded = walk(raw, seen);
			if (encoded === void 0) continue;
			out[k] = encoded;
		}
		return out;
	}
	return v;
}
/**
* Reconstruct a `RegExp` from a decoded envelope payload. Re-validates the
* payload locally (independent of `isEnvelope`) so the constructor input is
* narrowed at this single call site: bounded `source` length, spec-defined
* flag whitelist, no duplicate flags. A malformed or hostile envelope yields
* an empty regex (`/(?:)/`) rather than throwing, keeping frame decoding
* resilient.
*
* NOTE: `source` is intentionally NOT escaped — a `RegExp` envelope's whole
* purpose is to round-trip a pattern, so metacharacters must reach
* `new RegExp(...)` verbatim. Safety comes from the bounded length, the
* flag whitelist, and the `try/catch` fallback, not from escaping.
*/
function decodeRegExpEnvelope(v) {
	if (!v || typeof v !== "object") return /(?:)/;
	const candidate = v;
	if (typeof candidate.source !== "string") return /(?:)/;
	if (typeof candidate.flags !== "string") return /(?:)/;
	if (candidate.source.length > 1024) return /(?:)/;
	const flags = candidate.flags;
	if (!/^[dgimsuvy]*$/.test(flags)) return /(?:)/;
	if (new Set(flags).size !== flags.length) return /(?:)/;
	try {
		return new RegExp(candidate.source, flags);
	} catch {
		return /(?:)/;
	}
}
/**
* Decode a value previously produced by `encode`. Reconstructs envelope-tagged
* types and recursively decodes nested values. Plain objects that happen to
* carry a `CODEC_TAG` key but do not match an envelope shape are preserved.
*/
function decode(value) {
	if (value === null) return null;
	if (typeof value !== "object") return value;
	if (Array.isArray(value)) return value.map(decode);
	if ("__m_codec__" in value && isEnvelope(value)) {
		const env = value;
		switch (env[CODEC_TAG]) {
			case "Undefined": return;
			case "Date": return new Date(env.v);
			case "BigInt": return BigInt(env.v);
			case "RegExp": return decodeRegExpEnvelope(env.v);
			case "URL": return new URL(env.v);
			case "Map": return new Map(env.v.map(([k, val]) => [decode(k), decode(val)]));
			case "Set": return new Set(env.v.map(decode));
			case "Error": return rehydrateError(decodeSerializedError(env.v));
			case "Class": {
				const reg = getClassCodec(env.n);
				const data = decode(env.v);
				return reg ? reg.fromData(data) : data;
			}
		}
	}
	const out = {};
	for (const k of Object.keys(value)) {
		if (k === "__proto__") continue;
		out[k] = decode(value[k]);
	}
	return out;
}
/**
* Recursively decode any envelopes embedded inside a SerializedError's custom
* fields (e.g. an error with a `details: Map` field). The top-level shape
* stays a SerializedError so `rehydrateError` can consume it.
*/
function decodeSerializedError(s) {
	return decode(s);
}
//#endregion
//#region src/events/unix-socket-pubsub.ts
const DEFAULT_MAX_REMOTE_CLIENT_QUEUED_BYTES = 64 * 1024 * 1024;
const DEFAULT_MAX_INBOUND_FRAME_BYTES = 64 * 1024 * 1024;
const DEFAULT_MEMBERSHIP_ACK_TIMEOUT_MS = 5e3;
const NEWLINE_BYTE = 10;
/**
* Max number of times a local subscriber callback may be redelivered after a
* nack. MUST be >= the consumer-side retry budget
* (`WorkflowEventProcessor.MAX_DELIVERY_ATTEMPTS`) — otherwise the transport
* gives up before the consumer can exhaust its budget and surface the terminal
* failure, which would leave the run silently hung.
*
* An invariant test in
* `packages/core/src/events/unix-socket-pubsub-redelivery-budget.test.ts`
* pins this ordering against the consumer constant so the two constants stay
* in sync as the consumer budget changes.
*/
const MAX_LOCAL_REDELIVERIES = 6;
const REDELIVERY_DELAY_MS = 100;
function serializeFrame(frame) {
	return `${JSON.stringify(encode(frame))}\n`;
}
function writeSerializedFrame(socket, serializedFrame) {
	return new Promise((resolve, reject) => {
		let writeCompleted = false;
		let drainCompleted = true;
		let settled = false;
		const cleanup = () => {
			socket.off("error", onError);
			socket.off("close", onClose);
			socket.off("drain", onDrain);
		};
		const settle = (error) => {
			if (settled) return;
			settled = true;
			cleanup();
			if (error) {
				reject(error);
				return;
			}
			resolve();
		};
		const maybeResolve = () => {
			if (writeCompleted && drainCompleted) settle();
		};
		const onError = (error) => settle(error);
		const onClose = () => settle(/* @__PURE__ */ new Error("UnixSocketPubSub socket closed before write completed"));
		const onDrain = () => {
			drainCompleted = true;
			maybeResolve();
		};
		socket.once("error", onError);
		socket.once("close", onClose);
		let drained;
		try {
			drained = socket.write(serializedFrame, (error) => {
				if (error) {
					settle(error);
					return;
				}
				writeCompleted = true;
				maybeResolve();
			});
		} catch (error) {
			settle(error);
			return;
		}
		if (!drained) {
			drainCompleted = false;
			socket.once("drain", onDrain);
		}
	});
}
function writeFrame(socket, frame) {
	return writeSerializedFrame(socket, serializeFrame(frame));
}
function membershipKey(topic, group) {
	return JSON.stringify([topic, group ?? null]);
}
function readFrames(socket, onFrame, maxFrameBytes) {
	let pending = null;
	let pendingBytes = 0;
	const appendPending = (bytes) => {
		const needed = pendingBytes + bytes.length;
		if (!pending || pending.length < needed) {
			const capacity = Math.min(maxFrameBytes, Math.max(needed, pending ? pending.length * 2 : 4096));
			const grown = Buffer.allocUnsafe(capacity);
			if (pending) pending.copy(grown, 0, 0, pendingBytes);
			pending = grown;
		}
		bytes.copy(pending, pendingBytes);
		pendingBytes = needed;
	};
	socket.on("data", (chunk) => {
		if (socket.destroyed) return;
		let offset = 0;
		while (offset < chunk.length) {
			const newlineIndex = chunk.indexOf(NEWLINE_BYTE, offset);
			if (newlineIndex === -1) {
				const rest = chunk.subarray(offset);
				if (pendingBytes + rest.length > maxFrameBytes) {
					pending = null;
					pendingBytes = 0;
					socket.destroy();
					return;
				}
				appendPending(rest);
				return;
			}
			const tail = chunk.subarray(offset, newlineIndex);
			offset = newlineIndex + 1;
			const lineBytes = pendingBytes + tail.length;
			if (lineBytes > maxFrameBytes) {
				pending = null;
				pendingBytes = 0;
				socket.destroy();
				return;
			}
			const line = pending ? Buffer.concat([pending.subarray(0, pendingBytes), tail], lineBytes) : tail;
			pending = null;
			pendingBytes = 0;
			const text = line.toString("utf8");
			if (!text.trim()) continue;
			try {
				onFrame(decode(JSON.parse(text)));
			} catch {}
			if (socket.destroyed) return;
		}
	});
}
var UnixSocketPubSub = class extends require_event_emitter.PubSub {
	socketPath;
	#server;
	#clientSocket;
	#isBroker = false;
	#closed = false;
	#starting;
	#subscriptions = /* @__PURE__ */ new Map();
	#localGroupCursors = /* @__PURE__ */ new Map();
	#brokerGroupCursors = /* @__PURE__ */ new Map();
	#subscribeWaiters = /* @__PURE__ */ new Map();
	#unsubscribeWaiters = /* @__PURE__ */ new Map();
	#brokerClients = /* @__PURE__ */ new Map();
	#pendingWrites = /* @__PURE__ */ new Set();
	#recovering;
	#maxRemoteClientQueuedBytes;
	#maxInboundFrameBytes;
	#membershipAckTimeoutMs;
	constructor(socketPath, options = {}) {
		super();
		this.socketPath = socketPath;
		this.#maxRemoteClientQueuedBytes = options.maxRemoteClientQueuedBytes ?? DEFAULT_MAX_REMOTE_CLIENT_QUEUED_BYTES;
		const maxInboundFrameBytes = options.maxInboundFrameBytes ?? DEFAULT_MAX_INBOUND_FRAME_BYTES;
		if (!Number.isFinite(maxInboundFrameBytes) || maxInboundFrameBytes <= 0) throw new Error("UnixSocketPubSub maxInboundFrameBytes must be a positive finite number");
		this.#maxInboundFrameBytes = maxInboundFrameBytes;
		const membershipAckTimeoutMs = options.membershipAckTimeoutMs ?? DEFAULT_MEMBERSHIP_ACK_TIMEOUT_MS;
		if (!Number.isFinite(membershipAckTimeoutMs) || membershipAckTimeoutMs <= 0) throw new Error("UnixSocketPubSub membershipAckTimeoutMs must be a positive finite number");
		this.#membershipAckTimeoutMs = membershipAckTimeoutMs;
	}
	get supportedModes() {
		return ["push"];
	}
	get isBroker() {
		return this.#isBroker;
	}
	/** Number of remote clients currently connected to this broker. Always 0 for non-broker instances. */
	get remoteClientCount() {
		return this.#isBroker ? this.#brokerClients.size : 0;
	}
	async publish(topic, event, options) {
		await this.#ensureStarted();
		if (options?.localOnly) {
			const localEvent = {
				...event,
				id: (0, crypto.randomUUID)(),
				createdAt: /* @__PURE__ */ new Date(),
				deliveryAttempt: 1
			};
			this.#deliverLocal(topic, localEvent);
			return;
		}
		if (this.#isBroker) {
			await this.#publishFromBroker(topic, event, void 0, options?.localOnly);
			return;
		}
		const socket = this.#clientSocket;
		if (!socket || socket.destroyed) await this.#ensureStarted(true);
		await this.#sendToBroker({
			type: "publish",
			topic,
			event,
			localOnly: options?.localOnly
		});
	}
	async subscribe(topic, cb, options) {
		const subscriptions = this.#subscriptions.get(topic) ?? /* @__PURE__ */ new Map();
		const existing = subscriptions.get(cb);
		if (existing && existing.group === options?.group) {
			await this.#ensureStarted();
			if (!this.#isBroker) await this.#waitForPendingMembershipAcknowledgement(this.#subscribeWaiters, membershipKey(topic, existing.group));
			return;
		}
		if (existing) await this.unsubscribe(topic, cb);
		const group = options?.group;
		const hadMembership = this.#hasLocalMembership(topic, group);
		const wasConnected = Boolean(this.#clientSocket && !this.#clientSocket.destroyed);
		subscriptions.set(cb, {
			callback: cb,
			group
		});
		this.#subscriptions.set(topic, subscriptions);
		try {
			await this.#ensureStarted();
			if (!this.#isBroker && wasConnected) if (hadMembership) await this.#waitForPendingMembershipAcknowledgement(this.#subscribeWaiters, membershipKey(topic, group));
			else await this.#sendSubscribeToBroker(topic, group);
		} catch (error) {
			subscriptions.delete(cb);
			if (subscriptions.size === 0) this.#subscriptions.delete(topic);
			throw error;
		}
	}
	async unsubscribe(topic, cb) {
		const subscriptions = this.#subscriptions.get(topic);
		const subscription = subscriptions?.get(cb);
		if (!subscriptions || !subscription) return;
		const membershipWillEnd = ![...subscriptions.values()].some((candidate) => candidate.callback !== cb && candidate.group === subscription.group);
		if (membershipWillEnd && !this.#isBroker && this.#clientSocket && !this.#clientSocket.destroyed) {
			await this.#sendUnsubscribeToBroker(topic, subscription.group);
			if ([...subscriptions.values()].some((candidate) => candidate.callback !== cb && candidate.group === subscription.group)) await this.#sendSubscribeToBroker(topic, subscription.group);
		}
		subscriptions.delete(cb);
		if (subscriptions.size === 0) this.#subscriptions.delete(topic);
		if (membershipWillEnd && subscription.group !== void 0) {
			this.#localGroupCursors.delete(membershipKey(topic, subscription.group));
			this.#evictBrokerGroupCursor(topic, subscription.group);
		}
	}
	async flush() {
		await Promise.allSettled([...this.#pendingWrites]);
	}
	#hasLocalMembership(topic, group) {
		return [...this.#subscriptions.get(topic)?.values() ?? []].some((subscription) => subscription.group === group);
	}
	#localGroups(topic) {
		return [...new Set([...this.#subscriptions.get(topic)?.values() ?? []].map((subscription) => subscription.group).filter((group) => group !== void 0))];
	}
	async close() {
		this.#closed = true;
		this.#subscriptions.clear();
		this.#localGroupCursors.clear();
		this.#brokerGroupCursors.clear();
		this.#clientSocket?.destroy();
		this.#clientSocket = void 0;
		this.#rejectMembershipWaiters(/* @__PURE__ */ new Error("UnixSocketPubSub is closed"));
		const clientClosures = [...this.#brokerClients.values()].map((client) => new Promise((resolve) => {
			if (client.socket.destroyed) {
				this.#removeBrokerClient(client);
				resolve();
				return;
			}
			client.socket.once("close", resolve);
			this.#removeBrokerClient(client);
		}));
		await Promise.allSettled(clientClosures);
		if (this.#server) {
			await new Promise((resolve) => this.#server?.close(() => resolve()));
			this.#server = void 0;
		}
		if (this.#isBroker) await (0, fs_promises.unlink)(this.socketPath).catch(() => {});
		this.#isBroker = false;
	}
	async #ensureStarted(forceReconnect = false) {
		if (this.#closed) throw new Error("UnixSocketPubSub is closed");
		if (!forceReconnect && (this.#isBroker || this.#clientSocket && !this.#clientSocket.destroyed)) return;
		if (this.#starting) return this.#starting;
		this.#starting = this.#start(forceReconnect).finally(() => {
			this.#starting = void 0;
		});
		return this.#starting;
	}
	async #start(forceReconnect) {
		if (forceReconnect) {
			this.#clientSocket?.destroy();
			this.#clientSocket = void 0;
			this.#isBroker = false;
		}
		this.#throwIfClosed();
		await (0, fs_promises.mkdir)((0, path.dirname)(this.socketPath), { recursive: true });
		this.#throwIfClosed();
		try {
			await this.#listen();
			this.#throwIfClosed();
			this.#isBroker = true;
			return;
		} catch (error) {
			if (this.#closed) {
				await this.close();
				throw new Error("UnixSocketPubSub is closed");
			}
			const code = error.code;
			if (code !== "EADDRINUSE" && code !== "EEXIST") throw error;
		}
		try {
			await this.#connectClient();
			this.#throwIfClosed();
		} catch (error) {
			if (this.#closed) {
				await this.close();
				throw new Error("UnixSocketPubSub is closed");
			}
			const code = error.code;
			if (code === "ECONNREFUSED" || code === "ENOENT" || code === "ENOTSOCK") {
				this.#throwIfClosed();
				await this.#electBroker();
				return;
			}
			throw error;
		}
	}
	#throwIfClosed() {
		if (this.#closed) throw new Error("UnixSocketPubSub is closed");
	}
	#listen() {
		return new Promise((resolve, reject) => {
			const server = net.default.createServer((socket) => this.#handleBrokerClient(socket));
			const onError = (error) => {
				server.off("listening", onListening);
				reject(error);
			};
			const onListening = () => {
				server.off("error", onError);
				this.#server = server;
				resolve();
			};
			server.once("error", onError);
			server.once("listening", onListening);
			server.listen(this.socketPath);
		});
	}
	#connectClient() {
		return new Promise((resolve, reject) => {
			const socket = net.default.createConnection(this.socketPath);
			const onError = (error) => {
				socket.off("connect", onConnect);
				reject(error);
			};
			const onConnect = () => {
				socket.off("error", onError);
				this.#clientSocket = socket;
				this.#isBroker = false;
				readFrames(socket, (frame) => this.#handleServerFrame(frame), this.#maxInboundFrameBytes);
				socket.on("close", () => this.#handleClientDisconnect(socket, /* @__PURE__ */ new Error("UnixSocketPubSub broker connection closed")));
				socket.on("error", (error) => this.#handleClientDisconnect(socket, error));
				this.#resubscribeClient().then(resolve, reject);
			};
			socket.once("error", onError);
			socket.once("connect", onConnect);
		});
	}
	async #resubscribeClient() {
		for (const [topic, subscriptions] of this.#subscriptions) {
			const groups = new Set([...subscriptions.values()].map((subscription) => subscription.group));
			for (const group of groups) await this.#sendSubscribeToBroker(topic, group);
		}
	}
	#handleClientDisconnect(socket, error) {
		if (this.#clientSocket !== socket) return;
		this.#clientSocket = void 0;
		this.#rejectMembershipWaiters(error);
		if (!this.#closed) this.#recoverClientConnection();
	}
	async #recoverClientConnection() {
		if (this.#recovering) return this.#recovering;
		this.#recovering = this.#recoverClientConnectionLoop().finally(() => {
			this.#recovering = void 0;
		});
		return this.#recovering;
	}
	async #recoverClientConnectionLoop() {
		while (!this.#closed && !this.#isBroker && !(this.#clientSocket && !this.#clientSocket.destroyed)) try {
			await this.#ensureStarted(true);
			return;
		} catch {
			if (this.#closed) return;
			await new Promise((resolve) => setTimeout(resolve, 10));
		}
	}
	/**
	* Serializes broker election across processes using an exclusive lock file.
	* Only the lock winner unlinks the stale socket and listens; losers wait
	* then connect as clients to the newly elected broker.
	*/
	async #electBroker() {
		const lockPath = this.socketPath + ".elect";
		let lockFd;
		try {
			lockFd = await (0, fs_promises.open)(lockPath, "wx");
		} catch (e) {
			if (e.code === "EEXIST") {
				if (await this.#isElectionLockStale(lockPath)) {
					await (0, fs_promises.unlink)(lockPath).catch(() => {});
					throw new Error("Stale broker election lock removed");
				}
				await new Promise((resolve) => setTimeout(resolve, 150));
				try {
					await this.#connectClient();
					this.#throwIfClosed();
					return;
				} catch {
					throw new Error("Broker election in progress by another process");
				}
			}
			throw e;
		}
		try {
			try {
				await this.#connectClient();
				this.#throwIfClosed();
				return;
			} catch {}
			await (0, fs_promises.unlink)(this.socketPath).catch(() => {});
			this.#throwIfClosed();
			await this.#listen();
			this.#throwIfClosed();
			this.#isBroker = true;
		} finally {
			await lockFd.close().catch(() => {});
			await (0, fs_promises.unlink)(lockPath).catch(() => {});
		}
	}
	async #isElectionLockStale(lockPath) {
		try {
			const lockStat = await (0, fs_promises.stat)(lockPath);
			return Date.now() - lockStat.mtimeMs > 2e3;
		} catch {
			return true;
		}
	}
	async #sendSubscribeToBroker(topic, group) {
		await this.#sendMembershipFrameToBroker({
			type: "subscribe",
			topic,
			group
		}, this.#subscribeWaiters);
	}
	async #sendUnsubscribeToBroker(topic, group) {
		await this.#sendMembershipFrameToBroker({
			type: "unsubscribe",
			topic,
			group
		}, this.#unsubscribeWaiters);
	}
	async #sendMembershipFrameToBroker(frame, waiterMap) {
		const key = membershipKey(frame.topic, frame.group);
		const acknowledged = new Promise((resolve, reject) => {
			const waiters = waiterMap.get(key) ?? [];
			waiters.push({
				resolve,
				reject
			});
			waiterMap.set(key, waiters);
		});
		try {
			await this.#sendToBroker(frame);
		} catch (error) {
			this.#settleMembershipWaiters(waiterMap, key, error instanceof Error ? error : new Error(String(error)));
		}
		const ackTimeout = setTimeout(() => {
			this.#settleMembershipWaiters(waiterMap, key);
		}, this.#membershipAckTimeoutMs);
		ackTimeout.unref?.();
		try {
			await acknowledged;
		} finally {
			clearTimeout(ackTimeout);
		}
	}
	#waitForPendingMembershipAcknowledgement(waiterMap, key) {
		const waiters = waiterMap.get(key);
		if (!waiters) return Promise.resolve();
		return new Promise((resolve, reject) => {
			waiters.push({
				resolve,
				reject
			});
		});
	}
	#settleMembershipWaiters(waiterMap, key, error) {
		const waiters = waiterMap.get(key);
		waiterMap.delete(key);
		if (error) {
			waiters?.forEach((waiter) => waiter.reject(error));
			return;
		}
		waiters?.forEach((waiter) => waiter.resolve());
	}
	#rejectMembershipWaiters(error) {
		for (const waiterMap of [this.#subscribeWaiters, this.#unsubscribeWaiters]) for (const key of waiterMap.keys()) this.#settleMembershipWaiters(waiterMap, key, error);
	}
	#handleBrokerClient(socket) {
		const client = {
			socket,
			subscriptions: /* @__PURE__ */ new Set(),
			writeChain: Promise.resolve(),
			queuedBytes: 0
		};
		this.#brokerClients.set(socket, client);
		readFrames(socket, (frame) => {
			const clientFrame = frame;
			if (clientFrame.type === "subscribe") {
				client.subscriptions.add(membershipKey(clientFrame.topic, clientFrame.group));
				this.#enqueueBrokerClientWrite(client, {
					type: "subscribed",
					topic: clientFrame.topic,
					...clientFrame.group !== void 0 ? { group: clientFrame.group } : {}
				});
			} else if (clientFrame.type === "unsubscribe") {
				client.subscriptions.delete(membershipKey(clientFrame.topic, clientFrame.group));
				if (clientFrame.group !== void 0) this.#evictBrokerGroupCursor(clientFrame.topic, clientFrame.group);
				this.#enqueueBrokerClientWrite(client, {
					type: "unsubscribed",
					topic: clientFrame.topic,
					...clientFrame.group !== void 0 ? { group: clientFrame.group } : {}
				});
			} else if (clientFrame.type === "publish") this.#publishFromBroker(clientFrame.topic, clientFrame.event, client, clientFrame.localOnly);
		}, this.#maxInboundFrameBytes);
		socket.on("close", () => this.#removeBrokerClient(client));
		socket.on("error", () => this.#removeBrokerClient(client));
	}
	#enqueueBrokerClientWrite(client, frame) {
		if (this.#brokerClients.get(client.socket) !== client || client.socket.destroyed) return;
		const serializedFrame = serializeFrame(frame);
		const queuedBytes = Buffer.byteLength(serializedFrame);
		if (client.queuedBytes + queuedBytes > this.#maxRemoteClientQueuedBytes) {
			this.#removeBrokerClient(client);
			return;
		}
		client.queuedBytes += queuedBytes;
		const write = client.writeChain.catch(() => {}).then(async () => {
			if (this.#brokerClients.get(client.socket) !== client || client.socket.destroyed) return;
			await writeSerializedFrame(client.socket, serializedFrame);
		}).catch(() => {
			this.#removeBrokerClient(client);
		}).finally(() => {
			client.queuedBytes = Math.max(0, client.queuedBytes - queuedBytes);
		});
		client.writeChain = write;
		this.#pendingWrites.add(write);
		write.finally(() => this.#pendingWrites.delete(write));
	}
	#evictBrokerGroupCursor(topic, group) {
		const key = membershipKey(topic, group);
		if (this.#hasLocalMembership(topic, group)) return;
		if ([...this.#brokerClients.values()].some((client) => client.subscriptions.has(key))) return;
		this.#brokerGroupCursors.delete(key);
	}
	#removeBrokerClient(client) {
		if (this.#brokerClients.get(client.socket) !== client) return;
		const subscriptions = [...client.subscriptions];
		this.#brokerClients.delete(client.socket);
		client.subscriptions.clear();
		for (const subscription of subscriptions) {
			const [topic, group] = JSON.parse(subscription);
			if (group !== null) this.#evictBrokerGroupCursor(topic, group);
		}
		client.queuedBytes = 0;
		client.writeChain = Promise.resolve();
		if (!client.socket.destroyed) client.socket.destroy();
	}
	#handleServerFrame(frame) {
		if (frame.type === "subscribed") {
			this.#settleMembershipWaiters(this.#subscribeWaiters, membershipKey(frame.topic, frame.group));
			return;
		}
		if (frame.type === "unsubscribed") {
			this.#settleMembershipWaiters(this.#unsubscribeWaiters, membershipKey(frame.topic, frame.group));
			return;
		}
		if (frame.type !== "event") return;
		if (frame.group !== void 0) this.#deliverLocalGroup(frame.topic, frame.group, frame.event);
		else this.#deliverLocalFanout(frame.topic, frame.event);
	}
	async #publishFromBroker(topic, event, sourceClient, localOnly) {
		const brokerEvent = {
			...event,
			id: (0, crypto.randomUUID)(),
			createdAt: /* @__PURE__ */ new Date(),
			deliveryAttempt: 1
		};
		this.#deliverLocalFanout(topic, brokerEvent);
		if (localOnly) {
			if (sourceClient && sourceClient.subscriptions.has(membershipKey(topic)) && !sourceClient.socket.destroyed) this.#enqueueBrokerClientWrite(sourceClient, {
				type: "event",
				topic,
				event: brokerEvent
			});
			return;
		}
		const fanoutFrame = {
			type: "event",
			topic,
			event: brokerEvent
		};
		for (const client of this.#brokerClients.values()) {
			if (!client.subscriptions.has(membershipKey(topic)) || client.socket.destroyed) continue;
			this.#enqueueBrokerClientWrite(client, fanoutFrame);
		}
		const groups = new Set(this.#localGroups(topic));
		for (const client of this.#brokerClients.values()) for (const subscription of client.subscriptions) {
			const [subscriptionTopic, group] = JSON.parse(subscription);
			if (subscriptionTopic === topic && group !== null) groups.add(group);
		}
		for (const group of groups) {
			const members = [];
			if (this.#hasLocalMembership(topic, group)) members.push("local");
			for (const client of this.#brokerClients.values()) if (client.subscriptions.has(membershipKey(topic, group)) && !client.socket.destroyed) members.push(client);
			if (members.length === 0) continue;
			const key = membershipKey(topic, group);
			const cursor = this.#brokerGroupCursors.get(key) ?? 0;
			const member = members[cursor % members.length];
			this.#brokerGroupCursors.set(key, (cursor + 1) % members.length);
			if (member === "local") this.#deliverLocalGroup(topic, group, brokerEvent);
			else this.#enqueueBrokerClientWrite(member, {
				type: "event",
				topic,
				event: brokerEvent,
				group
			});
		}
	}
	#deliverLocal(topic, event) {
		this.#deliverLocalFanout(topic, event);
		for (const group of this.#localGroups(topic)) this.#deliverLocalGroup(topic, group, event);
	}
	#deliverLocalFanout(topic, event) {
		for (const subscription of this.#subscriptions.get(topic)?.values() ?? []) {
			if (subscription.group !== void 0) continue;
			this.#invokeLocalCallback(topic, event, subscription.callback, subscription.group, 0);
		}
	}
	#deliverLocalGroup(topic, group, event) {
		const members = [...this.#subscriptions.get(topic)?.values() ?? []].filter((subscription) => subscription.group === group);
		if (members.length === 0) return;
		const key = membershipKey(topic, group);
		const cursor = this.#localGroupCursors.get(key) ?? 0;
		const member = members[cursor % members.length];
		this.#localGroupCursors.set(key, (cursor + 1) % members.length);
		this.#invokeLocalCallback(topic, event, member.callback, group, 0);
	}
	#invokeLocalCallback(topic, event, cb, group, attempt) {
		let nacked = false;
		const nack = async () => {
			if (nacked || this.#closed) return;
			nacked = true;
			if (attempt >= 6) return;
			const currentSubscription = this.#subscriptions.get(topic)?.get(cb);
			if (!currentSubscription || currentSubscription.group !== group) return;
			setTimeout(() => {
				if (this.#closed) return;
				const currentSubscription = this.#subscriptions.get(topic)?.get(cb);
				if (!currentSubscription || currentSubscription.group !== group) return;
				const redeliveredEvent = {
					...event,
					deliveryAttempt: (event.deliveryAttempt ?? 1) + 1
				};
				this.#invokeLocalCallback(topic, redeliveredEvent, cb, group, attempt + 1);
			}, REDELIVERY_DELAY_MS * (attempt + 1)).unref?.();
		};
		try {
			const result = cb(event, async () => {}, nack);
			if (result && typeof result.catch === "function") result.catch(() => {});
		} catch {}
	}
	async #sendToBroker(frame) {
		const maxRetries = 3;
		let lastError;
		for (let attempt = 0; attempt <= maxRetries; attempt++) try {
			if (attempt === 0) await this.#sendToActiveBroker(frame);
			else {
				if (this.#closed) throw lastError;
				const failedSocket = this.#clientSocket;
				this.#clientSocket = void 0;
				failedSocket?.destroy();
				await this.#ensureStarted(true);
				await this.#sendToActiveBroker(frame);
			}
			return;
		} catch (error) {
			lastError = error;
			if (this.#closed) throw error;
			const code = error?.code;
			if (!(code === "EPIPE" || code === "ECONNRESET" || code === "ENOTCONN" || error?.message?.includes("socket closed before write completed") || error?.message?.includes("broker connection closed") || error?.message?.includes("not connected to a broker")) || attempt === maxRetries) throw error;
			await new Promise((resolve) => setTimeout(resolve, 10 * (attempt + 1)));
		}
	}
	async #sendToActiveBroker(frame) {
		const socket = this.#clientSocket;
		if (!socket || socket.destroyed) await this.#ensureStarted(true);
		if (this.#isBroker) {
			await this.#handlePromotedBrokerFrame(frame);
			return;
		}
		const activeSocket = this.#clientSocket;
		if (!activeSocket || activeSocket.destroyed) throw new Error("UnixSocketPubSub is not connected to a broker");
		await writeFrame(activeSocket, frame);
	}
	async #handlePromotedBrokerFrame(frame) {
		if (frame.type === "subscribe") this.#settleMembershipWaiters(this.#subscribeWaiters, membershipKey(frame.topic, frame.group));
		else if (frame.type === "unsubscribe") this.#settleMembershipWaiters(this.#unsubscribeWaiters, membershipKey(frame.topic, frame.group));
		else if (frame.type === "publish") await this.#publishFromBroker(frame.topic, frame.event);
	}
};
//#endregion
exports.CachingPubSub = require_topics.CachingPubSub;
exports.EventEmitterPubSub = require_event_emitter.EventEmitterPubSub;
exports.MAX_LOCAL_REDELIVERIES = MAX_LOCAL_REDELIVERIES;
exports.NoopLeaseProvider = require_event_emitter.NoopLeaseProvider;
exports.PubSub = require_event_emitter.PubSub;
exports.RUN_LOCAL_TOPIC_PREFIXES = require_topics.RUN_LOCAL_TOPIC_PREFIXES;
exports.UnixSocketPubSub = UnixSocketPubSub;
exports.isLeaseProvider = require_event_emitter.isLeaseProvider;
exports.isRunLocalTopic = require_topics.isRunLocalTopic;
exports.withCaching = require_topics.withCaching;

//# sourceMappingURL=index.cjs.map