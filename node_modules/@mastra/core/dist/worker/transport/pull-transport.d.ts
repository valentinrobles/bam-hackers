import type { PubSub } from '../../events/pubsub.js';
import type { IMastraLogger } from '../../logger/index.js';
import type { EventRouter, WorkerTransport, WorkerTransportStopOptions } from './transport.js';
export declare class PullTransport implements WorkerTransport {
    #private;
    constructor({ pubsub, group, topic, logger, drainTimeout, }: {
        pubsub: PubSub;
        group: string;
        /** Pubsub topic to subscribe to. Defaults to the workflows topic. */
        topic?: string;
        logger?: IMastraLogger;
        /** Max time stop() waits for in-flight route() calls, in ms. Defaults to 5000. */
        drainTimeout?: number;
    });
    start(router: EventRouter): Promise<void>;
    stop(options?: WorkerTransportStopOptions): Promise<void>;
}
//# sourceMappingURL=pull-transport.d.ts.map