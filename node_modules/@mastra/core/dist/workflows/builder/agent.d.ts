import { Agent, type AgentConfig } from '../../agent/index.js';
export interface WorkflowBuilderAgentOptions<TAgentId extends string> extends Omit<AgentConfig<TAgentId>, 'instructions'> {
    surfaceInstructions: string;
}
/**
 * Creates a Workflow Builder agent with the shared composition contract and a
 * concrete surface policy for resource discovery and mutation semantics.
 */
export declare function createWorkflowBuilderAgent<TAgentId extends string>({ surfaceInstructions, ...config }: WorkflowBuilderAgentOptions<TAgentId>): Agent<TAgentId>;
//# sourceMappingURL=agent.d.ts.map