import type { ActorSignal } from '../../auth/ee/index.js';
import type { RequestContext } from '../../di/index.js';
import type { PubSub } from '../../events/pubsub.js';
import type { ObservabilityContext } from '../../observability/index.js';
import type { DefaultExecutionEngine } from '../default.js';
import type { ConditionFunction, LoopConditionFunction } from '../step.js';
import type { DefaultEngineType, ExecutionContext, ForeachOptions, OutputWriter, RestartExecutionParams, SerializedStepFlowEntry, SingleStepEntry, StepFlowEntry, StepResult, TimeTravelExecutionParams } from '../types.js';
/**
 * Builds the display name for a control-flow container span. When the graph
 * entry carries an authored `id`, it is used so observability tools can
 * identify the specific operation (e.g. `parallel: 'check-document'`).
 * Otherwise the generic structural name is used as a fallback.
 */
export declare function getControlFlowSpanName(entry: {
    id?: string;
}, fallbackName: string): string;
/**
 * Exposes the authored entry identity and metadata on container span
 * attributes so processors and exporters can identify the operation without
 * relying on structural matching. Only includes fields that are present.
 */
export declare function getControlFlowIdentityAttributes(entry: {
    id?: string;
    description?: string;
    metadata?: Record<string, any>;
}): Record<string, any>;
export interface ExecuteParallelParams extends ObservabilityContext {
    workflowId: string;
    runId: string;
    resourceId?: string;
    entry: {
        type: 'parallel';
        id?: string;
        description?: string;
        metadata?: Record<string, any>;
        steps: SingleStepEntry[];
    };
    serializedStepGraph: SerializedStepFlowEntry[];
    prevStep: StepFlowEntry;
    stepResults: Record<string, StepResult<any, any, any, any>>;
    restart?: RestartExecutionParams;
    timeTravel?: TimeTravelExecutionParams;
    resume?: {
        steps: string[];
        stepResults: Record<string, StepResult<any, any, any, any>>;
        resumePayload: any;
        resumePath: number[];
    };
    executionContext: ExecutionContext;
    pubsub: PubSub;
    abortController: AbortController;
    requestContext: RequestContext;
    actor?: ActorSignal;
    outputWriter?: OutputWriter;
    disableScorers?: boolean;
    perStep?: boolean;
}
export declare function executeParallel(engine: DefaultExecutionEngine, params: ExecuteParallelParams): Promise<StepResult<any, any, any, any>>;
export interface ExecuteConditionalParams extends ObservabilityContext {
    workflowId: string;
    runId: string;
    resourceId?: string;
    serializedStepGraph: SerializedStepFlowEntry[];
    entry: {
        type: 'conditional';
        id?: string;
        description?: string;
        metadata?: Record<string, any>;
        steps: SingleStepEntry[];
        conditions: ConditionFunction<any, any, any, any, any, DefaultEngineType>[];
    };
    prevOutput: any;
    stepResults: Record<string, StepResult<any, any, any, any>>;
    resume?: {
        steps: string[];
        stepResults: Record<string, StepResult<any, any, any, any>>;
        resumePayload: any;
        resumePath: number[];
    };
    restart?: RestartExecutionParams;
    timeTravel?: TimeTravelExecutionParams;
    executionContext: ExecutionContext;
    pubsub: PubSub;
    abortController: AbortController;
    requestContext: RequestContext;
    actor?: ActorSignal;
    outputWriter?: OutputWriter;
    disableScorers?: boolean;
    perStep?: boolean;
}
export declare function executeConditional(engine: DefaultExecutionEngine, params: ExecuteConditionalParams): Promise<StepResult<any, any, any, any>>;
export interface ExecuteLoopParams extends ObservabilityContext {
    workflowId: string;
    runId: string;
    resourceId?: string;
    entry: {
        type: 'loop';
        id?: string;
        description?: string;
        metadata?: Record<string, any>;
        step: SingleStepEntry;
        condition: LoopConditionFunction<any, any, any, any, any, DefaultEngineType>;
        loopType: 'dowhile' | 'dountil';
    };
    prevStep: StepFlowEntry;
    prevOutput: any;
    stepResults: Record<string, StepResult<any, any, any, any>>;
    restart?: RestartExecutionParams;
    timeTravel?: TimeTravelExecutionParams;
    resume?: {
        steps: string[];
        stepResults: Record<string, StepResult<any, any, any, any>>;
        resumePayload: any;
        resumePath: number[];
    };
    executionContext: ExecutionContext;
    pubsub: PubSub;
    abortController: AbortController;
    requestContext: RequestContext;
    actor?: ActorSignal;
    outputWriter?: OutputWriter;
    disableScorers?: boolean;
    serializedStepGraph: SerializedStepFlowEntry[];
    perStep?: boolean;
}
export declare function executeLoop(engine: DefaultExecutionEngine, params: ExecuteLoopParams): Promise<StepResult<any, any, any, any>>;
export interface ExecuteForeachParams extends ObservabilityContext {
    workflowId: string;
    runId: string;
    resourceId?: string;
    entry: {
        type: 'foreach';
        id?: string;
        description?: string;
        metadata?: Record<string, any>;
        step: SingleStepEntry;
        opts: ForeachOptions;
    };
    prevStep: StepFlowEntry;
    prevOutput: any;
    stepResults: Record<string, StepResult<any, any, any, any>>;
    restart?: RestartExecutionParams;
    timeTravel?: TimeTravelExecutionParams;
    resume?: {
        steps: string[];
        stepResults: Record<string, StepResult<any, any, any, any>>;
        resumePayload: any;
        resumePath: number[];
        forEachIndex?: number;
    };
    executionContext: ExecutionContext;
    pubsub: PubSub;
    abortController: AbortController;
    requestContext: RequestContext;
    actor?: ActorSignal;
    outputWriter?: OutputWriter;
    disableScorers?: boolean;
    serializedStepGraph: SerializedStepFlowEntry[];
    perStep?: boolean;
}
export declare function executeForeach(engine: DefaultExecutionEngine, params: ExecuteForeachParams): Promise<StepResult<any, any, any, any>>;
//# sourceMappingURL=control-flow.d.ts.map