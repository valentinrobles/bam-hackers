Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils = require("../utils-7WDNT3Ca.cjs");
let fs = require("fs");
let fs_promises = require("fs/promises");
let path = require("path");
let module$1 = require("module");
//#region src/validator/custom-resolver.ts
const STUB_PREFIX = "mastra-stub:";
let _stubbedExternals = null;
function getStubbedExternals() {
	if (_stubbedExternals === null) try {
		_stubbedExternals = JSON.parse(process.env.STUBBED_EXTERNALS || "[]");
	} catch {
		_stubbedExternals = [];
	}
	return _stubbedExternals;
}
const cache = /* @__PURE__ */ new Map();
/**
* Check if a module is a Node.js builtin module
* @param specifier - Module specifier
* @returns True if it's a builtin module
*/
function isBuiltinModule(specifier) {
	return module$1.builtinModules.includes(specifier) || specifier.startsWith("node:") || module$1.builtinModules.includes(specifier.replace(/^node:/, ""));
}
/**
* Check if a module specifier is a relative or absolute path
* @param specifier - Module specifier
* @returns True if it's a relative or absolute path
*/
function isRelativePath(specifier) {
	return specifier.startsWith("./") || specifier.startsWith("../") || specifier.startsWith("/") || /^[a-zA-Z]:\\/.test(specifier);
}
/**
* Get the path to resolve any external packages from
*
* @param url
* @returns
*/
async function getParentPath(specifier, url) {
	if (!cache.size) {
		let moduleResolveMapLocation = process.env.MODULE_MAP;
		if (!moduleResolveMapLocation) moduleResolveMapLocation = (0, path.join)(process.cwd(), "module-resolve-map.json");
		let moduleResolveMap = {};
		if ((0, fs.existsSync)(moduleResolveMapLocation)) moduleResolveMap = JSON.parse(await (0, fs_promises.readFile)(moduleResolveMapLocation, "utf-8"));
		for (const [id, rest] of Object.entries(moduleResolveMap)) cache.set(id, rest);
	}
	const importers = cache.get(url);
	if (!importers) return null;
	const matchedPackage = Object.keys(importers).find((external) => require_utils.isDependencyPartOfPackage(specifier, external));
	if (!matchedPackage) return null;
	return importers[matchedPackage];
}
async function resolve(specifier, context, nextResolve) {
	if (isBuiltinModule(specifier)) return nextResolve(specifier, context);
	if (isRelativePath(specifier)) return nextResolve(specifier, context);
	const stubbedExternals = getStubbedExternals();
	if (stubbedExternals.length > 0) {
		if (stubbedExternals.some((ext) => require_utils.isDependencyPartOfPackage(specifier, ext))) return {
			url: `${STUB_PREFIX}${specifier}`,
			shortCircuit: true
		};
	}
	if (context.parentURL) {
		const parentPath = await getParentPath(specifier, context.parentURL);
		if (parentPath) return nextResolve(specifier, {
			...context,
			parentURL: parentPath
		});
	}
	return nextResolve(specifier, context);
}
async function load(url, context, nextLoad) {
	if (url.startsWith(STUB_PREFIX)) return {
		format: "module",
		source: "export default {}",
		shortCircuit: true
	};
	return nextLoad(url, context);
}
//#endregion
exports.load = load;
exports.resolve = resolve;

//# sourceMappingURL=custom-resolver.cjs.map