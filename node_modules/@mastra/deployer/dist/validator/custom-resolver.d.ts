import type { LoadHookContext, ResolveHookContext } from 'node:module';
export declare function resolve(specifier: string, context: ResolveHookContext, nextResolve: (specifier: string, context: ResolveHookContext) => Promise<{
    url: string;
}>): Promise<{
    url: string;
} | {
    url: string;
    shortCircuit: boolean;
}>;
export declare function load(url: string, context: LoadHookContext, nextLoad: (url: string, context: LoadHookContext) => Promise<{
    format: string;
    source: string;
}>): Promise<{
    format: string;
    source: string;
} | {
    format: string;
    source: string;
    shortCircuit: boolean;
}>;
//# sourceMappingURL=custom-resolver.d.ts.map