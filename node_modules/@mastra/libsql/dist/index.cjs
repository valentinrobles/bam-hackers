Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
let _libsql_client = require("@libsql/client");
let _mastra_core_error = require("@mastra/core/error");
let _mastra_core_storage = require("@mastra/core/storage");
let _mastra_core_utils = require("@mastra/core/utils");
let _mastra_core_vector = require("@mastra/core/vector");
let _mastra_core_vector_filter = require("@mastra/core/vector/filter");
let fs_promises = require("fs/promises");
let path = require("path");
let _mastra_core_base = require("@mastra/core/base");
let crypto$1 = require("crypto");
let _mastra_core_agent = require("@mastra/core/agent");
let _mastra_core_evals = require("@mastra/core/evals");
let _mastra_core_storage_domains_skills = require("@mastra/core/storage/domains/skills");
//#region src/shared/single-connection-client.ts
/**
* Whether `@libsql/client` backs this database with exactly one connection.
*
* An in-memory database exists only on the connection that opened it, and each
* embedded-replica connection carries its own sync state, so `@libsql/client`
* (>= 0.18.0) gives both a pool of one. Any `execute`/`batch` issued while an
* interactive `transaction()` holds that connection is rejected immediately
* with `TRANSACTION_ACTIVE` instead of waiting for the transaction to settle.
*/
function isSingleConnectionDatabase({ url, syncUrl }) {
	return url.includes(":memory:") || Boolean(syncUrl);
}
/**
* Wraps a single-connection client so client calls queue behind open
* transactions rather than failing with `TRANSACTION_ACTIVE`.
*
* `transaction()` takes the gate and releases it when the transaction commits,
* rolls back, or closes. `execute`, `batch`, `executeMultiple`, and `migrate`
* wait for the gate to be free before running but do not hold it — the driver
* executes them synchronously on the connection, so they cannot interleave
* with each other. Every other member passes through untouched.
*
* Callers must not issue client calls from inside their own open transaction
* (use `tx.execute`); such a call would wait for the transaction it is part of.
*/
function gateSingleConnectionClient(client) {
	let gate = Promise.resolve();
	const waitForGate = (run) => gate.then(run, run);
	const transaction = async (mode) => {
		let release;
		const held = new Promise((resolve) => {
			release = resolve;
		});
		const previous = gate;
		gate = previous.then(() => held, () => held);
		await previous.then(() => void 0, () => void 0);
		let tx;
		try {
			tx = await client.transaction(mode);
		} catch (error) {
			release();
			throw error;
		}
		return new Proxy(tx, { get(target, prop) {
			if (prop === "commit" || prop === "rollback") return async () => {
				try {
					await target[prop].call(target);
				} finally {
					release();
				}
			};
			if (prop === "close") return () => {
				try {
					target.close();
				} finally {
					release();
				}
			};
			const value = Reflect.get(target, prop);
			return typeof value === "function" ? value.bind(target) : value;
		} });
	};
	return new Proxy(client, { get(target, prop) {
		switch (prop) {
			case "transaction": return transaction;
			case "execute":
			case "batch":
			case "executeMultiple":
			case "migrate": return (...args) => waitForGate(() => target[prop].apply(target, args));
			default: {
				const value = Reflect.get(target, prop);
				return typeof value === "function" ? value.bind(target) : value;
			}
		}
	} });
}
//#endregion
//#region src/vector/filter.ts
/**
* Translates MongoDB-style filters to LibSQL compatible filters.
*
* Key differences from MongoDB:
*
* Logical Operators ($and, $or, $nor):
* - Can be used at the top level or nested within fields
* - Can take either a single condition or an array of conditions
*
*/
var LibSQLFilterTranslator = class extends _mastra_core_vector_filter.BaseFilterTranslator {
	getSupportedOperators() {
		return {
			..._mastra_core_vector_filter.BaseFilterTranslator.DEFAULT_OPERATORS,
			regex: [],
			custom: ["$contains", "$size"]
		};
	}
	translate(filter) {
		if (this.isEmpty(filter)) return filter;
		this.validateFilter(filter);
		return this.translateNode(filter);
	}
	translateNode(node, currentPath = "") {
		if (this.isRegex(node)) throw new Error("Direct regex pattern format is not supported in LibSQL");
		const withPath = (result) => currentPath ? { [currentPath]: result } : result;
		if (this.isPrimitive(node)) return withPath({ $eq: this.normalizeComparisonValue(node) });
		if (Array.isArray(node)) return withPath({ $in: this.normalizeArrayValues(node) });
		const entries = Object.entries(node);
		const result = {};
		for (const [key, value] of entries) {
			const newPath = currentPath ? `${currentPath}.${key}` : key;
			if (this.isLogicalOperator(key)) result[key] = Array.isArray(value) ? value.map((filter) => this.translateNode(filter)) : this.translateNode(value);
			else if (this.isOperator(key)) if (this.isArrayOperator(key) && !Array.isArray(value) && key !== "$elemMatch") result[key] = [value];
			else if (this.isBasicOperator(key) && Array.isArray(value)) result[key] = JSON.stringify(value);
			else result[key] = value;
			else if (typeof value === "object" && value !== null) if (Object.keys(value).some((k) => this.isOperator(k))) result[newPath] = this.translateNode(value);
			else Object.assign(result, this.translateNode(value, newPath));
			else result[newPath] = this.translateNode(value);
		}
		return result;
	}
};
//#endregion
//#region src/vector/sql-builder.ts
const createBasicOperator = (symbol) => {
	return (key, value) => {
		const jsonPath = getJsonPath(key);
		return {
			sql: `CASE 
        WHEN ? IS NULL THEN json_extract(metadata, ${jsonPath}) IS ${symbol === "=" ? "" : "NOT"} NULL
        ELSE json_extract(metadata, ${jsonPath}) ${symbol} ?
      END`,
			needsValue: true,
			transformValue: () => {
				return [value, value];
			}
		};
	};
};
const createNumericOperator = (symbol) => {
	return (key, value) => {
		const jsonPath = getJsonPath(key);
		if (typeof value === "number" || typeof value === "string" && !isNaN(Number(value)) && value.trim() !== "") return {
			sql: `CAST(json_extract(metadata, ${jsonPath}) AS NUMERIC) ${symbol} ?`,
			needsValue: true
		};
		else return {
			sql: `CAST(json_extract(metadata, ${jsonPath}) AS TEXT) ${symbol} ?`,
			needsValue: true
		};
	};
};
const validateJsonArray = (key) => {
	const jsonPath = getJsonPath(key);
	return `json_valid(json_extract(metadata, ${jsonPath}))
   AND json_type(json_extract(metadata, ${jsonPath})) = 'array'`;
};
const pattern = /json_extract\(metadata, '\$\.(?:"[^"]*"(?:\."[^"]*")*|[^']+)'\)/g;
function buildElemMatchConditions(value) {
	return Object.entries(value).map(([field, fieldValue]) => {
		if (field.startsWith("$")) {
			const { sql, values } = buildCondition$1("elem.value", { [field]: fieldValue }, "");
			return {
				sql: sql.replace(pattern, "elem.value"),
				values
			};
		} else if (typeof fieldValue === "object" && !Array.isArray(fieldValue)) {
			const { sql, values } = buildCondition$1(field, fieldValue, "");
			const jsonPath = parseJsonPathKey(field);
			return {
				sql: sql.replace(pattern, `json_extract(elem.value, '$.${jsonPath}')`),
				values
			};
		} else return {
			sql: `json_extract(elem.value, '$.${parseJsonPathKey(field)}') = ?`,
			values: [fieldValue]
		};
	});
}
const FILTER_OPERATORS = {
	$eq: createBasicOperator("="),
	$ne: createBasicOperator("!="),
	$gt: createNumericOperator(">"),
	$gte: createNumericOperator(">="),
	$lt: createNumericOperator("<"),
	$lte: createNumericOperator("<="),
	$in: (key, value) => {
		const jsonPath = getJsonPath(key);
		const arr = Array.isArray(value) ? value : [value];
		if (arr.length === 0) return {
			sql: "1 = 0",
			needsValue: true,
			transformValue: () => []
		};
		const paramPlaceholders = arr.map(() => "?").join(",");
		return {
			sql: `(
      CASE
        WHEN ${validateJsonArray(key)} THEN
          EXISTS (
            SELECT 1 FROM json_each(json_extract(metadata, ${jsonPath})) as elem
            WHERE elem.value IN (SELECT value FROM json_each(?))
          )
        ELSE json_extract(metadata, ${jsonPath}) IN (${paramPlaceholders})
      END
    )`,
			needsValue: true,
			transformValue: () => [JSON.stringify(arr), ...arr]
		};
	},
	$nin: (key, value) => {
		const jsonPath = getJsonPath(key);
		const arr = Array.isArray(value) ? value : [value];
		if (arr.length === 0) return {
			sql: "1 = 1",
			needsValue: true,
			transformValue: () => []
		};
		const paramPlaceholders = arr.map(() => "?").join(",");
		return {
			sql: `(
      CASE
        WHEN ${validateJsonArray(key)} THEN
          NOT EXISTS (
            SELECT 1 FROM json_each(json_extract(metadata, ${jsonPath})) as elem
            WHERE elem.value IN (SELECT value FROM json_each(?))
          )
        ELSE json_extract(metadata, ${jsonPath}) NOT IN (${paramPlaceholders})
      END
    )`,
			needsValue: true,
			transformValue: () => [JSON.stringify(arr), ...arr]
		};
	},
	$all: (key, value) => {
		const jsonPath = getJsonPath(key);
		let sql;
		const arrayValue = Array.isArray(value) ? value : [value];
		if (arrayValue.length === 0) sql = "1 = 0";
		else sql = `(
      CASE
        WHEN ${validateJsonArray(key)} THEN
          NOT EXISTS (
            SELECT value
            FROM json_each(?)
            WHERE value NOT IN (
              SELECT value
              FROM json_each(json_extract(metadata, ${jsonPath}))
            )
          )
        ELSE FALSE
      END
    )`;
		return {
			sql,
			needsValue: true,
			transformValue: () => {
				if (arrayValue.length === 0) return [];
				return [JSON.stringify(arrayValue)];
			}
		};
	},
	$elemMatch: (key, value) => {
		const jsonPath = getJsonPath(key);
		if (typeof value !== "object" || Array.isArray(value)) throw new Error("$elemMatch requires an object with conditions");
		const conditions = buildElemMatchConditions(value);
		return {
			sql: `(
        CASE
          WHEN ${validateJsonArray(key)} THEN
            EXISTS (
              SELECT 1
              FROM json_each(json_extract(metadata, ${jsonPath})) as elem
              WHERE ${conditions.map((c) => c.sql).join(" AND ")}
            )
          ELSE FALSE
        END
      )`,
			needsValue: true,
			transformValue: () => conditions.flatMap((c) => c.values)
		};
	},
	$exists: (key, value) => {
		const jsonPath = getJsonPath(key);
		return {
			sql: value === false ? `json_extract(metadata, ${jsonPath}) IS NULL` : `json_extract(metadata, ${jsonPath}) IS NOT NULL`,
			needsValue: false
		};
	},
	$and: (key) => ({
		sql: `(${key})`,
		needsValue: false
	}),
	$or: (key) => ({
		sql: `(${key})`,
		needsValue: false
	}),
	$not: (key) => ({
		sql: `NOT (${key})`,
		needsValue: false
	}),
	$nor: (key) => ({
		sql: `NOT (${key})`,
		needsValue: false
	}),
	$size: (key, value) => {
		const jsonPath = getJsonPath(key);
		return {
			sql: `(
    CASE
      WHEN json_type(json_extract(metadata, ${jsonPath})) = 'array' THEN
        json_array_length(json_extract(metadata, ${jsonPath})) = ?
      ELSE FALSE
    END
  )`,
			needsValue: true
		};
	},
	$contains: (key, value) => {
		const jsonPathKey = parseJsonPathKey(key);
		let sql;
		if (Array.isArray(value)) sql = `(
        SELECT ${validateJsonArray(jsonPathKey)}
        AND EXISTS (
          SELECT 1
          FROM json_each(json_extract(metadata, '$."${jsonPathKey}"')) as m
          WHERE m.value IN (SELECT value FROM json_each(?))
        )
      )`;
		else if (typeof value === "string") sql = `lower(json_extract(metadata, '$."${jsonPathKey}"')) LIKE '%' || lower(?) || '%' ESCAPE '\\'`;
		else sql = `json_extract(metadata, '$."${jsonPathKey}"') = ?`;
		return {
			sql,
			needsValue: true,
			transformValue: () => {
				if (Array.isArray(value)) return [JSON.stringify(value)];
				if (typeof value === "object" && value !== null) return [JSON.stringify(value)];
				if (typeof value === "string") return [escapeLikePattern$1(value)];
				return [value];
			}
		};
	}
};
function isFilterResult(obj) {
	return obj && typeof obj === "object" && typeof obj.sql === "string" && Array.isArray(obj.values);
}
const parseJsonPathKey = (key) => {
	const parsedKey = (0, _mastra_core_utils.parseFieldKey)(key);
	if (parsedKey.includes(".")) return parsedKey.split(".").map((segment) => `"${segment}"`).join(".");
	return parsedKey;
};
const getJsonPath = (key) => {
	return `'$.${parseJsonPathKey(key)}'`;
};
function escapeLikePattern$1(str) {
	return str.replace(/([%_\\])/g, "\\$1");
}
function buildFilterQuery(filter) {
	if (!filter) return {
		sql: "",
		values: []
	};
	const values = [];
	const conditions = Object.entries(filter).map(([key, value]) => {
		const condition = buildCondition$1(key, value, "");
		values.push(...condition.values);
		return condition.sql;
	}).join(" AND ");
	return {
		sql: conditions ? `WHERE ${conditions}` : "",
		values
	};
}
function buildCondition$1(key, value, parentPath) {
	if ([
		"$and",
		"$or",
		"$not",
		"$nor"
	].includes(key)) return handleLogicalOperator(key, value, parentPath);
	if (!value || typeof value !== "object") return {
		sql: `json_extract(metadata, ${getJsonPath(key)}) = ?`,
		values: [value]
	};
	return handleOperator(key, value);
}
function handleLogicalOperator(key, value, parentPath) {
	if (!value || Array.isArray(value) && value.length === 0) switch (key) {
		case "$and":
		case "$nor": return {
			sql: "true",
			values: []
		};
		case "$or": return {
			sql: "false",
			values: []
		};
		case "$not": throw new Error("$not operator cannot be empty");
		default: return {
			sql: "true",
			values: []
		};
	}
	if (key === "$not") {
		const conditions = Object.entries(value).map(([fieldKey, fieldValue]) => buildCondition$1(fieldKey, fieldValue, key));
		return {
			sql: `NOT (${conditions.map((c) => c.sql).join(" AND ")})`,
			values: conditions.flatMap((c) => c.values)
		};
	}
	const values = [];
	const joinOperator = key === "$or" || key === "$nor" ? "OR" : "AND";
	const joined = (Array.isArray(value) ? value.map((f) => {
		return (!!f ? Object.entries(f) : []).map(([k, v]) => buildCondition$1(k, v, key));
	}) : [buildCondition$1(key, value, parentPath)]).flat().map((c) => {
		values.push(...c.values);
		return c.sql;
	}).join(` ${joinOperator} `);
	return {
		sql: key === "$nor" ? `NOT (${joined})` : `(${joined})`,
		values
	};
}
function handleOperator(key, value) {
	if (typeof value === "object" && !Array.isArray(value)) {
		const results = Object.entries(value).map(([operator, operatorValue]) => operator === "$not" ? {
			sql: `NOT (${Object.entries(operatorValue).map(([op, val]) => processOperator(key, op, val).sql).join(" AND ")})`,
			values: Object.entries(operatorValue).flatMap(([op, val]) => processOperator(key, op, val).values)
		} : processOperator(key, operator, operatorValue));
		return {
			sql: `(${results.map((r) => r.sql).join(" AND ")})`,
			values: results.flatMap((r) => r.values)
		};
	}
	const [[operator, operatorValue] = []] = Object.entries(value);
	return processOperator(key, operator, operatorValue);
}
const processOperator = (key, operator, operatorValue) => {
	if (!operator.startsWith("$") || !FILTER_OPERATORS[operator]) throw new Error(`Invalid operator: ${operator}`);
	const operatorFn = FILTER_OPERATORS[operator];
	const operatorResult = operatorFn(key, operatorValue);
	if (!operatorResult.needsValue) return {
		sql: operatorResult.sql,
		values: []
	};
	const transformed = operatorResult.transformValue ? operatorResult.transformValue() : operatorValue;
	if (isFilterResult(transformed)) return transformed;
	return {
		sql: operatorResult.sql,
		values: Array.isArray(transformed) ? transformed : [transformed]
	};
};
//#endregion
//#region src/vector/write-lock.ts
const databaseWriteChains = /* @__PURE__ */ new Map();
async function getLocalFileDatabaseKey({ url, syncUrl, cwd }) {
	if (!url.startsWith("file:") || url.includes(":memory:") || syncUrl) return;
	const uriPath = url.slice(5).split(/[?#]/, 1)[0];
	const decodedPath = decodeURIComponent(uriPath);
	const absolutePath = (0, path.isAbsolute)(decodedPath) ? decodedPath : (0, path.resolve)(cwd, decodedPath);
	try {
		return await (0, fs_promises.realpath)(absolutePath);
	} catch (error) {
		if (!(error instanceof Error) || !("code" in error) || error.code !== "ENOENT") throw error;
	}
	return (0, path.join)(await (0, fs_promises.realpath)((0, path.dirname)(absolutePath)), (0, path.basename)(absolutePath));
}
function withLocalFileDatabaseWriteLock(key, fn) {
	if (!key) return fn();
	const result = (databaseWriteChains.get(key) ?? Promise.resolve()).then(fn, fn);
	const tail = result.then(() => void 0, () => void 0);
	databaseWriteChains.set(key, tail);
	tail.then(() => {
		if (databaseWriteChains.get(key) === tail) databaseWriteChains.delete(key);
	});
	return result;
}
//#endregion
//#region src/vector/index.ts
var LibSQLVector = class extends _mastra_core_vector.MastraVector {
	turso;
	maxRetries;
	initialBackoffMs;
	overFetchMultiplier;
	isMemoryDb;
	initialization;
	databaseKey;
	vectorIndexes = /* @__PURE__ */ new Set();
	constructor({ url, authToken, syncUrl, syncInterval, maxRetries = 5, initialBackoffMs = 100, vectorTopKOverFetchMultiplier = 10, id }) {
		super({ id });
		this.isMemoryDb = url.includes(":memory:");
		const isLocalDb = (url.startsWith("file:") || this.isMemoryDb) && !syncUrl;
		const cwd = process.cwd();
		const client = (0, _libsql_client.createClient)({
			url,
			syncUrl,
			authToken,
			syncInterval,
			...isLocalDb ? { timeout: 5e3 } : {}
		});
		this.turso = isSingleConnectionDatabase({
			url,
			syncUrl
		}) ? gateSingleConnectionClient(client) : client;
		this.maxRetries = maxRetries;
		this.initialBackoffMs = initialBackoffMs;
		if (!Number.isInteger(vectorTopKOverFetchMultiplier) || vectorTopKOverFetchMultiplier < 1) throw new Error("vectorTopKOverFetchMultiplier must be a positive integer");
		this.overFetchMultiplier = vectorTopKOverFetchMultiplier;
		this.initialization = this.initialize({
			url,
			syncUrl,
			cwd,
			isLocalDb
		});
	}
	async initialize({ url, syncUrl, cwd, isLocalDb }) {
		if (isLocalDb) {
			await this.applyLocalPragmas();
			this.databaseKey = await getLocalFileDatabaseKey({
				url,
				syncUrl,
				cwd
			});
		}
		if (!this.isMemoryDb) this.vectorIndexes = await this.discoverVectorIndexes();
	}
	async applyLocalPragmas() {
		for (const [label, sql] of [["journal_mode=WAL", "PRAGMA journal_mode=WAL;"], ["busy_timeout=5000", "PRAGMA busy_timeout = 5000;"]]) try {
			await this.turso.execute(sql);
			this.logger.debug(`LibSQLStore: PRAGMA ${label} set.`);
		} catch (err) {
			this.logger.warn(`LibSQLStore: Failed to set PRAGMA ${label}.`, err);
		}
	}
	async ensureInitialized() {
		await this.initialization;
	}
	async executeMutation(operation, isTransaction = false) {
		await this.ensureInitialized();
		return withLocalFileDatabaseWriteLock(this.databaseKey, () => this.executeWriteOperationWithRetry(operation, isTransaction));
	}
	/**
	* Closes the underlying libsql client, releasing this vector store's OS file handles.
	*
	* Safe to call more than once; subsequent calls are no-ops.
	*/
	async close() {
		await this.ensureInitialized();
		if (!this.turso.closed) this.turso.close();
	}
	async discoverVectorIndexes() {
		try {
			const result = await this.turso.execute({
				sql: `SELECT name FROM sqlite_master WHERE type='index' AND name LIKE '%_vector_idx'`,
				args: []
			});
			return new Set(result.rows.map((row) => row.name));
		} catch {
			return /* @__PURE__ */ new Set();
		}
	}
	async executeWriteOperationWithRetry(operation, isTransaction = false) {
		let attempts = 0;
		let backoff = this.initialBackoffMs;
		while (attempts < this.maxRetries) try {
			return await operation();
		} catch (error) {
			if (error.code === "SQLITE_BUSY" || error.code === "SQLITE_LOCKED" || error.code === "SQLITE_LOCKED_SHAREDCACHE" || error.message && error.message.toLowerCase().includes("database is locked") || error.message && error.message.toLowerCase().includes("database table is locked")) {
				attempts++;
				if (attempts >= this.maxRetries) {
					this.logger.error(`LibSQLVector: Operation failed after ${this.maxRetries} attempts due to: ${error.message}`, error);
					throw error;
				}
				this.logger.warn(`LibSQLVector: Attempt ${attempts} failed due to ${isTransaction ? "transaction " : ""}database lock. Retrying in ${backoff}ms...`);
				await new Promise((resolve) => setTimeout(resolve, backoff));
				backoff *= 2;
			} else throw error;
		}
		throw new Error("LibSQLVector: Max retries reached, but no error was re-thrown from the loop.");
	}
	transformFilter(filter) {
		return new LibSQLFilterTranslator().translate(filter);
	}
	hasVectorIndex(parsedIndexName) {
		return this.vectorIndexes.has(`${parsedIndexName}_vector_idx`);
	}
	async queryWithIndex(parsedIndexName, vectorStr, topK, filter, includeVector, minScore) {
		const { sql: filterQuery, values: filterValues } = buildFilterQuery(this.transformFilter(filter));
		const hasFilter = filterQuery.length > 0;
		const fetchCount = hasFilter ? topK * this.overFetchMultiplier : topK * 2;
		const embeddingSelect = includeVector ? ", vector_extract(t.embedding) as embedding" : "";
		const filterCondition = hasFilter ? filterQuery.replace(/^\s*WHERE\s+/i, "") : "";
		const query = `
      WITH candidates AS (
        SELECT t.vector_id AS id,
               (1 - vector_distance_cos(t.embedding, vector32(?))) AS score,
               t.metadata
               ${embeddingSelect}
        FROM vector_top_k('${parsedIndexName}_vector_idx', vector32(?), ?) AS v
        JOIN "${parsedIndexName}" AS t ON t.rowid = v.id
      )
      SELECT * FROM candidates
      ${hasFilter ? `WHERE ${filterCondition} AND score > ?` : "WHERE score > ?"}
      ORDER BY score DESC
      LIMIT ?`;
		const args = [
			vectorStr,
			vectorStr,
			fetchCount,
			...filterValues,
			minScore,
			topK
		];
		return (await this.turso.execute({
			sql: query,
			args
		})).rows.map(({ id, score, metadata, embedding }) => ({
			id,
			score,
			metadata: JSON.parse(metadata ?? "{}"),
			...includeVector && embedding && { vector: JSON.parse(embedding) }
		}));
	}
	async query({ indexName, queryVector, topK = 10, filter, includeVector = false, minScore = -1 }) {
		(0, _mastra_core_vector.validateTopK)("LIBSQL", topK);
		if (!queryVector) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "QUERY", "MISSING_VECTOR"),
			text: "queryVector is required for LibSQL queries. Metadata-only queries are not supported by this vector store.",
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { indexName }
		});
		if (!Array.isArray(queryVector) || !queryVector.every((x) => typeof x === "number" && Number.isFinite(x))) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "QUERY", "INVALID_ARGS"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { message: "queryVector must be an array of finite numbers" }
		});
		try {
			await this.ensureInitialized();
			const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
			const vectorStr = `[${queryVector.join(",")}]`;
			if (!this.isMemoryDb && this.hasVectorIndex(parsedIndexName)) try {
				const indexedResults = await this.queryWithIndex(parsedIndexName, vectorStr, topK, filter, includeVector, minScore);
				if (!filter || indexedResults.length >= topK) return indexedResults;
			} catch (err) {
				this.logger.warn("LibSQLVector: indexed query failed, falling back to brute-force", err);
			}
			const { sql: filterQuery, values: filterValues } = buildFilterQuery(this.transformFilter(filter));
			filterValues.push(minScore);
			filterValues.push(topK);
			const query = `
      WITH vector_scores AS (
        SELECT
          vector_id as id,
          (1-vector_distance_cos(embedding, '${vectorStr}')) as score,
          metadata
          ${includeVector ? ", vector_extract(embedding) as embedding" : ""}
        FROM ${parsedIndexName}
        ${filterQuery}
      )
      SELECT *
      FROM vector_scores
      WHERE score > ?
      ORDER BY score DESC
      LIMIT ?`;
			return (await this.turso.execute({
				sql: query,
				args: filterValues
			})).rows.map(({ id, score, metadata, embedding }) => ({
				id,
				score,
				metadata: JSON.parse(metadata ?? "{}"),
				...includeVector && embedding && { vector: JSON.parse(embedding) }
			}));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "QUERY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async upsert(args) {
		try {
			return await this.executeMutation(() => this.doUpsert(args), true);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPSERT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async doUpsert({ indexName, vectors, metadata, ids }) {
		(0, _mastra_core_vector.validateUpsertInput)("LIBSQL", vectors, metadata, ids);
		const tx = await this.turso.transaction("write");
		try {
			const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
			const vectorIds = ids || vectors.map(() => crypto.randomUUID());
			for (let i = 0; i < vectors.length; i++) {
				const query = `
            INSERT INTO ${parsedIndexName} (vector_id, embedding, metadata)
            VALUES (?, vector32(?), ?)
            ON CONFLICT(vector_id) DO UPDATE SET
              embedding = vector32(?),
              metadata = ?
          `;
				await tx.execute({
					sql: query,
					args: [
						vectorIds[i],
						JSON.stringify(vectors[i]),
						JSON.stringify(metadata?.[i] || {}),
						JSON.stringify(vectors[i]),
						JSON.stringify(metadata?.[i] || {})
					]
				});
			}
			await tx.commit();
			return vectorIds;
		} catch (error) {
			!tx.closed && await tx.rollback();
			if (error instanceof Error && error.message?.includes("dimensions are different")) {
				const match = error.message.match(/dimensions are different: (\d+) != (\d+)/);
				if (match) {
					const [, actual, expected] = match;
					throw new Error(`Vector dimension mismatch: Index "${indexName}" expects ${expected} dimensions but got ${actual} dimensions. Either use a matching embedding model or delete and recreate the index with the new dimension.`);
				}
			}
			throw error;
		}
	}
	async createIndex(args) {
		try {
			return await this.executeMutation(() => this.doCreateIndex(args));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "CREATE_INDEX", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					indexName: args.indexName,
					dimension: args.dimension
				}
			}, error);
		}
	}
	async doCreateIndex({ indexName, dimension }) {
		if (!Number.isInteger(dimension) || dimension <= 0) throw new Error("Dimension must be a positive integer");
		const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
		await this.turso.execute({
			sql: `
          CREATE TABLE IF NOT EXISTS ${parsedIndexName} (
            id SERIAL PRIMARY KEY,
            vector_id TEXT UNIQUE NOT NULL,
            embedding F32_BLOB(${dimension}),
            metadata TEXT DEFAULT '{}'
          );
        `,
			args: []
		});
		await this.turso.execute({
			sql: `
          CREATE INDEX IF NOT EXISTS ${parsedIndexName}_vector_idx
          ON ${parsedIndexName} (libsql_vector_idx(embedding))
        `,
			args: []
		});
		this.vectorIndexes.add(`${parsedIndexName}_vector_idx`);
	}
	async deleteIndex(args) {
		try {
			return await this.executeMutation(() => this.doDeleteIndex(args));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_INDEX", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { indexName: args.indexName }
			}, error);
		}
	}
	async doDeleteIndex({ indexName }) {
		const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
		await this.turso.execute({
			sql: `DROP TABLE IF EXISTS ${parsedIndexName}`,
			args: []
		});
		this.vectorIndexes.delete(`${parsedIndexName}_vector_idx`);
	}
	async listIndexes() {
		try {
			await this.ensureInitialized();
			return (await this.turso.execute({
				sql: `
        SELECT name FROM sqlite_master 
        WHERE type='table' 
        AND sql LIKE '%F32_BLOB%';
      `,
				args: []
			})).rows.map((row) => row.name);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "LIST_INDEXES", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	/**
	* Retrieves statistics about a vector index.
	*
	* @param {string} indexName - The name of the index to describe
	* @returns A promise that resolves to the index statistics including dimension, count and metric
	*/
	async describeIndex({ indexName }) {
		try {
			await this.ensureInitialized();
			const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
			const tableInfo = await this.turso.execute({
				sql: `
        SELECT sql 
        FROM sqlite_master 
        WHERE type='table' 
        AND name = ?;
      `,
				args: [parsedIndexName]
			});
			if (!tableInfo.rows[0]?.sql) throw new Error(`Table ${parsedIndexName} not found`);
			const dimension = parseInt(tableInfo.rows[0].sql.match(/F32_BLOB\((\d+)\)/)?.[1] || "0");
			const countQuery = `
        SELECT COUNT(*) as count
        FROM ${parsedIndexName};
      `;
			return {
				dimension,
				count: (await this.turso.execute({
					sql: countQuery,
					args: []
				}))?.rows?.[0]?.count ?? 0,
				metric: "cosine"
			};
		} catch (e) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DESCRIBE_INDEX", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { indexName }
			}, e);
		}
	}
	/**
	* Updates a vector by its ID with the provided vector and/or metadata.
	*
	* @param indexName - The name of the index containing the vector.
	* @param id - The ID of the vector to update.
	* @param update - An object containing the vector and/or metadata to update.
	* @param update.vector - An optional array of numbers representing the new vector.
	* @param update.metadata - An optional record containing the new metadata.
	* @returns A promise that resolves when the update is complete.
	* @throws Will throw an error if no updates are provided or if the update operation fails.
	*/
	updateVector(args) {
		return this.executeMutation(() => this.doUpdateVector(args));
	}
	async doUpdateVector(params) {
		const { indexName, update } = params;
		const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
		if ("id" in params && params.id && "filter" in params && params.filter) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPDATE_VECTOR", "MUTUALLY_EXCLUSIVE"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { indexName },
			text: "id and filter are mutually exclusive - provide only one"
		});
		if (!update.vector && !update.metadata) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPDATE_VECTOR", "NO_PAYLOAD"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { indexName },
			text: "No updates provided"
		});
		const updates = [];
		const args = [];
		if (update.vector) {
			updates.push("embedding = vector32(?)");
			args.push(JSON.stringify(update.vector));
		}
		if (update.metadata) {
			updates.push("metadata = ?");
			args.push(JSON.stringify(update.metadata));
		}
		if (updates.length === 0) return;
		let whereClause;
		let whereValues;
		if ("id" in params && params.id) {
			whereClause = "vector_id = ?";
			whereValues = [params.id];
		} else if ("filter" in params && params.filter) {
			const filter = params.filter;
			if (!filter || Object.keys(filter).length === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPDATE_VECTOR", "EMPTY_FILTER"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { indexName },
				text: "Cannot update with empty filter"
			});
			const { sql: filterSql, values: filterValues } = buildFilterQuery(this.transformFilter(filter));
			if (!filterSql || filterSql.trim() === "") throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPDATE_VECTOR", "INVALID_FILTER"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { indexName },
				text: "Filter produced empty WHERE clause"
			});
			const normalizedCondition = filterSql.replace(/^\s*WHERE\s+/i, "").trim().toLowerCase();
			if ([
				"true",
				"1 = 1",
				"1=1"
			].includes(normalizedCondition)) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPDATE_VECTOR", "MATCH_ALL_FILTER"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: {
					indexName,
					filterSql: normalizedCondition
				},
				text: "Filter matches all vectors. Provide a specific filter to update targeted vectors."
			});
			whereClause = filterSql.replace(/^WHERE\s+/i, "");
			whereValues = filterValues;
		} else throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPDATE_VECTOR", "NO_TARGET"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { indexName },
			text: "Either id or filter must be provided"
		});
		const query = `
      UPDATE ${parsedIndexName}
      SET ${updates.join(", ")}
      WHERE ${whereClause};
    `;
		try {
			await this.turso.execute({
				sql: query,
				args: [...args, ...whereValues]
			});
		} catch (error) {
			const errorDetails = { indexName };
			if ("id" in params && params.id) errorDetails.id = params.id;
			if ("filter" in params && params.filter) errorDetails.filter = JSON.stringify(params.filter);
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "UPDATE_VECTOR", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: errorDetails
			}, error);
		}
	}
	/**
	* Deletes a vector by its ID.
	* @param indexName - The name of the index containing the vector.
	* @param id - The ID of the vector to delete.
	* @returns A promise that resolves when the deletion is complete.
	* @throws Will throw an error if the deletion operation fails.
	*/
	async deleteVector(args) {
		try {
			return await this.executeMutation(() => this.doDeleteVector(args));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTOR", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					indexName: args.indexName,
					...args.id && { id: args.id }
				}
			}, error);
		}
	}
	async doDeleteVector({ indexName, id }) {
		const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
		await this.turso.execute({
			sql: `DELETE FROM ${parsedIndexName} WHERE vector_id = ?`,
			args: [id]
		});
	}
	deleteVectors(args) {
		return this.executeMutation(() => this.doDeleteVectors(args));
	}
	async doDeleteVectors({ indexName, filter, ids }) {
		const parsedIndexName = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
		if (!filter && !ids) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTORS", "NO_TARGET"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { indexName },
			text: "Either filter or ids must be provided"
		});
		if (filter && ids) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTORS", "MUTUALLY_EXCLUSIVE"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { indexName },
			text: "Cannot provide both filter and ids - they are mutually exclusive"
		});
		let query;
		let values;
		if (ids) {
			if (ids.length === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTORS", "EMPTY_IDS"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { indexName },
				text: "Cannot delete with empty ids array"
			});
			query = `DELETE FROM ${parsedIndexName} WHERE vector_id IN (${ids.map(() => "?").join(", ")})`;
			values = ids;
		} else {
			if (!filter || Object.keys(filter).length === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTORS", "EMPTY_FILTER"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { indexName },
				text: "Cannot delete with empty filter. Use deleteIndex to delete all vectors."
			});
			const { sql: filterSql, values: filterValues } = buildFilterQuery(this.transformFilter(filter));
			if (!filterSql || filterSql.trim() === "") throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTORS", "INVALID_FILTER"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { indexName },
				text: "Filter produced empty WHERE clause"
			});
			const normalizedCondition = filterSql.replace(/^\s*WHERE\s+/i, "").trim().toLowerCase();
			if ([
				"true",
				"1 = 1",
				"1=1"
			].includes(normalizedCondition)) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTORS", "MATCH_ALL_FILTER"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: {
					indexName,
					filterSql: normalizedCondition
				},
				text: "Filter matches all vectors. Use deleteIndex to delete all vectors from an index."
			});
			query = `DELETE FROM ${parsedIndexName} ${filterSql}`;
			values = filterValues;
		}
		try {
			await this.turso.execute({
				sql: query,
				args: values
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "DELETE_VECTORS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					indexName,
					...filter && { filter: JSON.stringify(filter) },
					...ids && { idsCount: ids.length }
				}
			}, error);
		}
	}
	async truncateIndex(args) {
		try {
			return await this.executeMutation(() => this._doTruncateIndex(args));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createVectorErrorId)("LIBSQL", "TRUNCATE_INDEX", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { indexName: args.indexName }
			}, error);
		}
	}
	async _doTruncateIndex({ indexName }) {
		await this.turso.execute({
			sql: `DELETE FROM ${(0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name")}`,
			args: []
		});
	}
};
//#endregion
//#region src/storage/db/utils.ts
const MAX_DEPTH = 100;
/**
* Safely serializes a value to JSON string with pre-sanitization.
* Handles RPC proxies (Cloudflare Workers), functions, symbols, BigInt, and circular references.
*
* Pre-sanitization is required because RPC proxies throw on toJSON property access,
* which happens before JSON.stringify's replacer can intervene.
*
* @param value - The value to serialize
* @returns JSON string representation, with non-serializable values removed
*/
const safeStringify = (value) => {
	const ancestors = /* @__PURE__ */ new Set();
	const sanitize = (val, depth = 0) => {
		if (val === null || val === void 0) return val;
		if (typeof val === "function") return void 0;
		if (typeof val === "symbol") return void 0;
		if (typeof val === "bigint") return val.toString();
		if (typeof val !== "object") return val;
		if (depth > MAX_DEPTH) return "[Max depth exceeded]";
		if (ancestors.has(val)) return void 0;
		try {
			val.toJSON;
			Object.keys(val);
		} catch {
			return;
		}
		if (typeof val.toJSON === "function") return sanitize(val.toJSON(), depth + 1);
		ancestors.add(val);
		try {
			if (Array.isArray(val)) return val.map((item) => sanitize(item, depth + 1));
			const result = {};
			for (const key of Object.keys(val)) {
				const sanitized = sanitize(val[key], depth + 1);
				if (sanitized !== void 0) result[key] = sanitized;
			}
			return result;
		} finally {
			ancestors.delete(val);
		}
	};
	return JSON.stringify(sanitize(value)) ?? "null";
};
/**
* Builds a SQL column list for SELECT statements, wrapping JSONB columns with json()
* to convert binary JSONB to TEXT.
*
* The json() function handles both:
* - Binary JSONB data (converts to TEXT)
* - Legacy TEXT JSON data (returns as-is)
*
* Note: json_valid() was considered for guarding against malformed legacy TEXT,
* but it doesn't work correctly with binary JSONB data (returns false for valid JSONB blobs).
*
* @param tableName - The table name to get the schema for
* @returns A comma-separated column list with json() wrappers for JSONB columns
*/
function buildSelectColumns(tableName) {
	const schema = _mastra_core_storage.TABLE_SCHEMAS[tableName];
	return Object.keys(schema).map((col) => {
		const colDef = schema[col];
		const parsedCol = (0, _mastra_core_utils.parseSqlIdentifier)(col, "column name");
		return colDef?.type === "jsonb" ? `json("${parsedCol}") as "${parsedCol}"` : `"${parsedCol}"`;
	}).join(", ");
}
/**
* Same as `buildSelectColumns` but qualifies each column with the given table alias.
* Used by queries that JOIN multiple tables and need unambiguous column references.
*/
function buildSelectColumnsWithAlias(tableName, alias) {
	const parsedAlias = (0, _mastra_core_utils.parseSqlIdentifier)(alias, "table alias");
	const schema = _mastra_core_storage.TABLE_SCHEMAS[tableName];
	return Object.keys(schema).map((col) => {
		const colDef = schema[col];
		const parsedCol = (0, _mastra_core_utils.parseSqlIdentifier)(col, "column name");
		return colDef?.type === "jsonb" ? `json(${parsedAlias}."${parsedCol}") as "${parsedCol}"` : `${parsedAlias}."${parsedCol}"`;
	}).join(", ");
}
/**
* Checks if an error is a SQLite lock/busy error that should be retried
*/
function isLockError(error) {
	return error.code === "SQLITE_BUSY" || error.code === "SQLITE_LOCKED" || error.message?.toLowerCase().includes("database is locked") || error.message?.toLowerCase().includes("database table is locked") || error.message?.toLowerCase().includes("table is locked") || error.constructor.name === "SqliteError" && error.message?.toLowerCase().includes("locked");
}
function createExecuteWriteOperationWithRetry({ logger, maxRetries, initialBackoffMs }) {
	return async function executeWriteOperationWithRetry(operationFn, operationDescription) {
		let attempts = 0;
		let backoff = initialBackoffMs;
		while (attempts < maxRetries) try {
			return await operationFn();
		} catch (error) {
			logger.debug(`LibSQLStore: Error caught in retry loop for ${operationDescription}`, {
				errorType: error.constructor.name,
				errorCode: error.code,
				errorMessage: error.message,
				attempts,
				maxRetries
			});
			if (isLockError(error)) {
				attempts++;
				if (attempts >= maxRetries) {
					logger.error(`LibSQLStore: Operation failed after ${maxRetries} attempts due to database lock: ${error.message}`, {
						error,
						attempts,
						maxRetries
					});
					throw error;
				}
				logger.warn(`LibSQLStore: Attempt ${attempts} failed due to database lock during ${operationDescription}. Retrying in ${backoff}ms...`, {
					errorMessage: error.message,
					attempts,
					backoff,
					maxRetries
				});
				await new Promise((resolve) => setTimeout(resolve, backoff));
				backoff *= 2;
			} else {
				logger.error(`LibSQLStore: Non-lock error during ${operationDescription}, not retrying`, { error });
				throw error;
			}
		}
		throw new Error(`LibSQLStore: Unexpected exit from retry loop for ${operationDescription}`);
	};
}
function prepareStatement({ tableName, record }) {
	const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
	const schema = _mastra_core_storage.TABLE_SCHEMAS[tableName];
	const columnNames = Object.keys(record);
	const columns = columnNames.map((col) => (0, _mastra_core_utils.parseSqlIdentifier)(col, "column name"));
	const values = columnNames.map((col) => {
		const v = record[col];
		if (typeof v === `undefined` || v === null) return null;
		if (schema[col]?.type === "jsonb") return safeStringify(v);
		if (v instanceof Date) return v.toISOString();
		return typeof v === "object" ? safeStringify(v) : v;
	});
	const placeholders = columnNames.map((col) => {
		return schema[col]?.type === "jsonb" ? "jsonb(?)" : "?";
	}).join(", ");
	return {
		sql: `INSERT OR REPLACE INTO ${parsedTableName} (${columns.join(", ")}) VALUES (${placeholders})`,
		args: values
	};
}
function prepareUpdateStatement({ tableName, updates, keys }) {
	const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
	const schema = _mastra_core_storage.TABLE_SCHEMAS[tableName];
	const updateColumnNames = Object.keys(updates);
	const updateColumns = updateColumnNames.map((col) => (0, _mastra_core_utils.parseSqlIdentifier)(col, "column name"));
	const updateValues = updateColumnNames.map((col) => {
		const colDef = schema[col];
		const v = updates[col];
		if (colDef?.type === "jsonb") return transformToSqlValue(v, true);
		return transformToSqlValue(v, false);
	});
	const setClause = updateColumns.map((col, i) => {
		return schema[updateColumnNames[i]]?.type === "jsonb" ? `${col} = jsonb(?)` : `${col} = ?`;
	}).join(", ");
	const whereClause = prepareWhereClause(keys, schema);
	return {
		sql: `UPDATE ${parsedTableName} SET ${setClause}${whereClause.sql}`,
		args: [...updateValues, ...whereClause.args]
	};
}
function transformToSqlValue(value, forceJsonStringify = false) {
	if (typeof value === "undefined" || value === null) return null;
	if (forceJsonStringify) return safeStringify(value);
	if (value instanceof Date) return value.toISOString();
	return typeof value === "object" ? safeStringify(value) : value;
}
function prepareDeleteStatement({ tableName, keys }) {
	const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
	const whereClause = prepareWhereClause(keys, _mastra_core_storage.TABLE_SCHEMAS[tableName]);
	return {
		sql: `DELETE FROM ${parsedTableName}${whereClause.sql}`,
		args: whereClause.args
	};
}
function prepareWhereClause(filters, schema) {
	const conditions = [];
	const args = [];
	for (const [columnName, filterValue] of Object.entries(filters)) {
		if (!schema[columnName]) throw new Error(`Unknown column: ${columnName}`);
		const result = buildCondition((0, _mastra_core_utils.parseSqlIdentifier)(columnName, "column name"), filterValue);
		conditions.push(result.condition);
		args.push(...result.args);
	}
	return {
		sql: conditions.length > 0 ? ` WHERE ${conditions.join(" AND ")}` : "",
		args
	};
}
function buildCondition(columnName, filterValue) {
	if (filterValue === null) return {
		condition: `${columnName} IS NULL`,
		args: []
	};
	if (typeof filterValue === "object" && filterValue !== null && ("startAt" in filterValue || "endAt" in filterValue)) return buildDateRangeCondition(columnName, filterValue);
	return {
		condition: `${columnName} = ?`,
		args: [transformToSqlValue(filterValue)]
	};
}
function buildDateRangeCondition(columnName, range) {
	const conditions = [];
	const args = [];
	if (range.startAt !== void 0) {
		conditions.push(`${columnName} >= ?`);
		args.push(transformToSqlValue(range.startAt));
	}
	if (range.endAt !== void 0) {
		conditions.push(`${columnName} <= ?`);
		args.push(transformToSqlValue(range.endAt));
	}
	if (conditions.length === 0) throw new Error("Date range must specify at least startAt or endAt");
	return {
		condition: conditions.join(" AND "),
		args
	};
}
/**
* Transforms SQL row data back to a typed object format
* Reverses the transformations done in prepareStatement
*/
function transformFromSqlRow({ tableName, sqlRow }) {
	const result = {};
	const jsonColumns = new Set(Object.keys(_mastra_core_storage.TABLE_SCHEMAS[tableName]).filter((key) => _mastra_core_storage.TABLE_SCHEMAS[tableName][key].type === "jsonb").map((key) => key));
	const dateColumns = new Set(Object.keys(_mastra_core_storage.TABLE_SCHEMAS[tableName]).filter((key) => _mastra_core_storage.TABLE_SCHEMAS[tableName][key].type === "timestamp").map((key) => key));
	const booleanColumns = new Set(Object.keys(_mastra_core_storage.TABLE_SCHEMAS[tableName]).filter((key) => _mastra_core_storage.TABLE_SCHEMAS[tableName][key].type === "boolean").map((key) => key));
	for (const [key, value] of Object.entries(sqlRow)) {
		if (value === null || value === void 0) {
			result[key] = value;
			continue;
		}
		if (dateColumns.has(key) && typeof value === "string") {
			result[key] = new Date(value);
			continue;
		}
		if (jsonColumns.has(key) && typeof value === "string") {
			result[key] = (0, _mastra_core_storage.safelyParseJSON)(value);
			continue;
		}
		if (booleanColumns.has(key)) {
			result[key] = Boolean(value);
			continue;
		}
		result[key] = value;
	}
	return result;
}
//#endregion
//#region src/storage/db/write-lock.ts
/**
* Per-client write serialization.
*
* `@libsql/client` >= 0.18.0 pools connections for local `file:` databases, but
* SQLite still admits one writer at a time: an interactive
* `client.transaction('write')` holds `BEGIN` open across every
* `await tx.execute(...)`, and any other write on the same database in that
* window contends on the file lock and can fail with `SQLITE_BUSY` once
* `busy_timeout` expires.
*
* This is dormant under the default engine but the evented engine runs many
* concurrent workflow snapshot writes per agent run, so a write issued by an
* unrelated domain (e.g. creating a dataset experiment) can fail spuriously.
*
* Serializing every write on a given client closes that window: writes — both
* autocommit statements and full interactive transactions — run one at a time,
* so none can interleave with an open transaction. Reads are intentionally not
* gated; WAL readers never observe a partial write and must not queue behind a
* long-running writer.
*
* `:memory:` databases and embedded replicas get a single pooled connection
* instead; see `shared/single-connection-client.ts`, which gates *all* calls
* (reads included) behind open transactions for those clients.
*/
const clientWriteChains = /* @__PURE__ */ new WeakMap();
/**
* Runs `fn` after every previously-enqueued write on `client` has settled, and
* returns its result. The chain advances regardless of whether `fn` resolves or
* rejects, so one failed write never wedges the queue.
*/
function withClientWriteLock(client, fn) {
	const result = (clientWriteChains.get(client) ?? Promise.resolve()).then(fn, fn);
	clientWriteChains.set(client, result.then(() => void 0, () => void 0));
	return result;
}
//#endregion
//#region src/storage/db/index.ts
/**
* Default SQLite `busy_timeout` (ms) for local LibSQL connections. Chosen to
* comfortably exceed the write-retry backoff window so contended writes block
* briefly rather than surfacing as `SQLITE_BUSY` errors.
*/
const DEFAULT_CONNECTION_TIMEOUT_MS = 5e3;
/**
* Resolves a LibSQLDomainConfig to a Client instance.
* Creates a new client if credentials are provided, or returns the existing client.
*
* @param config - The domain configuration
* @returns The resolved LibSQL client
*/
function resolveClient(config) {
	if ("client" in config) return config.client;
	const isLocal = config.url.startsWith("file:") || config.url.includes(":memory:");
	const timeout = config.connectionTimeoutMs ?? 5e3;
	return (0, _libsql_client.createClient)({
		url: config.url,
		...config.authToken ? { authToken: config.authToken } : {},
		...isLocal ? { timeout } : {}
	});
}
/**
* Guard prune batch limits: a non-positive limit deletes nothing while callers'
* drain checks (`affected < limit`) never fire, which turns the prune loop into
* an infinite spin. Fail loudly instead.
*/
function assertPositiveLimit(limit) {
	if (!Number.isSafeInteger(limit) || limit <= 0) throw new Error(`prune limit must be a positive integer; received ${limit}`);
}
var LibSQLDB = class extends _mastra_core_base.MastraBase {
	client;
	maxRetries;
	initialBackoffMs;
	executeWriteOperationWithRetry;
	/** Cache of actual table columns: tableName -> Promise<Set<columnName>> (stores in-flight promise to coalesce concurrent calls) */
	tableColumnsCache = /* @__PURE__ */ new Map();
	constructor({ client, maxRetries, initialBackoffMs }) {
		super({
			component: "STORAGE",
			name: "LIBSQL_DB_LAYER"
		});
		this.client = client;
		this.maxRetries = maxRetries ?? 5;
		this.initialBackoffMs = initialBackoffMs ?? 100;
		this.executeWriteOperationWithRetry = createExecuteWriteOperationWithRetry({
			logger: this.logger,
			maxRetries: this.maxRetries,
			initialBackoffMs: this.initialBackoffMs
		});
	}
	/**
	* Gets the set of column names that actually exist in the database table.
	* Results are cached; the cache is invalidated when alterTable() adds new columns.
	*/
	async getTableColumns(tableName) {
		const cached = this.tableColumnsCache.get(tableName);
		if (cached) return cached;
		const promise = (async () => {
			try {
				const sanitizedTable = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
				const result = await this.client.execute({ sql: `PRAGMA table_info("${sanitizedTable}")` });
				const columns = new Set((result.rows || []).map((row) => row.name));
				if (columns.size === 0) this.tableColumnsCache.delete(tableName);
				return columns;
			} catch (error) {
				this.tableColumnsCache.delete(tableName);
				throw error;
			}
		})();
		this.tableColumnsCache.set(tableName, promise);
		return promise;
	}
	/**
	* Filters a record to only include columns that exist in the actual database table.
	* Unknown columns are silently dropped to ensure forward compatibility.
	*/
	async filterRecordToKnownColumns(tableName, record) {
		const knownColumns = await this.getTableColumns(tableName);
		if (knownColumns.size === 0) return record;
		const filtered = {};
		for (const [key, value] of Object.entries(record)) if (knownColumns.has(key)) filtered[key] = value;
		return filtered;
	}
	/**
	* Checks if a column exists in the specified table.
	*
	* @param table - The name of the table to check
	* @param column - The name of the column to look for
	* @returns `true` if the column exists in the table, `false` otherwise
	*/
	async hasColumn(table, column) {
		const sanitizedTable = (0, _mastra_core_utils.parseSqlIdentifier)(table, "table name");
		return (await this.client.execute({ sql: `PRAGMA table_info("${sanitizedTable}")` })).rows?.some((row) => row.name === column);
	}
	/**
	* Internal insert implementation without retry logic.
	*/
	async doInsert({ tableName, record }) {
		const filteredRecord = await this.filterRecordToKnownColumns(tableName, record);
		if (Object.keys(filteredRecord).length === 0) return;
		await withClientWriteLock(this.client, () => this.client.execute(prepareStatement({
			tableName,
			record: filteredRecord
		})));
	}
	/**
	* Inserts or replaces a record in the specified table with automatic retry on lock errors.
	*
	* @param args - The insert arguments
	* @param args.tableName - The name of the table to insert into
	* @param args.record - The record to insert (key-value pairs)
	*/
	insert(args) {
		return this.executeWriteOperationWithRetry(() => this.doInsert(args), `insert into table ${args.tableName}`);
	}
	/** Inserts a record without replacing an existing primary key. */
	insertOnly(args) {
		return this.executeWriteOperationWithRetry(async () => {
			const filteredRecord = await this.filterRecordToKnownColumns(args.tableName, args.record);
			if (Object.keys(filteredRecord).length === 0) return;
			const statement = prepareStatement({
				tableName: args.tableName,
				record: filteredRecord
			});
			if (!statement.sql.startsWith("INSERT OR REPLACE")) throw new Error(`Unexpected insert statement generated for table ${args.tableName}`);
			statement.sql = statement.sql.replace("INSERT OR REPLACE", "INSERT");
			await withClientWriteLock(this.client, () => this.client.execute(statement));
		}, `insert into table ${args.tableName}`);
	}
	/**
	* Internal update implementation without retry logic.
	*/
	async doUpdate({ tableName, keys, data }) {
		const filteredData = await this.filterRecordToKnownColumns(tableName, data);
		if (Object.keys(filteredData).length === 0) return;
		await withClientWriteLock(this.client, () => this.client.execute(prepareUpdateStatement({
			tableName,
			updates: filteredData,
			keys
		})));
	}
	/**
	* Updates a record in the specified table with automatic retry on lock errors.
	*
	* @param args - The update arguments
	* @param args.tableName - The name of the table to update
	* @param args.keys - The key(s) identifying the record to update
	* @param args.data - The fields to update (key-value pairs)
	*/
	update(args) {
		return this.executeWriteOperationWithRetry(() => this.doUpdate(args), `update table ${args.tableName}`);
	}
	/**
	* Internal batch insert implementation without retry logic.
	*/
	async doBatchInsert({ tableName, records }) {
		if (records.length === 0) return;
		const nonEmptyRecords = (await Promise.all(records.map((r) => this.filterRecordToKnownColumns(tableName, r)))).filter((r) => Object.keys(r).length > 0);
		if (nonEmptyRecords.length === 0) return;
		const batchStatements = nonEmptyRecords.map((r) => prepareStatement({
			tableName,
			record: r
		}));
		await withClientWriteLock(this.client, () => this.client.batch(batchStatements, "write"));
	}
	/**
	* Inserts multiple records in a single batch transaction with automatic retry on lock errors.
	*
	* @param args - The batch insert arguments
	* @param args.tableName - The name of the table to insert into
	* @param args.records - Array of records to insert
	* @throws {MastraError} When the batch insert fails after retries
	*/
	async batchInsert(args) {
		return this.executeWriteOperationWithRetry(() => this.doBatchInsert(args), `batch insert into table ${args.tableName}`).catch((error) => {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BATCH_INSERT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { tableName: args.tableName }
			}, error);
		});
	}
	/**
	* Internal batch update implementation without retry logic.
	* Each record can be updated based on single or composite keys.
	*/
	async doBatchUpdate({ tableName, updates }) {
		if (updates.length === 0) return;
		const filteredUpdates = [];
		for (const { keys, data } of updates) {
			const filteredData = await this.filterRecordToKnownColumns(tableName, data);
			if (Object.keys(filteredData).length > 0) filteredUpdates.push({
				keys,
				data: filteredData
			});
		}
		if (filteredUpdates.length === 0) return;
		const batchStatements = filteredUpdates.map(({ keys, data }) => prepareUpdateStatement({
			tableName,
			updates: data,
			keys
		}));
		await withClientWriteLock(this.client, () => this.client.batch(batchStatements, "write"));
	}
	/**
	* Updates multiple records in a single batch transaction with automatic retry on lock errors.
	* Each record can be updated based on single or composite keys.
	*
	* @param args - The batch update arguments
	* @param args.tableName - The name of the table to update
	* @param args.updates - Array of update operations, each containing keys and data
	* @throws {MastraError} When the batch update fails after retries
	*/
	async batchUpdate(args) {
		return this.executeWriteOperationWithRetry(() => this.doBatchUpdate(args), `batch update in table ${args.tableName}`).catch((error) => {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BATCH_UPDATE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { tableName: args.tableName }
			}, error);
		});
	}
	/**
	* Internal batch delete implementation without retry logic.
	* Each record can be deleted based on single or composite keys.
	*/
	async doBatchDelete({ tableName, keys }) {
		if (keys.length === 0) return;
		const batchStatements = keys.map((keyObj) => prepareDeleteStatement({
			tableName,
			keys: keyObj
		}));
		await withClientWriteLock(this.client, () => this.client.batch(batchStatements, "write"));
	}
	/**
	* Deletes multiple records in a single batch transaction with automatic retry on lock errors.
	* Each record can be deleted based on single or composite keys.
	*
	* @param args - The batch delete arguments
	* @param args.tableName - The name of the table to delete from
	* @param args.keys - Array of key objects identifying records to delete
	* @throws {MastraError} When the batch delete fails after retries
	*/
	async batchDelete({ tableName, keys }) {
		return this.executeWriteOperationWithRetry(() => this.doBatchDelete({
			tableName,
			keys
		}), `batch delete from table ${tableName}`).catch((error) => {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BATCH_DELETE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { tableName }
			}, error);
		});
	}
	/**
	* Internal single-record delete implementation without retry logic.
	*/
	async doDelete({ tableName, keys }) {
		await withClientWriteLock(this.client, () => this.client.execute(prepareDeleteStatement({
			tableName,
			keys
		})));
	}
	/**
	* Deletes a single record from the specified table with automatic retry on lock errors.
	*
	* @param args - The delete arguments
	* @param args.tableName - The name of the table to delete from
	* @param args.keys - The key(s) identifying the record to delete
	* @throws {MastraError} When the delete fails after retries
	*/
	async delete(args) {
		return this.executeWriteOperationWithRetry(() => this.doDelete(args), `delete from table ${args.tableName}`).catch((error) => {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { tableName: args.tableName }
			}, error);
		});
	}
	/**
	* Selects a single record from the specified table by key(s).
	* Returns the most recently created record if multiple matches exist.
	* Automatically parses JSON string values back to objects/arrays.
	*
	* @typeParam R - The expected return type of the record
	* @param args - The select arguments
	* @param args.tableName - The name of the table to select from
	* @param args.keys - The key(s) identifying the record to select
	* @returns The matching record or `null` if not found
	*/
	async select({ tableName, keys }) {
		const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
		const columns = buildSelectColumns(tableName);
		const conditions = Object.keys(keys).map((key) => (0, _mastra_core_utils.parseSqlIdentifier)(key, "column name")).map((key) => `${key} = ?`).join(" AND ");
		const values = Object.values(keys);
		const result = await this.client.execute({
			sql: `SELECT ${columns} FROM ${parsedTableName} WHERE ${conditions} ORDER BY createdAt DESC LIMIT 1`,
			args: values
		});
		if (!result.rows || result.rows.length === 0) return null;
		const row = result.rows[0];
		return Object.fromEntries(Object.entries(row || {}).map(([k, v]) => {
			try {
				return [k, typeof v === "string" ? v.startsWith("{") || v.startsWith("[") ? JSON.parse(v) : v : v];
			} catch {
				return [k, v];
			}
		}));
	}
	/**
	* Selects multiple records from the specified table with optional filtering, ordering, and pagination.
	*
	* @typeParam R - The expected return type of each record
	* @param args - The select arguments
	* @param args.tableName - The name of the table to select from
	* @param args.whereClause - Optional WHERE clause with SQL string and arguments
	* @param args.orderBy - Optional ORDER BY clause (e.g., "createdAt DESC")
	* @param args.offset - Optional offset for pagination
	* @param args.limit - Optional limit for pagination
	* @param args.args - Optional additional query arguments
	* @returns Array of matching records
	*/
	async selectMany({ tableName, whereClause, orderBy, offset, limit, args }) {
		const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
		let statement = `SELECT ${buildSelectColumns(tableName)} FROM ${parsedTableName}`;
		if (whereClause?.sql) statement += ` ${whereClause.sql}`;
		if (orderBy) statement += ` ORDER BY ${orderBy}`;
		if (limit) statement += ` LIMIT ${limit}`;
		if (offset) statement += ` OFFSET ${offset}`;
		return ((await this.client.execute({
			sql: statement,
			args: [...whereClause?.args ?? [], ...args ?? []]
		})).rows ?? []).map((row) => {
			return Object.fromEntries(Object.entries(row || {}).map(([k, v]) => {
				try {
					return [k, typeof v === "string" ? v.startsWith("{") || v.startsWith("[") ? JSON.parse(v) : v : v];
				} catch {
					return [k, v];
				}
			}));
		});
	}
	/**
	* Returns the total count of records matching the optional WHERE clause.
	*
	* @param args - The count arguments
	* @param args.tableName - The name of the table to count from
	* @param args.whereClause - Optional WHERE clause with SQL string and arguments
	* @returns The total count of matching records
	*/
	async selectTotalCount({ tableName, whereClause }) {
		const statement = `SELECT COUNT(*) as count FROM ${(0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name")} ${whereClause ? `${whereClause.sql}` : ""}`;
		const result = await this.client.execute({
			sql: statement,
			args: whereClause?.args ?? []
		});
		if (!result.rows || result.rows.length === 0) return 0;
		return result.rows[0]?.count ?? 0;
	}
	/**
	* Maps a storage column type to its SQLite equivalent.
	*/
	getSqlType(type) {
		switch (type) {
			case "bigint": return "INTEGER";
			case "timestamp": return "TEXT";
			case "float": return "REAL";
			case "boolean": return "INTEGER";
			case "jsonb": return "TEXT";
			default: return (0, _mastra_core_storage.getSqlType)(type);
		}
	}
	/**
	* Creates a table if it doesn't exist based on the provided schema.
	*
	* @param args - The create table arguments
	* @param args.tableName - The name of the table to create
	* @param args.schema - The schema definition for the table columns
	*/
	async createTable({ tableName, schema, compositePrimaryKey }) {
		try {
			const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
			if (compositePrimaryKey) {
				for (const col of compositePrimaryKey) if (!(col in schema)) throw new Error(`compositePrimaryKey column "${col}" does not exist in schema for table "${tableName}"`);
			}
			const compositePKSet = compositePrimaryKey ? new Set(compositePrimaryKey) : null;
			const columnDefinitions = Object.entries(schema).map(([colName, colDef]) => {
				return `"${colName}" ${this.getSqlType(colDef.type)} ${colDef.nullable === false ? "NOT NULL" : ""} ${colDef.primaryKey && !compositePKSet?.has(colName) ? "PRIMARY KEY" : ""}`.trim();
			});
			const tableConstraints = [];
			if (compositePrimaryKey) {
				const pkCols = compositePrimaryKey.map((c) => `"${c}"`).join(", ");
				tableConstraints.push(`PRIMARY KEY (${pkCols})`);
			}
			if (tableName === _mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT) tableConstraints.push("UNIQUE (workflow_name, run_id)");
			if (tableName === _mastra_core_storage.TABLE_SPANS) tableConstraints.push("UNIQUE (spanId, traceId)");
			const sql = `CREATE TABLE IF NOT EXISTS ${parsedTableName} (\n  ${[...columnDefinitions, ...tableConstraints].join(",\n  ")}\n)`;
			await this.client.execute(sql);
			this.logger.debug(`LibSQLDB: Created table ${tableName}`);
			if (tableName === _mastra_core_storage.TABLE_SPANS) await this.migrateSpansTable();
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_TABLE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { tableName }
			}, error);
		} finally {
			this.tableColumnsCache.delete(tableName);
		}
	}
	/**
	* Migrates the spans table schema from OLD_SPAN_SCHEMA to current SPAN_SCHEMA.
	* This adds new columns that don't exist in old schema and ensures required indexes exist.
	*/
	async migrateSpansTable() {
		const schema = _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_SPANS];
		try {
			const existingColumnsRaw = await this.getTableColumns(_mastra_core_storage.TABLE_SPANS);
			const existingColumns = new Set([...existingColumnsRaw].map((column) => column.toLowerCase()));
			let addedColumns = false;
			for (const [columnName, columnDef] of Object.entries(schema)) if (!existingColumns.has(columnName.toLowerCase())) {
				const alterSql = `ALTER TABLE "${_mastra_core_storage.TABLE_SPANS}" ADD COLUMN "${columnName}" ${this.getSqlType(columnDef.type)}`;
				await this.client.execute(alterSql);
				addedColumns = true;
				this.logger.debug(`LibSQLDB: Added column '${columnName}' to ${_mastra_core_storage.TABLE_SPANS}`);
			}
			if (addedColumns) this.tableColumnsCache.delete(_mastra_core_storage.TABLE_SPANS);
			if (!await this.spansUniqueIndexExists()) {
				const duplicateInfo = await this.checkForDuplicateSpans();
				if (duplicateInfo.hasDuplicates) {
					const errorMessage = `
===========================================================================
MIGRATION REQUIRED: Duplicate spans detected in ${_mastra_core_storage.TABLE_SPANS}\n===========================================================================\n\nFound ${duplicateInfo.duplicateCount} duplicate (traceId, spanId) combinations.\n\nThe spans table requires a unique constraint on (traceId, spanId), but your\ndatabase contains duplicate entries that must be resolved first.\n\nTo fix this, run the manual migration command:\n\n  npx mastra migrate\n\nThis command will:\n  1. Remove duplicate spans (keeping the most complete/recent version)\n  2. Add the required unique constraint\n\nNote: This migration may take some time for large tables.\n===========================================================================\n`;
					throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "MIGRATION_REQUIRED", "DUPLICATE_SPANS"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						text: errorMessage
					});
				} else {
					await this.client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS "mastra_ai_spans_spanid_traceid_idx" ON "${_mastra_core_storage.TABLE_SPANS}" ("spanId", "traceId")`);
					this.logger.debug(`LibSQLDB: Created unique index on (spanId, traceId) for ${_mastra_core_storage.TABLE_SPANS}`);
				}
			}
			await this.client.execute(`CREATE INDEX IF NOT EXISTS "mastra_ai_spans_roots_started_at_idx" ON "${_mastra_core_storage.TABLE_SPANS}" ("startedAt" DESC) WHERE "parentSpanId" IS NULL`);
			await this.client.execute(`CREATE INDEX IF NOT EXISTS "mastra_ai_spans_trace_started_at_idx" ON "${_mastra_core_storage.TABLE_SPANS}" ("traceId", "startedAt")`);
			this.logger.debug(`LibSQLDB: Ensured trace read indexes for ${_mastra_core_storage.TABLE_SPANS}`);
			this.logger.info(`LibSQLDB: Migration completed for ${_mastra_core_storage.TABLE_SPANS}`);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			this.logger.warn(`LibSQLDB: Failed to migrate spans table ${_mastra_core_storage.TABLE_SPANS}:`, error);
		}
	}
	/**
	* Checks if the unique index on (spanId, traceId) already exists on the spans table.
	* Used to skip deduplication when the index already exists (migration already complete).
	*/
	async spansUniqueIndexExists() {
		try {
			return ((await this.client.execute(`SELECT 1 FROM sqlite_master WHERE type = 'index' AND name = 'mastra_ai_spans_spanid_traceid_idx'`)).rows?.length ?? 0) > 0;
		} catch {
			return false;
		}
	}
	/**
	* Checks for duplicate (traceId, spanId) combinations in the spans table.
	* Returns information about duplicates for logging/CLI purposes.
	*/
	async checkForDuplicateSpans() {
		try {
			const result = await this.client.execute(`
        SELECT COUNT(*) as duplicate_count FROM (
          SELECT "spanId", "traceId"
          FROM "${_mastra_core_storage.TABLE_SPANS}"
          GROUP BY "spanId", "traceId"
          HAVING COUNT(*) > 1
        )
      `);
			const duplicateCount = Number(result.rows?.[0]?.duplicate_count ?? 0);
			return {
				hasDuplicates: duplicateCount > 0,
				duplicateCount
			};
		} catch (error) {
			this.logger.debug(`LibSQLDB: Could not check for duplicates: ${error}`);
			return {
				hasDuplicates: false,
				duplicateCount: 0
			};
		}
	}
	/**
	* Manually run the spans migration to deduplicate and add the unique constraint.
	* This is intended to be called from the CLI when duplicates are detected.
	*
	* @returns Migration result with status and details
	*/
	async migrateSpans() {
		if (await this.spansUniqueIndexExists()) return {
			success: true,
			alreadyMigrated: true,
			duplicatesRemoved: 0,
			message: `Migration already complete. Unique index exists on ${_mastra_core_storage.TABLE_SPANS}.`
		};
		const duplicateInfo = await this.checkForDuplicateSpans();
		if (duplicateInfo.hasDuplicates) {
			this.logger.info(`Found ${duplicateInfo.duplicateCount} duplicate (traceId, spanId) combinations. Starting deduplication...`);
			await this.deduplicateSpans();
		} else this.logger.info(`No duplicate spans found.`);
		await this.client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS "mastra_ai_spans_spanid_traceid_idx" ON "${_mastra_core_storage.TABLE_SPANS}" ("spanId", "traceId")`);
		return {
			success: true,
			alreadyMigrated: false,
			duplicatesRemoved: duplicateInfo.duplicateCount,
			message: duplicateInfo.hasDuplicates ? `Migration complete. Removed duplicates and added unique index to ${_mastra_core_storage.TABLE_SPANS}.` : `Migration complete. Added unique index to ${_mastra_core_storage.TABLE_SPANS}.`
		};
	}
	/**
	* Check migration status for the spans table.
	* Returns information about whether migration is needed.
	*/
	async checkSpansMigrationStatus() {
		if (await this.spansUniqueIndexExists()) return {
			needsMigration: false,
			hasDuplicates: false,
			duplicateCount: 0,
			constraintExists: true,
			tableName: _mastra_core_storage.TABLE_SPANS
		};
		const duplicateInfo = await this.checkForDuplicateSpans();
		return {
			needsMigration: true,
			hasDuplicates: duplicateInfo.hasDuplicates,
			duplicateCount: duplicateInfo.duplicateCount,
			constraintExists: false,
			tableName: _mastra_core_storage.TABLE_SPANS
		};
	}
	/**
	* Deduplicates spans table by removing duplicate (spanId, traceId) combinations.
	* Keeps the "best" record for each duplicate group based on:
	* 1. Completed spans (endedAt IS NOT NULL) over incomplete ones
	* 2. Most recently updated (updatedAt DESC)
	* 3. Most recently created (createdAt DESC) as tiebreaker
	*/
	async deduplicateSpans() {
		try {
			const duplicateCheck = await this.client.execute(`
        SELECT COUNT(*) as duplicate_count FROM (
          SELECT "spanId", "traceId"
          FROM "${_mastra_core_storage.TABLE_SPANS}"
          GROUP BY "spanId", "traceId"
          HAVING COUNT(*) > 1
        )
      `);
			const duplicateCount = Number(duplicateCheck.rows?.[0]?.duplicate_count ?? 0);
			if (duplicateCount === 0) {
				this.logger.debug(`LibSQLDB: No duplicate spans found, skipping deduplication`);
				return;
			}
			this.logger.warn(`LibSQLDB: Found ${duplicateCount} duplicate (spanId, traceId) combinations, deduplicating...`);
			const deletedCount = (await this.client.execute(`
        DELETE FROM "${_mastra_core_storage.TABLE_SPANS}"
        WHERE rowid NOT IN (
          SELECT MIN(best_rowid) FROM (
            SELECT
              rowid as best_rowid,
              "spanId",
              "traceId",
              ROW_NUMBER() OVER (
                PARTITION BY "spanId", "traceId"
                ORDER BY
                  CASE WHEN "endedAt" IS NOT NULL THEN 0 ELSE 1 END,
                  "updatedAt" DESC,
                  "createdAt" DESC
              ) as rn
            FROM "${_mastra_core_storage.TABLE_SPANS}"
          ) ranked
          WHERE rn = 1
          GROUP BY "spanId", "traceId"
        )
        AND ("spanId", "traceId") IN (
          SELECT "spanId", "traceId"
          FROM "${_mastra_core_storage.TABLE_SPANS}"
          GROUP BY "spanId", "traceId"
          HAVING COUNT(*) > 1
        )
      `)).rowsAffected ?? 0;
			this.logger.warn(`LibSQLDB: Deleted ${deletedCount} duplicate span records`);
		} catch (error) {
			this.logger.warn(`LibSQLDB: Failed to deduplicate spans:`, error);
		}
	}
	/**
	* Gets a default value for a column type (used when adding NOT NULL columns).
	*/
	getDefaultValue(type) {
		switch (type) {
			case "text":
			case "uuid": return "DEFAULT ''";
			case "integer":
			case "bigint":
			case "float": return "DEFAULT 0";
			case "boolean": return "DEFAULT 0";
			case "jsonb": return "DEFAULT '{}'";
			case "timestamp": return "DEFAULT CURRENT_TIMESTAMP";
			default: return "DEFAULT ''";
		}
	}
	/**
	* Alters an existing table to add missing columns.
	* Used for schema migrations when new columns are added.
	*
	* @param args - The alter table arguments
	* @param args.tableName - The name of the table to alter
	* @param args.schema - The full schema definition for the table
	* @param args.ifNotExists - Array of column names to add if they don't exist
	*/
	async alterTable({ tableName, schema, ifNotExists }) {
		const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
		try {
			const tableInfo = await this.client.execute({ sql: `PRAGMA table_info("${parsedTableName}")` });
			const existingColumns = new Set((tableInfo.rows || []).map((row) => row.name?.toLowerCase()));
			for (const columnName of ifNotExists) if (!existingColumns.has(columnName.toLowerCase()) && schema[columnName]) {
				const columnDef = schema[columnName];
				const alterSql = `ALTER TABLE ${parsedTableName} ADD COLUMN "${columnName}" ${this.getSqlType(columnDef.type)} ${columnDef.nullable ? "DEFAULT NULL" : this.getDefaultValue(columnDef.type)}`;
				await this.client.execute(alterSql);
				this.logger.debug(`LibSQLDB: Added column ${columnName} to table ${tableName}`);
			}
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "ALTER_TABLE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { tableName }
			}, error);
		} finally {
			this.tableColumnsCache.delete(tableName);
		}
	}
	/**
	* Deletes all records from the specified table.
	* Errors are logged but not thrown.
	*
	* @param args - The delete arguments
	* @param args.tableName - The name of the table to clear
	*/
	async deleteData({ tableName }) {
		const parsedTableName = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
		try {
			await withClientWriteLock(this.client, () => this.client.execute(`DELETE FROM ${parsedTableName}`));
		} catch (e) {
			const mastraError = new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CLEAR_TABLE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { tableName }
			}, e);
			this.logger?.trackException?.(mastraError);
			this.logger?.error?.(mastraError.toString());
		}
	}
	/**
	* Delete up to `limit` of the oldest rows whose `column` timestamp is strictly
	* before `cutoffMs`, in a single bounded statement. Returns the number of rows
	* removed so the caller can decide whether more batches remain.
	*
	* Deletes by `rowid` so the range scan is bounded by LIMIT and the write set
	* stays small — short locks, bounded WAL growth. Requires an index on
	* `column` to be fast at scale (see ensureIndex).
	*/
	async pruneBatch({ tableName, column, cutoff, limit }) {
		assertPositiveLimit(limit);
		const parsedTable = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
		const sql = `DELETE FROM "${parsedTable}" WHERE rowid IN (SELECT rowid FROM "${parsedTable}" WHERE "${(0, _mastra_core_utils.parseSqlIdentifier)(column, "column name")}" < ? LIMIT ?)`;
		const result = await this.executeWriteOperationWithRetry(() => withClientWriteLock(this.client, () => this.client.execute({
			sql,
			args: [cutoff, limit]
		})), `prune ${tableName}`);
		return Number(result.rowsAffected ?? 0);
	}
	/**
	* Delete up to `limit` aged parent rows *and* their child rows together, in a
	* single transaction. Used for whole-unit (parent-driven) pruning where
	* children must die with their parent (e.g. an aged experiment and its
	* experiment_results) — deleting per unit means a bound or abort between
	* batches never leaves a parent hollow (kept, but with its children gone) or
	* children orphaned.
	*
	* Both deletes target the same parent set via an identical deterministic
	* subquery (`ORDER BY rowid LIMIT ?`); the batch runs as one write
	* transaction, so the set cannot change between the two statements.
	*/
	async pruneUnitsBatch({ parentTable, parentKey, parentColumn, childTable, childForeignKey, cutoff, limit }) {
		assertPositiveLimit(limit);
		const parsedChild = (0, _mastra_core_utils.parseSqlIdentifier)(childTable, "table name");
		const parsedChildFk = (0, _mastra_core_utils.parseSqlIdentifier)(childForeignKey, "column name");
		const parsedParent = (0, _mastra_core_utils.parseSqlIdentifier)(parentTable, "table name");
		const parsedParentKey = (0, _mastra_core_utils.parseSqlIdentifier)(parentKey, "column name");
		const agedParents = `SELECT "${parsedParentKey}" FROM "${parsedParent}" WHERE "${(0, _mastra_core_utils.parseSqlIdentifier)(parentColumn, "column name")}" < ? ORDER BY rowid LIMIT ?`;
		const results = await this.executeWriteOperationWithRetry(() => withClientWriteLock(this.client, () => this.client.batch([{
			sql: `DELETE FROM "${parsedChild}" WHERE "${parsedChildFk}" IN (${agedParents})`,
			args: [cutoff, limit]
		}, {
			sql: `DELETE FROM "${parsedParent}" WHERE "${parsedParentKey}" IN (${agedParents})`,
			args: [cutoff, limit]
		}], "write")), `prune units ${parentTable}`);
		return {
			children: Number(results[0]?.rowsAffected ?? 0),
			parents: Number(results[1]?.rowsAffected ?? 0)
		};
	}
	/** Create an index if it does not already exist. */
	async ensureIndex({ indexName, tableName, column }) {
		const parsedTable = (0, _mastra_core_utils.parseSqlIdentifier)(tableName, "table name");
		const parsedColumn = (0, _mastra_core_utils.parseSqlIdentifier)(column, "column name");
		const parsedIndex = (0, _mastra_core_utils.parseSqlIdentifier)(indexName, "index name");
		await this.client.execute(`CREATE INDEX IF NOT EXISTS "${parsedIndex}" ON "${parsedTable}" ("${parsedColumn}")`);
	}
};
//#endregion
//#region src/storage/domains/agents/index.ts
var AgentsLibSQL = class extends _mastra_core_storage.AgentsStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#migrateFromLegacySchema();
		await this.#migrateVersionsSchema();
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_AGENTS,
			schema: _mastra_core_storage.AGENTS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
			schema: _mastra_core_storage.AGENT_VERSIONS_SCHEMA
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_AGENTS,
			schema: _mastra_core_storage.AGENTS_SCHEMA,
			ifNotExists: [
				"status",
				"authorId",
				"visibility",
				"favoriteCount"
			]
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
			schema: _mastra_core_storage.AGENT_VERSIONS_SCHEMA,
			ifNotExists: [
				"mcpClients",
				"requestContextSchema",
				"workspace",
				"skills",
				"skillsFormat",
				"durable",
				"browser",
				"toolProviders"
			]
		});
		await this.#migrateToolsToJsonbFormat();
		await this.#cleanupStaleDrafts();
	}
	/**
	* Migrates from the legacy flat agent schema (where config fields like name, instructions, model
	* were stored directly on mastra_agents) to the new versioned schema (thin agent record + versions table).
	* SQLite cannot drop columns or alter NOT NULL constraints, so we must recreate the table.
	*/
	async #migrateFromLegacySchema() {
		const legacyTable = `${_mastra_core_storage.TABLE_AGENTS}_legacy`;
		if (await this.#db.hasColumn(_mastra_core_storage.TABLE_AGENTS, "name")) {
			await this.#client.execute({ sql: `ALTER TABLE "${_mastra_core_storage.TABLE_AGENTS}" RENAME TO "${legacyTable}"` });
			await this.#client.execute({ sql: `DROP TABLE IF EXISTS "${_mastra_core_storage.TABLE_AGENT_VERSIONS}"` });
		}
		if (!await this.#db.hasColumn(legacyTable, "name")) return;
		const oldAgents = (await this.#client.execute({ sql: `SELECT * FROM "${legacyTable}"` })).rows || [];
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_AGENTS,
			schema: _mastra_core_storage.AGENTS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
			schema: _mastra_core_storage.AGENT_VERSIONS_SCHEMA
		});
		for (const row of oldAgents) {
			const agentId = row.id;
			if (!agentId) continue;
			const versionId = crypto.randomUUID();
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_AGENTS,
				record: {
					id: agentId,
					status: "published",
					activeVersionId: versionId,
					authorId: row.ownerId ?? row.authorId ?? null,
					metadata: row.metadata ?? null,
					createdAt: row.createdAt ?? now,
					updatedAt: row.updatedAt ?? now
				}
			});
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				record: {
					id: versionId,
					agentId,
					versionNumber: 1,
					name: row.name ?? agentId,
					description: row.description ?? null,
					instructions: this.serializeInstructions(row.instructions ?? ""),
					model: row.model ?? "{}",
					tools: row.tools ?? null,
					defaultOptions: row.defaultOptions ?? null,
					workflows: row.workflows ?? null,
					agents: row.agents ?? null,
					integrationTools: row.integrationTools ?? null,
					toolProviders: row.toolProviders ?? null,
					inputProcessors: row.inputProcessors ?? null,
					outputProcessors: row.outputProcessors ?? null,
					memory: row.memory ?? null,
					scorers: row.scorers ?? null,
					changedFields: null,
					changeMessage: "Migrated from legacy schema",
					createdAt: row.createdAt ?? now
				}
			});
		}
		await this.#client.execute({ sql: `DROP TABLE IF EXISTS "${legacyTable}"` });
	}
	/**
	* Migrates the agent_versions table from the old snapshot-based schema (single `snapshot` JSON column)
	* to the new flat schema (individual config columns). This handles the case where the agents table
	* was already migrated but the versions table still has the old schema.
	*/
	async #migrateVersionsSchema() {
		if (!await this.#db.hasColumn(_mastra_core_storage.TABLE_AGENT_VERSIONS, "snapshot")) return;
		await this.#client.execute({ sql: `DROP TABLE IF EXISTS "${_mastra_core_storage.TABLE_AGENT_VERSIONS}"` });
		await this.#client.execute({ sql: `DROP TABLE IF EXISTS "${_mastra_core_storage.TABLE_AGENTS}_legacy"` });
	}
	/**
	* Removes stale draft agent records that have no versions at all.
	* These are left behind when createAgent partially fails (inserts thin record
	* but fails to create the version due to schema mismatch).
	*
	* A legitimate draft (never published) will have rows in the versions table,
	* so we must only delete records with zero associated versions.
	*/
	async #cleanupStaleDrafts() {
		try {
			await this.#client.execute({ sql: `DELETE FROM "${_mastra_core_storage.TABLE_AGENTS}"
              WHERE status = 'draft'
                AND activeVersionId IS NULL
                AND NOT EXISTS (
                  SELECT 1 FROM "${_mastra_core_storage.TABLE_AGENT_VERSIONS}"
                  WHERE "${_mastra_core_storage.TABLE_AGENT_VERSIONS}".agentId = "${_mastra_core_storage.TABLE_AGENTS}".id
                )` });
		} catch {}
	}
	/**
	* Migrates the tools field from string[] format to JSONB format { "tool-key": { "description": "..." } }.
	* This handles the transition from the old format where tools were stored as an array of string keys
	* to the new format where tools can have per-agent description overrides.
	*/
	async #migrateToolsToJsonbFormat() {
		try {
			const result = await this.#client.execute({ sql: `SELECT id, tools FROM "${_mastra_core_storage.TABLE_AGENT_VERSIONS}" WHERE tools IS NOT NULL` });
			if (!result.rows || result.rows.length === 0) return;
			for (const row of result.rows) {
				const toolsValue = row.tools;
				let parsedTools;
				try {
					if (typeof toolsValue === "string") parsedTools = JSON.parse(toolsValue);
					else if (toolsValue instanceof ArrayBuffer) {
						const decoder = new TextDecoder();
						parsedTools = JSON.parse(decoder.decode(toolsValue));
					} else parsedTools = toolsValue;
				} catch {
					continue;
				}
				if (Array.isArray(parsedTools)) {
					const toolsObject = {};
					for (const toolKey of parsedTools) if (typeof toolKey === "string") toolsObject[toolKey] = {};
					await this.#client.execute({
						sql: `UPDATE "${_mastra_core_storage.TABLE_AGENT_VERSIONS}" SET tools = ? WHERE id = ?`,
						args: [JSON.stringify(toolsObject), row.id]
					});
				}
			}
			this.logger?.info?.(`Migrated agent version tools from array to object format`);
		} catch (error) {
			this.logger?.warn?.("Failed to migrate tools to JSONB format:", error);
		}
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_AGENTS });
	}
	parseJson(value, fieldName) {
		if (!value) return void 0;
		if (value instanceof ArrayBuffer || value && value.constructor && value.constructor.name === "ArrayBuffer") try {
			const jsonString = new TextDecoder().decode(value);
			return JSON.parse(jsonString);
		} catch (error) {
			console.error(`Failed to parse ArrayBuffer for ${fieldName}:`, error);
			return;
		}
		if (typeof value !== "string") return value;
		try {
			return JSON.parse(value);
		} catch (error) {
			const details = { value: value.length > 100 ? value.substring(0, 100) + "..." : value };
			if (fieldName) details.field = fieldName;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "PARSE_JSON", "INVALID_JSON"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `Failed to parse JSON${fieldName ? ` for field "${fieldName}"` : ""}: ${error instanceof Error ? error.message : "Unknown error"}`,
				details
			}, error);
		}
	}
	parseRow(row) {
		return {
			id: row.id,
			status: row.status,
			activeVersionId: row.activeVersionId,
			authorId: row.authorId,
			visibility: row.visibility ?? void 0,
			metadata: this.parseJson(row.metadata, "metadata"),
			favoriteCount: row.favoriteCount === null || row.favoriteCount === void 0 ? 0 : Number(row.favoriteCount),
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	async getById(id) {
		try {
			const result = await this.#db.select({
				tableName: _mastra_core_storage.TABLE_AGENTS,
				keys: { id }
			});
			return result ? this.parseRow(result) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_AGENT_BY_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId: id }
			}, error);
		}
	}
	async create(input) {
		const { agent } = input;
		try {
			const now = /* @__PURE__ */ new Date();
			const visibility = agent.visibility ?? (agent.authorId ? "private" : null);
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_AGENTS,
				record: {
					id: agent.id,
					status: "draft",
					activeVersionId: null,
					authorId: agent.authorId ?? null,
					visibility,
					metadata: agent.metadata ?? null,
					favoriteCount: 0,
					createdAt: now,
					updatedAt: now
				}
			});
			const { id: _id, authorId: _authorId, visibility: _visibility, metadata: _metadata, ...snapshotConfig } = agent;
			const versionId = crypto.randomUUID();
			await this.createVersion({
				id: versionId,
				agentId: agent.id,
				versionNumber: 1,
				...snapshotConfig,
				changedFields: Object.keys(snapshotConfig),
				changeMessage: "Initial version"
			});
			const created = await this.getById(agent.id);
			if (!created) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_AGENT", "NOT_FOUND_AFTER_CREATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `Agent ${agent.id} not found after creation`,
				details: { agentId: agent.id }
			});
			return created;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_AGENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId: agent.id }
			}, error);
		}
	}
	async update(input) {
		const { id, ...updates } = input;
		try {
			const existing = await this.getById(id);
			if (!existing) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_AGENT", "NOT_FOUND"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				text: `Agent ${id} not found`,
				details: { agentId: id }
			});
			const { authorId, activeVersionId, metadata, status, visibility } = updates;
			const updateData = { updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
			if (authorId !== void 0) updateData.authorId = authorId;
			if (activeVersionId !== void 0) updateData.activeVersionId = activeVersionId;
			if (status !== void 0) updateData.status = status;
			if (visibility !== void 0) updateData.visibility = visibility;
			if (metadata !== void 0) updateData.metadata = {
				...existing.metadata,
				...metadata
			};
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_AGENTS,
				keys: { id },
				data: updateData
			});
			const updatedAgent = await this.getById(id);
			if (!updatedAgent) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_AGENT", "NOT_FOUND_AFTER_UPDATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `Agent ${id} not found after update`,
				details: { agentId: id }
			});
			return updatedAgent;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_AGENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId: id }
			}, error);
		}
	}
	async delete(id) {
		try {
			await this.deleteVersionsByParentId(id);
			await this.#db.delete({
				tableName: _mastra_core_storage.TABLE_AGENTS,
				keys: { id }
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_AGENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId: id }
			}, error);
		}
	}
	async list(args) {
		const { page = 0, perPage: perPageInput, orderBy, authorId, metadata, status, visibility, entityIds, pinFavoritedFor, favoritedOnly } = args || {};
		const { field, direction } = this.parseOrderBy(orderBy);
		if (page < 0) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_AGENTS", "INVALID_PAGE"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { page }
		}, /* @__PURE__ */ new Error("page must be >= 0"));
		const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
		const { offset, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
		try {
			if (entityIds && entityIds.length === 0) return {
				agents: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			const conditions = [];
			const queryParams = [];
			if (status) {
				conditions.push("a.status = ?");
				queryParams.push(status);
			}
			if (authorId !== void 0) {
				conditions.push("a.authorId = ?");
				queryParams.push(authorId);
			}
			if (visibility !== void 0) {
				conditions.push("a.visibility = ?");
				queryParams.push(visibility);
			}
			if (metadata && Object.keys(metadata).length > 0) for (const [key, value] of Object.entries(metadata)) {
				if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_AGENTS", "INVALID_METADATA_KEY"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `Invalid metadata key: ${key}. Keys must be alphanumeric with underscores.`,
					details: { key }
				});
				conditions.push(`json_extract(a.metadata, '$.${key}') = ?`);
				queryParams.push(typeof value === "string" ? value : JSON.stringify(value));
			}
			if (entityIds && entityIds.length > 0) {
				const placeholders = entityIds.map(() => "?").join(", ");
				conditions.push(`a.id IN (${placeholders})`);
				queryParams.push(...entityIds);
			}
			const joinUserId = pinFavoritedFor;
			const useJoin = Boolean(joinUserId);
			let joinClause = "";
			const joinParams = [];
			if (useJoin && joinUserId) {
				joinClause = `LEFT JOIN "${_mastra_core_storage.TABLE_FAVORITES}" s ON s."entityType" = 'agent' AND s."entityId" = a.id AND s."userId" = ?`;
				joinParams.push(joinUserId);
				if (favoritedOnly) conditions.push("s.\"userId\" IS NOT NULL");
			} else if (favoritedOnly) conditions.push("1=0");
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_AGENTS}" a ${joinClause} ${whereClause}`,
				args: [...joinParams, ...queryParams]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				agents: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			const orderByParts = [];
			if (useJoin && joinUserId) orderByParts.push(`(s."userId" IS NOT NULL) DESC`);
			orderByParts.push(`a."${field}" ${direction}`);
			orderByParts.push(`a."id" ASC`);
			const orderByClause = `ORDER BY ${orderByParts.join(", ")}`;
			const limitValue = perPageInput === false ? total : perPage;
			const selectCols = buildSelectColumnsWithAlias(_mastra_core_storage.TABLE_AGENTS, "a");
			return {
				agents: ((await this.#client.execute({
					sql: `SELECT ${selectCols} FROM "${_mastra_core_storage.TABLE_AGENTS}" a ${joinClause} ${whereClause} ${orderByClause} LIMIT ? OFFSET ?`,
					args: [
						...joinParams,
						...queryParams,
						limitValue,
						offset
					]
				})).rows ?? []).map((row) => this.parseRow(row)),
				total,
				page,
				perPage: perPageForResponse,
				hasMore: perPageInput === false ? false : offset + perPage < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_AGENTS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createVersion(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				record: {
					id: input.id,
					agentId: input.agentId,
					versionNumber: input.versionNumber,
					name: input.name ?? null,
					description: input.description ?? null,
					instructions: this.serializeInstructions(input.instructions),
					model: input.model,
					tools: input.tools ?? null,
					defaultOptions: input.defaultOptions ?? null,
					workflows: input.workflows ?? null,
					agents: input.agents ?? null,
					integrationTools: input.integrationTools ?? null,
					toolProviders: input.toolProviders ?? null,
					inputProcessors: input.inputProcessors ?? null,
					outputProcessors: input.outputProcessors ?? null,
					memory: input.memory ?? null,
					scorers: input.scorers ?? null,
					mcpClients: input.mcpClients ?? null,
					requestContextSchema: input.requestContextSchema ?? null,
					workspace: input.workspace ?? null,
					skills: input.skills ?? null,
					skillsFormat: input.skillsFormat ?? null,
					durable: input.durable ?? null,
					browser: input.browser ?? null,
					changedFields: input.changedFields ?? null,
					changeMessage: input.changeMessage ?? null,
					createdAt: now
				}
			});
			return {
				...input,
				createdAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					versionId: input.id,
					agentId: input.agentId
				}
			}, error);
		}
	}
	async getVersion(id) {
		try {
			const result = await this.#db.select({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				keys: { id }
			});
			if (!result) return null;
			return this.parseVersionRow(result);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { versionId: id }
			}, error);
		}
	}
	async getVersions(ids) {
		if (ids.length === 0) return [];
		try {
			return (await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				whereClause: {
					sql: `WHERE id IN (${ids.map(() => "?").join(", ")})`,
					args: ids
				}
			}) ?? []).map((row) => this.parseVersionRow(row));
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { count: ids.length }
			}, error);
		}
	}
	async getVersionByNumber(agentId, versionNumber) {
		try {
			const rows = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				whereClause: {
					sql: "WHERE agentId = ? AND versionNumber = ?",
					args: [agentId, versionNumber]
				},
				limit: 1
			});
			if (!rows || rows.length === 0) return null;
			return this.parseVersionRow(rows[0]);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_VERSION_BY_NUMBER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					agentId,
					versionNumber
				}
			}, error);
		}
	}
	async getLatestVersion(agentId) {
		try {
			const rows = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				whereClause: {
					sql: "WHERE agentId = ?",
					args: [agentId]
				},
				orderBy: "versionNumber DESC",
				limit: 1
			});
			if (!rows || rows.length === 0) return null;
			return this.parseVersionRow(rows[0]);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_LATEST_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId }
			}, error);
		}
	}
	async listVersions(input) {
		const { agentId, page = 0, perPage: perPageInput, orderBy } = input;
		if (page < 0) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_VERSIONS", "INVALID_PAGE"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { page }
		}, /* @__PURE__ */ new Error("page must be >= 0"));
		const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 20);
		const { offset, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
		try {
			const { field, direction } = this.parseVersionOrderBy(orderBy);
			const total = await this.#db.selectTotalCount({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				whereClause: {
					sql: "WHERE agentId = ?",
					args: [agentId]
				}
			});
			if (total === 0) return {
				versions: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			const limitValue = perPageInput === false ? total : perPage;
			return {
				versions: (await this.#db.selectMany({
					tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
					whereClause: {
						sql: "WHERE agentId = ?",
						args: [agentId]
					},
					orderBy: `"${field}" ${direction}`,
					limit: limitValue,
					offset
				})).map((row) => this.parseVersionRow(row)),
				total,
				page,
				perPage: perPageForResponse,
				hasMore: perPageInput === false ? false : offset + perPage < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId }
			}, error);
		}
	}
	async deleteVersion(id) {
		try {
			await this.#db.delete({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				keys: { id }
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { versionId: id }
			}, error);
		}
	}
	async deleteVersionsByParentId(entityId) {
		try {
			const versions = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				whereClause: {
					sql: "WHERE agentId = ?",
					args: [entityId]
				}
			});
			for (const version of versions) await this.#db.delete({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				keys: { id: version.id }
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_VERSIONS_BY_AGENT_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId: entityId }
			}, error);
		}
	}
	async countVersions(agentId) {
		try {
			return await this.#db.selectTotalCount({
				tableName: _mastra_core_storage.TABLE_AGENT_VERSIONS,
				whereClause: {
					sql: "WHERE agentId = ?",
					args: [agentId]
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "COUNT_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { agentId }
			}, error);
		}
	}
	serializeInstructions(instructions) {
		return Array.isArray(instructions) ? JSON.stringify(instructions) : instructions;
	}
	deserializeInstructions(raw) {
		if (!raw) return raw;
		try {
			const parsed = JSON.parse(raw);
			if (Array.isArray(parsed)) return parsed;
		} catch {}
		return raw;
	}
	parseVersionRow(row) {
		return {
			id: row.id,
			agentId: row.agentId,
			versionNumber: row.versionNumber,
			name: row.name,
			description: row.description,
			instructions: this.deserializeInstructions(row.instructions),
			model: this.parseJson(row.model, "model"),
			tools: this.parseJson(row.tools, "tools"),
			defaultOptions: this.parseJson(row.defaultOptions, "defaultOptions"),
			workflows: this.parseJson(row.workflows, "workflows"),
			agents: this.parseJson(row.agents, "agents"),
			integrationTools: this.parseJson(row.integrationTools, "integrationTools"),
			toolProviders: this.parseJson(row.toolProviders, "toolProviders"),
			inputProcessors: this.parseJson(row.inputProcessors, "inputProcessors"),
			outputProcessors: this.parseJson(row.outputProcessors, "outputProcessors"),
			memory: this.parseJson(row.memory, "memory"),
			scorers: this.parseJson(row.scorers, "scorers"),
			mcpClients: this.parseJson(row.mcpClients, "mcpClients"),
			requestContextSchema: this.parseJson(row.requestContextSchema, "requestContextSchema"),
			workspace: this.parseJson(row.workspace, "workspace"),
			skills: this.parseJson(row.skills, "skills"),
			skillsFormat: row.skillsFormat,
			durable: this.parseJson(row.durable, "durable"),
			browser: this.parseJson(row.browser, "browser"),
			changedFields: this.parseJson(row.changedFields, "changedFields"),
			changeMessage: row.changeMessage,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/retention.ts
const DEFAULT_BATCH_SIZE = 1e3;
/**
* Lazily create the single-column anchor index a prune target relies on.
* Called from the prune path (not init) so only deployments that actually
* configure retention pay the index's write/disk overhead. Best-effort: a
* failure is logged and pruning proceeds (correct, just slower).
*/
async function ensureAnchorIndex(db, target, logger) {
	if (!target.indexed) return;
	try {
		await db.ensureIndex({
			indexName: `idx_retention_${target.table}_${target.column}`,
			tableName: target.table,
			column: target.column
		});
	} catch (error) {
		logger?.warn?.(`Failed to ensure retention index on ${target.table}(${target.column}):`, error);
	}
}
async function sleep(ms, signal) {
	if (ms <= 0 || signal?.aborted) return;
	await new Promise((resolve) => {
		const timer = setTimeout(() => {
			signal?.removeEventListener("abort", onAbort);
			resolve();
		}, ms);
		const onAbort = () => {
			clearTimeout(timer);
			resolve();
		};
		signal?.addEventListener("abort", onAbort, { once: true });
	});
}
/**
* Runs the bounded, batched, cancellable delete loop for a set of tables in the
* given order (callers pass children before parents for cascade-safe pruning),
* and returns one {@link PruneResult} per table.
*
* The loop:
* - deletes in chunks of `batchSize` (default 1000), each its own statement;
* - stops a table's loop when a batch deletes fewer rows than requested (drained),
*   or when `maxBatches`/`maxRows` is hit, or the `signal` aborts — the latter
*   three leave `done: false` so the caller can resume;
* - pauses `pauseMs` between batches when set, to avoid starving live traffic.
*
* `prune()` only deletes rows; it never reclaims disk. Freed pages are reused
* by future writes so the file stops growing. Handing disk back to the OS is
* left to the underlying database and the operator to manage.
*/
/** Convert a policy's `maxAge` into a cutoff bound matching the anchor's storage type. */
function cutoffFor(policy, anchorType, now = Date.now()) {
	const cutoffMs = now - (0, _mastra_core_storage.parseDuration)(policy.maxAge);
	return anchorType === "epoch-ms" ? cutoffMs : new Date(cutoffMs).toISOString();
}
/**
* Run the bounded/cancellable batched-delete loop for a single logical target,
* delegating the actual delete of up to `limit` rows to `deleteBatch`. Returns
* `{ deleted, done }`; `done: false` means the loop stopped on a bound or the
* abort signal and eligible rows may remain.
*/
async function runBatchedDelete({ deleteBatch, batchSize, options }) {
	if (!Number.isSafeInteger(batchSize) || batchSize <= 0) throw new Error(`retention batchSize must be a positive integer; received ${batchSize}`);
	let deleted = 0;
	let batches = 0;
	while (true) {
		if (options?.signal?.aborted) return {
			deleted,
			done: false
		};
		if (options?.maxBatches !== void 0 && batches >= options.maxBatches) return {
			deleted,
			done: false
		};
		let limit = batchSize;
		if (options?.maxRows !== void 0) {
			const remaining = options.maxRows - deleted;
			if (remaining <= 0) return {
				deleted,
				done: false
			};
			limit = Math.min(limit, remaining);
		}
		const affected = await deleteBatch(limit);
		deleted += affected;
		batches += 1;
		if (affected < limit) return {
			deleted,
			done: true
		};
		if (options?.pauseMs) await sleep(options.pauseMs, options.signal);
	}
}
async function runPrune({ db, domain, targets, options, logger }) {
	const results = [];
	const now = Date.now();
	for (const target of targets) {
		if (options?.signal?.aborted) {
			results.push({
				domain,
				table: target.table,
				deleted: 0,
				done: false
			});
			continue;
		}
		await ensureAnchorIndex(db, target, logger);
		const cutoff = cutoffFor(target.policy, target.anchorType, now);
		const { deleted, done } = await runBatchedDelete({
			deleteBatch: (limit) => db.pruneBatch({
				tableName: target.table,
				column: target.column,
				cutoff,
				limit
			}),
			batchSize: target.policy.batchSize ?? DEFAULT_BATCH_SIZE,
			options
		});
		results.push({
			domain,
			table: target.table,
			deleted,
			done
		});
	}
	return results;
}
/**
* Resolve a domain's `{ tableKey: policy }` map plus its descriptor into an
* ordered list of {@link PruneTarget}s. `order` lists table keys children-first
* so cascade-dependent rows are removed before their parents. Table keys not in
* `policies` are skipped (unset = keep forever).
*/
function resolveTargets({ policies, descriptor, order }) {
	const targets = [];
	for (const key of order) {
		const policy = policies[key];
		const entry = descriptor[key];
		if (!policy || !entry) continue;
		targets.push({
			table: entry.table,
			column: entry.column,
			anchorType: entry.anchorType ?? "timestamp",
			indexed: entry.indexed ?? true,
			policy
		});
	}
	return targets;
}
//#endregion
//#region src/storage/domains/background-tasks/index.ts
function serializeJson(v) {
	if (v === void 0) return null;
	let serialized;
	try {
		serialized = JSON.stringify(v);
	} catch (error) {
		throw new Error("Failed to serialize background task value as JSON", { cause: error });
	}
	if (serialized === void 0) throw new Error("Failed to serialize background task value as JSON");
	return serialized;
}
function parseJson$4(val) {
	if (val == null) return void 0;
	if (typeof val === "string") try {
		return JSON.parse(val);
	} catch {
		return val;
	}
	return val;
}
function rowToTask(row) {
	return {
		id: String(row.id),
		status: String(row.status),
		toolName: String(row.tool_name),
		toolCallId: String(row.tool_call_id),
		args: parseJson$4(row.args) ?? {},
		agentId: String(row.agent_id),
		threadId: row.thread_id != null ? String(row.thread_id) : void 0,
		resourceId: row.resource_id != null ? String(row.resource_id) : void 0,
		runId: String(row.run_id),
		result: parseJson$4(row.result),
		error: parseJson$4(row.error),
		suspendPayload: parseJson$4(row.suspend_payload),
		retryCount: Number(row.retry_count),
		maxRetries: Number(row.max_retries),
		timeoutMs: Number(row.timeout_ms),
		createdAt: new Date(String(row.createdAt)),
		startedAt: row.startedAt ? new Date(String(row.startedAt)) : void 0,
		suspendedAt: row.suspendedAt ? new Date(String(row.suspendedAt)) : void 0,
		completedAt: row.completedAt ? new Date(String(row.completedAt)) : void 0
	};
}
var BackgroundTasksLibSQL = class BackgroundTasksLibSQL extends _mastra_core_storage.BackgroundTasksStorage {
	/**
	* Completed/failed task records accumulate. Anchored on `completedAt`, which
	* is NULL while a task is in-flight (pending/running/suspended) — so `completedAt
	* < cutoff` never prunes a live task, no explicit status filter needed.
	*/
	static retentionTables = { backgroundTasks: {
		table: _mastra_core_storage.TABLE_BACKGROUND_TASKS,
		column: "completedAt",
		indexed: true
	} };
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_BACKGROUND_TASKS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_BACKGROUND_TASKS]
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_BACKGROUND_TASKS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_BACKGROUND_TASKS],
			ifNotExists: ["suspend_payload", "suspendedAt"]
		});
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_BACKGROUND_TASKS });
	}
	/** Delete completed tasks older than the `backgroundTasks` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: BackgroundTasksLibSQL.retentionTables,
			order: ["backgroundTasks"]
		});
		return runPrune({
			db: this.#db,
			domain: "backgroundTasks",
			targets,
			options,
			logger: this.logger
		});
	}
	async createTask(task) {
		await this.#db.insert({
			tableName: _mastra_core_storage.TABLE_BACKGROUND_TASKS,
			record: {
				id: task.id,
				tool_call_id: task.toolCallId,
				tool_name: task.toolName,
				agent_id: task.agentId,
				thread_id: task.threadId ?? null,
				resource_id: task.resourceId ?? null,
				run_id: task.runId,
				status: task.status,
				args: task.args,
				result: task.result ?? null,
				error: task.error ?? null,
				suspend_payload: task.suspendPayload ?? null,
				retry_count: task.retryCount,
				max_retries: task.maxRetries,
				timeout_ms: task.timeoutMs,
				createdAt: task.createdAt.toISOString(),
				startedAt: task.startedAt?.toISOString() ?? null,
				suspendedAt: task.suspendedAt?.toISOString() ?? null,
				completedAt: task.completedAt?.toISOString() ?? null
			}
		});
	}
	async updateTask(taskId, update, options) {
		const setClauses = [];
		const params = [];
		if ("status" in update) {
			setClauses.push("status = ?");
			params.push(update.status);
		}
		if ("result" in update) {
			setClauses.push("result = jsonb(?)");
			params.push(serializeJson(update.result));
		}
		if ("error" in update) {
			setClauses.push("error = jsonb(?)");
			params.push(serializeJson(update.error));
		}
		if ("suspendPayload" in update) {
			setClauses.push("suspend_payload = jsonb(?)");
			params.push(serializeJson(update.suspendPayload));
		}
		if ("retryCount" in update) {
			setClauses.push("retry_count = ?");
			params.push(update.retryCount);
		}
		if ("startedAt" in update) {
			setClauses.push("startedAt = ?");
			params.push(update.startedAt?.toISOString() ?? null);
		}
		if ("suspendedAt" in update) {
			setClauses.push("suspendedAt = ?");
			params.push(update.suspendedAt?.toISOString() ?? null);
		}
		if ("completedAt" in update) {
			setClauses.push("completedAt = ?");
			params.push(update.completedAt?.toISOString() ?? null);
		}
		if (setClauses.length === 0) return false;
		params.push(taskId);
		let where = "id = ?";
		if (options?.expectedStatus) {
			where += " AND status = ?";
			params.push(options.expectedStatus);
		}
		return (await this.#client.execute({
			sql: `UPDATE ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} SET ${setClauses.join(", ")} WHERE ${where}`,
			args: params
		})).rowsAffected > 0;
	}
	async getTask(taskId) {
		const row = (await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_BACKGROUND_TASKS)} FROM ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} WHERE id = ?`,
			args: [taskId]
		})).rows[0];
		return row ? rowToTask(row) : null;
	}
	async listTasks(filter) {
		const conditions = [];
		const params = [];
		if (filter.status) {
			const statuses = Array.isArray(filter.status) ? filter.status : [filter.status];
			conditions.push(`status IN (${statuses.map(() => "?").join(", ")})`);
			params.push(...statuses);
		}
		if (filter.agentId) {
			conditions.push("agent_id = ?");
			params.push(filter.agentId);
		}
		if (filter.threadId) {
			conditions.push("thread_id = ?");
			params.push(filter.threadId);
		}
		if (filter.runId) {
			conditions.push("run_id = ?");
			params.push(filter.runId);
		}
		if (filter.resourceId) {
			conditions.push("resource_id = ?");
			params.push(filter.resourceId);
		}
		if (filter.toolName) {
			conditions.push("tool_name = ?");
			params.push(filter.toolName);
		}
		if (filter.toolCallId) {
			conditions.push("tool_call_id = ?");
			params.push(filter.toolCallId);
		}
		const dateCol = filter.dateFilterBy === "startedAt" ? "startedAt" : filter.dateFilterBy === "suspendedAt" ? "suspendedAt" : filter.dateFilterBy === "completedAt" ? "completedAt" : "createdAt";
		if (filter.fromDate) {
			conditions.push(`${dateCol} >= ?`);
			params.push(filter.fromDate.toISOString());
		}
		if (filter.toDate) {
			conditions.push(`${dateCol} < ?`);
			params.push(filter.toDate.toISOString());
		}
		const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
		const countResult = await this.#client.execute({
			sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} ${where}`,
			args: [...params]
		});
		const total = Number(countResult.rows[0]?.count ?? 0);
		const orderCol = filter.orderBy === "startedAt" ? "startedAt" : filter.orderBy === "suspendedAt" ? "suspendedAt" : filter.orderBy === "completedAt" ? "completedAt" : "createdAt";
		const direction = filter.orderDirection === "desc" ? "DESC" : "ASC";
		let sql = `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_BACKGROUND_TASKS)} FROM ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} ${where} ORDER BY ${orderCol} ${direction}`;
		if (filter.perPage != null) {
			sql += " LIMIT ?";
			params.push(filter.perPage);
			if (filter.page != null) {
				sql += " OFFSET ?";
				params.push(filter.page * filter.perPage);
			}
		}
		return {
			tasks: (await this.#client.execute({
				sql,
				args: params
			})).rows.map((row) => rowToTask(row)),
			total
		};
	}
	async deleteTask(taskId) {
		await this.#client.execute({
			sql: `DELETE FROM ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} WHERE id = ?`,
			args: [taskId]
		});
	}
	async deleteTasks(filter) {
		const conditions = [];
		const params = [];
		if (filter.status) {
			const statuses = Array.isArray(filter.status) ? filter.status : [filter.status];
			conditions.push(`status IN (${statuses.map(() => "?").join(", ")})`);
			params.push(...statuses);
		}
		const dateCol = filter.dateFilterBy === "startedAt" ? "startedAt" : filter.dateFilterBy === "suspendedAt" ? "suspendedAt" : filter.dateFilterBy === "completedAt" ? "completedAt" : "createdAt";
		if (filter.fromDate) {
			conditions.push(`${dateCol} >= ?`);
			params.push(filter.fromDate.toISOString());
		}
		if (filter.toDate) {
			conditions.push(`${dateCol} < ?`);
			params.push(filter.toDate.toISOString());
		}
		if (filter.agentId) {
			conditions.push("agent_id = ?");
			params.push(filter.agentId);
		}
		if (filter.runId) {
			conditions.push("run_id = ?");
			params.push(filter.runId);
		}
		if (conditions.length === 0) return;
		await this.#client.execute({
			sql: `DELETE FROM ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} WHERE ${conditions.join(" AND ")}`,
			args: params
		});
	}
	async getRunningCount() {
		const result = await this.#client.execute({
			sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} WHERE status = 'running'`,
			args: []
		});
		return Number(result.rows[0]?.count ?? 0);
	}
	async getRunningCountByAgent(agentId) {
		const result = await this.#client.execute({
			sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_BACKGROUND_TASKS} WHERE status = 'running' AND agent_id = ?`,
			args: [agentId]
		});
		return Number(result.rows[0]?.count ?? 0);
	}
};
//#endregion
//#region src/storage/domains/blobs/index.ts
var BlobsLibSQL = class extends _mastra_core_storage.BlobStore {
	#db;
	#client;
	static MANAGED_TABLES = [_mastra_core_storage.TABLE_SKILL_BLOBS];
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SKILL_BLOBS,
			schema: _mastra_core_storage.SKILL_BLOBS_SCHEMA
		});
	}
	async put(entry) {
		const now = entry.createdAt ?? /* @__PURE__ */ new Date();
		await this.#client.execute({
			sql: `INSERT OR IGNORE INTO "${_mastra_core_storage.TABLE_SKILL_BLOBS}" ("hash", "content", "size", "mimeType", "createdAt") VALUES (?, ?, ?, ?, ?)`,
			args: [
				entry.hash,
				entry.content,
				entry.size,
				entry.mimeType ?? null,
				now.toISOString()
			]
		});
	}
	async get(hash) {
		const result = await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILL_BLOBS)} FROM "${_mastra_core_storage.TABLE_SKILL_BLOBS}" WHERE "hash" = ?`,
			args: [hash]
		});
		if (!result.rows.length) return null;
		return this.#parseRow(result.rows[0]);
	}
	async has(hash) {
		return (await this.#client.execute({
			sql: `SELECT 1 FROM "${_mastra_core_storage.TABLE_SKILL_BLOBS}" WHERE "hash" = ? LIMIT 1`,
			args: [hash]
		})).rows.length > 0;
	}
	async delete(hash) {
		return (await this.#client.execute({
			sql: `DELETE FROM "${_mastra_core_storage.TABLE_SKILL_BLOBS}" WHERE "hash" = ?`,
			args: [hash]
		})).rowsAffected > 0;
	}
	async putMany(entries) {
		if (entries.length === 0) return;
		await this.#db.batchInsert({
			tableName: _mastra_core_storage.TABLE_SKILL_BLOBS,
			records: entries.map((entry) => ({
				hash: entry.hash,
				content: entry.content,
				size: entry.size,
				mimeType: entry.mimeType ?? null,
				createdAt: (entry.createdAt ?? /* @__PURE__ */ new Date()).toISOString()
			}))
		});
	}
	async getMany(hashes) {
		const result = /* @__PURE__ */ new Map();
		if (hashes.length === 0) return result;
		const batchSize = 500;
		for (let i = 0; i < hashes.length; i += batchSize) {
			const batch = hashes.slice(i, i + batchSize);
			const placeholders = batch.map(() => "?").join(", ");
			const queryResult = await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILL_BLOBS)} FROM "${_mastra_core_storage.TABLE_SKILL_BLOBS}" WHERE "hash" IN (${placeholders})`,
				args: batch
			});
			for (const row of queryResult.rows) {
				const entry = this.#parseRow(row);
				result.set(entry.hash, entry);
			}
		}
		return result;
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SKILL_BLOBS });
	}
	#parseRow(row) {
		return {
			hash: row.hash,
			content: row.content,
			size: Number(row.size),
			mimeType: row.mimeType || void 0,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/domains/channels/index.ts
var ChannelsLibSQL = class extends _mastra_core_storage.ChannelsStorage {
	#db;
	#client;
	static MANAGED_TABLES = [_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS, _mastra_core_storage.TABLE_CHANNEL_CONFIG];
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS]
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_CHANNEL_CONFIG,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_CHANNEL_CONFIG]
		});
		await this.#client.batch([{
			sql: `CREATE UNIQUE INDEX IF NOT EXISTS idx_channel_installations_webhook ON "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" ("webhookId")`,
			args: []
		}, {
			sql: `CREATE INDEX IF NOT EXISTS idx_channel_installations_platform_agent ON "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" ("platform", "agentId")`,
			args: []
		}], "write");
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_CHANNEL_CONFIG });
	}
	async saveInstallation(installation) {
		const now = (/* @__PURE__ */ new Date()).toISOString();
		await this.#client.execute({
			sql: `
        INSERT INTO "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" (id, platform, agentId, status, webhookId, data, configHash, error, createdAt, updatedAt)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        ON CONFLICT(id) DO UPDATE SET
          platform = excluded.platform,
          agentId = excluded.agentId,
          status = excluded.status,
          webhookId = excluded.webhookId,
          data = excluded.data,
          configHash = excluded.configHash,
          error = excluded.error,
          updatedAt = excluded.updatedAt
      `,
			args: [
				installation.id,
				installation.platform,
				installation.agentId,
				installation.status,
				installation.webhookId ?? null,
				JSON.stringify(installation.data),
				installation.configHash ?? null,
				installation.error ?? null,
				installation.createdAt?.toISOString() ?? now,
				now
			]
		});
	}
	async getInstallation(id) {
		const row = (await this.#client.execute({
			sql: `SELECT * FROM "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" WHERE id = ?`,
			args: [id]
		})).rows?.[0];
		return row ? this.#parseInstallationRow(row) : null;
	}
	async getInstallationByAgent(platform, agentId) {
		const row = (await this.#client.execute({
			sql: `SELECT * FROM "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" WHERE platform = ? AND agentId = ? ORDER BY CASE status WHEN 'active' THEN 0 WHEN 'pending' THEN 1 ELSE 2 END, updatedAt DESC LIMIT 1`,
			args: [platform, agentId]
		})).rows?.[0];
		return row ? this.#parseInstallationRow(row) : null;
	}
	async getInstallationByWebhookId(webhookId) {
		const row = (await this.#client.execute({
			sql: `SELECT * FROM "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" WHERE webhookId = ?`,
			args: [webhookId]
		})).rows?.[0];
		return row ? this.#parseInstallationRow(row) : null;
	}
	async listInstallations(platform) {
		return (await this.#client.execute({
			sql: `SELECT * FROM "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" WHERE platform = ? ORDER BY createdAt DESC`,
			args: [platform]
		})).rows.map((row) => this.#parseInstallationRow(row));
	}
	async deleteInstallation(id) {
		await this.#client.execute({
			sql: `DELETE FROM "${_mastra_core_storage.TABLE_CHANNEL_INSTALLATIONS}" WHERE id = ?`,
			args: [id]
		});
	}
	async saveConfig(config) {
		await this.#client.execute({
			sql: `
        INSERT INTO "${_mastra_core_storage.TABLE_CHANNEL_CONFIG}" (platform, data, updatedAt)
        VALUES (?, ?, ?)
        ON CONFLICT(platform) DO UPDATE SET
          data = excluded.data,
          updatedAt = excluded.updatedAt
      `,
			args: [
				config.platform,
				JSON.stringify(config.data),
				config.updatedAt.toISOString()
			]
		});
	}
	async getConfig(platform) {
		const row = (await this.#client.execute({
			sql: `SELECT * FROM "${_mastra_core_storage.TABLE_CHANNEL_CONFIG}" WHERE platform = ?`,
			args: [platform]
		})).rows?.[0];
		if (!row) return null;
		return {
			platform: row.platform,
			data: JSON.parse(row.data || "{}"),
			updatedAt: new Date(row.updatedAt)
		};
	}
	async deleteConfig(platform) {
		await this.#client.execute({
			sql: `DELETE FROM "${_mastra_core_storage.TABLE_CHANNEL_CONFIG}" WHERE platform = ?`,
			args: [platform]
		});
	}
	#parseInstallationRow(row) {
		return {
			id: row.id,
			platform: row.platform,
			agentId: row.agentId,
			status: row.status,
			webhookId: row.webhookId || void 0,
			data: JSON.parse(row.data || "{}"),
			configHash: row.configHash || void 0,
			error: row.error || void 0,
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
};
//#endregion
//#region src/storage/domains/utils.ts
/**
* Build additional `AND col = ?` conditions for a tenancy read-scope filter.
* Returned in the shape expected by libsql's parameterized SQL builders.
* When `filters` is undefined or empty, returns empty arrays (no scoping).
*
* Shared across domains (datasets, experiments, ...) so tenancy predicates stay
* consistent and cross-tenant reads/deletes do not leak.
*/
function tenancyWhere(filters) {
	const conditions = [];
	const params = [];
	if (filters?.organizationId !== void 0) {
		conditions.push("organizationId = ?");
		params.push(filters.organizationId);
	}
	if (filters?.projectId !== void 0) {
		conditions.push("projectId = ?");
		params.push(filters.projectId);
	}
	return {
		conditions,
		params
	};
}
/**
* Build a WHERE clause that scopes a query to `<idColumn> = ?` plus any tenancy
* predicates. Small DRY helper on top of {@link tenancyWhere} for the common
* pattern used by tenancy-aware getters and deletes.
*
* @example
*   const { sql, args } = buildScopedWhere('id', args.id, args.filters);
*   await client.execute({ sql: `SELECT * FROM t WHERE ${sql}`, args });
*/
function buildScopedWhere(idColumn, idValue, filters) {
	const { conditions, params } = tenancyWhere(filters);
	return {
		sql: [`${idColumn} = ?`, ...conditions].join(" AND "),
		args: [idValue, ...params]
	};
}
//#endregion
//#region src/storage/domains/datasets/index.ts
/** Serialize a value for a jsonb column. Returns null for null/undefined. */
function jsonbArg(value) {
	return value === void 0 || value === null ? null : JSON.stringify(value);
}
var DatasetsLibSQL = class extends _mastra_core_storage.DatasetsStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_DATASETS,
			schema: _mastra_core_storage.DATASETS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_DATASET_ITEMS,
			schema: _mastra_core_storage.DATASET_ITEMS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_DATASET_VERSIONS,
			schema: _mastra_core_storage.DATASET_VERSIONS_SCHEMA
		});
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "requestContextSchema", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "tags", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "targetType", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "targetIds", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "scorerIds", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "organizationId", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "projectId", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "candidateKey", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASETS, "candidateId", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "requestContext", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "source", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "expectedTrajectory", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "organizationId", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "projectId", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "toolMocks", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "unmockedToolPolicy", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "scorerIds", "TEXT");
		await this.#addColumnIfNotExists(_mastra_core_storage.TABLE_DATASET_ITEMS, "externalId", "TEXT");
		await this.#client.batch([
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_dataset_items_dataset_validto ON "${_mastra_core_storage.TABLE_DATASET_ITEMS}" ("datasetId", "validTo")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_dataset_items_dataset_version ON "${_mastra_core_storage.TABLE_DATASET_ITEMS}" ("datasetId", "datasetVersion")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_dataset_items_dataset_externalid_version ON "${_mastra_core_storage.TABLE_DATASET_ITEMS}" ("datasetId", "externalId", "datasetVersion")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_dataset_items_dataset_validto_deleted ON "${_mastra_core_storage.TABLE_DATASET_ITEMS}" ("datasetId", "validTo", "isDeleted")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_dataset_versions_dataset_version ON "${_mastra_core_storage.TABLE_DATASET_VERSIONS}" ("datasetId", "version")`,
				args: []
			},
			{
				sql: `CREATE UNIQUE INDEX IF NOT EXISTS idx_dataset_versions_dataset_version_unique ON "${_mastra_core_storage.TABLE_DATASET_VERSIONS}" ("datasetId", "version")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_datasets_tenancy_createdat ON "${_mastra_core_storage.TABLE_DATASETS}" ("organizationId", "projectId", "createdAt", "id")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_datasets_tenancy_candidate ON "${_mastra_core_storage.TABLE_DATASETS}" ("organizationId", "projectId", "candidateKey", "candidateId")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_dataset_items_tenancy_list ON "${_mastra_core_storage.TABLE_DATASET_ITEMS}" ("organizationId", "projectId", "datasetId", "validTo", "isDeleted")`,
				args: []
			}
		], "write");
	}
	async #addColumnIfNotExists(table, column, sqlType) {
		if (!await this.#db.hasColumn(table, column)) await this.#client.execute({
			sql: `ALTER TABLE "${table}" ADD COLUMN "${column}" ${sqlType}`,
			args: []
		});
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_DATASET_VERSIONS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_DATASET_ITEMS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_DATASETS });
	}
	async experimentTablesExist() {
		const row = (await this.#client.execute({
			sql: `SELECT COUNT(*) AS c FROM sqlite_master WHERE type = 'table' AND name IN (?, ?)`,
			args: [_mastra_core_storage.TABLE_EXPERIMENTS, _mastra_core_storage.TABLE_EXPERIMENT_RESULTS]
		})).rows?.[0];
		return Number(row?.c ?? 0) === 2;
	}
	transformDatasetRow(row) {
		return {
			id: row.id,
			name: row.name,
			description: row.description,
			metadata: row.metadata ? (0, _mastra_core_storage.safelyParseJSON)(row.metadata) : void 0,
			inputSchema: row.inputSchema ? (0, _mastra_core_storage.safelyParseJSON)(row.inputSchema) : void 0,
			groundTruthSchema: row.groundTruthSchema ? (0, _mastra_core_storage.safelyParseJSON)(row.groundTruthSchema) : void 0,
			requestContextSchema: row.requestContextSchema ? (0, _mastra_core_storage.safelyParseJSON)(row.requestContextSchema) : void 0,
			tags: row.tags ? (0, _mastra_core_storage.safelyParseJSON)(row.tags) : void 0,
			targetType: row.targetType || void 0,
			targetIds: row.targetIds ? (0, _mastra_core_storage.safelyParseJSON)(row.targetIds) : void 0,
			scorerIds: row.scorerIds ? (0, _mastra_core_storage.safelyParseJSON)(row.scorerIds) : void 0,
			version: row.version,
			organizationId: row.organizationId ?? null,
			projectId: row.projectId ?? null,
			candidateKey: row.candidateKey ?? null,
			candidateId: row.candidateId ?? null,
			createdAt: (0, _mastra_core_storage.ensureDate)(row.createdAt),
			updatedAt: (0, _mastra_core_storage.ensureDate)(row.updatedAt)
		};
	}
	transformItemRow(row) {
		const metadata = row.metadata ? (0, _mastra_core_storage.safelyParseJSON)(row.metadata) : void 0;
		const emptyValue = metadata?.__purged === true ? null : void 0;
		return {
			id: row.id,
			datasetId: row.datasetId,
			datasetVersion: row.datasetVersion,
			externalId: row.externalId ?? null,
			organizationId: row.organizationId ?? null,
			projectId: row.projectId ?? null,
			input: (0, _mastra_core_storage.safelyParseJSON)(row.input),
			groundTruth: row.groundTruth ? (0, _mastra_core_storage.safelyParseJSON)(row.groundTruth) : emptyValue,
			expectedTrajectory: row.expectedTrajectory ? (0, _mastra_core_storage.safelyParseJSON)(row.expectedTrajectory) : emptyValue,
			toolMocks: row.toolMocks ? (0, _mastra_core_storage.safelyParseJSON)(row.toolMocks) : emptyValue,
			unmockedToolPolicy: row.unmockedToolPolicy ?? emptyValue,
			scorerIds: row.scorerIds ? (0, _mastra_core_storage.safelyParseJSON)(row.scorerIds) : emptyValue,
			requestContext: row.requestContext ? (0, _mastra_core_storage.safelyParseJSON)(row.requestContext) : emptyValue,
			metadata,
			source: row.source ? (0, _mastra_core_storage.safelyParseJSON)(row.source) : emptyValue,
			createdAt: (0, _mastra_core_storage.ensureDate)(row.createdAt),
			updatedAt: (0, _mastra_core_storage.ensureDate)(row.updatedAt)
		};
	}
	transformItemRowFull(row) {
		const metadata = row.metadata ? (0, _mastra_core_storage.safelyParseJSON)(row.metadata) : void 0;
		const emptyValue = metadata?.__purged === true ? null : void 0;
		return {
			id: row.id,
			datasetId: row.datasetId,
			datasetVersion: row.datasetVersion,
			externalId: row.externalId ?? null,
			organizationId: row.organizationId ?? null,
			projectId: row.projectId ?? null,
			validTo: row.validTo,
			isDeleted: Boolean(row.isDeleted),
			input: (0, _mastra_core_storage.safelyParseJSON)(row.input),
			groundTruth: row.groundTruth ? (0, _mastra_core_storage.safelyParseJSON)(row.groundTruth) : emptyValue,
			expectedTrajectory: row.expectedTrajectory ? (0, _mastra_core_storage.safelyParseJSON)(row.expectedTrajectory) : emptyValue,
			toolMocks: row.toolMocks ? (0, _mastra_core_storage.safelyParseJSON)(row.toolMocks) : emptyValue,
			unmockedToolPolicy: row.unmockedToolPolicy ?? emptyValue,
			scorerIds: row.scorerIds ? (0, _mastra_core_storage.safelyParseJSON)(row.scorerIds) : emptyValue,
			requestContext: row.requestContext ? (0, _mastra_core_storage.safelyParseJSON)(row.requestContext) : emptyValue,
			metadata,
			source: row.source ? (0, _mastra_core_storage.safelyParseJSON)(row.source) : emptyValue,
			createdAt: (0, _mastra_core_storage.ensureDate)(row.createdAt),
			updatedAt: (0, _mastra_core_storage.ensureDate)(row.updatedAt)
		};
	}
	transformDatasetVersionRow(row) {
		return {
			id: row.id,
			datasetId: row.datasetId,
			version: row.version,
			createdAt: (0, _mastra_core_storage.ensureDate)(row.createdAt)
		};
	}
	async createDataset(input) {
		try {
			const id = input.id ?? crypto.randomUUID();
			if (input.id !== void 0) this.validateCallerDefinedDatasetId(input.id);
			const now = /* @__PURE__ */ new Date();
			const nowIso = now.toISOString();
			await this.#db.insertOnly({
				tableName: _mastra_core_storage.TABLE_DATASETS,
				record: {
					id,
					name: input.name,
					description: input.description ?? null,
					metadata: input.metadata,
					inputSchema: input.inputSchema ?? null,
					groundTruthSchema: input.groundTruthSchema ?? null,
					requestContextSchema: input.requestContextSchema ?? null,
					targetType: input.targetType ?? null,
					targetIds: input.targetIds ?? null,
					scorerIds: input.scorerIds ?? null,
					version: 0,
					organizationId: input.organizationId ?? null,
					projectId: input.projectId ?? null,
					candidateKey: input.candidateKey ?? null,
					candidateId: input.candidateId ?? null,
					createdAt: nowIso,
					updatedAt: nowIso
				}
			});
			return {
				id,
				name: input.name,
				description: input.description,
				metadata: input.metadata,
				inputSchema: input.inputSchema ?? void 0,
				groundTruthSchema: input.groundTruthSchema ?? void 0,
				requestContextSchema: input.requestContextSchema ?? void 0,
				targetType: input.targetType ?? void 0,
				targetIds: input.targetIds ?? void 0,
				scorerIds: input.scorerIds ?? void 0,
				version: 0,
				organizationId: input.organizationId ?? null,
				projectId: input.projectId ?? null,
				candidateKey: input.candidateKey ?? null,
				candidateId: input.candidateId ?? null,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (input.id !== void 0 && (0, _mastra_core_storage.hasErrorCode)(error, /* @__PURE__ */ new Set([
				"SQLITE_CONSTRAINT",
				"SQLITE_CONSTRAINT_PRIMARYKEY",
				"SQLITE_CONSTRAINT_UNIQUE"
			]))) {
				const existing = await this.getDatasetById({ id: input.id });
				if (existing) return this.resolveExistingDataset(existing, {
					...input,
					id: input.id
				});
			}
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_DATASET", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getDatasetById({ id, filters }) {
		try {
			const { conditions, params } = tenancyWhere(filters);
			const whereSql = ["id = ?", ...conditions].join(" AND ");
			const result = await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASETS)} FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE ${whereSql}`,
				args: [id, ...params]
			});
			return result.rows?.[0] ? this.transformDatasetRow(result.rows[0]) : null;
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_DATASET", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async _doUpdateDataset(args) {
		try {
			const existing = await this.getDatasetById({
				id: args.id,
				filters: args.filters
			});
			if (!existing) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_DATASET", "NOT_FOUND"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { datasetId: args.id }
			});
			const now = (/* @__PURE__ */ new Date()).toISOString();
			const updates = ["updatedAt = ?"];
			const values = [now];
			if (args.name !== void 0) {
				updates.push("name = ?");
				values.push(args.name);
			}
			if (args.description !== void 0) {
				updates.push("description = ?");
				values.push(args.description);
			}
			if (args.metadata !== void 0) {
				updates.push("metadata = ?");
				values.push(JSON.stringify(args.metadata));
			}
			if (args.inputSchema !== void 0) {
				updates.push("inputSchema = ?");
				values.push(args.inputSchema === null ? null : JSON.stringify(args.inputSchema));
			}
			if (args.groundTruthSchema !== void 0) {
				updates.push("groundTruthSchema = ?");
				values.push(args.groundTruthSchema === null ? null : JSON.stringify(args.groundTruthSchema));
			}
			if (args.requestContextSchema !== void 0) {
				updates.push("requestContextSchema = ?");
				values.push(args.requestContextSchema === null ? null : JSON.stringify(args.requestContextSchema));
			}
			if (args.tags !== void 0) {
				updates.push("tags = ?");
				values.push(args.tags === null ? null : JSON.stringify(args.tags));
			}
			if (args.targetType !== void 0) {
				updates.push("targetType = ?");
				values.push(args.targetType === null ? null : args.targetType);
			}
			if (args.targetIds !== void 0) {
				updates.push("targetIds = ?");
				values.push(args.targetIds === null ? null : JSON.stringify(args.targetIds));
			}
			if (args.scorerIds !== void 0) {
				updates.push("scorerIds = ?");
				values.push(args.scorerIds === null ? null : JSON.stringify(args.scorerIds));
			}
			values.push(args.id);
			await this.#client.execute({
				sql: `UPDATE ${_mastra_core_storage.TABLE_DATASETS} SET ${updates.join(", ")} WHERE id = ?`,
				args: values
			});
			return {
				...existing,
				name: args.name ?? existing.name,
				description: args.description ?? existing.description,
				metadata: args.metadata ?? existing.metadata,
				inputSchema: (args.inputSchema !== void 0 ? args.inputSchema : existing.inputSchema) ?? void 0,
				groundTruthSchema: (args.groundTruthSchema !== void 0 ? args.groundTruthSchema : existing.groundTruthSchema) ?? void 0,
				requestContextSchema: (args.requestContextSchema !== void 0 ? args.requestContextSchema : existing.requestContextSchema) ?? void 0,
				tags: (args.tags !== void 0 ? args.tags : existing.tags) ?? void 0,
				targetType: (args.targetType !== void 0 ? args.targetType : existing.targetType) ?? void 0,
				targetIds: (args.targetIds !== void 0 ? args.targetIds : existing.targetIds) ?? void 0,
				scorerIds: (args.scorerIds !== void 0 ? args.scorerIds : existing.scorerIds) ?? void 0,
				updatedAt: new Date(now)
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_DATASET", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteDataset({ id, filters }) {
		try {
			const { conditions, params } = tenancyWhere(filters);
			const scopedWhere = ["id = ?", ...conditions].join(" AND ");
			if (!(await this.#client.execute({
				sql: `SELECT id FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE ${scopedWhere}`,
				args: [id, ...params]
			})).rows?.[0]) return;
			const experimentTablesExist = await this.experimentTablesExist();
			const statements = [];
			if (experimentTablesExist) {
				statements.push({
					sql: `DELETE FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE experimentId IN (SELECT id FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} WHERE datasetId = ?)`,
					args: [id]
				});
				statements.push({
					sql: `UPDATE ${_mastra_core_storage.TABLE_EXPERIMENTS} SET datasetId = NULL, datasetVersion = NULL WHERE datasetId = ?`,
					args: [id]
				});
			}
			statements.push({
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_DATASET_VERSIONS} WHERE datasetId = ?`,
				args: [id]
			});
			statements.push({
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE datasetId = ?`,
				args: [id]
			});
			statements.push({
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE ${scopedWhere}`,
				args: [id, ...params]
			});
			await this.#client.batch(statements, "write");
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_DATASET", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listDatasets(args) {
		try {
			const { page, perPage: perPageInput } = args.pagination;
			const filterConditions = [];
			const filterParams = [];
			if (args.filters?.organizationId !== void 0) {
				filterConditions.push("organizationId = ?");
				filterParams.push(args.filters.organizationId);
			}
			if (args.filters?.projectId !== void 0) {
				filterConditions.push("projectId = ?");
				filterParams.push(args.filters.projectId);
			}
			if (args.filters?.candidateKey !== void 0) {
				filterConditions.push("candidateKey = ?");
				filterParams.push(args.filters.candidateKey);
			}
			if (args.filters?.candidateId !== void 0) {
				filterConditions.push("candidateId = ?");
				filterParams.push(args.filters.candidateId);
			}
			if (args.filters?.targetType !== void 0) {
				filterConditions.push("targetType = ?");
				filterParams.push(args.filters.targetType);
			}
			if (args.filters?.targetIds !== void 0 && args.filters.targetIds.length > 0) {
				const placeholders = args.filters.targetIds.map(() => "?").join(",");
				filterConditions.push(`EXISTS (SELECT 1 FROM json_each(${_mastra_core_storage.TABLE_DATASETS}.targetIds) WHERE value IN (${placeholders}))`);
				for (const id of args.filters.targetIds) filterParams.push(id);
			}
			if (args.filters?.name !== void 0 && args.filters.name.length > 0) {
				filterConditions.push("LOWER(name) LIKE ?");
				filterParams.push(`%${args.filters.name.toLowerCase()}%`);
			}
			const whereClause = filterConditions.length > 0 ? `WHERE ${filterConditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_DATASETS} ${whereClause}`,
				args: filterParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				datasets: [],
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				}
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				datasets: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASETS)} FROM ${_mastra_core_storage.TABLE_DATASETS} ${whereClause} ORDER BY createdAt DESC, id ASC LIMIT ? OFFSET ?`,
					args: [
						...filterParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformDatasetRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_DATASETS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async _doAddItem(args) {
		try {
			const dataset = await this.getDatasetById({ id: args.datasetId });
			const id = crypto.randomUUID();
			const versionId = crypto.randomUUID();
			const now = /* @__PURE__ */ new Date();
			const nowIso = now.toISOString();
			const results = await this.#client.batch([
				{
					sql: `UPDATE ${_mastra_core_storage.TABLE_DATASETS} SET version = version + 1 WHERE id = ? RETURNING version`,
					args: [args.datasetId]
				},
				{
					sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_ITEMS} (id,datasetId,datasetVersion,externalId,organizationId,projectId,validTo,isDeleted,input,groundTruth,expectedTrajectory,toolMocks,unmockedToolPolicy,scorerIds,requestContext,metadata,source,createdAt,updatedAt) VALUES (?,?,(SELECT version FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?),?,(SELECT organizationId FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?),(SELECT projectId FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?),NULL,0,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,?)`,
					args: [
						id,
						args.datasetId,
						args.datasetId,
						args.externalId ?? null,
						args.datasetId,
						args.datasetId,
						jsonbArg(args.input),
						jsonbArg(args.groundTruth),
						jsonbArg(args.expectedTrajectory),
						jsonbArg(args.toolMocks),
						args.unmockedToolPolicy ?? null,
						jsonbArg(args.scorerIds),
						jsonbArg(args.requestContext),
						jsonbArg(args.metadata),
						jsonbArg(args.source),
						nowIso,
						nowIso
					]
				},
				{
					sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_VERSIONS} (id, datasetId, version, createdAt) VALUES (?, ?, (SELECT version FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?), ?)`,
					args: [
						versionId,
						args.datasetId,
						args.datasetId,
						nowIso
					]
				}
			], "write");
			const newVersion = Number(results[0].rows[0].version);
			return {
				id,
				datasetId: args.datasetId,
				datasetVersion: newVersion,
				externalId: args.externalId ?? null,
				organizationId: dataset?.organizationId ?? null,
				projectId: dataset?.projectId ?? null,
				input: args.input,
				groundTruth: args.groundTruth,
				expectedTrajectory: args.expectedTrajectory,
				toolMocks: args.toolMocks,
				unmockedToolPolicy: args.unmockedToolPolicy,
				scorerIds: args.scorerIds,
				requestContext: args.requestContext,
				metadata: args.metadata,
				source: args.source,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "ADD_ITEM", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async _doUpdateItem(args) {
		try {
			return await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const itemResult = await tx.execute({
						sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE id = ? AND validTo IS NULL AND isDeleted = 0`,
						args: [args.id]
					});
					const existing = itemResult.rows[0] ? this.transformItemRow(itemResult.rows[0]) : null;
					if (!existing) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_ITEM", "NOT_FOUND"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: { itemId: args.id }
					});
					if (existing.datasetId !== args.datasetId) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_ITEM", "DATASET_MISMATCH"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: {
							itemId: args.id,
							expectedDatasetId: args.datasetId,
							actualDatasetId: existing.datasetId
						}
					});
					if (existing.metadata?.__purged === true) throw new _mastra_core_error.MastraError({
						id: "DATASET_ITEM_PURGED",
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: {
							datasetId: args.datasetId,
							itemId: args.id
						},
						text: `Purged dataset item cannot be updated: ${args.id}`
					});
					const dataset = (await tx.execute({
						sql: `SELECT organizationId, projectId FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?`,
						args: [args.datasetId]
					})).rows[0];
					const organizationId = typeof dataset?.organizationId === "string" ? dataset.organizationId : null;
					const projectId = typeof dataset?.projectId === "string" ? dataset.projectId : null;
					const versionId = crypto.randomUUID();
					const now = /* @__PURE__ */ new Date();
					const nowIso = now.toISOString();
					const mergedInput = args.input !== void 0 ? args.input : existing.input;
					const mergedGroundTruth = args.groundTruth !== void 0 ? args.groundTruth : existing.groundTruth;
					const mergedExpectedTrajectory = args.expectedTrajectory !== void 0 ? args.expectedTrajectory : existing.expectedTrajectory;
					const mergedToolMocks = args.toolMocks !== void 0 ? args.toolMocks : existing.toolMocks;
					const mergedUnmockedToolPolicy = args.unmockedToolPolicy !== void 0 ? args.unmockedToolPolicy : existing.unmockedToolPolicy;
					const mergedScorerIds = args.scorerIds !== void 0 ? args.scorerIds ?? void 0 : existing.scorerIds;
					const mergedRequestContext = args.requestContext !== void 0 ? args.requestContext : existing.requestContext;
					const mergedMetadata = args.metadata !== void 0 ? args.metadata : existing.metadata;
					const mergedSource = args.source !== void 0 ? args.source : existing.source;
					const versionResult = await tx.execute({
						sql: `UPDATE ${_mastra_core_storage.TABLE_DATASETS} SET version = version + 1 WHERE id = ? RETURNING version`,
						args: [args.datasetId]
					});
					const newVersion = Number(versionResult.rows[0].version);
					await tx.execute({
						sql: `UPDATE ${_mastra_core_storage.TABLE_DATASET_ITEMS} SET validTo = ? WHERE id = ? AND validTo IS NULL AND isDeleted = 0`,
						args: [newVersion, args.id]
					});
					await tx.execute({
						sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_ITEMS} (id,datasetId,datasetVersion,externalId,organizationId,projectId,validTo,isDeleted,input,groundTruth,expectedTrajectory,toolMocks,unmockedToolPolicy,scorerIds,requestContext,metadata,source,createdAt,updatedAt) VALUES (?,?,?,?,?,?,NULL,0,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,?)`,
						args: [
							args.id,
							args.datasetId,
							newVersion,
							existing.externalId ?? null,
							organizationId,
							projectId,
							jsonbArg(mergedInput),
							jsonbArg(mergedGroundTruth),
							jsonbArg(mergedExpectedTrajectory),
							jsonbArg(mergedToolMocks),
							mergedUnmockedToolPolicy ?? null,
							jsonbArg(mergedScorerIds),
							jsonbArg(mergedRequestContext),
							jsonbArg(mergedMetadata),
							jsonbArg(mergedSource),
							existing.createdAt.toISOString(),
							nowIso
						]
					});
					await tx.execute({
						sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_VERSIONS} (id, datasetId, version, createdAt) VALUES (?, ?, ?, ?)`,
						args: [
							versionId,
							args.datasetId,
							newVersion,
							nowIso
						]
					});
					await tx.commit();
					return {
						...existing,
						datasetVersion: newVersion,
						organizationId,
						projectId,
						input: mergedInput,
						groundTruth: mergedGroundTruth,
						expectedTrajectory: mergedExpectedTrajectory,
						toolMocks: mergedToolMocks,
						unmockedToolPolicy: mergedUnmockedToolPolicy,
						scorerIds: mergedScorerIds,
						requestContext: mergedRequestContext,
						metadata: mergedMetadata,
						source: mergedSource,
						updatedAt: now
					};
				} catch (error) {
					if (!tx.closed) await tx.rollback().catch((rollbackError) => {
						throw new AggregateError([error, rollbackError], "Transaction and rollback both failed");
					});
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_ITEM", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async _doDeleteItem({ id, datasetId }) {
		try {
			await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const itemResult = await tx.execute({
						sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE id = ? AND validTo IS NULL AND isDeleted = 0`,
						args: [id]
					});
					const existing = itemResult.rows[0] ? this.transformItemRow(itemResult.rows[0]) : null;
					if (!existing) {
						await tx.commit();
						return;
					}
					if (existing.datasetId !== datasetId) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_ITEM", "DATASET_MISMATCH"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: {
							itemId: id,
							expectedDatasetId: datasetId,
							actualDatasetId: existing.datasetId
						}
					});
					const versionId = crypto.randomUUID();
					const nowIso = (/* @__PURE__ */ new Date()).toISOString();
					const dataset = (await tx.execute({
						sql: `UPDATE ${_mastra_core_storage.TABLE_DATASETS} SET version = version + 1 WHERE id = ? RETURNING version, organizationId, projectId`,
						args: [datasetId]
					})).rows[0];
					const newVersion = Number(dataset.version);
					await tx.execute({
						sql: `UPDATE ${_mastra_core_storage.TABLE_DATASET_ITEMS} SET validTo = ? WHERE id = ? AND validTo IS NULL AND isDeleted = 0`,
						args: [newVersion, id]
					});
					await tx.execute({
						sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_ITEMS} (id,datasetId,datasetVersion,externalId,organizationId,projectId,validTo,isDeleted,input,groundTruth,expectedTrajectory,toolMocks,unmockedToolPolicy,scorerIds,requestContext,metadata,source,createdAt,updatedAt) VALUES (?,?,?,?,?,?,NULL,1,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,?)`,
						args: [
							id,
							datasetId,
							newVersion,
							existing.externalId ?? null,
							dataset.organizationId ?? null,
							dataset.projectId ?? null,
							jsonbArg(existing.input),
							jsonbArg(existing.groundTruth),
							jsonbArg(existing.expectedTrajectory),
							jsonbArg(existing.toolMocks),
							existing.unmockedToolPolicy ?? null,
							jsonbArg(existing.scorerIds),
							jsonbArg(existing.requestContext),
							jsonbArg(existing.metadata),
							jsonbArg(existing.source),
							existing.createdAt.toISOString(),
							nowIso
						]
					});
					await tx.execute({
						sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_VERSIONS} (id, datasetId, version, createdAt) VALUES (?, ?, ?, ?)`,
						args: [
							versionId,
							datasetId,
							newVersion,
							nowIso
						]
					});
					await tx.commit();
				} catch (error) {
					if (!tx.closed) await tx.rollback().catch((rollbackError) => {
						throw new AggregateError([error, rollbackError], "Transaction and rollback both failed");
					});
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_ITEM", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async _doPurgeItem({ id, datasetId }) {
		try {
			const experimentTablesExist = await this.experimentTablesExist();
			await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					if (!(await tx.execute({
						sql: `SELECT id FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE id = ? AND datasetId = ? LIMIT 1`,
						args: [id, datasetId]
					})).rows[0]) {
						await tx.commit();
						return;
					}
					const purgedAt = (/* @__PURE__ */ new Date()).toISOString();
					const purgedMetadata = JSON.stringify({
						__purged: true,
						purgedAt
					});
					await tx.execute({
						sql: `UPDATE ${_mastra_core_storage.TABLE_DATASET_ITEMS} SET input = jsonb('null'), groundTruth = NULL, expectedTrajectory = NULL, toolMocks = NULL, unmockedToolPolicy = NULL, scorerIds = NULL, requestContext = NULL, metadata = jsonb(?), source = NULL WHERE id = ? AND datasetId = ?`,
						args: [
							purgedMetadata,
							id,
							datasetId
						]
					});
					if (experimentTablesExist) await tx.execute({
						sql: `UPDATE ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} SET input = jsonb('null'), output = NULL, groundTruth = NULL, error = NULL, toolMockReport = NULL, tags = NULL, comment = NULL, metadata = jsonb(?) WHERE itemId = ? AND experimentId IN (SELECT id FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} WHERE datasetId = ?)`,
						args: [
							purgedMetadata,
							id,
							datasetId
						]
					});
					await tx.commit();
				} catch (error) {
					if (!tx.closed) await tx.rollback().catch((rollbackError) => {
						throw new AggregateError([error, rollbackError], "Transaction and rollback both failed");
					});
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "PURGE_ITEM", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getItemById(args) {
		try {
			let result;
			if (args.datasetVersion !== void 0) result = await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE id = ? AND datasetVersion <= ? AND (validTo IS NULL OR validTo > ?) AND isDeleted = 0 ORDER BY datasetVersion DESC LIMIT 1`,
				args: [
					args.id,
					args.datasetVersion,
					args.datasetVersion
				]
			});
			else result = await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE id = ? AND validTo IS NULL AND isDeleted = 0`,
				args: [args.id]
			});
			return result.rows?.[0] ? this.transformItemRow(result.rows[0]) : null;
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_ITEM", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getItemsByVersion({ datasetId, version }) {
		try {
			return (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE datasetId = ? AND datasetVersion <= ? AND (validTo IS NULL OR validTo > ?) AND isDeleted = 0 ORDER BY createdAt DESC, id ASC`,
				args: [
					datasetId,
					version,
					version
				]
			})).rows?.map((row) => this.transformItemRow(row)) ?? [];
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_ITEMS_BY_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getItemHistory(itemId) {
		try {
			return (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE id = ? ORDER BY datasetVersion DESC`,
				args: [itemId]
			})).rows?.map((row) => this.transformItemRowFull(row)) ?? [];
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_ITEM_HISTORY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listItems(args) {
		try {
			const { page, perPage: perPageInput } = args.pagination;
			if (args.version !== void 0) {
				const conditions = [
					"datasetId = ?",
					"datasetVersion <= ?",
					"(validTo IS NULL OR validTo > ?)",
					"isDeleted = 0"
				];
				const queryParams = [
					args.datasetId,
					args.version,
					args.version
				];
				if (args.filters?.organizationId !== void 0) {
					conditions.push("organizationId = ?");
					queryParams.push(args.filters.organizationId);
				}
				if (args.filters?.projectId !== void 0) {
					conditions.push("projectId = ?");
					queryParams.push(args.filters.projectId);
				}
				if (args.search) {
					conditions.push(`(LOWER(json(input)) LIKE ? OR LOWER(COALESCE(json(groundTruth), '')) LIKE ?)`);
					const searchPattern = `%${args.search.toLowerCase()}%`;
					queryParams.push(searchPattern, searchPattern);
				}
				const whereClause = `WHERE ${conditions.join(" AND ")}`;
				const countResult = await this.#client.execute({
					sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} ${whereClause}`,
					args: queryParams
				});
				const total = Number(countResult.rows?.[0]?.count ?? 0);
				if (total === 0) return {
					items: [],
					pagination: {
						total: 0,
						page,
						perPage: perPageInput,
						hasMore: false
					}
				};
				const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
				const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
				const limitValue = perPageInput === false ? total : perPage;
				const end = perPageInput === false ? total : start + perPage;
				return {
					items: (await this.#client.execute({
						sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} ${whereClause} ORDER BY createdAt DESC, id ASC LIMIT ? OFFSET ?`,
						args: [
							...queryParams,
							limitValue,
							start
						]
					})).rows?.map((row) => this.transformItemRow(row)) ?? [],
					pagination: {
						total,
						page,
						perPage: perPageForResponse,
						hasMore: end < total
					}
				};
			}
			const conditions = [
				"datasetId = ?",
				"validTo IS NULL",
				"isDeleted = 0"
			];
			const queryParams = [args.datasetId];
			if (args.filters?.organizationId !== void 0) {
				conditions.push("organizationId = ?");
				queryParams.push(args.filters.organizationId);
			}
			if (args.filters?.projectId !== void 0) {
				conditions.push("projectId = ?");
				queryParams.push(args.filters.projectId);
			}
			if (args.search) {
				conditions.push(`(LOWER(json(input)) LIKE ? OR LOWER(COALESCE(json(groundTruth), '')) LIKE ?)`);
				const searchPattern = `%${args.search.toLowerCase()}%`;
				queryParams.push(searchPattern, searchPattern);
			}
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				items: [],
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				}
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				items: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} ${whereClause} ORDER BY createdAt DESC, id ASC LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformItemRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_ITEMS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createDatasetVersion(datasetId, version) {
		try {
			const id = crypto.randomUUID();
			const now = /* @__PURE__ */ new Date();
			const nowIso = now.toISOString();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_DATASET_VERSIONS,
				record: {
					id,
					datasetId,
					version,
					createdAt: nowIso
				}
			});
			return {
				id,
				datasetId,
				version,
				createdAt: now
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_DATASET_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listDatasetVersions(input) {
		try {
			const { page, perPage: perPageInput } = input.pagination;
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_DATASET_VERSIONS} WHERE datasetId = ?`,
				args: [input.datasetId]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				versions: [],
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				}
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				versions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_VERSIONS)} FROM ${_mastra_core_storage.TABLE_DATASET_VERSIONS} WHERE datasetId = ? ORDER BY version DESC LIMIT ? OFFSET ?`,
					args: [
						input.datasetId,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformDatasetVersionRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_DATASET_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async _doBatchInsertItems(input) {
		try {
			if (input.items.length === 0) return [];
			return await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const dataset = (await tx.execute({
						sql: `SELECT version, organizationId, projectId FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?`,
						args: [input.datasetId]
					})).rows[0];
					if (!dataset) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BULK_ADD_ITEMS", "DATASET_NOT_FOUND"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: { datasetId: input.datasetId }
					});
					const externalIds = [...new Set(input.items.flatMap((item) => item.externalId ? [item.externalId] : []))];
					const historyResult = externalIds.length ? await tx.execute({
						sql: `SELECT id, datasetId, datasetVersion, externalId, organizationId, projectId, validTo, isDeleted, json(input) AS input, json(groundTruth) AS groundTruth, json(expectedTrajectory) AS expectedTrajectory, json(toolMocks) AS toolMocks, unmockedToolPolicy, json(scorerIds) AS scorerIds, json(requestContext) AS requestContext, json(metadata) AS metadata, json(source) AS source, createdAt, updatedAt FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE datasetId = ? AND externalId IN (${externalIds.map(() => "?").join(",")}) ORDER BY datasetVersion`,
						args: [input.datasetId, ...externalIds]
					}) : { rows: [] };
					const plan = this.planDatasetItemBatch(input.items, historyResult.rows.map((row) => this.transformItemRowFull(row)), () => crypto.randomUUID());
					const resolved = new Map([...plan.existingCurrentItems].map(([id, row]) => [id, this.datasetItemFromRow(row)]));
					if (plan.inserts.length > 0) {
						const newVersion = Number(dataset.version) + 1;
						const now = /* @__PURE__ */ new Date();
						const nowIso = now.toISOString();
						await tx.execute({
							sql: `UPDATE ${_mastra_core_storage.TABLE_DATASETS} SET version = ? WHERE id = ?`,
							args: [newVersion, input.datasetId]
						});
						for (const { id, item } of plan.inserts) {
							await tx.execute({
								sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_ITEMS} (id,datasetId,datasetVersion,externalId,organizationId,projectId,validTo,isDeleted,input,groundTruth,expectedTrajectory,toolMocks,unmockedToolPolicy,scorerIds,requestContext,metadata,source,createdAt,updatedAt) VALUES (?,?,?,?,?,?,NULL,0,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,?)`,
								args: [
									id,
									input.datasetId,
									newVersion,
									item.externalId ?? null,
									dataset.organizationId ?? null,
									dataset.projectId ?? null,
									jsonbArg(item.input),
									jsonbArg(item.groundTruth),
									jsonbArg(item.expectedTrajectory),
									jsonbArg(item.toolMocks),
									item.unmockedToolPolicy ?? null,
									jsonbArg(item.scorerIds),
									jsonbArg(item.requestContext),
									jsonbArg(item.metadata),
									jsonbArg(item.source),
									nowIso,
									nowIso
								]
							});
							resolved.set(id, {
								id,
								datasetId: input.datasetId,
								datasetVersion: newVersion,
								externalId: item.externalId ?? null,
								organizationId: dataset.organizationId,
								projectId: dataset.projectId,
								input: item.input,
								groundTruth: item.groundTruth,
								expectedTrajectory: item.expectedTrajectory,
								toolMocks: item.toolMocks,
								unmockedToolPolicy: item.unmockedToolPolicy,
								scorerIds: item.scorerIds,
								requestContext: item.requestContext,
								metadata: item.metadata,
								source: item.source,
								createdAt: now,
								updatedAt: now
							});
						}
						await tx.execute({
							sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_VERSIONS} (id, datasetId, version, createdAt) VALUES (?, ?, ?, ?)`,
							args: [
								crypto.randomUUID(),
								input.datasetId,
								newVersion,
								nowIso
							]
						});
					}
					await tx.commit();
					return plan.resolvedIds.map((id) => resolved.get(id));
				} catch (error) {
					if (!tx.closed) await tx.rollback();
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BULK_ADD_ITEMS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async _doBatchDeleteItems(input) {
		try {
			await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const dataset = (await tx.execute({
						sql: `SELECT organizationId, projectId FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?`,
						args: [input.datasetId]
					})).rows[0];
					if (!dataset) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BULK_DELETE_ITEMS", "DATASET_NOT_FOUND"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: { datasetId: input.datasetId }
					});
					const currentItems = [];
					for (const itemId of input.itemIds) {
						const itemResult = await tx.execute({
							sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_DATASET_ITEMS)} FROM ${_mastra_core_storage.TABLE_DATASET_ITEMS} WHERE id = ? AND validTo IS NULL AND isDeleted = 0`,
							args: [itemId]
						});
						const item = itemResult.rows[0] ? this.transformItemRow(itemResult.rows[0]) : null;
						if (item && item.datasetId === input.datasetId) currentItems.push(item);
					}
					if (currentItems.length === 0) {
						await tx.commit();
						return;
					}
					const nowIso = (/* @__PURE__ */ new Date()).toISOString();
					const versionId = crypto.randomUUID();
					const statements = [{
						sql: `UPDATE ${_mastra_core_storage.TABLE_DATASETS} SET version = version + 1 WHERE id = ? RETURNING version`,
						args: [input.datasetId]
					}];
					for (const item of currentItems) {
						statements.push({
							sql: `UPDATE ${_mastra_core_storage.TABLE_DATASET_ITEMS} SET validTo = (SELECT version FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?) WHERE id = ? AND validTo IS NULL AND isDeleted = 0`,
							args: [input.datasetId, item.id]
						});
						statements.push({
							sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_ITEMS} (id,datasetId,datasetVersion,externalId,organizationId,projectId,validTo,isDeleted,input,groundTruth,expectedTrajectory,toolMocks,unmockedToolPolicy,scorerIds,requestContext,metadata,source,createdAt,updatedAt) VALUES (?,?,(SELECT version FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?),?,?,?,NULL,1,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,jsonb(?),jsonb(?),jsonb(?),jsonb(?),?,?)`,
							args: [
								item.id,
								input.datasetId,
								input.datasetId,
								item.externalId ?? null,
								dataset.organizationId ?? null,
								dataset.projectId ?? null,
								jsonbArg(item.input),
								jsonbArg(item.groundTruth),
								jsonbArg(item.expectedTrajectory),
								jsonbArg(item.toolMocks),
								item.unmockedToolPolicy ?? null,
								jsonbArg(item.scorerIds),
								jsonbArg(item.requestContext),
								jsonbArg(item.metadata),
								jsonbArg(item.source),
								item.createdAt.toISOString(),
								nowIso
							]
						});
					}
					statements.push({
						sql: `INSERT INTO ${_mastra_core_storage.TABLE_DATASET_VERSIONS} (id, datasetId, version, createdAt) VALUES (?, ?, (SELECT version FROM ${_mastra_core_storage.TABLE_DATASETS} WHERE id = ?), ?)`,
						args: [
							versionId,
							input.datasetId,
							input.datasetId,
							nowIso
						]
					});
					for (const statement of statements) await tx.execute(statement);
					await tx.commit();
				} catch (error) {
					if (!tx.closed) await tx.rollback().catch((rollbackError) => {
						throw new AggregateError([error, rollbackError], "Transaction and rollback both failed");
					});
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BULK_DELETE_ITEMS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/experiments/index.ts
const DEFAULT_PRUNE_BATCH_SIZE = 1e3;
const TAGS_IS_JSON = `CASE typeof(tags) WHEN 'blob' THEN 1 WHEN 'text' THEN json_valid(tags) ELSE 0 END`;
var ExperimentsLibSQL = class extends _mastra_core_storage.ExperimentsStorage {
	/**
	* An experiment is pruned as a whole unit: when `experiments.completedAt` is
	* older than the policy, the run and all its `experiment_results` rows are
	* deleted together (results cascade with their parent, matching
	* `deleteExperiment`). Results are not an independent retention key. NULL
	* `completedAt` (still running) is never pruned.
	*/
	static retentionTables = { experiments: {
		table: _mastra_core_storage.TABLE_EXPERIMENTS,
		column: "completedAt",
		indexed: true
	} };
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async #withPurgeBarrier(experimentId, itemId, fn) {
		return withClientWriteLock(this.#client, async () => {
			const tx = await this.#client.transaction("write");
			try {
				const row = (await tx.execute({
					sql: `SELECT json_extract(i."metadata", '$.__purged') AS "purged",
                       json_extract(i."metadata", '$.purgedAt') AS "purgedAt"
                FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} e
                JOIN ${_mastra_core_storage.TABLE_DATASET_ITEMS} i ON i."datasetId" = e."datasetId" AND i."id" = ?
                WHERE e."id" = ? AND json_extract(i."metadata", '$.__purged') = 1
                LIMIT 1`,
					args: [itemId, experimentId]
				})).rows[0];
				const result = await fn(tx, row?.purged === 1 ? {
					__purged: true,
					purgedAt: row.purgedAt
				} : null);
				await tx.commit();
				return result;
			} catch (error) {
				if (!tx.closed) await tx.rollback().catch((rollbackError) => {
					throw new AggregateError([error, rollbackError], "Transaction and rollback both failed");
				});
				throw error;
			} finally {
				tx.close();
			}
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_EXPERIMENTS,
			schema: _mastra_core_storage.EXPERIMENTS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_EXPERIMENT_RESULTS,
			schema: _mastra_core_storage.EXPERIMENT_RESULTS_SCHEMA
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_EXPERIMENTS,
			schema: _mastra_core_storage.EXPERIMENTS_SCHEMA,
			ifNotExists: [
				"agentVersion",
				"organizationId",
				"projectId",
				"provenance",
				"runnerAttestation",
				"experimentSetId",
				"comparisonId",
				"variantId",
				"trialIndex",
				"scorerIds"
			]
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_EXPERIMENT_RESULTS,
			schema: _mastra_core_storage.EXPERIMENT_RESULTS_SCHEMA,
			ifNotExists: [
				"status",
				"tags",
				"comment",
				"toolMockReport",
				"metadata",
				"organizationId",
				"projectId",
				"attempt"
			]
		});
		await this.#client.batch([
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_experiments_datasetid ON "${_mastra_core_storage.TABLE_EXPERIMENTS}" ("datasetId")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_experiments_grouping ON "${_mastra_core_storage.TABLE_EXPERIMENTS}" ("experimentSetId", "comparisonId", "variantId", "trialIndex")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_experiment_results_experimentid ON "${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS}" ("experimentId")`,
				args: []
			},
			{
				sql: `DROP INDEX IF EXISTS idx_experiment_results_exp_item`,
				args: []
			},
			{
				sql: `CREATE UNIQUE INDEX IF NOT EXISTS idx_experiment_results_exp_item_attempt ON "${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS}" ("experimentId", "itemId", "attempt")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_experiments_org_project ON "${_mastra_core_storage.TABLE_EXPERIMENTS}" ("organizationId", "projectId")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_experiment_results_org_project ON "${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS}" ("organizationId", "projectId")`,
				args: []
			}
		], "write");
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_EXPERIMENT_RESULTS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_EXPERIMENTS });
	}
	/**
	* Prune whole experiments older than the `experiments` policy's `maxAge`.
	*
	* Each batch selects up to `batchSize` aged experiments and deletes their
	* `experiment_results` rows and the experiment rows in one transaction —
	* mirroring `deleteExperiment` — so hitting `maxBatches`/`maxRows` or the
	* abort signal between batches never leaves a run hollow (parent kept,
	* results gone). NULL `completedAt` (still running) is excluded by the
	* `< cutoff` predicate. Bounds count whole experiments, not rows.
	*/
	async prune(policies, options) {
		const policy = policies["experiments"];
		if (!policy || options?.signal?.aborted) return policy ? [{
			domain: "experiments",
			table: _mastra_core_storage.TABLE_EXPERIMENT_RESULTS,
			deleted: 0,
			done: false
		}, {
			domain: "experiments",
			table: _mastra_core_storage.TABLE_EXPERIMENTS,
			deleted: 0,
			done: false
		}] : [];
		try {
			await this.#db.ensureIndex({
				indexName: `idx_retention_${_mastra_core_storage.TABLE_EXPERIMENTS}_completedAt`,
				tableName: _mastra_core_storage.TABLE_EXPERIMENTS,
				column: "completedAt"
			});
		} catch (error) {
			this.logger?.warn?.(`Failed to ensure retention index on ${_mastra_core_storage.TABLE_EXPERIMENTS}(completedAt):`, error);
		}
		const cutoff = cutoffFor(policy, "timestamp");
		const batchSize = policy.batchSize ?? DEFAULT_PRUNE_BATCH_SIZE;
		let childDeleted = 0;
		const parent = await runBatchedDelete({
			deleteBatch: async (limit) => {
				const { parents, children } = await this.#db.pruneUnitsBatch({
					parentTable: _mastra_core_storage.TABLE_EXPERIMENTS,
					parentKey: "id",
					parentColumn: "completedAt",
					childTable: _mastra_core_storage.TABLE_EXPERIMENT_RESULTS,
					childForeignKey: "experimentId",
					cutoff,
					limit
				});
				childDeleted += children;
				return parents;
			},
			batchSize,
			options
		});
		return [{
			domain: "experiments",
			table: _mastra_core_storage.TABLE_EXPERIMENT_RESULTS,
			deleted: childDeleted,
			done: parent.done
		}, {
			domain: "experiments",
			table: _mastra_core_storage.TABLE_EXPERIMENTS,
			deleted: parent.deleted,
			done: parent.done
		}];
	}
	transformExperimentRow(row) {
		return {
			id: row.id,
			datasetId: row.datasetId ?? null,
			datasetVersion: row.datasetVersion != null ? row.datasetVersion : null,
			agentVersion: row.agentVersion ?? null,
			organizationId: row.organizationId ?? null,
			projectId: row.projectId ?? null,
			targetType: row.targetType ?? null,
			targetId: row.targetId ?? null,
			scorerIds: row.scorerIds ? (0, _mastra_core_storage.safelyParseJSON)(row.scorerIds) : null,
			name: row.name ?? void 0,
			description: row.description ?? void 0,
			metadata: row.metadata ? (0, _mastra_core_storage.safelyParseJSON)(row.metadata) : void 0,
			provenance: row.provenance ? (0, _mastra_core_storage.safelyParseJSON)(row.provenance) : null,
			runnerAttestation: row.runnerAttestation ? (0, _mastra_core_storage.safelyParseJSON)(row.runnerAttestation) : null,
			experimentSetId: row.experimentSetId ?? null,
			comparisonId: row.comparisonId ?? null,
			variantId: row.variantId ?? null,
			trialIndex: row.trialIndex != null ? row.trialIndex : null,
			status: row.status,
			totalItems: row.totalItems,
			succeededCount: row.succeededCount,
			failedCount: row.failedCount,
			skippedCount: row.skippedCount ?? 0,
			startedAt: row.startedAt ? (0, _mastra_core_storage.ensureDate)(row.startedAt) : null,
			completedAt: row.completedAt ? (0, _mastra_core_storage.ensureDate)(row.completedAt) : null,
			createdAt: (0, _mastra_core_storage.ensureDate)(row.createdAt),
			updatedAt: (0, _mastra_core_storage.ensureDate)(row.updatedAt)
		};
	}
	transformExperimentResultRow(row) {
		return {
			id: row.id,
			experimentId: row.experimentId,
			itemId: row.itemId,
			itemDatasetVersion: row.itemDatasetVersion != null ? row.itemDatasetVersion : null,
			organizationId: row.organizationId ?? null,
			projectId: row.projectId ?? null,
			input: (0, _mastra_core_storage.safelyParseJSON)(row.input),
			output: row.output ? (0, _mastra_core_storage.safelyParseJSON)(row.output) : null,
			groundTruth: row.groundTruth ? (0, _mastra_core_storage.safelyParseJSON)(row.groundTruth) : null,
			metadata: row.metadata ? (0, _mastra_core_storage.safelyParseJSON)(row.metadata) : null,
			error: row.error ? (0, _mastra_core_storage.safelyParseJSON)(row.error) : null,
			startedAt: (0, _mastra_core_storage.ensureDate)(row.startedAt),
			completedAt: (0, _mastra_core_storage.ensureDate)(row.completedAt),
			retryCount: row.retryCount,
			attempt: row.attempt != null ? Number(row.attempt) : 0,
			traceId: row.traceId ?? null,
			status: row.status ?? null,
			tags: row.tags ? (0, _mastra_core_storage.safelyParseJSON)(row.tags) : null,
			comment: row.comment ?? null,
			toolMockReport: row.toolMockReport ? (0, _mastra_core_storage.safelyParseJSON)(row.toolMockReport) : null,
			createdAt: (0, _mastra_core_storage.ensureDate)(row.createdAt)
		};
	}
	async createExperiment(input) {
		try {
			const id = input.id ?? crypto.randomUUID();
			const now = /* @__PURE__ */ new Date();
			const nowIso = now.toISOString();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_EXPERIMENTS,
				record: {
					id,
					datasetId: input.datasetId ?? null,
					datasetVersion: input.datasetVersion ?? null,
					agentVersion: input.agentVersion ?? null,
					organizationId: input.organizationId ?? null,
					projectId: input.projectId ?? null,
					targetType: input.targetType ?? null,
					targetId: input.targetId ?? null,
					scorerIds: input.scorerIds ?? null,
					name: input.name ?? null,
					description: input.description ?? null,
					metadata: input.metadata ?? null,
					provenance: input.provenance ?? null,
					runnerAttestation: input.runnerAttestation ?? null,
					experimentSetId: input.experimentSetId ?? null,
					comparisonId: input.comparisonId ?? null,
					variantId: input.variantId ?? null,
					trialIndex: input.trialIndex ?? null,
					status: "pending",
					totalItems: input.totalItems,
					succeededCount: 0,
					failedCount: 0,
					skippedCount: 0,
					startedAt: null,
					completedAt: null,
					createdAt: nowIso,
					updatedAt: nowIso
				}
			});
			return {
				id,
				datasetId: input.datasetId,
				datasetVersion: input.datasetVersion,
				agentVersion: input.agentVersion ?? null,
				organizationId: input.organizationId ?? null,
				projectId: input.projectId ?? null,
				targetType: input.targetType ?? null,
				targetId: input.targetId ?? null,
				scorerIds: input.scorerIds ?? null,
				name: input.name,
				description: input.description,
				metadata: input.metadata,
				provenance: input.provenance ?? null,
				runnerAttestation: input.runnerAttestation ?? null,
				experimentSetId: input.experimentSetId ?? null,
				comparisonId: input.comparisonId ?? null,
				variantId: input.variantId ?? null,
				trialIndex: input.trialIndex ?? null,
				status: "pending",
				totalItems: input.totalItems,
				succeededCount: 0,
				failedCount: 0,
				skippedCount: 0,
				startedAt: null,
				completedAt: null,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_EXPERIMENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async updateExperiment(input) {
		try {
			if (!await this.getExperimentById({ id: input.id })) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_EXPERIMENT", "NOT_FOUND"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { experimentId: input.id }
			});
			const now = (/* @__PURE__ */ new Date()).toISOString();
			const updates = ["updatedAt = ?"];
			const values = [now];
			if (input.status !== void 0) {
				updates.push("status = ?");
				values.push(input.status);
			}
			if (input.succeededCount !== void 0) {
				updates.push("succeededCount = ?");
				values.push(input.succeededCount);
			}
			if (input.failedCount !== void 0) {
				updates.push("failedCount = ?");
				values.push(input.failedCount);
			}
			if (input.totalItems !== void 0) {
				updates.push("totalItems = ?");
				values.push(input.totalItems);
			}
			if (input.startedAt !== void 0) {
				updates.push("startedAt = ?");
				values.push(input.startedAt?.toISOString() ?? null);
			}
			if (input.completedAt !== void 0) {
				updates.push("completedAt = ?");
				values.push(input.completedAt?.toISOString() ?? null);
			}
			if (input.skippedCount !== void 0) {
				updates.push("skippedCount = ?");
				values.push(input.skippedCount);
			}
			if (input.name !== void 0) {
				updates.push("name = ?");
				values.push(input.name);
			}
			if (input.description !== void 0) {
				updates.push("description = ?");
				values.push(input.description);
			}
			if (input.metadata !== void 0) {
				updates.push("metadata = ?");
				values.push(JSON.stringify(input.metadata));
			}
			values.push(input.id);
			await this.#client.execute({
				sql: `UPDATE ${_mastra_core_storage.TABLE_EXPERIMENTS} SET ${updates.join(", ")} WHERE id = ?`,
				args: values
			});
			return await this.getExperimentById({ id: input.id });
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_EXPERIMENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getExperimentById(args) {
		try {
			const scoped = buildScopedWhere("id", args.id, args.filters);
			const result = await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_EXPERIMENTS)} FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} WHERE ${scoped.sql}`,
				args: scoped.args
			});
			return result.rows?.[0] ? this.transformExperimentRow(result.rows[0]) : null;
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_EXPERIMENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listExperiments(args) {
		try {
			const { page, perPage: perPageInput } = args.pagination;
			const conditions = [];
			const queryParams = [];
			if (args.datasetId) {
				conditions.push("datasetId = ?");
				queryParams.push(args.datasetId);
			}
			if (args.targetType) {
				conditions.push("targetType = ?");
				queryParams.push(args.targetType);
			}
			if (args.targetId) {
				conditions.push("targetId = ?");
				queryParams.push(args.targetId);
			}
			if (args.agentVersion) {
				conditions.push("agentVersion = ?");
				queryParams.push(args.agentVersion);
			}
			if (args.status) {
				conditions.push("status = ?");
				queryParams.push(args.status);
			}
			if (args.experimentSetId !== void 0) {
				conditions.push("experimentSetId = ?");
				queryParams.push(args.experimentSetId);
			}
			if (args.comparisonId !== void 0) {
				conditions.push("comparisonId = ?");
				queryParams.push(args.comparisonId);
			}
			if (args.variantId !== void 0) {
				conditions.push("variantId = ?");
				queryParams.push(args.variantId);
			}
			if (args.trialIndex !== void 0) {
				conditions.push("trialIndex = ?");
				queryParams.push(args.trialIndex);
			}
			if (args.filters) {
				const { organizationId, projectId } = args.filters;
				if (organizationId !== void 0) {
					conditions.push("organizationId = ?");
					queryParams.push(organizationId);
				}
				if (projectId !== void 0) {
					conditions.push("projectId = ?");
					queryParams.push(projectId);
				}
			}
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				experiments: [],
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				}
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				experiments: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_EXPERIMENTS)} FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} ${whereClause} ORDER BY createdAt DESC LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformExperimentRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_EXPERIMENTS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteExperiment(args) {
		try {
			const parentScoped = buildScopedWhere("id", args.id, args.filters);
			const { conditions, params } = tenancyWhere(args.filters);
			const cascadeWhere = conditions.length ? `experimentId IN (SELECT id FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} WHERE ${["id = ?", ...conditions].join(" AND ")})` : `experimentId = ?`;
			const cascadeArgs = conditions.length ? [args.id, ...params] : [args.id];
			await this.#client.batch([{
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE ${cascadeWhere}`,
				args: cascadeArgs
			}, {
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} WHERE ${parentScoped.sql}`,
				args: parentScoped.args
			}], "write");
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_EXPERIMENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async addExperimentResult(input) {
		try {
			const id = input.id ?? crypto.randomUUID();
			const now = /* @__PURE__ */ new Date();
			const nowIso = now.toISOString();
			const purgeMetadata = await this.#withPurgeBarrier(input.experimentId, input.itemId, async (tx, marker) => {
				await tx.execute({
					sql: `INSERT INTO ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} (
            "id", "experimentId", "itemId", "itemDatasetVersion", "organizationId", "projectId",
            "input", "output", "groundTruth", "metadata", "error", "startedAt", "completedAt",
            "retryCount", "attempt", "traceId", "status", "tags", "toolMockReport", "createdAt"
          ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
					args: [
						id,
						input.experimentId,
						input.itemId,
						input.itemDatasetVersion ?? null,
						input.organizationId ?? null,
						input.projectId ?? null,
						JSON.stringify(marker ? null : input.input),
						JSON.stringify(marker ? null : input.output ?? null),
						JSON.stringify(marker ? null : input.groundTruth ?? null),
						JSON.stringify(marker ?? input.metadata ?? null),
						JSON.stringify(marker ? null : input.error ?? null),
						input.startedAt.toISOString(),
						input.completedAt.toISOString(),
						input.retryCount,
						input.attempt ?? 0,
						input.traceId ?? null,
						input.status ?? null,
						JSON.stringify(marker ? null : input.tags ?? null),
						JSON.stringify(marker ? null : input.toolMockReport ?? null),
						nowIso
					]
				});
				return marker;
			});
			return {
				id,
				experimentId: input.experimentId,
				itemId: input.itemId,
				itemDatasetVersion: input.itemDatasetVersion,
				organizationId: input.organizationId ?? null,
				projectId: input.projectId ?? null,
				input: purgeMetadata ? null : input.input,
				output: purgeMetadata ? null : input.output,
				groundTruth: purgeMetadata ? null : input.groundTruth,
				metadata: purgeMetadata ?? input.metadata ?? null,
				error: purgeMetadata ? null : input.error,
				startedAt: input.startedAt,
				completedAt: input.completedAt,
				retryCount: input.retryCount,
				attempt: input.attempt ?? 0,
				traceId: input.traceId ?? null,
				status: input.status ?? null,
				tags: purgeMetadata ? null : input.tags ?? null,
				toolMockReport: purgeMetadata ? null : input.toolMockReport ?? null,
				createdAt: now
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "ADD_EXPERIMENT_RESULT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async upsertExperimentResult(input) {
		try {
			const attempt = input.attempt ?? 0;
			return await this.#withPurgeBarrier(input.experimentId, input.itemId, async (tx, marker) => {
				const id = crypto.randomUUID();
				await tx.execute({
					sql: `INSERT INTO ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} (
            "id", "experimentId", "itemId", "itemDatasetVersion", "organizationId", "projectId",
            "input", "output", "groundTruth", "metadata", "error", "startedAt", "completedAt",
            "retryCount", "attempt", "traceId", "status", "tags", "toolMockReport", "createdAt"
          ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
          ON CONFLICT("experimentId", "itemId", "attempt") DO UPDATE SET
            "itemDatasetVersion" = excluded."itemDatasetVersion", "organizationId" = excluded."organizationId",
            "projectId" = excluded."projectId", "input" = excluded."input", "output" = excluded."output",
            "groundTruth" = excluded."groundTruth", "metadata" = excluded."metadata", "error" = excluded."error",
            "startedAt" = excluded."startedAt", "completedAt" = excluded."completedAt",
            "retryCount" = excluded."retryCount", "attempt" = excluded."attempt", "traceId" = excluded."traceId",
            "status" = excluded."status", "tags" = excluded."tags", "toolMockReport" = excluded."toolMockReport",
            "comment" = CASE WHEN ? THEN NULL ELSE "comment" END`,
					args: [
						id,
						input.experimentId,
						input.itemId,
						input.itemDatasetVersion ?? null,
						input.organizationId ?? null,
						input.projectId ?? null,
						JSON.stringify(marker ? null : input.input),
						JSON.stringify(marker ? null : input.output ?? null),
						JSON.stringify(marker ? null : input.groundTruth ?? null),
						JSON.stringify(marker ?? input.metadata ?? null),
						JSON.stringify(marker ? null : input.error ?? null),
						input.startedAt.toISOString(),
						input.completedAt.toISOString(),
						input.retryCount,
						attempt,
						input.traceId ?? null,
						input.status ?? null,
						JSON.stringify(marker ? null : input.tags ?? null),
						JSON.stringify(marker ? null : input.toolMockReport ?? null),
						(/* @__PURE__ */ new Date()).toISOString(),
						Boolean(marker)
					]
				});
				const row = (await tx.execute({
					sql: `SELECT * FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE "experimentId" = ? AND "itemId" = ? AND COALESCE("attempt", 0) = ?`,
					args: [
						input.experimentId,
						input.itemId,
						attempt
					]
				})).rows[0];
				if (!row) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPSERT_EXPERIMENT_RESULT", "NOT_FOUND"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER
				});
				return this.transformExperimentResultRow(row);
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPSERT_EXPERIMENT_RESULT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async updateExperimentResult(input) {
		try {
			const ownerRow = (await this.#client.execute({
				sql: `SELECT "experimentId", "itemId" FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE "id" = ?${input.experimentId ? " AND \"experimentId\" = ?" : ""}`,
				args: input.experimentId ? [input.id, input.experimentId] : [input.id]
			})).rows[0];
			if (!ownerRow) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_EXPERIMENT_RESULT", "NOT_FOUND"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { resultId: input.id }
			});
			return await this.#withPurgeBarrier(String(ownerRow.experimentId), String(ownerRow.itemId), async (tx, marker) => {
				if ((await tx.execute({
					sql: `UPDATE ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS}
                SET "status" = CASE WHEN ? THEN ? ELSE "status" END,
                    "tags" = CASE WHEN ? THEN NULL WHEN ? THEN ? ELSE "tags" END,
                    "comment" = CASE WHEN ? THEN NULL WHEN ? THEN ? ELSE "comment" END
                WHERE "id" = ?${input.experimentId ? " AND \"experimentId\" = ?" : ""}`,
					args: [
						input.status !== void 0,
						input.status ?? null,
						Boolean(marker),
						input.tags !== void 0,
						JSON.stringify(input.tags ?? null),
						Boolean(marker),
						input.comment !== void 0,
						input.comment ?? null,
						input.id,
						...input.experimentId ? [input.experimentId] : []
					]
				})).rowsAffected === 0) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_EXPERIMENT_RESULT", "NOT_FOUND"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					details: { resultId: input.id }
				});
				const result = await tx.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_EXPERIMENT_RESULTS)} FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE "id" = ?`,
					args: [input.id]
				});
				return this.transformExperimentResultRow(result.rows[0]);
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_EXPERIMENT_RESULT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getExperimentResultById(args) {
		try {
			const scoped = buildScopedWhere("id", args.id, args.filters);
			const result = await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_EXPERIMENT_RESULTS)} FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE ${scoped.sql}`,
				args: scoped.args
			});
			return result.rows?.[0] ? this.transformExperimentResultRow(result.rows[0]) : null;
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_EXPERIMENT_RESULT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listExperimentResults(args) {
		try {
			const { page, perPage: perPageInput } = args.pagination;
			const conditions = ["experimentId = ?"];
			const queryParams = [args.experimentId];
			if (args.traceId) {
				conditions.push("traceId = ?");
				queryParams.push(args.traceId);
			}
			if (args.status) {
				conditions.push("status = ?");
				queryParams.push(args.status);
			}
			for (const tag of args.tags ?? []) {
				conditions.push(`CASE WHEN ${TAGS_IS_JSON} THEN EXISTS (SELECT 1 FROM json_each(tags) WHERE value = ?) ELSE 0 END`);
				queryParams.push(tag);
			}
			if (args.filters) {
				const { organizationId, projectId } = args.filters;
				if (organizationId !== void 0) {
					conditions.push("organizationId = ?");
					queryParams.push(organizationId);
				}
				if (projectId !== void 0) {
					conditions.push("projectId = ?");
					queryParams.push(projectId);
				}
			}
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				results: [],
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				}
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				results: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_EXPERIMENT_RESULTS)} FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} ${whereClause} ORDER BY startedAt ASC LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformExperimentResultRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_EXPERIMENT_RESULTS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteExperimentResults(args) {
		try {
			const { conditions, params } = tenancyWhere(args.filters);
			if (conditions.length) {
				await this.#client.execute({
					sql: `DELETE FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE experimentId IN (SELECT id FROM ${_mastra_core_storage.TABLE_EXPERIMENTS} WHERE ${["id = ?", ...conditions].join(" AND ")})`,
					args: [args.experimentId, ...params]
				});
				return;
			}
			await this.#client.execute({
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS} WHERE experimentId = ?`,
				args: [args.experimentId]
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_EXPERIMENT_RESULTS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getReviewSummary() {
		try {
			return ((await this.#client.execute({
				sql: `SELECT
          "experimentId",
          COUNT(*) as total,
          SUM(CASE WHEN status = 'needs-review' THEN 1 ELSE 0 END) as "needsReview",
          SUM(CASE WHEN status = 'reviewed' THEN 1 ELSE 0 END) as reviewed,
          SUM(CASE WHEN status = 'complete' THEN 1 ELSE 0 END) as complete
        FROM ${_mastra_core_storage.TABLE_EXPERIMENT_RESULTS}
        GROUP BY "experimentId"`,
				args: []
			})).rows ?? []).map((row) => ({
				experimentId: row.experimentId,
				total: Number(row.total ?? 0),
				needsReview: Number(row.needsReview ?? 0),
				reviewed: Number(row.reviewed ?? 0),
				complete: Number(row.complete ?? 0)
			}));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_REVIEW_SUMMARY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/favorites/index.ts
/**
* Maps a favorite entity type to its parent entity table.
*/
const ENTITY_TABLE = {
	agent: _mastra_core_storage.TABLE_AGENTS,
	skill: _mastra_core_storage.TABLE_SKILLS
};
var FavoritesLibSQL = class extends _mastra_core_storage.FavoritesStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_FAVORITES,
			schema: _mastra_core_storage.FAVORITES_SCHEMA,
			compositePrimaryKey: [
				"userId",
				"entityType",
				"entityId"
			]
		});
		await this.#client.execute(`CREATE INDEX IF NOT EXISTS idx_favorites_entity ON "${_mastra_core_storage.TABLE_FAVORITES}" ("entityType", "entityId")`);
	}
	async dangerouslyClearAll() {
		const tx = await this.#client.transaction("write");
		try {
			await tx.execute(`DELETE FROM "${_mastra_core_storage.TABLE_FAVORITES}"`);
			await tx.execute(`UPDATE "${_mastra_core_storage.TABLE_AGENTS}" SET "favoriteCount" = 0 WHERE "favoriteCount" > 0`);
			await tx.execute(`UPDATE "${_mastra_core_storage.TABLE_SKILLS}" SET "favoriteCount" = 0 WHERE "favoriteCount" > 0`);
			await tx.commit();
		} catch (error) {
			if (!tx.closed) await tx.rollback();
			throw error;
		}
	}
	async favorite(input) {
		const { userId, entityType, entityId } = input;
		const entityTable = ENTITY_TABLE[entityType];
		try {
			const tx = await this.#client.transaction("write");
			try {
				if (!(await tx.execute({
					sql: `SELECT "favoriteCount" FROM "${entityTable}" WHERE id = ?`,
					args: [entityId]
				})).rows?.[0]) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "FAVORITE", "ENTITY_NOT_FOUND"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `${entityType} ${entityId} not found`,
					details: {
						entityType,
						entityId
					}
				});
				if (((await tx.execute({
					sql: `INSERT OR IGNORE INTO "${_mastra_core_storage.TABLE_FAVORITES}" ("userId", "entityType", "entityId", "createdAt") VALUES (?, ?, ?, ?)`,
					args: [
						userId,
						entityType,
						entityId,
						(/* @__PURE__ */ new Date()).toISOString()
					]
				})).rowsAffected ?? 0) > 0) await tx.execute({
					sql: `UPDATE "${entityTable}" SET "favoriteCount" = COALESCE("favoriteCount", 0) + 1 WHERE id = ?`,
					args: [entityId]
				});
				const after = await tx.execute({
					sql: `SELECT "favoriteCount" FROM "${entityTable}" WHERE id = ?`,
					args: [entityId]
				});
				const favoriteCount = Number(after.rows?.[0]?.favoriteCount ?? 0);
				await tx.commit();
				return {
					favorited: true,
					favoriteCount
				};
			} catch (error) {
				if (!tx.closed) await tx.rollback();
				throw error;
			}
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "FAVORITE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					entityType,
					entityId
				}
			}, error);
		}
	}
	async unfavorite(input) {
		const { userId, entityType, entityId } = input;
		const entityTable = ENTITY_TABLE[entityType];
		try {
			const tx = await this.#client.transaction("write");
			try {
				if (!(await tx.execute({
					sql: `SELECT "favoriteCount" FROM "${entityTable}" WHERE id = ?`,
					args: [entityId]
				})).rows?.[0]) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UNFAVORITE", "ENTITY_NOT_FOUND"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `${entityType} ${entityId} not found`,
					details: {
						entityType,
						entityId
					}
				});
				if (((await tx.execute({
					sql: `DELETE FROM "${_mastra_core_storage.TABLE_FAVORITES}" WHERE "userId" = ? AND "entityType" = ? AND "entityId" = ?`,
					args: [
						userId,
						entityType,
						entityId
					]
				})).rowsAffected ?? 0) > 0) await tx.execute({
					sql: `UPDATE "${entityTable}" SET "favoriteCount" = MAX(COALESCE("favoriteCount", 0) - 1, 0) WHERE id = ?`,
					args: [entityId]
				});
				const after = await tx.execute({
					sql: `SELECT "favoriteCount" FROM "${entityTable}" WHERE id = ?`,
					args: [entityId]
				});
				const favoriteCount = Number(after.rows?.[0]?.favoriteCount ?? 0);
				await tx.commit();
				return {
					favorited: false,
					favoriteCount
				};
			} catch (error) {
				if (!tx.closed) await tx.rollback();
				throw error;
			}
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UNFAVORITE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					entityType,
					entityId
				}
			}, error);
		}
	}
	async isFavorited(input) {
		try {
			return ((await this.#client.execute({
				sql: `SELECT 1 FROM "${_mastra_core_storage.TABLE_FAVORITES}" WHERE "userId" = ? AND "entityType" = ? AND "entityId" = ? LIMIT 1`,
				args: [
					input.userId,
					input.entityType,
					input.entityId
				]
			})).rows?.length ?? 0) > 0;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "IS_FAVORITED", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async isFavoritedBatch(input) {
		const { userId, entityType, entityIds } = input;
		if (entityIds.length === 0) return /* @__PURE__ */ new Set();
		try {
			const placeholders = entityIds.map(() => "?").join(", ");
			const args = [
				userId,
				entityType,
				...entityIds
			];
			const result = await this.#client.execute({
				sql: `SELECT "entityId" FROM "${_mastra_core_storage.TABLE_FAVORITES}" WHERE "userId" = ? AND "entityType" = ? AND "entityId" IN (${placeholders})`,
				args
			});
			const set = /* @__PURE__ */ new Set();
			for (const row of result.rows ?? []) set.add(row.entityId);
			return set;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "IS_FAVORITED_BATCH", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listFavoritedIds(input) {
		try {
			return ((await this.#client.execute({
				sql: `SELECT "entityId" FROM "${_mastra_core_storage.TABLE_FAVORITES}" WHERE "userId" = ? AND "entityType" = ? ORDER BY "createdAt" DESC, "entityId" ASC`,
				args: [input.userId, input.entityType]
			})).rows ?? []).map((row) => row.entityId);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_FAVORITED_IDS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteFavoritesForEntity(input) {
		const entityTable = ENTITY_TABLE[input.entityType];
		try {
			const tx = await this.#client.transaction("write");
			try {
				const result = await tx.execute({
					sql: `DELETE FROM "${_mastra_core_storage.TABLE_FAVORITES}" WHERE "entityType" = ? AND "entityId" = ?`,
					args: [input.entityType, input.entityId]
				});
				await tx.execute({
					sql: `UPDATE "${entityTable}" SET "favoriteCount" = 0 WHERE id = ?`,
					args: [input.entityId]
				});
				await tx.commit();
				return Number(result.rowsAffected ?? 0);
			} catch (txError) {
				if (!tx.closed) await tx.rollback();
				throw txError;
			}
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_FAVORITES_FOR_ENTITY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					entityType: input.entityType,
					entityId: input.entityId
				}
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/harness/index.ts
const toDate$1 = (value) => new Date(value);
const toOptionalDate = (value) => {
	if (value == null) return void 0;
	return toDate$1(value);
};
function cloneJson(value) {
	return value === void 0 ? void 0 : structuredClone(value);
}
function hydratePendingItem(item) {
	const record = {
		id: item.id,
		kind: item.kind,
		status: item.status,
		sessionId: item.sessionId,
		createdAt: toDate$1(item.createdAt),
		updatedAt: toDate$1(item.updatedAt)
	};
	if (item.runId !== void 0) record.runId = item.runId;
	if (item.traceId !== void 0) record.traceId = item.traceId;
	if (item.runtimeCompatibilityGeneration !== void 0) record.runtimeCompatibilityGeneration = item.runtimeCompatibilityGeneration;
	if (item.payload !== void 0) record.payload = cloneJson(item.payload);
	if (item.response !== void 0) record.response = cloneJson(item.response);
	return record;
}
function rowToSession(row) {
	const record = {
		id: row.id,
		ownerId: row.ownerId,
		resourceId: row.resourceId,
		threadId: row.threadId,
		origin: row.origin,
		modeId: row.modeId,
		modelId: row.modelId,
		createdAt: toDate$1(row.createdAt),
		lastActivityAt: toDate$1(row.lastActivityAt)
	};
	if (row.parentSessionId != null) record.parentSessionId = row.parentSessionId;
	if (row.subagentDepth != null) record.subagentDepth = row.subagentDepth;
	if (row.source != null) record.source = {
		...row.source,
		type: row.source.type
	};
	if (row.runtimeCompatibilityGeneration != null) record.runtimeCompatibilityGeneration = row.runtimeCompatibilityGeneration;
	if (row.title != null) record.title = row.title;
	if (row.metadata != null) record.metadata = cloneJson(row.metadata);
	if (row.state != null) record.state = cloneJson(row.state);
	if (row.pending != null) record.pending = row.pending.map(hydratePendingItem);
	const closingAt = toOptionalDate(row.closingAt);
	const closeDeadlineAt = toOptionalDate(row.closeDeadlineAt);
	const closedAt = toOptionalDate(row.closedAt);
	const deletedAt = toOptionalDate(row.deletedAt);
	if (closingAt) record.closingAt = closingAt;
	if (closeDeadlineAt) record.closeDeadlineAt = closeDeadlineAt;
	if (closedAt) record.closedAt = closedAt;
	if (deletedAt) record.deletedAt = deletedAt;
	return record;
}
function sessionToRecord(record) {
	return {
		id: record.id,
		ownerId: record.ownerId,
		resourceId: record.resourceId,
		threadId: record.threadId,
		parentSessionId: record.parentSessionId ?? null,
		subagentDepth: record.subagentDepth ?? null,
		source: record.source ?? null,
		origin: record.origin,
		runtimeCompatibilityGeneration: record.runtimeCompatibilityGeneration ?? null,
		modeId: record.modeId,
		modelId: record.modelId,
		title: record.title ?? null,
		metadata: record.metadata ?? null,
		state: record.state ?? null,
		pending: record.pending ?? null,
		createdAt: record.createdAt,
		lastActivityAt: record.lastActivityAt,
		closingAt: record.closingAt ?? null,
		closeDeadlineAt: record.closeDeadlineAt ?? null,
		closedAt: record.closedAt ?? null,
		deletedAt: record.deletedAt ?? null
	};
}
var HarnessLibSQL = class HarnessLibSQL extends _mastra_core_storage.HarnessStorage {
	/** Session records accumulate over time. Single table, anchored on `createdAt`. */
	static retentionTables = { sessions: {
		table: _mastra_core_storage.TABLE_HARNESS_SESSIONS,
		column: "createdAt",
		indexed: true
	} };
	#db;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_HARNESS_SESSIONS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_HARNESS_SESSIONS]
		});
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_HARNESS_SESSIONS });
	}
	/** Delete harness sessions older than the `sessions` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: HarnessLibSQL.retentionTables,
			order: ["sessions"]
		});
		return runPrune({
			db: this.#db,
			domain: "harness",
			targets,
			options,
			logger: this.logger
		});
	}
	async loadSession(sessionId) {
		const row = await this.#db.select({
			tableName: _mastra_core_storage.TABLE_HARNESS_SESSIONS,
			keys: { id: sessionId }
		});
		return row ? rowToSession(row) : null;
	}
	async saveSession(record) {
		await this.#db.insert({
			tableName: _mastra_core_storage.TABLE_HARNESS_SESSIONS,
			record: sessionToRecord(record)
		});
	}
	async listSessions() {
		return (await this.#db.selectMany({
			tableName: _mastra_core_storage.TABLE_HARNESS_SESSIONS,
			orderBy: "\"lastActivityAt\" DESC"
		})).map(rowToSession);
	}
};
//#endregion
//#region src/storage/domains/knowledge/index.ts
let assertDescriptionWithinBound;
async function assertKnowledgeDescriptionWithinBoundCompat(description) {
	let assertWithinBound = assertDescriptionWithinBound;
	if (!assertWithinBound) {
		const resolvedAssert = (await import("@mastra/core/storage")).assertKnowledgeDescriptionWithinBound ?? ((value) => {
			if (value !== void 0 && value.length > 400) throw new Error("Knowledge node description exceeds the 400 UTF-16 code unit limit");
		});
		assertDescriptionWithinBound = resolvedAssert;
		assertWithinBound = resolvedAssert;
	}
	assertWithinBound(description);
}
const visibleSql = `(scopeKey = ? OR substr(?, 1, length(scopeKey) + 1) = scopeKey || char(31))`;
function parseJson$3(value) {
	if (typeof value === "string") return JSON.parse(value);
	if (value instanceof Uint8Array) return JSON.parse(new TextDecoder().decode(value));
	if (value instanceof ArrayBuffer) return JSON.parse(new TextDecoder().decode(value));
	return value;
}
function toDate(value) {
	return new Date(String(value));
}
function optionalDate(value) {
	return value == null ? void 0 : toDate(value);
}
function canonicalName(name) {
	return name.trim().toLocaleLowerCase();
}
function escapeLikePattern(value) {
	return value.replaceAll("=", "==").replaceAll("%", "=%").replaceAll("_", "=_");
}
function nodeReferenceId(node) {
	return typeof node === "string" ? node : node.id;
}
function parseNode(row) {
	return {
		id: String(row.id),
		type: "node",
		name: String(row.name),
		kind: row.kind == null ? "" : String(row.kind),
		content: row.content == null ? void 0 : String(row.content),
		description: row.description == null ? void 0 : String(row.description),
		scope: parseJson$3(row.scopeJson ?? row.scope),
		version: Number(row.version),
		mergedInto: row.mergedInto == null ? void 0 : String(row.mergedInto),
		createdAt: toDate(row.createdAt),
		updatedAt: toDate(row.updatedAt)
	};
}
function parseKnowledge(row) {
	return {
		id: String(row.id),
		node: String(row.node),
		text: String(row.text),
		scope: parseJson$3(row.scopeJson ?? row.scope),
		sourceThreadId: String(row.sourceThreadId),
		capturedAt: toDate(row.capturedAt),
		when: optionalDate(row.when),
		maxScope: row.maxScope == null ? void 0 : String(row.maxScope),
		metadata: (row.metadataJson ?? row.metadata) == null ? void 0 : parseJson$3(row.metadataJson ?? row.metadata),
		deletedAt: optionalDate(row.deletedAt),
		deletedBy: row.deletedBy == null ? void 0 : String(row.deletedBy)
	};
}
function parseOutbox(row) {
	return {
		id: String(row.id),
		idempotencyKey: String(row.idempotencyKey),
		documentId: String(row.documentId),
		documentType: String(row.documentType),
		operation: String(row.operation),
		scope: parseJson$3(row.scopeJson ?? row.scope),
		status: String(row.status),
		attempts: Number(row.attempts),
		availableAt: toDate(row.availableAt),
		claimedAt: optionalDate(row.claimedAt),
		claimedBy: row.claimedBy == null ? void 0 : String(row.claimedBy),
		createdAt: toDate(row.createdAt),
		completedAt: optionalDate(row.completedAt)
	};
}
var KnowledgeLibSQL = class extends _mastra_core_storage.KnowledgeStorage {
	#client;
	#db;
	constructor(config) {
		super();
		this.#client = resolveClient(config);
		this.#db = new LibSQLDB({
			client: this.#client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_KNOWLEDGE_NODES,
			schema: _mastra_core_storage.KNOWLEDGE_NODES_SCHEMA
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_KNOWLEDGE_NODES,
			schema: _mastra_core_storage.KNOWLEDGE_NODES_SCHEMA,
			ifNotExists: ["description"]
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_KNOWLEDGE_RECORDS,
			schema: _mastra_core_storage.KNOWLEDGE_RECORDS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS,
			schema: _mastra_core_storage.KNOWLEDGE_MENTIONS_SCHEMA,
			compositePrimaryKey: [
				"sourceType",
				"sourceId",
				"recordId"
			]
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_KNOWLEDGE_CURSORS,
			schema: _mastra_core_storage.KNOWLEDGE_CURSORS_SCHEMA,
			compositePrimaryKey: ["sourceThreadId", "agent"]
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_KNOWLEDGE_ACTIVITY,
			schema: _mastra_core_storage.KNOWLEDGE_ACTIVITY_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX,
			schema: _mastra_core_storage.KNOWLEDGE_SEMANTIC_OUTBOX_SCHEMA
		});
		await this.#client.batch([
			{
				sql: `CREATE UNIQUE INDEX IF NOT EXISTS idx_knowledge_nodes_identity ON "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" (type, scopeKey, canonicalName)`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_knowledge_nodes_scope ON "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" (scopeKey, type)`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_knowledge_records_node_latest ON "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" (node, id DESC)`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_knowledge_records_thread_latest ON "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" (sourceThreadId, id DESC)`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_knowledge_mentions_record ON "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" (recordId, sourceType, sourceId)`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_knowledge_activity_latest ON "${_mastra_core_storage.TABLE_KNOWLEDGE_ACTIVITY}" (id DESC)`,
				args: []
			},
			{
				sql: `CREATE UNIQUE INDEX IF NOT EXISTS idx_knowledge_outbox_idempotency ON "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" (idempotencyKey)`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_knowledge_outbox_claim ON "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" (status, availableAt, createdAt)`,
				args: []
			}
		], "write");
	}
	async dangerouslyClearAll() {
		await this.#transaction(async (tx) => {
			for (const table of [
				_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS,
				_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS,
				_mastra_core_storage.TABLE_KNOWLEDGE_NODES,
				_mastra_core_storage.TABLE_KNOWLEDGE_CURSORS,
				_mastra_core_storage.TABLE_KNOWLEDGE_ACTIVITY,
				_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX
			]) await tx.execute(`DELETE FROM "${table}"`);
		});
	}
	async createNode(input) {
		await assertKnowledgeDescriptionWithinBoundCompat(input.description);
		const scope = (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope);
		return this.#transaction(async (tx) => {
			const existing = await this.#getNodeByName(tx, input.name, scope);
			if (existing) {
				const terminal = await this.#resolveTerminalNode(tx, existing.id);
				if (!(0, _mastra_core_storage.isKnowledgeScopeVisible)(terminal.scope, scope)) throw new Error(`Merged knowledge node is not visible from scope: ${input.name}`);
				return terminal;
			}
			const now = /* @__PURE__ */ new Date();
			const node = {
				id: input.id ?? crypto.randomUUID(),
				type: "node",
				name: input.name.trim(),
				kind: input.kind,
				content: input.content,
				description: input.description,
				scope,
				version: 1,
				createdAt: now,
				updatedAt: now
			};
			await tx.execute({
				sql: `INSERT INTO "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" (id,type,name,canonicalName,kind,content,description,scope,scopeKey,version,mergedInto,createdAt,updatedAt) VALUES (?,?,?,?,?,?,?,jsonb(?),?,?,NULL,?,?)`,
				args: [
					node.id,
					"node",
					node.name,
					canonicalName(node.name),
					node.kind,
					node.content ?? null,
					node.description ?? null,
					JSON.stringify(scope),
					(0, _mastra_core_storage.knowledgeScopeKey)(scope),
					node.version,
					now.toISOString(),
					now.toISOString()
				]
			});
			await this.#replaceMentions(tx, "node", node.id, node.content ?? "", input.resolutionScope ?? scope, scope);
			await this.#activity(tx, "node-created", "node", node.id, scope);
			await this.#outbox(tx, "node", node.id, "upsert", node.version, scope);
			return node;
		});
	}
	async getNode(id) {
		return this.#getNode(this.#client, id);
	}
	async getNodeByName(input) {
		return this.#getNodeByName(this.#client, input.name, (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
	}
	async resolveNode(input) {
		return this.#resolveNode(this.#client, input.name, (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
	}
	async listNodes(input) {
		const key = (0, _mastra_core_storage.knowledgeScopeKey)((0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
		const clauses = [
			`type = 'node'`,
			"mergedInto IS NULL",
			visibleSql
		];
		const args = [key, key];
		if (input.namePrefix) {
			clauses.push("canonicalName LIKE ? ESCAPE '='");
			args.push(`${escapeLikePattern(canonicalName(input.namePrefix))}%`);
		}
		if (input.kind) {
			clauses.push("kind = ?");
			args.push(input.kind);
		}
		if (input.hasContent !== void 0) clauses.push(input.hasContent ? "content IS NOT NULL AND content <> ''" : "(content IS NULL OR content = '')");
		if (input.cursor) {
			const cursor = (0, _mastra_core_storage.parseKnowledgeNodeCursor)(input.cursor, {
				namePrefix: input.namePrefix,
				kind: input.kind,
				hasContent: input.hasContent
			});
			clauses.push("(updatedAt < ? OR (updatedAt = ? AND (name > ? OR (name = ? AND id > ?))))");
			const updatedAt = cursor.updatedAt.toISOString();
			args.push(updatedAt, updatedAt, cursor.name, cursor.name, cursor.id);
		}
		args.push(input.limit ?? 100);
		return (await this.#client.execute({
			sql: `SELECT *, json(scope) AS scopeJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" WHERE ${clauses.join(" AND ")} ORDER BY updatedAt DESC, name ASC, id ASC LIMIT ?`,
			args
		})).rows.map(parseNode);
	}
	async updateNode(input) {
		await assertKnowledgeDescriptionWithinBoundCompat(input.description);
		return this.#transaction(async (tx) => {
			const existing = await this.#getNode(tx, input.id);
			if (!existing) throw new _mastra_core_storage.KnowledgeNotFoundError("node", input.id);
			if (existing.mergedInto) throw new Error(`Cannot update merged knowledge node: ${input.id}`);
			const scope = (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope ?? existing.scope);
			const name = (input.name ?? existing.name).trim();
			const content = input.content ?? existing.content;
			const description = input.description ?? existing.description;
			const now = /* @__PURE__ */ new Date();
			if ((await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" SET name=?,canonicalName=?,kind=?,content=?,description=?,scope=jsonb(?),scopeKey=?,version=version+1,updatedAt=? WHERE id=? AND type='node' AND version=?`,
				args: [
					name,
					canonicalName(name),
					input.kind ?? existing.kind,
					content ?? null,
					description ?? null,
					JSON.stringify(scope),
					(0, _mastra_core_storage.knowledgeScopeKey)(scope),
					now.toISOString(),
					input.id,
					input.version
				]
			})).rowsAffected === 0) throw new _mastra_core_storage.KnowledgeConflictError(input.id);
			if (input.content !== void 0 || input.name !== void 0 || input.scope !== void 0) await this.#replaceMentions(tx, "node", input.id, content ?? "", input.resolutionScope ?? scope, scope);
			await this.#activity(tx, "node-updated", "node", input.id, scope);
			if ((0, _mastra_core_storage.knowledgeScopeKey)(existing.scope) !== (0, _mastra_core_storage.knowledgeScopeKey)(scope)) {
				await this.#outbox(tx, "node", input.id, "delete", (0, _mastra_core_storage.createKnowledgeUlid)(), existing.scope);
				const knowledge = await tx.execute({
					sql: `SELECT id,json(scope) AS scopeJson,deletedAt FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" WHERE node=?`,
					args: [input.id]
				});
				for (const row of knowledge.rows) {
					const recordScope = parseJson$3(row.scopeJson);
					await this.#outbox(tx, "record", String(row.id), "delete", (0, _mastra_core_storage.createKnowledgeUlid)(), recordScope);
					if (row.deletedAt == null) await this.#outbox(tx, "record", String(row.id), "upsert", (0, _mastra_core_storage.createKnowledgeUlid)(), recordScope);
				}
			}
			await this.#outbox(tx, "node", input.id, "upsert", input.version + 1, scope);
			return {
				...existing,
				name,
				kind: input.kind ?? existing.kind,
				content,
				description,
				scope,
				version: input.version + 1,
				updatedAt: now
			};
		});
	}
	async mergeNodes(input) {
		if (input.sourceId === input.targetId) throw new Error("Cannot merge a knowledge node into itself");
		return this.#transaction(async (tx) => {
			const source = await this.#getNode(tx, input.sourceId);
			if (!source) throw new _mastra_core_storage.KnowledgeNotFoundError("node", input.sourceId);
			const target = await this.#resolveTerminalNode(tx, input.targetId);
			if (!target) throw new _mastra_core_storage.KnowledgeNotFoundError("node", input.targetId);
			if (target.id === source.id) throw new Error("Cannot create a knowledge merge cycle");
			if (!(0, _mastra_core_storage.isKnowledgeScopeVisible)(target.scope, source.scope)) throw new Error("Cannot merge a knowledge node into a target that is narrower than its source scope");
			const affected = await tx.execute({
				sql: `SELECT DISTINCT m.sourceType,m.sourceId,json(COALESCE(f.scope,r.scope)) AS scopeJson,CASE WHEN f.deletedAt IS NULL THEN 0 ELSE 1 END AS deleted FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" m LEFT JOIN "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" f ON m.sourceType='record' AND f.id=m.sourceId LEFT JOIN "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" r ON m.sourceType='node' AND r.id=m.sourceId WHERE m.recordId=?`,
				args: [source.id]
			});
			const movedKnowledge = await tx.execute({
				sql: `SELECT id,json(scope) AS scopeJson,deletedAt FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" WHERE node=?`,
				args: [source.id]
			});
			if ((await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" SET mergedInto=?,version=version+1,updatedAt=? WHERE id=? AND type='node' AND version=? AND mergedInto IS NULL`,
				args: [
					target.id,
					(/* @__PURE__ */ new Date()).toISOString(),
					source.id,
					input.sourceVersion
				]
			})).rowsAffected === 0) throw new _mastra_core_storage.KnowledgeConflictError(source.id);
			await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" SET node=? WHERE node=?`,
				args: [target.id, source.id]
			});
			await tx.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" WHERE recordId=? AND EXISTS (SELECT 1 FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" target WHERE target.sourceType="${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}".sourceType AND target.sourceId="${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}".sourceId AND target.recordId=?)`,
				args: [source.id, target.id]
			});
			await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" SET recordId=? WHERE recordId=?`,
				args: [target.id, source.id]
			});
			for (const row of movedKnowledge.rows) await this.#outbox(tx, "record", String(row.id), row.deletedAt == null ? "upsert" : "delete", (0, _mastra_core_storage.createKnowledgeUlid)(), parseJson$3(row.scopeJson));
			for (const row of affected.rows) await this.#outbox(tx, String(row.sourceType), String(row.sourceId), Number(row.deleted) ? "delete" : "upsert", (0, _mastra_core_storage.createKnowledgeUlid)(), parseJson$3(row.scopeJson));
			let mergedTarget = target;
			if (target.description === void 0 && source.description) {
				const adoptedAt = /* @__PURE__ */ new Date();
				if ((await tx.execute({
					sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" SET description=?,version=version+1,updatedAt=? WHERE id=? AND type='node' AND version=? AND description IS NULL`,
					args: [
						source.description,
						adoptedAt.toISOString(),
						target.id,
						target.version
					]
				})).rowsAffected > 0) {
					mergedTarget = {
						...target,
						description: source.description,
						version: target.version + 1,
						updatedAt: adoptedAt
					};
					await this.#activity(tx, "node-updated", "node", target.id, target.scope);
				}
			}
			await this.#activity(tx, "node-merged", "node", source.id, source.scope);
			await this.#outbox(tx, "node", source.id, "delete", input.sourceVersion + 1, source.scope);
			await this.#outbox(tx, "node", target.id, "upsert", (0, _mastra_core_storage.createKnowledgeUlid)(), mergedTarget.scope);
			return mergedTarget;
		});
	}
	async appendKnowledge(input) {
		const scope = (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope);
		const resolutionScope = (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.resolutionScope);
		const defaultScope = (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.defaultScope);
		(0, _mastra_core_storage.assertKnowledgeScopeWithinCeiling)(scope, input.maxScope);
		return this.#transaction(async (tx) => {
			const node = nodeReferenceId(input.node);
			const parent = await this.#resolveTerminalNode(tx, node);
			if (!parent) throw new _mastra_core_storage.KnowledgeNotFoundError("node", node);
			const record = {
				id: input.id ?? (0, _mastra_core_storage.createKnowledgeUlid)(),
				node: parent.id,
				text: input.text,
				scope,
				sourceThreadId: input.sourceThreadId,
				capturedAt: /* @__PURE__ */ new Date(),
				when: input.when ? new Date(input.when) : void 0,
				maxScope: input.maxScope,
				metadata: input.metadata
			};
			await tx.execute({
				sql: `INSERT INTO "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" (id,node,text,scope,scopeKey,sourceThreadId,capturedAt,"when",maxScope,metadata,deletedAt,deletedBy) VALUES (?,?,?,jsonb(?),?,?,?,?,?,jsonb(?),NULL,NULL)`,
				args: [
					record.id,
					record.node,
					record.text,
					JSON.stringify(scope),
					(0, _mastra_core_storage.knowledgeScopeKey)(scope),
					record.sourceThreadId,
					record.capturedAt.toISOString(),
					record.when?.toISOString() ?? null,
					record.maxScope ?? null,
					record.metadata ? JSON.stringify(record.metadata) : null
				]
			});
			await this.#replaceMentions(tx, "record", record.id, record.text, resolutionScope, defaultScope);
			await this.#activity(tx, "record-created", "record", record.id, scope, record.sourceThreadId);
			await this.#outbox(tx, "record", record.id, "upsert", record.id, scope);
			return record;
		});
	}
	async getKnowledge(input) {
		const result = await this.#client.execute({
			sql: `SELECT *,json(scope) AS scopeJson,json(metadata) AS metadataJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" WHERE id=?${input.includeDeleted ? "" : " AND deletedAt IS NULL"}`,
			args: [input.id]
		});
		return result.rows[0] ? parseKnowledge(result.rows[0]) : null;
	}
	async listKnowledgeAbout(input) {
		return this.#queryKnowledge(input, "about");
	}
	async listKnowledgeMentioning(input) {
		return this.#queryKnowledge(input, "mentioning");
	}
	async listKnowledgeRelatedTo(input) {
		return this.#queryKnowledge(input, "related");
	}
	async knowledgeBySource(input) {
		const key = (0, _mastra_core_storage.knowledgeScopeKey)((0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
		const args = [
			input.sourceThreadId,
			key,
			key
		];
		if (input.after) args.push(input.after);
		const limit = input.limit ?? 100;
		args.push(limit + 1);
		const records = (await this.#client.execute({
			sql: `SELECT *,json(scope) AS scopeJson,json(metadata) AS metadataJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" WHERE sourceThreadId=? AND ${visibleSql}${input.includeDeleted ? "" : " AND deletedAt IS NULL"}${input.after ? " AND id > ?" : ""} ORDER BY id ASC LIMIT ?`,
			args
		})).rows.map(parseKnowledge);
		return {
			records: records.slice(0, limit),
			nextCursor: records.length > limit ? records[limit - 1]?.id : void 0
		};
	}
	async removeKnowledge(input) {
		return this.#transaction(async (tx) => {
			const record = await this.#getKnowledge(tx, input.id, true);
			if (!record) throw new _mastra_core_storage.KnowledgeNotFoundError("record", input.id);
			if (record.deletedAt) return record;
			const deletedAt = /* @__PURE__ */ new Date();
			await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" SET deletedAt=?,deletedBy=? WHERE id=? AND deletedAt IS NULL`,
				args: [
					deletedAt.toISOString(),
					input.deletedBy,
					input.id
				]
			});
			await this.#activity(tx, "record-deleted", "record", input.id, record.scope, record.sourceThreadId);
			await this.#outbox(tx, "record", input.id, "delete", deletedAt.toISOString(), record.scope);
			return {
				...record,
				deletedAt,
				deletedBy: input.deletedBy
			};
		});
	}
	async restoreKnowledge(input) {
		return this.#transaction(async (tx) => {
			const record = await this.#getKnowledge(tx, input.id, true);
			if (!record) throw new _mastra_core_storage.KnowledgeNotFoundError("record", input.id);
			if (!record.deletedAt) return record;
			await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" SET deletedAt=NULL,deletedBy=NULL WHERE id=?`,
				args: [input.id]
			});
			await this.#activity(tx, "record-restored", "record", input.id, record.scope, record.sourceThreadId);
			await this.#outbox(tx, "record", input.id, "upsert", (0, _mastra_core_storage.createKnowledgeUlid)(), record.scope);
			return {
				...record,
				deletedAt: void 0,
				deletedBy: void 0
			};
		});
	}
	async rescopeKnowledge(input) {
		const scope = (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope);
		return this.#transaction(async (tx) => {
			const record = await this.#getKnowledge(tx, input.id, true);
			if (!record) throw new _mastra_core_storage.KnowledgeNotFoundError("record", input.id);
			(0, _mastra_core_storage.assertKnowledgeScopeWithinCeiling)(scope, record.maxScope);
			await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" SET scope=jsonb(?),scopeKey=? WHERE id=?`,
				args: [
					JSON.stringify(scope),
					(0, _mastra_core_storage.knowledgeScopeKey)(scope),
					input.id
				]
			});
			await this.#activity(tx, "record-rescoped", "record", input.id, scope, record.sourceThreadId);
			if ((0, _mastra_core_storage.knowledgeScopeKey)(record.scope) !== (0, _mastra_core_storage.knowledgeScopeKey)(scope)) await this.#outbox(tx, "record", input.id, "delete", (0, _mastra_core_storage.createKnowledgeUlid)(), record.scope);
			if (!record.deletedAt) await this.#outbox(tx, "record", input.id, "upsert", (0, _mastra_core_storage.createKnowledgeUlid)(), scope);
			return {
				...record,
				scope
			};
		});
	}
	async raiseKnowledgeCeiling(input) {
		return this.#transaction(async (tx) => {
			const record = await this.#getKnowledge(tx, input.id, true);
			if (!record) throw new _mastra_core_storage.KnowledgeNotFoundError("record", input.id);
			(0, _mastra_core_storage.assertKnowledgeScopeWithinCeiling)(record.scope, input.maxScope);
			(0, _mastra_core_storage.assertKnowledgeCeilingRaised)(record.maxScope, input.maxScope);
			await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" SET maxScope=? WHERE id=?`,
				args: [input.maxScope ?? null, input.id]
			});
			return {
				...record,
				maxScope: input.maxScope
			};
		});
	}
	async search(input) {
		const key = (0, _mastra_core_storage.knowledgeScopeKey)((0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
		const normalizedQuery = input.query.trim().toLocaleLowerCase();
		if (!normalizedQuery) return [];
		const query = `%${escapeLikePattern(normalizedQuery)}%`;
		const results = (await this.#client.execute({
			sql: `SELECT *,json(scope) AS scopeJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" WHERE mergedInto IS NULL AND ${visibleSql} AND (canonicalName LIKE ? ESCAPE '=' OR lower(COALESCE(kind,'')) LIKE ? ESCAPE '=' OR lower(COALESCE(content,'')) LIKE ? ESCAPE '=' OR lower(COALESCE(description,'')) LIKE ? ESCAPE '=') ORDER BY updatedAt DESC LIMIT ?`,
			args: [
				key,
				key,
				query,
				query,
				query,
				query,
				input.limit ?? 20
			]
		})).rows.map((row) => ({
			type: String(row.type),
			id: String(row.id),
			recordId: String(row.id),
			name: String(row.name),
			text: [
				String(row.name),
				...row.description ? [String(row.description)] : [],
				...row.content ? [String(row.content)] : []
			].join("\n"),
			scope: parseJson$3(row.scopeJson)
		}));
		if (results.length < (input.limit ?? 20)) {
			const knowledge = await this.#client.execute({
				sql: `SELECT k.*,json(k.scope) AS scopeJson,json(k.metadata) AS metadataJson,n.name FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" k JOIN "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" n ON n.id=k.node AND n.type='node' AND n.mergedInto IS NULL WHERE k.deletedAt IS NULL AND ${visibleSql.replaceAll("scopeKey", "k.scopeKey")} AND lower(k.text) LIKE ? ESCAPE '=' ORDER BY k.id DESC LIMIT ?`,
				args: [
					key,
					key,
					query,
					(input.limit ?? 20) - results.length
				]
			});
			results.push(...knowledge.rows.map((row) => ({
				type: "record",
				id: String(row.id),
				recordId: String(row.node),
				name: String(row.name),
				text: String(row.text),
				scope: parseJson$3(row.scopeJson)
			})));
		}
		return results;
	}
	async getCurationCursor(input) {
		const row = (await this.#client.execute({
			sql: `SELECT * FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_CURSORS}" WHERE sourceThreadId=? AND agent=?`,
			args: [input.sourceThreadId, input.agent]
		})).rows[0];
		return row ? {
			sourceThreadId: String(row.sourceThreadId),
			agent: String(row.agent),
			lastKnowledgeId: String(row.lastKnowledgeId),
			updatedAt: toDate(row.updatedAt)
		} : null;
	}
	async advanceCurationCursor(input) {
		const updatedAt = /* @__PURE__ */ new Date();
		if ((await this.#db.executeWriteOperationWithRetry(() => withClientWriteLock(this.#client, () => this.#client.execute({
			sql: `INSERT INTO "${_mastra_core_storage.TABLE_KNOWLEDGE_CURSORS}" (sourceThreadId,agent,lastKnowledgeId,updatedAt) VALUES (?,?,?,?) ON CONFLICT(sourceThreadId,agent) DO UPDATE SET lastKnowledgeId=excluded.lastKnowledgeId,updatedAt=excluded.updatedAt WHERE excluded.lastKnowledgeId >= "${_mastra_core_storage.TABLE_KNOWLEDGE_CURSORS}".lastKnowledgeId`,
			args: [
				input.sourceThreadId,
				input.agent,
				input.lastKnowledgeId,
				updatedAt.toISOString()
			]
		})), "advance knowledge curation cursor")).rowsAffected === 0) throw new Error("Knowledge curation cursor cannot move backwards");
		return {
			...input,
			updatedAt
		};
	}
	async listActivity(input) {
		const key = (0, _mastra_core_storage.knowledgeScopeKey)((0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
		return (await this.#client.execute({
			sql: `SELECT *,json(scope) AS scopeJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_ACTIVITY}" WHERE ${visibleSql}${input.after ? " AND id < ?" : ""} ORDER BY id DESC LIMIT ?`,
			args: [
				key,
				key,
				...input.after ? [input.after] : [],
				input.limit ?? 100
			]
		})).rows.map((row) => ({
			id: String(row.id),
			action: String(row.action),
			recordType: String(row.recordType),
			recordId: String(row.recordId),
			scope: parseJson$3(row.scopeJson),
			sourceThreadId: row.sourceThreadId == null ? void 0 : String(row.sourceThreadId),
			createdAt: toDate(row.createdAt)
		}));
	}
	async listSemanticOutbox(input = {}) {
		const clauses = [];
		const args = [];
		if (input.status) {
			clauses.push("status=?");
			args.push(input.status);
		}
		if (input.scope) {
			const key = (0, _mastra_core_storage.knowledgeScopeKey)((0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
			clauses.push(visibleSql);
			args.push(key, key);
		}
		args.push(input.limit ?? 100);
		return (await this.#client.execute({
			sql: `SELECT *,json(scope) AS scopeJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}"${clauses.length ? ` WHERE ${clauses.join(" AND ")}` : ""} ORDER BY createdAt ASC,id ASC LIMIT ?`,
			args
		})).rows.map(parseOutbox);
	}
	async claimSemanticOutbox(input) {
		const now = input.now ?? /* @__PURE__ */ new Date();
		const stale = new Date(now.getTime() - (input.claimTimeoutMs ?? 6e4));
		return this.#transaction(async (tx) => {
			const clauses = [
				`availableAt <= ?`,
				`(status='pending' OR (status='processing' AND claimedAt <= ?))`,
				`NOT EXISTS (SELECT 1 FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" AS earlier WHERE earlier.documentId = "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}".documentId AND earlier.status != 'completed' AND (earlier.createdAt < "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}".createdAt OR (earlier.createdAt = "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}".createdAt AND earlier.id < "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}".id)))`
			];
			const args = [now.toISOString(), stale.toISOString()];
			if (input.scope) {
				const key = (0, _mastra_core_storage.knowledgeScopeKey)((0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope));
				clauses.push(visibleSql);
				args.push(key, key);
			}
			args.push(input.limit ?? 100);
			const ids = (await tx.execute({
				sql: `SELECT id FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" WHERE ${clauses.join(" AND ")} ORDER BY createdAt ASC,id ASC LIMIT ?`,
				args
			})).rows.map((row) => String(row.id));
			for (const id of ids) await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" SET status='processing',attempts=attempts+1,claimedAt=?,claimedBy=? WHERE id=?`,
				args: [
					now.toISOString(),
					input.workerId,
					id
				]
			});
			if (!ids.length) return [];
			return (await tx.execute({
				sql: `SELECT *,json(scope) AS scopeJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" WHERE id IN (${ids.map(() => "?").join(",")}) ORDER BY createdAt ASC,id ASC`,
				args: ids
			})).rows.map(parseOutbox);
		});
	}
	async completeSemanticOutbox(input) {
		if (!input.ids.length) return;
		const now = (/* @__PURE__ */ new Date()).toISOString();
		await this.#transaction(async (tx) => {
			for (const id of input.ids) await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" SET status='completed',completedAt=? WHERE id=? AND status='processing' AND claimedBy=?`,
				args: [
					now,
					id,
					input.workerId
				]
			});
		});
	}
	async releaseSemanticOutbox(input) {
		if (!input.ids.length) return;
		await this.#transaction(async (tx) => {
			for (const id of input.ids) await tx.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" SET status='pending',availableAt=?,claimedAt=NULL,claimedBy=NULL WHERE id=? AND status='processing' AND claimedBy=?`,
				args: [
					(input.retryAt ?? /* @__PURE__ */ new Date()).toISOString(),
					id,
					input.workerId
				]
			});
		});
	}
	async #transaction(operation) {
		return this.#db.executeWriteOperationWithRetry(() => withClientWriteLock(this.#client, async () => {
			const tx = await this.#client.transaction("write");
			try {
				const result = await operation(tx);
				await tx.commit();
				return result;
			} catch (error) {
				if (!tx.closed) await tx.rollback();
				throw error;
			}
		}), "write knowledge state");
	}
	async #getNode(executor, id) {
		const result = await executor.execute({
			sql: `SELECT *,json(scope) AS scopeJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" WHERE id=? AND type='node'`,
			args: [id]
		});
		return result.rows[0] ? parseNode(result.rows[0]) : null;
	}
	async #getNodeByName(executor, name, scope) {
		const result = await executor.execute({
			sql: `SELECT *,json(scope) AS scopeJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" WHERE type='node' AND scopeKey=? AND canonicalName=?`,
			args: [(0, _mastra_core_storage.knowledgeScopeKey)(scope), canonicalName(name)]
		});
		return result.rows[0] ? parseNode(result.rows[0]) : null;
	}
	async #resolveNode(executor, name, scope) {
		for (let length = scope.length; length > 0; length--) {
			const node = await this.#getNodeByName(executor, name, scope.slice(0, length));
			if (node) {
				const terminal = await this.#resolveTerminalNode(executor, node.id);
				if (terminal && (0, _mastra_core_storage.isKnowledgeScopeVisible)(terminal.scope, scope)) return terminal;
			}
		}
		return null;
	}
	async #resolveTerminalNode(executor, id) {
		let node = await this.#getNode(executor, id);
		const seen = /* @__PURE__ */ new Set();
		while (node?.mergedInto) {
			if (seen.has(node.id)) throw new Error(`Knowledge merge cycle detected at ${node.id}`);
			seen.add(node.id);
			node = await this.#getNode(executor, node.mergedInto);
		}
		return node;
	}
	async #getKnowledge(executor, id, includeDeleted) {
		const result = await executor.execute({
			sql: `SELECT *,json(scope) AS scopeJson,json(metadata) AS metadataJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" WHERE id=?${includeDeleted ? "" : " AND deletedAt IS NULL"}`,
			args: [id]
		});
		return result.rows[0] ? parseKnowledge(result.rows[0]) : null;
	}
	async #queryKnowledge(input, relationship) {
		const scope = (0, _mastra_core_storage.canonicalizeKnowledgeScope)(input.scope);
		const node = await this.#resolveTerminalNode(this.#client, nodeReferenceId(input.node));
		if (!node) return { records: [] };
		const key = (0, _mastra_core_storage.knowledgeScopeKey)(scope);
		const includesMentions = relationship !== "about";
		const relationSql = relationship === "about" ? "k.node=?" : relationship === "mentioning" ? "m.recordId=?" : "(k.node=? OR m.recordId=?)";
		const args = [
			node.id,
			...relationship === "related" ? [node.id] : [],
			key,
			key
		];
		if (input.after) args.push(input.after);
		args.push((input.limit ?? 100) + 1);
		const result = await this.#client.execute({
			sql: `SELECT DISTINCT k.*,json(k.scope) AS scopeJson,json(k.metadata) AS metadataJson FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_RECORDS}" k${includesMentions ? ` LEFT JOIN "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" m ON m.sourceType='record' AND m.sourceId=k.id` : ""} WHERE ${relationSql} AND ${visibleSql.replaceAll("scopeKey", "k.scopeKey")}${input.includeDeleted ? "" : " AND k.deletedAt IS NULL"}${input.after ? " AND k.id < ?" : ""} ORDER BY k.id DESC LIMIT ?`,
			args
		});
		const limit = input.limit ?? 100;
		const rows = result.rows.slice(0, limit);
		return {
			records: rows.map(parseKnowledge),
			nextCursor: result.rows.length > limit ? String(rows.at(-1)?.id) : void 0
		};
	}
	async #replaceMentions(tx, sourceType, sourceId, text, resolutionScope, defaultScope) {
		await tx.execute({
			sql: `DELETE FROM "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" WHERE sourceType=? AND sourceId=?`,
			args: [sourceType, sourceId]
		});
		for (const name of (0, _mastra_core_storage.parseKnowledgeWikilinks)(text)) {
			let node = await this.#resolveNode(tx, name, resolutionScope);
			if (!node) {
				node = await this.#getNodeByName(tx, name, defaultScope);
				if (node) node = await this.#resolveTerminalNode(tx, node.id);
				if (!node) {
					const now = /* @__PURE__ */ new Date();
					node = {
						id: crypto.randomUUID(),
						type: "node",
						name,
						kind: "node",
						scope: defaultScope,
						version: 1,
						createdAt: now,
						updatedAt: now
					};
					await tx.execute({
						sql: `INSERT INTO "${_mastra_core_storage.TABLE_KNOWLEDGE_NODES}" (id,type,name,canonicalName,kind,content,scope,scopeKey,version,mergedInto,createdAt,updatedAt) VALUES (?,?,?,?,?,NULL,jsonb(?),?,?,NULL,?,?)`,
						args: [
							node.id,
							"node",
							node.name,
							canonicalName(node.name),
							node.kind,
							JSON.stringify(defaultScope),
							(0, _mastra_core_storage.knowledgeScopeKey)(defaultScope),
							1,
							now.toISOString(),
							now.toISOString()
						]
					});
					await this.#activity(tx, "node-created", "node", node.id, defaultScope);
					await this.#outbox(tx, "node", node.id, "upsert", 1, defaultScope);
				}
			}
			await tx.execute({
				sql: `INSERT OR IGNORE INTO "${_mastra_core_storage.TABLE_KNOWLEDGE_MENTIONS}" (sourceType,sourceId,recordId) VALUES (?,?,?)`,
				args: [
					sourceType,
					sourceId,
					node.id
				]
			});
		}
	}
	async #activity(executor, action, recordType, recordId, scope, sourceThreadId) {
		const now = /* @__PURE__ */ new Date();
		await executor.execute({
			sql: `INSERT INTO "${_mastra_core_storage.TABLE_KNOWLEDGE_ACTIVITY}" (id,action,recordType,recordId,scope,scopeKey,sourceThreadId,createdAt) VALUES (?,?,?,?,jsonb(?),?,?,?)`,
			args: [
				(0, _mastra_core_storage.createKnowledgeUlid)(),
				action,
				recordType,
				recordId,
				JSON.stringify(scope),
				(0, _mastra_core_storage.knowledgeScopeKey)(scope),
				sourceThreadId ?? null,
				now.toISOString()
			]
		});
	}
	async #outbox(executor, documentType, id, operation, version, scope) {
		const documentId = (0, _mastra_core_storage.knowledgeSemanticDocumentId)(documentType, id);
		const idempotencyKey = (0, _mastra_core_storage.knowledgeSemanticIdempotencyKey)(documentId, operation, version);
		const now = /* @__PURE__ */ new Date();
		await executor.execute({
			sql: `INSERT OR IGNORE INTO "${_mastra_core_storage.TABLE_KNOWLEDGE_SEMANTIC_OUTBOX}" (id,idempotencyKey,documentId,documentType,operation,scope,scopeKey,status,attempts,availableAt,claimedAt,claimedBy,createdAt,completedAt) VALUES (?,?,?,?,?,jsonb(?),?,'pending',0,?,NULL,NULL,?,NULL)`,
			args: [
				(0, _mastra_core_storage.createKnowledgeUlid)(),
				idempotencyKey,
				documentId,
				documentType,
				operation,
				JSON.stringify(scope),
				(0, _mastra_core_storage.knowledgeScopeKey)(scope),
				now.toISOString(),
				now.toISOString()
			]
		});
	}
};
//#endregion
//#region src/storage/domains/mcp-clients/index.ts
var MCPClientsLibSQL = class extends _mastra_core_storage.MCPClientsStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_MCP_CLIENTS,
			schema: _mastra_core_storage.MCP_CLIENTS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS,
			schema: _mastra_core_storage.MCP_CLIENT_VERSIONS_SCHEMA
		});
		await this.#client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS idx_mcp_client_versions_client_version ON "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" ("mcpClientId", "versionNumber")`);
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_MCP_CLIENTS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS });
	}
	async getById(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_CLIENTS)} FROM "${_mastra_core_storage.TABLE_MCP_CLIENTS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseMCPClientRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_MCP_CLIENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async create(input) {
		const { mcpClient } = input;
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_MCP_CLIENTS,
				record: {
					id: mcpClient.id,
					status: "draft",
					activeVersionId: null,
					authorId: mcpClient.authorId ?? null,
					metadata: mcpClient.metadata ?? null,
					createdAt: now.toISOString(),
					updatedAt: now.toISOString()
				}
			});
			const { id: _id, authorId: _authorId, metadata: _metadata, ...snapshotConfig } = mcpClient;
			const versionId = crypto.randomUUID();
			try {
				await this.createVersion({
					id: versionId,
					mcpClientId: mcpClient.id,
					versionNumber: 1,
					...snapshotConfig,
					changedFields: Object.keys(snapshotConfig),
					changeMessage: "Initial version"
				});
			} catch (versionError) {
				await this.#db.delete({
					tableName: _mastra_core_storage.TABLE_MCP_CLIENTS,
					keys: { id: mcpClient.id }
				});
				throw versionError;
			}
			return {
				id: mcpClient.id,
				status: "draft",
				activeVersionId: void 0,
				authorId: mcpClient.authorId,
				metadata: mcpClient.metadata,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_MCP_CLIENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async update(input) {
		const { id, ...updates } = input;
		try {
			const existing = await this.getById(id);
			if (!existing) throw new Error(`MCP client with id ${id} not found`);
			const { authorId, activeVersionId, metadata, status } = updates;
			const updateData = { updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
			if (authorId !== void 0) updateData.authorId = authorId;
			if (activeVersionId !== void 0) updateData.activeVersionId = activeVersionId;
			if (status !== void 0) updateData.status = status;
			if (metadata !== void 0) updateData.metadata = {
				...existing.metadata,
				...metadata
			};
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_MCP_CLIENTS,
				keys: { id },
				data: updateData
			});
			const updated = await this.getById(id);
			if (!updated) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_MCP_CLIENT", "NOT_FOUND_AFTER_UPDATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `MCP client ${id} not found after update`,
				details: { id }
			});
			return updated;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_MCP_CLIENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async delete(id) {
		try {
			await this.deleteVersionsByParentId(id);
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_MCP_CLIENTS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_MCP_CLIENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async list(args) {
		try {
			const { page = 0, perPage: perPageInput, orderBy, authorId, metadata, status = "published" } = args || {};
			const { field, direction } = this.parseOrderBy(orderBy);
			const conditions = [];
			const queryParams = [];
			conditions.push("status = ?");
			queryParams.push(status);
			if (authorId !== void 0) {
				conditions.push("authorId = ?");
				queryParams.push(authorId);
			}
			if (metadata && Object.keys(metadata).length > 0) for (const [key, value] of Object.entries(metadata)) {
				if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MCP_CLIENTS", "INVALID_METADATA_KEY"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `Invalid metadata key: ${key}. Keys must be alphanumeric with underscores.`,
					details: { key }
				});
				conditions.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(typeof value === "string" ? value : JSON.stringify(value));
			}
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_MCP_CLIENTS}" ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				mcpClients: [],
				total: 0,
				page,
				perPage: perPageInput ?? 100,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				mcpClients: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_CLIENTS)} FROM "${_mastra_core_storage.TABLE_MCP_CLIENTS}" ${whereClause} ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseMCPClientRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MCP_CLIENTS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createVersion(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS,
				record: {
					id: input.id,
					mcpClientId: input.mcpClientId,
					versionNumber: input.versionNumber,
					name: input.name,
					description: input.description ?? null,
					servers: input.servers ?? null,
					changedFields: input.changedFields ?? null,
					changeMessage: input.changeMessage ?? null,
					createdAt: now.toISOString()
				}
			});
			return {
				...input,
				createdAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_MCP_CLIENT_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersion(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_MCP_CLIENT_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersionByNumber(mcpClientId, versionNumber) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE mcpClientId = ? AND versionNumber = ?`,
				args: [mcpClientId, versionNumber]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_MCP_CLIENT_VERSION_BY_NUMBER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getLatestVersion(mcpClientId) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE mcpClientId = ? ORDER BY versionNumber DESC LIMIT 1`,
				args: [mcpClientId]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_LATEST_MCP_CLIENT_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listVersions(input) {
		try {
			const { mcpClientId, page = 0, perPage: perPageInput, orderBy } = input;
			const { field, direction } = this.parseVersionOrderBy(orderBy);
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE mcpClientId = ?`,
				args: [mcpClientId]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				versions: [],
				total: 0,
				page,
				perPage: perPageInput ?? 20,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 20);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				versions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE mcpClientId = ? ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						mcpClientId,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseVersionRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MCP_CLIENT_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersion(id) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_MCP_CLIENT_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersionsByParentId(entityId) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE "mcpClientId" = ?`,
				args: [entityId]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_MCP_CLIENT_VERSIONS_BY_CLIENT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async countVersions(mcpClientId) {
		try {
			const result = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_MCP_CLIENT_VERSIONS}" WHERE mcpClientId = ?`,
				args: [mcpClientId]
			});
			return Number(result.rows?.[0]?.count ?? 0);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "COUNT_MCP_CLIENT_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	#parseMCPClientRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			status: row.status ?? "draft",
			activeVersionId: row.activeVersionId ?? void 0,
			authorId: row.authorId ?? void 0,
			metadata: safeParseJSON(row.metadata),
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	#parseVersionRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			mcpClientId: row.mcpClientId,
			versionNumber: Number(row.versionNumber),
			name: row.name,
			description: row.description ?? void 0,
			servers: safeParseJSON(row.servers),
			changedFields: safeParseJSON(row.changedFields),
			changeMessage: row.changeMessage ?? void 0,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/domains/mcp-servers/index.ts
var MCPServersLibSQL = class extends _mastra_core_storage.MCPServersStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_MCP_SERVERS,
			schema: _mastra_core_storage.MCP_SERVERS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_MCP_SERVER_VERSIONS,
			schema: _mastra_core_storage.MCP_SERVER_VERSIONS_SCHEMA
		});
		await this.#client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS idx_mcp_server_versions_server_version ON "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" ("mcpServerId", "versionNumber")`);
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_MCP_SERVERS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_MCP_SERVER_VERSIONS });
	}
	async getById(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_SERVERS)} FROM "${_mastra_core_storage.TABLE_MCP_SERVERS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseMCPServerRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_MCP_SERVER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async create(input) {
		const { mcpServer } = input;
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_MCP_SERVERS,
				record: {
					id: mcpServer.id,
					status: "draft",
					activeVersionId: null,
					authorId: mcpServer.authorId ?? null,
					metadata: mcpServer.metadata ?? null,
					createdAt: now.toISOString(),
					updatedAt: now.toISOString()
				}
			});
			const { id: _id, authorId: _authorId, metadata: _metadata, ...snapshotConfig } = mcpServer;
			const versionId = crypto.randomUUID();
			try {
				await this.createVersion({
					id: versionId,
					mcpServerId: mcpServer.id,
					versionNumber: 1,
					...snapshotConfig,
					changedFields: Object.keys(snapshotConfig),
					changeMessage: "Initial version"
				});
			} catch (versionError) {
				await this.#db.delete({
					tableName: _mastra_core_storage.TABLE_MCP_SERVERS,
					keys: { id: mcpServer.id }
				});
				throw versionError;
			}
			return {
				id: mcpServer.id,
				status: "draft",
				activeVersionId: void 0,
				authorId: mcpServer.authorId,
				metadata: mcpServer.metadata,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_MCP_SERVER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async update(input) {
		const { id, ...updates } = input;
		try {
			const existing = await this.getById(id);
			if (!existing) throw new Error(`MCP server with id ${id} not found`);
			const { authorId, activeVersionId, metadata, status } = updates;
			const updateData = { updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
			if (authorId !== void 0) updateData.authorId = authorId;
			if (activeVersionId !== void 0) updateData.activeVersionId = activeVersionId;
			if (status !== void 0) updateData.status = status;
			if (metadata !== void 0) updateData.metadata = {
				...existing.metadata,
				...metadata
			};
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_MCP_SERVERS,
				keys: { id },
				data: updateData
			});
			const updated = await this.getById(id);
			if (!updated) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_MCP_SERVER", "NOT_FOUND_AFTER_UPDATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `MCP server ${id} not found after update`,
				details: { id }
			});
			return updated;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_MCP_SERVER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async delete(id) {
		try {
			await this.deleteVersionsByParentId(id);
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_MCP_SERVERS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_MCP_SERVER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async list(args) {
		try {
			const { page = 0, perPage: perPageInput, orderBy, authorId, metadata, status = "published" } = args || {};
			const { field, direction } = this.parseOrderBy(orderBy);
			const conditions = [];
			const queryParams = [];
			conditions.push("status = ?");
			queryParams.push(status);
			if (authorId !== void 0) {
				conditions.push("authorId = ?");
				queryParams.push(authorId);
			}
			if (metadata && Object.keys(metadata).length > 0) for (const [key, value] of Object.entries(metadata)) {
				if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MCP_SERVERS", "INVALID_METADATA_KEY"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `Invalid metadata key: ${key}. Keys must be alphanumeric with underscores.`,
					details: { key }
				});
				conditions.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(typeof value === "string" ? value : JSON.stringify(value));
			}
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_MCP_SERVERS}" ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				mcpServers: [],
				total: 0,
				page,
				perPage: perPageInput ?? 100,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				mcpServers: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_SERVERS)} FROM "${_mastra_core_storage.TABLE_MCP_SERVERS}" ${whereClause} ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseMCPServerRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MCP_SERVERS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createVersion(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_MCP_SERVER_VERSIONS,
				record: {
					id: input.id,
					mcpServerId: input.mcpServerId,
					versionNumber: input.versionNumber,
					name: input.name,
					version: input.version,
					description: input.description ?? null,
					instructions: input.instructions ?? null,
					repository: input.repository ?? null,
					releaseDate: input.releaseDate ?? null,
					isLatest: input.isLatest ?? null,
					packageCanonical: input.packageCanonical ?? null,
					tools: input.tools ?? null,
					agents: input.agents ?? null,
					workflows: input.workflows ?? null,
					changedFields: input.changedFields ?? null,
					changeMessage: input.changeMessage ?? null,
					createdAt: now.toISOString()
				}
			});
			return {
				...input,
				createdAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_MCP_SERVER_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersion(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_MCP_SERVER_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersionByNumber(mcpServerId, versionNumber) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE mcpServerId = ? AND versionNumber = ?`,
				args: [mcpServerId, versionNumber]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_MCP_SERVER_VERSION_BY_NUMBER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getLatestVersion(mcpServerId) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE mcpServerId = ? ORDER BY versionNumber DESC LIMIT 1`,
				args: [mcpServerId]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_LATEST_MCP_SERVER_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listVersions(input) {
		try {
			const { mcpServerId, page = 0, perPage: perPageInput, orderBy } = input;
			const { field, direction } = this.parseVersionOrderBy(orderBy);
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE mcpServerId = ?`,
				args: [mcpServerId]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				versions: [],
				total: 0,
				page,
				perPage: perPageInput ?? 20,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 20);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				versions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS)} FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE mcpServerId = ? ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						mcpServerId,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseVersionRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MCP_SERVER_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersion(id) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_MCP_SERVER_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersionsByParentId(entityId) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE "mcpServerId" = ?`,
				args: [entityId]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_MCP_SERVER_VERSIONS_BY_SERVER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async countVersions(mcpServerId) {
		try {
			const result = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_MCP_SERVER_VERSIONS}" WHERE mcpServerId = ?`,
				args: [mcpServerId]
			});
			return Number(result.rows?.[0]?.count ?? 0);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "COUNT_MCP_SERVER_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	#safeParseJSON(val) {
		if (val === null || val === void 0) return void 0;
		if (typeof val === "string") try {
			return JSON.parse(val);
		} catch {
			return val;
		}
		return val;
	}
	#parseMCPServerRow(row) {
		return {
			id: row.id,
			status: row.status ?? "draft",
			activeVersionId: row.activeVersionId ?? void 0,
			authorId: row.authorId ?? void 0,
			metadata: this.#safeParseJSON(row.metadata),
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	#parseVersionRow(row) {
		return {
			id: row.id,
			mcpServerId: row.mcpServerId,
			versionNumber: Number(row.versionNumber),
			name: row.name,
			version: row.version,
			description: row.description ?? void 0,
			instructions: row.instructions ?? void 0,
			repository: this.#safeParseJSON(row.repository),
			releaseDate: row.releaseDate ?? void 0,
			isLatest: row.isLatest === null || row.isLatest === void 0 ? void 0 : Boolean(row.isLatest),
			packageCanonical: row.packageCanonical ?? void 0,
			tools: this.#safeParseJSON(row.tools),
			agents: this.#safeParseJSON(row.agents),
			workflows: this.#safeParseJSON(row.workflows),
			changedFields: this.#safeParseJSON(row.changedFields),
			changeMessage: row.changeMessage ?? void 0,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/domains/memory/index.ts
/**
* Local constant for the observational memory table name.
* Defined locally to avoid a static import that crashes on older @mastra/core
* versions that don't export TABLE_OBSERVATIONAL_MEMORY.
*/
const OM_TABLE = "mastra_observational_memory";
function addSqliteMetadataFilter(conditions, params, metadataFilter) {
	if (!metadataFilter) return;
	for (const [key, value] of Object.entries(metadataFilter)) {
		const path = `$.metadata.${key}`;
		conditions.push(`CASE WHEN json_valid(content) THEN json_type(content, ?) IS NOT NULL ELSE 0 END`);
		params.push(path);
		addSqliteMetadataValuePredicate(conditions, params, path, value);
	}
}
function addSqliteMetadataValuePredicate(conditions, params, path, value) {
	if (value === null) {
		conditions.push(`CASE WHEN json_valid(content) THEN json_type(content, ?) = 'null' ELSE 0 END`);
		params.push(path);
		return;
	}
	if (typeof value === "string") {
		conditions.push(`CASE WHEN json_valid(content) THEN json_type(content, ?) = 'text' AND json_extract(content, ?) = ? ELSE 0 END`);
		params.push(path, path, value);
		return;
	}
	if (typeof value === "number") {
		conditions.push(`CASE WHEN json_valid(content) THEN json_type(content, ?) IN ('integer', 'real') AND json_extract(content, ?) = ? ELSE 0 END`);
		params.push(path, path, value);
		return;
	}
	conditions.push(`CASE WHEN json_valid(content) THEN json_type(content, ?) = ? AND json_extract(content, ?) = ? ELSE 0 END`);
	params.push(path, value ? "true" : "false", path, value ? 1 : 0);
}
var MemoryLibSQL = class MemoryLibSQL extends _mastra_core_storage.MemoryStorage {
	supportsPartialThreadUpdate = true;
	supportsObservationalMemory = true;
	/**
	* Retention-eligible tables. `threads`, `messages`, and `resources` all anchor
	* on `createdAt` and are indexed for fast batched deletes. Cascade order is
	* enforced in `prune()` (children before threads), not here.
	*/
	static retentionTables = {
		messages: {
			table: _mastra_core_storage.TABLE_MESSAGES,
			column: "createdAt",
			indexed: true
		},
		resources: {
			table: _mastra_core_storage.TABLE_RESOURCES,
			column: "createdAt",
			indexed: true
		},
		threads: {
			table: _mastra_core_storage.TABLE_THREADS,
			column: "createdAt",
			indexed: true
		}
	};
	#client;
	#db;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_THREADS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_THREADS]
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_MESSAGES,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_MESSAGES]
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_RESOURCES,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_RESOURCES]
		});
		const omSchema = _mastra_core_storage.OBSERVATIONAL_MEMORY_TABLE_SCHEMA?.[OM_TABLE];
		if (omSchema) {
			await this.#db.createTable({
				tableName: OM_TABLE,
				schema: omSchema
			});
			await this.#db.alterTable({
				tableName: OM_TABLE,
				schema: omSchema,
				ifNotExists: [
					"observedMessageIds",
					"observedTimezone",
					"bufferedObservations",
					"bufferedObservationTokens",
					"bufferedMessageIds",
					"bufferedReflection",
					"bufferedReflectionTokens",
					"bufferedReflectionInputTokens",
					"reflectedObservationLineCount",
					"bufferedObservationChunks",
					"isBufferingObservation",
					"isBufferingReflection",
					"lastBufferedAtTokens",
					"lastBufferedAtTime",
					"metadata"
				]
			});
		}
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_MESSAGES,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_MESSAGES],
			ifNotExists: ["resourceId"]
		});
		await this.#client.batch([{
			sql: `CREATE INDEX IF NOT EXISTS idx_messages_thread_created_at ON ${_mastra_core_storage.TABLE_MESSAGES} (thread_id, "createdAt")`,
			args: []
		}, {
			sql: `CREATE INDEX IF NOT EXISTS idx_messages_thread_resource_created_at ON ${_mastra_core_storage.TABLE_MESSAGES} (thread_id, "resourceId", "createdAt")`,
			args: []
		}], "write");
		if (omSchema) await this.#client.execute({
			sql: `CREATE INDEX IF NOT EXISTS idx_om_lookup_key ON "${OM_TABLE}" ("lookupKey")`,
			args: []
		});
	}
	async dangerouslyClearAll() {
		await this.init();
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_MESSAGES });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_THREADS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_RESOURCES });
		await this.#db.deleteData({ tableName: OM_TABLE });
	}
	/**
	* Delete rows older than each policy's `maxAge`, batched and cancellable.
	*
	* Deletes children before parents (messages/resources before threads) so a
	* `threads` policy doesn't strand rows behind a deleted parent — there are no
	* FKs in the LibSQL schema, so cascade ordering is explicit here.
	*/
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: MemoryLibSQL.retentionTables,
			order: [
				"messages",
				"resources",
				"threads"
			]
		});
		return runPrune({
			db: this.#db,
			domain: "memory",
			targets,
			options,
			logger: this.logger
		});
	}
	parseRow(row) {
		let content = row.content;
		try {
			content = JSON.parse(row.content);
		} catch {}
		const result = {
			id: row.id,
			content,
			role: row.role,
			createdAt: new Date(row.createdAt),
			threadId: row.thread_id,
			resourceId: row.resourceId
		};
		if (row.type && row.type !== `v2`) result.type = row.type;
		return result;
	}
	_sortMessages(messages, field, direction) {
		return messages.sort((a, b) => {
			const isDateField = field === "createdAt" || field === "updatedAt";
			const aValue = isDateField ? new Date(a[field]).getTime() : a[field];
			const bValue = isDateField ? new Date(b[field]).getTime() : b[field];
			if (typeof aValue === "number" && typeof bValue === "number") return direction === "ASC" ? aValue - bValue : bValue - aValue;
			return direction === "ASC" ? String(aValue).localeCompare(String(bValue)) : String(bValue).localeCompare(String(aValue));
		});
	}
	/**
	* Fetches included messages by ID, discovering their thread automatically.
	* This handles cross-thread includes where the include item doesn't specify a threadId.
	* When a resourceId is given, both the target lookup and the surrounding window stay
	* inside that resource, so an include never leaks another resource's messages.
	*/
	async _getIncludedMessages({ include, resourceId }) {
		if (!include || include.length === 0) return null;
		const targetIds = include.map((inc) => inc.id).filter(Boolean);
		if (targetIds.length === 0) return null;
		const resourceCondition = resourceId ? ` AND "resourceId" = ?` : "";
		const idPlaceholders = targetIds.map(() => "?").join(", ");
		const targetResult = await this.#client.execute({
			sql: `SELECT id, thread_id, "createdAt" FROM "${_mastra_core_storage.TABLE_MESSAGES}" WHERE id IN (${idPlaceholders})${resourceCondition}`,
			args: resourceId ? [...targetIds, resourceId] : targetIds
		});
		if (!targetResult.rows || targetResult.rows.length === 0) return null;
		const targetMap = new Map(targetResult.rows.map((r) => [r.id, {
			threadId: r.thread_id,
			createdAt: r.createdAt
		}]));
		const unionQueries = [];
		const params = [];
		for (const inc of include) {
			const { id, withPreviousMessages = 0, withNextMessages = 0 } = inc;
			const target = targetMap.get(id);
			if (!target) continue;
			unionQueries.push(`SELECT * FROM (
        SELECT id, content, role, type, "createdAt", thread_id, "resourceId"
        FROM "${_mastra_core_storage.TABLE_MESSAGES}"
        WHERE thread_id = ?
          AND "createdAt" <= ?${resourceCondition}
        ORDER BY "createdAt" DESC, id DESC
        LIMIT ?
      )`);
			params.push(target.threadId, target.createdAt);
			if (resourceId) params.push(resourceId);
			params.push(withPreviousMessages + 1);
			if (withNextMessages > 0) {
				unionQueries.push(`SELECT * FROM (
          SELECT id, content, role, type, "createdAt", thread_id, "resourceId"
          FROM "${_mastra_core_storage.TABLE_MESSAGES}"
          WHERE thread_id = ?
            AND "createdAt" > ?${resourceCondition}
          ORDER BY "createdAt" ASC, id ASC
          LIMIT ?
        )`);
				params.push(target.threadId, target.createdAt);
				if (resourceId) params.push(resourceId);
				params.push(withNextMessages);
			}
		}
		if (unionQueries.length === 0) return null;
		let finalQuery;
		if (unionQueries.length === 1) finalQuery = unionQueries[0];
		else finalQuery = `${unionQueries.join(" UNION ALL ")} ORDER BY "createdAt" ASC, id ASC`;
		const includedRows = (await this.#client.execute({
			sql: finalQuery,
			args: params
		})).rows?.map((row) => this.parseRow(row));
		const seen = /* @__PURE__ */ new Set();
		return includedRows.filter((row) => {
			if (seen.has(row.id)) return false;
			seen.add(row.id);
			return true;
		});
	}
	async listMessagesById({ messageIds }) {
		if (messageIds.length === 0) return { messages: [] };
		try {
			const sql = `
        SELECT 
          id, 
          content, 
          role, 
          type,
          "createdAt", 
          thread_id,
          "resourceId"
        FROM "${_mastra_core_storage.TABLE_MESSAGES}"
        WHERE id IN (${messageIds.map(() => "?").join(", ")})
        ORDER BY "createdAt" DESC
      `;
			const result = await this.#client.execute({
				sql,
				args: messageIds
			});
			if (!result.rows) return { messages: [] };
			return { messages: new _mastra_core_agent.MessageList().add(result.rows.map(this.parseRow), "memory").get.all.db() };
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MESSAGES_BY_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { messageIds: JSON.stringify(messageIds) }
			}, error);
		}
	}
	async listMessages(args) {
		const { threadId, resourceId, include, filter, perPage: perPageInput, page = 0, orderBy } = args;
		const threadIds = Array.isArray(threadId) ? threadId : [threadId];
		if (threadIds.length === 0 || threadIds.some((id) => !id.trim())) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MESSAGES", "INVALID_THREAD_ID"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
			details: { threadId: Array.isArray(threadId) ? threadId.join(",") : threadId }
		}, /* @__PURE__ */ new Error("threadId must be a non-empty string or array of non-empty strings"));
		if (page < 0) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MESSAGES", "INVALID_PAGE"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { page }
		}, /* @__PURE__ */ new Error("page must be >= 0"));
		const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 40);
		const { offset, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
		const metadataFilter = (0, _mastra_core_storage.validateStorageMetadataFilter)(filter?.metadata);
		try {
			const { field, direction } = this.parseOrderBy(orderBy, "ASC");
			const orderByStatement = `ORDER BY "${field}" ${direction}`;
			const conditions = [`thread_id IN (${threadIds.map(() => "?").join(", ")})`];
			const queryParams = [...threadIds];
			if (resourceId) {
				conditions.push(`"resourceId" = ?`);
				queryParams.push(resourceId);
			}
			if (filter?.dateRange?.start) {
				const startOp = filter.dateRange.startExclusive ? ">" : ">=";
				conditions.push(`"createdAt" ${startOp} ?`);
				queryParams.push(filter.dateRange.start instanceof Date ? filter.dateRange.start.toISOString() : filter.dateRange.start);
			}
			if (filter?.dateRange?.end) {
				const endOp = filter.dateRange.endExclusive ? "<" : "<=";
				conditions.push(`"createdAt" ${endOp} ?`);
				queryParams.push(filter.dateRange.end instanceof Date ? filter.dateRange.end.toISOString() : filter.dateRange.end);
			}
			addSqliteMetadataFilter(conditions, queryParams, metadataFilter);
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			if (perPage === 0 && (!include || include.length === 0)) return {
				messages: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			if (perPage === 0 && include && include.length > 0) {
				const includeMessages = await this._getIncludedMessages({
					include,
					resourceId
				});
				if (!includeMessages || includeMessages.length === 0) return {
					messages: [],
					total: 0,
					page,
					perPage: perPageForResponse,
					hasMore: false
				};
				const list = new _mastra_core_agent.MessageList().add(includeMessages, "memory");
				return {
					messages: this._sortMessages(list.get.all.db(), field, direction),
					total: 0,
					page,
					perPage: perPageForResponse,
					hasMore: false
				};
			}
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_MESSAGES} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			const limitValue = perPageInput === false ? total : perPage;
			const messages = ((await this.#client.execute({
				sql: `SELECT id, content, role, type, "createdAt", "resourceId", "thread_id" FROM ${_mastra_core_storage.TABLE_MESSAGES} ${whereClause} ${orderByStatement} LIMIT ? OFFSET ?`,
				args: [
					...queryParams,
					limitValue,
					offset
				]
			})).rows || []).map((row) => this.parseRow(row));
			const paginatedCount = messages.length;
			if (total === 0 && messages.length === 0 && (!include || include.length === 0)) return {
				messages: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			const messageIds = new Set(messages.map((m) => m.id));
			if (include && include.length > 0) {
				const includeMessages = await this._getIncludedMessages({
					include,
					resourceId
				});
				if (includeMessages) {
					for (const includeMsg of includeMessages) if (!messageIds.has(includeMsg.id)) {
						messages.push(includeMsg);
						messageIds.add(includeMsg.id);
					}
				}
			}
			const list = new _mastra_core_agent.MessageList().add(messages, "memory");
			const finalMessages = this._sortMessages(list.get.all.db(), field, direction);
			const threadIdSet = new Set(threadIds);
			const allThreadMessagesReturned = new Set(finalMessages.filter((m) => m.threadId && threadIdSet.has(m.threadId)).map((m) => m.id)).size >= total;
			return {
				messages: finalMessages,
				total,
				page,
				perPage: perPageForResponse,
				hasMore: metadataFilter ? perPageInput !== false && offset + paginatedCount < total : perPageInput !== false && !allThreadMessagesReturned && offset + perPage < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError && error.category === _mastra_core_error.ErrorCategory.USER) throw error;
			const mastraError = new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MESSAGES", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId: Array.isArray(threadId) ? threadId.join(",") : threadId,
					resourceId: resourceId ?? ""
				}
			}, error);
			this.logger?.error?.(mastraError.toString());
			this.logger?.trackException?.(mastraError);
			throw mastraError;
		}
	}
	async listMessagesByResourceId(args) {
		const { resourceId, include, filter, perPage: perPageInput, page = 0, orderBy } = args;
		if (!resourceId || typeof resourceId !== "string" || resourceId.trim().length === 0) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MESSAGES_BY_RESOURCE_ID", "INVALID_QUERY"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { resourceId: resourceId ?? "" }
		}, /* @__PURE__ */ new Error("resourceId is required"));
		if (page < 0) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MESSAGES_BY_RESOURCE_ID", "INVALID_PAGE"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			details: { page }
		}, /* @__PURE__ */ new Error("page must be >= 0"));
		const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 40);
		const { offset, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
		const metadataFilter = (0, _mastra_core_storage.validateStorageMetadataFilter)(filter?.metadata);
		try {
			const { field, direction } = this.parseOrderBy(orderBy, "ASC");
			const orderByStatement = `ORDER BY "${field}" ${direction}`;
			const conditions = [];
			const queryParams = [];
			conditions.push(`"resourceId" = ?`);
			queryParams.push(resourceId);
			if (filter?.dateRange?.start) {
				const startOp = filter.dateRange.startExclusive ? ">" : ">=";
				conditions.push(`"createdAt" ${startOp} ?`);
				queryParams.push(filter.dateRange.start instanceof Date ? filter.dateRange.start.toISOString() : filter.dateRange.start);
			}
			if (filter?.dateRange?.end) {
				const endOp = filter.dateRange.endExclusive ? "<" : "<=";
				conditions.push(`"createdAt" ${endOp} ?`);
				queryParams.push(filter.dateRange.end instanceof Date ? filter.dateRange.end.toISOString() : filter.dateRange.end);
			}
			addSqliteMetadataFilter(conditions, queryParams, metadataFilter);
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			if (perPage === 0 && (!include || include.length === 0)) return {
				messages: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			if (perPage === 0 && include && include.length > 0) {
				const includeMessages = await this._getIncludedMessages({
					include,
					resourceId
				});
				if (!includeMessages || includeMessages.length === 0) return {
					messages: [],
					total: 0,
					page,
					perPage: perPageForResponse,
					hasMore: false
				};
				const list = new _mastra_core_agent.MessageList().add(includeMessages, "memory");
				return {
					messages: this._sortMessages(list.get.all.db(), field, direction),
					total: 0,
					page,
					perPage: perPageForResponse,
					hasMore: false
				};
			}
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_MESSAGES} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			const limitValue = perPageInput === false ? total : perPage;
			const messages = ((await this.#client.execute({
				sql: `SELECT id, content, role, type, "createdAt", "resourceId", "thread_id" FROM ${_mastra_core_storage.TABLE_MESSAGES} ${whereClause} ${orderByStatement} LIMIT ? OFFSET ?`,
				args: [
					...queryParams,
					limitValue,
					offset
				]
			})).rows || []).map((row) => this.parseRow(row));
			if (total === 0 && messages.length === 0 && (!include || include.length === 0)) return {
				messages: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			const messageIds = new Set(messages.map((m) => m.id));
			if (include && include.length > 0) {
				const includeMessages = await this._getIncludedMessages({
					include,
					resourceId
				});
				if (includeMessages) {
					for (const includeMsg of includeMessages) if (!messageIds.has(includeMsg.id)) {
						messages.push(includeMsg);
						messageIds.add(includeMsg.id);
					}
				}
			}
			const list = new _mastra_core_agent.MessageList().add(messages, "memory");
			return {
				messages: this._sortMessages(list.get.all.db(), field, direction),
				total,
				page,
				perPage: perPageForResponse,
				hasMore: perPageInput !== false && offset + perPage < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError && error.category === _mastra_core_error.ErrorCategory.USER) throw error;
			const mastraError = new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_MESSAGES_BY_RESOURCE_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { resourceId }
			}, error);
			this.logger?.error?.(mastraError.toString());
			this.logger?.trackException?.(mastraError);
			throw mastraError;
		}
	}
	async saveMessages({ messages }) {
		if (messages.length === 0) return { messages };
		try {
			const threadId = messages[0]?.threadId;
			if (!threadId) throw new Error("Thread ID is required");
			const batchStatements = messages.map((message) => {
				const time = message.createdAt || /* @__PURE__ */ new Date();
				if (!message.threadId) throw new Error(`Expected to find a threadId for message, but couldn't find one. An unexpected error has occurred.`);
				if (!message.resourceId) throw new Error(`Expected to find a resourceId for message, but couldn't find one. An unexpected error has occurred.`);
				return {
					sql: `INSERT INTO "${_mastra_core_storage.TABLE_MESSAGES}" (id, thread_id, content, role, type, "createdAt", "resourceId") 
                  VALUES (?, ?, ?, ?, ?, ?, ?)
                  ON CONFLICT(id) DO UPDATE SET
                    thread_id=excluded.thread_id,
                    content=excluded.content,
                    role=excluded.role,
                    type=excluded.type,
                    "resourceId"=excluded."resourceId"
                `,
					args: [
						message.id,
						message.threadId,
						typeof message.content === "object" ? JSON.stringify(message.content) : message.content,
						message.role,
						message.type || "v2",
						time instanceof Date ? time.toISOString() : time,
						message.resourceId
					]
				};
			});
			const now = (/* @__PURE__ */ new Date()).toISOString();
			batchStatements.push({
				sql: `UPDATE "${_mastra_core_storage.TABLE_THREADS}" SET "updatedAt" = ? WHERE id = ?`,
				args: [now, threadId]
			});
			const BATCH_SIZE = 50;
			const messageStatements = batchStatements.slice(0, -1);
			const threadUpdateStatement = batchStatements[batchStatements.length - 1];
			for (let i = 0; i < messageStatements.length; i += BATCH_SIZE) {
				const batch = messageStatements.slice(i, i + BATCH_SIZE);
				if (batch.length > 0) await this.#client.batch(batch, "write");
			}
			if (threadUpdateStatement) await this.#client.execute(threadUpdateStatement);
			return { messages: new _mastra_core_agent.MessageList().add(messages, "memory").get.all.db() };
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SAVE_MESSAGES", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async updateMessages({ messages }) {
		if (messages.length === 0) return [];
		const messageIds = messages.map((m) => m.id);
		const selectSql = `SELECT * FROM ${_mastra_core_storage.TABLE_MESSAGES} WHERE id IN (${messageIds.map(() => "?").join(",")})`;
		const existingMessages = (await this.#client.execute({
			sql: selectSql,
			args: messageIds
		})).rows.map((row) => this.parseRow(row));
		if (existingMessages.length === 0) return [];
		const batchStatements = [];
		const threadIdsToUpdate = /* @__PURE__ */ new Set();
		const columnMapping = { threadId: "thread_id" };
		for (const existingMessage of existingMessages) {
			const updatePayload = messages.find((m) => m.id === existingMessage.id);
			if (!updatePayload) continue;
			const { id, ...fieldsToUpdate } = updatePayload;
			if (Object.keys(fieldsToUpdate).length === 0) continue;
			threadIdsToUpdate.add(existingMessage.threadId);
			if (updatePayload.threadId && updatePayload.threadId !== existingMessage.threadId) threadIdsToUpdate.add(updatePayload.threadId);
			const setClauses = [];
			const args = [];
			const updatableFields = { ...fieldsToUpdate };
			if (updatableFields.content) {
				const newContent = {
					...existingMessage.content,
					...updatableFields.content,
					...existingMessage.content?.metadata && updatableFields.content.metadata ? { metadata: {
						...existingMessage.content.metadata,
						...updatableFields.content.metadata
					} } : {}
				};
				setClauses.push(`${(0, _mastra_core_utils.parseSqlIdentifier)("content", "column name")} = ?`);
				args.push(JSON.stringify(newContent));
				delete updatableFields.content;
			}
			for (const key in updatableFields) if (Object.prototype.hasOwnProperty.call(updatableFields, key)) {
				const dbKey = columnMapping[key] || key;
				setClauses.push(`${(0, _mastra_core_utils.parseSqlIdentifier)(dbKey, "column name")} = ?`);
				let value = updatableFields[key];
				if (typeof value === "object" && value !== null) value = JSON.stringify(value);
				args.push(value);
			}
			if (setClauses.length === 0) continue;
			args.push(id);
			const sql = `UPDATE ${_mastra_core_storage.TABLE_MESSAGES} SET ${setClauses.join(", ")} WHERE id = ?`;
			batchStatements.push({
				sql,
				args
			});
		}
		if (batchStatements.length === 0) return existingMessages;
		const now = (/* @__PURE__ */ new Date()).toISOString();
		for (const threadId of threadIdsToUpdate) if (threadId) batchStatements.push({
			sql: `UPDATE ${_mastra_core_storage.TABLE_THREADS} SET updatedAt = ? WHERE id = ?`,
			args: [now, threadId]
		});
		await this.#client.batch(batchStatements, "write");
		return (await this.#client.execute({
			sql: selectSql,
			args: messageIds
		})).rows.map((row) => this.parseRow(row));
	}
	async deleteMessages(messageIds) {
		if (!messageIds || messageIds.length === 0) return;
		try {
			const BATCH_SIZE = 100;
			const threadIds = /* @__PURE__ */ new Set();
			const tx = await this.#client.transaction("write");
			try {
				for (let i = 0; i < messageIds.length; i += BATCH_SIZE) {
					const batch = messageIds.slice(i, i + BATCH_SIZE);
					const placeholders = batch.map(() => "?").join(",");
					(await tx.execute({
						sql: `SELECT DISTINCT thread_id FROM "${_mastra_core_storage.TABLE_MESSAGES}" WHERE id IN (${placeholders})`,
						args: batch
					})).rows?.forEach((row) => {
						if (row.thread_id) threadIds.add(row.thread_id);
					});
					await tx.execute({
						sql: `DELETE FROM "${_mastra_core_storage.TABLE_MESSAGES}" WHERE id IN (${placeholders})`,
						args: batch
					});
				}
				if (threadIds.size > 0) {
					const now = (/* @__PURE__ */ new Date()).toISOString();
					for (const threadId of threadIds) await tx.execute({
						sql: `UPDATE "${_mastra_core_storage.TABLE_THREADS}" SET "updatedAt" = ? WHERE id = ?`,
						args: [now, threadId]
					});
				}
				await tx.commit();
			} catch (error) {
				await tx.rollback();
				throw error;
			}
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_MESSAGES", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { messageIds: messageIds.join(", ") }
			}, error);
		}
	}
	async getResourceById({ resourceId }) {
		const result = await this.#db.select({
			tableName: _mastra_core_storage.TABLE_RESOURCES,
			keys: { id: resourceId }
		});
		if (!result) return null;
		return {
			...result,
			workingMemory: result.workingMemory && typeof result.workingMemory === "object" ? JSON.stringify(result.workingMemory) : result.workingMemory,
			metadata: typeof result.metadata === "string" ? JSON.parse(result.metadata) : result.metadata,
			createdAt: new Date(result.createdAt),
			updatedAt: new Date(result.updatedAt)
		};
	}
	async saveResource({ resource }) {
		await this.#db.insert({
			tableName: _mastra_core_storage.TABLE_RESOURCES,
			record: { ...resource }
		});
		return resource;
	}
	async updateResource({ resourceId, workingMemory, metadata }) {
		const existingResource = await this.getResourceById({ resourceId });
		if (!existingResource) {
			const newResource = {
				id: resourceId,
				workingMemory,
				metadata: metadata || {},
				createdAt: /* @__PURE__ */ new Date(),
				updatedAt: /* @__PURE__ */ new Date()
			};
			return this.saveResource({ resource: newResource });
		}
		const updatedResource = {
			...existingResource,
			workingMemory: workingMemory !== void 0 ? workingMemory : existingResource.workingMemory,
			metadata: {
				...existingResource.metadata,
				...metadata
			},
			updatedAt: /* @__PURE__ */ new Date()
		};
		const updates = [];
		const values = [];
		if (workingMemory !== void 0) {
			updates.push("workingMemory = ?");
			values.push(workingMemory);
		}
		if (metadata) {
			updates.push("metadata = jsonb(?)");
			values.push(JSON.stringify(updatedResource.metadata));
		}
		updates.push("updatedAt = ?");
		values.push(updatedResource.updatedAt.toISOString());
		values.push(resourceId);
		await this.#client.execute({
			sql: `UPDATE ${_mastra_core_storage.TABLE_RESOURCES} SET ${updates.join(", ")} WHERE id = ?`,
			args: values
		});
		return updatedResource;
	}
	async getThreadById({ threadId, resourceId }) {
		try {
			const keys = { id: threadId };
			if (resourceId !== void 0) keys.resourceId = resourceId;
			const result = await this.#db.select({
				tableName: _mastra_core_storage.TABLE_THREADS,
				keys
			});
			if (!result) return null;
			return {
				...result,
				metadata: typeof result.metadata === "string" ? JSON.parse(result.metadata) : result.metadata,
				createdAt: new Date(result.createdAt),
				updatedAt: new Date(result.updatedAt)
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_THREAD_BY_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { threadId }
			}, error);
		}
	}
	/**
	* Atomically reassign a thread and all of its messages to a different resource.
	*
	* Runs inside a single write transaction. SQLite serializes write transactions, so overlapping
	* transfers of the same thread cannot interleave the thread update with the message update:
	* either both the thread and every message move to the new resource, or neither does. The
	* thread's `createdAt` is preserved. Callers are responsible for authorizing the reassignment.
	*/
	async updateThreadResourceId({ threadId, resourceId }) {
		try {
			const tx = await this.#client.transaction("write");
			try {
				const row = (await tx.execute({
					sql: `SELECT * FROM "${_mastra_core_storage.TABLE_THREADS}" WHERE id = ?`,
					args: [threadId]
				})).rows?.[0];
				if (!row) throw new Error(`Thread "${threadId}" not found`);
				const currentResourceId = row.resourceId;
				const normalized = {
					id: row.id,
					resourceId: currentResourceId,
					title: row.title,
					metadata: typeof row.metadata === "string" ? JSON.parse(row.metadata) : row.metadata,
					createdAt: new Date(row.createdAt),
					updatedAt: new Date(row.updatedAt)
				};
				if (currentResourceId === resourceId) {
					await tx.commit();
					return normalized;
				}
				const now = /* @__PURE__ */ new Date();
				await tx.execute({
					sql: `UPDATE "${_mastra_core_storage.TABLE_THREADS}" SET "resourceId" = ?, "updatedAt" = ? WHERE id = ?`,
					args: [
						resourceId,
						now.toISOString(),
						threadId
					]
				});
				await tx.execute({
					sql: `UPDATE "${_mastra_core_storage.TABLE_MESSAGES}" SET "resourceId" = ? WHERE thread_id = ?`,
					args: [resourceId, threadId]
				});
				await tx.commit();
				return {
					...normalized,
					resourceId,
					updatedAt: now
				};
			} catch (error) {
				await tx.rollback();
				throw error;
			}
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_THREAD_RESOURCE_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId,
					resourceId
				}
			}, error);
		}
	}
	async listThreads(args) {
		const { page = 0, perPage: perPageInput, orderBy, filter } = args;
		try {
			this.validatePaginationInput(page, perPageInput ?? 100);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_THREADS", "INVALID_PAGE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: {
					page,
					...perPageInput !== void 0 && { perPage: perPageInput }
				}
			}, error instanceof Error ? error : /* @__PURE__ */ new Error("Invalid pagination parameters"));
		}
		const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
		try {
			this.validateMetadataKeys(filter?.metadata);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_THREADS", "INVALID_METADATA_KEY"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { metadataKeys: filter?.metadata ? Object.keys(filter.metadata).join(", ") : "" }
			}, error instanceof Error ? error : /* @__PURE__ */ new Error("Invalid metadata key"));
		}
		const { offset, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
		const { field, direction } = this.parseOrderBy(orderBy);
		try {
			const whereClauses = [];
			const queryParams = [];
			if (filter?.resourceId) {
				whereClauses.push("resourceId = ?");
				queryParams.push(filter.resourceId);
			}
			if (filter?.metadata && Object.keys(filter.metadata).length > 0) for (const [key, value] of Object.entries(filter.metadata)) if (value === null) whereClauses.push(`json_extract(metadata, '$.${key}') IS NULL`);
			else if (typeof value === "boolean") {
				whereClauses.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(value ? 1 : 0);
			} else if (typeof value === "number") {
				whereClauses.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(value);
			} else if (typeof value === "string") {
				whereClauses.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(value);
			} else throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_THREADS", "INVALID_METADATA_VALUE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				text: `Metadata filter value for key "${key}" must be a scalar type (string, number, boolean, or null), got ${typeof value}`,
				details: {
					key,
					valueType: typeof value
				}
			});
			const baseQuery = `FROM ${_mastra_core_storage.TABLE_THREADS} ${whereClauses.length > 0 ? `WHERE ${whereClauses.join(" AND ")}` : ""}`;
			const mapRowToStorageThreadType = (row) => ({
				id: row.id,
				resourceId: row.resourceId,
				title: row.title,
				createdAt: new Date(row.createdAt),
				updatedAt: new Date(row.updatedAt),
				metadata: typeof row.metadata === "string" ? JSON.parse(row.metadata) : row.metadata
			});
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count ${baseQuery}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				threads: [],
				total: 0,
				page,
				perPage: perPageForResponse,
				hasMore: false
			};
			const limitValue = perPageInput === false ? total : perPage;
			return {
				threads: ((await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_THREADS)} ${baseQuery} ORDER BY "${field}" ${direction} LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						offset
					]
				})).rows || []).map(mapRowToStorageThreadType),
				total,
				page,
				perPage: perPageForResponse,
				hasMore: perPageInput === false ? false : offset + perPage < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError && error.category === _mastra_core_error.ErrorCategory.USER) throw error;
			const mastraError = new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_THREADS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					...filter?.resourceId && { resourceId: filter.resourceId },
					hasMetadataFilter: !!filter?.metadata
				}
			}, error);
			this.logger?.trackException?.(mastraError);
			this.logger?.error?.(mastraError.toString());
			throw mastraError;
		}
	}
	async saveThread({ thread }) {
		try {
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_THREADS,
				record: { ...thread }
			});
			return thread;
		} catch (error) {
			const mastraError = new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SAVE_THREAD", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { threadId: thread.id }
			}, error);
			this.logger?.trackException?.(mastraError);
			this.logger?.error?.(mastraError.toString());
			throw mastraError;
		}
	}
	async updateThread({ id, title, metadata }) {
		const thread = await this.getThreadById({ threadId: id });
		if (!thread) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_THREAD", "NOT_FOUND"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			text: `Thread ${id} not found`,
			details: {
				status: 404,
				threadId: id
			}
		});
		const now = /* @__PURE__ */ new Date();
		const updatedThread = {
			...thread,
			title: title ?? thread.title,
			metadata: {
				...thread.metadata,
				...metadata
			},
			updatedAt: now
		};
		try {
			await this.#client.execute({
				sql: `UPDATE ${_mastra_core_storage.TABLE_THREADS} SET title = COALESCE(?, title), metadata = jsonb(?), updatedAt = ? WHERE id = ?`,
				args: [
					title ?? null,
					JSON.stringify(updatedThread.metadata),
					now.toISOString(),
					id
				]
			});
			return updatedThread;
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_THREAD", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				text: `Failed to update thread ${id}`,
				details: { threadId: id }
			}, error);
		}
	}
	async deleteThread({ threadId }) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_MESSAGES} WHERE thread_id = ?`,
				args: [threadId]
			});
			await this.#client.execute({
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_THREADS} WHERE id = ?`,
				args: [threadId]
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_THREAD", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { threadId }
			}, error);
		}
	}
	async copyThread(args) {
		const { sourceThreadId, newThreadId: providedThreadId, resourceId, title, metadata, options } = args;
		const sourceThread = await this.getThreadById({ threadId: sourceThreadId });
		if (!sourceThread) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CLONE_THREAD", "SOURCE_NOT_FOUND"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			text: `Source thread with id ${sourceThreadId} not found`,
			details: { sourceThreadId }
		});
		const newThreadId = providedThreadId || crypto.randomUUID();
		if (await this.getThreadById({ threadId: newThreadId })) throw new _mastra_core_error.MastraError({
			id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CLONE_THREAD", "THREAD_EXISTS"),
			domain: _mastra_core_error.ErrorDomain.STORAGE,
			category: _mastra_core_error.ErrorCategory.USER,
			text: `Thread with id ${newThreadId} already exists`,
			details: { newThreadId }
		});
		try {
			let messageQuery = `SELECT id, "createdAt"
                          FROM "${_mastra_core_storage.TABLE_MESSAGES}" WHERE thread_id = ?`;
			const messageParams = [sourceThreadId];
			if (options?.messageFilter?.startDate) {
				messageQuery += ` AND "createdAt" >= ?`;
				messageParams.push(options.messageFilter.startDate instanceof Date ? options.messageFilter.startDate.toISOString() : options.messageFilter.startDate);
			}
			if (options?.messageFilter?.endDate) {
				messageQuery += ` AND "createdAt" <= ?`;
				messageParams.push(options.messageFilter.endDate instanceof Date ? options.messageFilter.endDate.toISOString() : options.messageFilter.endDate);
			}
			if (options?.messageFilter?.messageIds && options.messageFilter.messageIds.length > 0) {
				messageQuery += ` AND id IN (${options.messageFilter.messageIds.map(() => "?").join(", ")})`;
				messageParams.push(...options.messageFilter.messageIds);
			}
			messageQuery += ` ORDER BY "createdAt" ASC`;
			if (options?.messageLimit && options.messageLimit > 0) {
				const limitQuery = `SELECT * FROM (${messageQuery.replace("ORDER BY \"createdAt\" ASC", "ORDER BY \"createdAt\" DESC")} LIMIT ?) ORDER BY "createdAt" ASC`;
				messageParams.push(options.messageLimit);
				messageQuery = limitQuery;
			}
			const sourceMessages = (await this.#client.execute({
				sql: messageQuery,
				args: messageParams
			})).rows || [];
			const now = /* @__PURE__ */ new Date();
			const nowStr = now.toISOString();
			const lastMessageId = sourceMessages.length > 0 ? sourceMessages[sourceMessages.length - 1].id : void 0;
			const cloneMetadata = {
				sourceThreadId,
				clonedAt: now,
				...lastMessageId && { lastMessageId }
			};
			const newThread = {
				id: newThreadId,
				resourceId: resourceId || sourceThread.resourceId,
				title: title || (sourceThread.title ? `Clone of ${sourceThread.title}` : ""),
				metadata: {
					...metadata,
					clone: cloneMetadata
				},
				createdAt: now,
				updatedAt: now
			};
			const tx = await this.#client.transaction("write");
			try {
				await tx.execute({
					sql: `INSERT INTO "${_mastra_core_storage.TABLE_THREADS}" (id, "resourceId", title, metadata, "createdAt", "updatedAt")
                VALUES (?, ?, ?, jsonb(?), ?, ?)`,
					args: [
						newThread.id,
						newThread.resourceId,
						newThread.title ?? "",
						JSON.stringify(newThread.metadata),
						nowStr,
						nowStr
					]
				});
				const messageIdMap = {};
				const targetResourceId = resourceId || sourceThread.resourceId;
				for (const sourceMsg of sourceMessages) {
					const newMessageId = crypto.randomUUID();
					const sourceMsgId = sourceMsg.id;
					messageIdMap[sourceMsgId] = newMessageId;
					const insertResult = await tx.execute({
						sql: `INSERT INTO "${_mastra_core_storage.TABLE_MESSAGES}" (id, thread_id, content, role, type, "createdAt", "resourceId")
                  SELECT ?, ?, content, role, type, "createdAt", ?
                  FROM "${_mastra_core_storage.TABLE_MESSAGES}" WHERE id = ?`,
						args: [
							newMessageId,
							newThreadId,
							targetResourceId,
							sourceMsgId
						]
					});
					if (insertResult.rowsAffected !== 1) throw new Error(`Failed to copy message ${sourceMsgId}: expected 1 row copied but got ${insertResult.rowsAffected}`);
				}
				await tx.commit();
				return {
					thread: newThread,
					messageIdMap
				};
			} catch (error) {
				await tx.rollback();
				throw error;
			}
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CLONE_THREAD", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					sourceThreadId,
					newThreadId
				}
			}, error);
		}
	}
	getOMKey(threadId, resourceId) {
		return threadId ? `thread:${threadId}` : `resource:${resourceId}`;
	}
	parseOMRow(row) {
		return {
			id: row.id,
			scope: row.scope,
			threadId: row.threadId || null,
			resourceId: row.resourceId,
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt),
			lastObservedAt: row.lastObservedAt ? new Date(row.lastObservedAt) : void 0,
			originType: row.originType || "initial",
			generationCount: Number(row.generationCount || 0),
			activeObservations: row.activeObservations || "",
			bufferedObservationChunks: row.bufferedObservationChunks ? typeof row.bufferedObservationChunks === "string" ? JSON.parse(row.bufferedObservationChunks) : row.bufferedObservationChunks : void 0,
			bufferedObservations: row.activeObservationsPendingUpdate || void 0,
			bufferedObservationTokens: row.bufferedObservationTokens ? Number(row.bufferedObservationTokens) : void 0,
			bufferedMessageIds: void 0,
			bufferedReflection: row.bufferedReflection || void 0,
			bufferedReflectionTokens: row.bufferedReflectionTokens ? Number(row.bufferedReflectionTokens) : void 0,
			bufferedReflectionInputTokens: row.bufferedReflectionInputTokens ? Number(row.bufferedReflectionInputTokens) : void 0,
			reflectedObservationLineCount: row.reflectedObservationLineCount ? Number(row.reflectedObservationLineCount) : void 0,
			totalTokensObserved: Number(row.totalTokensObserved || 0),
			observationTokenCount: Number(row.observationTokenCount || 0),
			pendingMessageTokens: Number(row.pendingMessageTokens || 0),
			isReflecting: Boolean(row.isReflecting),
			isObserving: Boolean(row.isObserving),
			isBufferingObservation: row.isBufferingObservation === true || row.isBufferingObservation === "true" || row.isBufferingObservation === 1,
			isBufferingReflection: row.isBufferingReflection === true || row.isBufferingReflection === "true" || row.isBufferingReflection === 1,
			lastBufferedAtTokens: typeof row.lastBufferedAtTokens === "number" ? row.lastBufferedAtTokens : parseInt(String(row.lastBufferedAtTokens ?? "0"), 10) || 0,
			lastBufferedAtTime: row.lastBufferedAtTime ? new Date(String(row.lastBufferedAtTime)) : null,
			config: row.config ? JSON.parse(row.config) : {},
			metadata: row.metadata ? JSON.parse(row.metadata) : void 0,
			observedMessageIds: row.observedMessageIds ? JSON.parse(row.observedMessageIds) : void 0,
			observedTimezone: row.observedTimezone || void 0
		};
	}
	async getObservationalMemory(threadId, resourceId) {
		try {
			const lookupKey = this.getOMKey(threadId, resourceId);
			const result = await this.#client.execute({
				sql: `SELECT * FROM "${OM_TABLE}" WHERE "lookupKey" = ? ORDER BY "generationCount" DESC LIMIT 1`,
				args: [lookupKey]
			});
			if (!result.rows || result.rows.length === 0) return null;
			return this.parseOMRow(result.rows[0]);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_OBSERVATIONAL_MEMORY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId,
					resourceId
				}
			}, error);
		}
	}
	async getObservationalMemoryHistory(threadId, resourceId, limit = 10, options) {
		try {
			const lookupKey = this.getOMKey(threadId, resourceId);
			const conditions = [`"lookupKey" = ?`];
			const args = [lookupKey];
			if (options?.from) {
				conditions.push(`"createdAt" >= ?`);
				args.push(options.from.toISOString());
			}
			if (options?.to) {
				conditions.push(`"createdAt" <= ?`);
				args.push(options.to.toISOString());
			}
			args.push(limit);
			let sql = `SELECT * FROM "${OM_TABLE}" WHERE ${conditions.join(" AND ")} ORDER BY "generationCount" DESC LIMIT ?`;
			if (options?.offset != null) {
				args.push(options.offset);
				sql += ` OFFSET ?`;
			}
			const result = await this.#client.execute({
				sql,
				args
			});
			if (!result.rows) return [];
			return result.rows.map((row) => this.parseOMRow(row));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_OBSERVATIONAL_MEMORY_HISTORY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId,
					resourceId,
					limit
				}
			}, error);
		}
	}
	async initializeObservationalMemory(input) {
		try {
			const id = crypto.randomUUID();
			const now = /* @__PURE__ */ new Date();
			const lookupKey = this.getOMKey(input.threadId, input.resourceId);
			const record = {
				id,
				scope: input.scope,
				threadId: input.threadId,
				resourceId: input.resourceId,
				createdAt: now,
				updatedAt: now,
				lastObservedAt: void 0,
				originType: "initial",
				generationCount: 0,
				activeObservations: "",
				totalTokensObserved: 0,
				observationTokenCount: 0,
				pendingMessageTokens: 0,
				isReflecting: false,
				isObserving: false,
				isBufferingObservation: false,
				isBufferingReflection: false,
				lastBufferedAtTokens: 0,
				lastBufferedAtTime: null,
				config: input.config,
				observedTimezone: input.observedTimezone
			};
			await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `INSERT INTO "${OM_TABLE}" (
            id, "lookupKey", scope, "resourceId", "threadId",
            "activeObservations", "activeObservationsPendingUpdate",
            "originType", config, "generationCount", "lastObservedAt", "lastReflectionAt",
            "pendingMessageTokens", "totalTokensObserved", "observationTokenCount",
            "isObserving", "isReflecting", "isBufferingObservation", "isBufferingReflection", "lastBufferedAtTokens", "lastBufferedAtTime",
            "observedTimezone", "createdAt", "updatedAt"
          ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
				args: [
					id,
					lookupKey,
					input.scope,
					input.resourceId,
					input.threadId || null,
					"",
					null,
					"initial",
					JSON.stringify(input.config),
					0,
					null,
					null,
					0,
					0,
					0,
					false,
					false,
					false,
					false,
					0,
					null,
					input.observedTimezone || null,
					now.toISOString(),
					now.toISOString()
				]
			}));
			return record;
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "INITIALIZE_OBSERVATIONAL_MEMORY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId: input.threadId,
					resourceId: input.resourceId
				}
			}, error);
		}
	}
	async insertObservationalMemoryRecord(record) {
		try {
			const lookupKey = this.getOMKey(record.threadId, record.resourceId);
			await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `INSERT INTO "${OM_TABLE}" (
            id, "lookupKey", scope, "resourceId", "threadId",
            "activeObservations", "activeObservationsPendingUpdate",
            "originType", config, "generationCount", "lastObservedAt", "lastReflectionAt",
            "pendingMessageTokens", "totalTokensObserved", "observationTokenCount",
            "observedMessageIds", "bufferedObservationChunks",
            "bufferedReflection", "bufferedReflectionTokens", "bufferedReflectionInputTokens",
            "reflectedObservationLineCount",
            "isObserving", "isReflecting", "isBufferingObservation", "isBufferingReflection",
            "lastBufferedAtTokens", "lastBufferedAtTime",
            "observedTimezone", metadata, "createdAt", "updatedAt"
          ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
				args: [
					record.id,
					lookupKey,
					record.scope,
					record.resourceId,
					record.threadId || null,
					record.activeObservations || "",
					null,
					record.originType || "initial",
					record.config ? JSON.stringify(record.config) : null,
					record.generationCount || 0,
					record.lastObservedAt ? record.lastObservedAt.toISOString() : null,
					null,
					record.pendingMessageTokens || 0,
					record.totalTokensObserved || 0,
					record.observationTokenCount || 0,
					record.observedMessageIds ? JSON.stringify(record.observedMessageIds) : null,
					record.bufferedObservationChunks ? JSON.stringify(record.bufferedObservationChunks) : null,
					record.bufferedReflection || null,
					record.bufferedReflectionTokens ?? null,
					record.bufferedReflectionInputTokens ?? null,
					record.reflectedObservationLineCount ?? null,
					record.isObserving || false,
					record.isReflecting || false,
					record.isBufferingObservation || false,
					record.isBufferingReflection || false,
					record.lastBufferedAtTokens || 0,
					record.lastBufferedAtTime ? record.lastBufferedAtTime.toISOString() : null,
					record.observedTimezone || null,
					record.metadata ? JSON.stringify(record.metadata) : null,
					record.createdAt.toISOString(),
					record.updatedAt.toISOString()
				]
			}));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "INSERT_OBSERVATIONAL_MEMORY_RECORD", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id: record.id,
					threadId: record.threadId,
					resourceId: record.resourceId
				}
			}, error);
		}
	}
	async updateActiveObservations(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			const observedMessageIdsJson = input.observedMessageIds ? JSON.stringify(input.observedMessageIds) : null;
			if ((await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `UPDATE "${OM_TABLE}" SET
            "activeObservations" = ?,
            "lastObservedAt" = ?,
            "pendingMessageTokens" = 0,
            "observationTokenCount" = ?,
            "totalTokensObserved" = "totalTokensObserved" + ?,
            "observedMessageIds" = ?,
            "updatedAt" = ?
          WHERE id = ?`,
				args: [
					input.observations,
					input.lastObservedAt.toISOString(),
					input.tokenCount,
					input.tokenCount,
					observedMessageIdsJson,
					now.toISOString(),
					input.id
				]
			}))).rowsAffected === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_ACTIVE_OBSERVATIONS", "NOT_FOUND"),
				text: `Observational memory record not found: ${input.id}`,
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.id }
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_ACTIVE_OBSERVATIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.id }
			}, error);
		}
	}
	async createReflectionGeneration(input) {
		try {
			return await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const record = await this.#insertReflectionGeneration(tx, input);
					await tx.commit();
					return record;
				} catch (error) {
					if (!tx.closed) await tx.rollback();
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_REFLECTION_GENERATION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { currentRecordId: input.currentRecord.id }
			}, error);
		}
	}
	/**
	* Inserts a new reflection-generation row. Called inside a locked write
	* transaction — either from {@link createReflectionGeneration} (standalone)
	* or from {@link swapBufferedReflectionToActive} (as part of a compound
	* swap). Must not acquire the write lock itself; the caller owns it.
	*/
	async #insertReflectionGeneration(tx, input) {
		const id = crypto.randomUUID();
		const now = /* @__PURE__ */ new Date();
		const lookupKey = this.getOMKey(input.currentRecord.threadId, input.currentRecord.resourceId);
		const record = {
			id,
			scope: input.currentRecord.scope,
			threadId: input.currentRecord.threadId,
			resourceId: input.currentRecord.resourceId,
			createdAt: now,
			updatedAt: now,
			lastObservedAt: input.currentRecord.lastObservedAt,
			originType: "reflection",
			generationCount: input.currentRecord.generationCount + 1,
			activeObservations: input.reflection,
			totalTokensObserved: input.currentRecord.totalTokensObserved,
			observationTokenCount: input.tokenCount,
			pendingMessageTokens: 0,
			isReflecting: false,
			isObserving: false,
			isBufferingObservation: false,
			isBufferingReflection: false,
			lastBufferedAtTokens: 0,
			lastBufferedAtTime: null,
			config: input.currentRecord.config,
			metadata: input.currentRecord.metadata,
			observedTimezone: input.currentRecord.observedTimezone
		};
		await tx.execute({
			sql: `INSERT INTO "${OM_TABLE}" (
        id, "lookupKey", scope, "resourceId", "threadId",
        "activeObservations", "activeObservationsPendingUpdate",
        "originType", config, "generationCount", "lastObservedAt", "lastReflectionAt",
        "pendingMessageTokens", "totalTokensObserved", "observationTokenCount",
        "isObserving", "isReflecting", "isBufferingObservation", "isBufferingReflection", "lastBufferedAtTokens", "lastBufferedAtTime",
        "observedTimezone", metadata, "createdAt", "updatedAt"
      ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
			args: [
				id,
				lookupKey,
				record.scope,
				record.resourceId,
				record.threadId || null,
				input.reflection,
				null,
				"reflection",
				JSON.stringify(record.config),
				input.currentRecord.generationCount + 1,
				record.lastObservedAt?.toISOString() || null,
				now.toISOString(),
				record.pendingMessageTokens,
				record.totalTokensObserved,
				record.observationTokenCount,
				false,
				false,
				false,
				false,
				0,
				null,
				record.observedTimezone || null,
				record.metadata ? JSON.stringify(record.metadata) : null,
				now.toISOString(),
				now.toISOString()
			]
		});
		return record;
	}
	async setReflectingFlag(id, isReflecting) {
		try {
			if ((await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `UPDATE "${OM_TABLE}" SET "isReflecting" = ?, "updatedAt" = ? WHERE id = ?`,
				args: [
					isReflecting,
					(/* @__PURE__ */ new Date()).toISOString(),
					id
				]
			}))).rowsAffected === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_REFLECTING_FLAG", "NOT_FOUND"),
				text: `Observational memory record not found: ${id}`,
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isReflecting
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_REFLECTING_FLAG", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isReflecting
				}
			}, error);
		}
	}
	async setObservingFlag(id, isObserving) {
		try {
			if ((await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `UPDATE "${OM_TABLE}" SET "isObserving" = ?, "updatedAt" = ? WHERE id = ?`,
				args: [
					isObserving,
					(/* @__PURE__ */ new Date()).toISOString(),
					id
				]
			}))).rowsAffected === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_OBSERVING_FLAG", "NOT_FOUND"),
				text: `Observational memory record not found: ${id}`,
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isObserving
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_OBSERVING_FLAG", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isObserving
				}
			}, error);
		}
	}
	async setBufferingObservationFlag(id, isBuffering, lastBufferedAtTokens) {
		try {
			const nowStr = (/* @__PURE__ */ new Date()).toISOString();
			let sql;
			let args;
			if (lastBufferedAtTokens !== void 0) {
				sql = `UPDATE "${OM_TABLE}" SET "isBufferingObservation" = ?, "lastBufferedAtTokens" = ?, "updatedAt" = ? WHERE id = ?`;
				args = [
					isBuffering,
					lastBufferedAtTokens,
					nowStr,
					id
				];
			} else {
				sql = `UPDATE "${OM_TABLE}" SET "isBufferingObservation" = ?, "updatedAt" = ? WHERE id = ?`;
				args = [
					isBuffering,
					nowStr,
					id
				];
			}
			if ((await withClientWriteLock(this.#client, () => this.#client.execute({
				sql,
				args
			}))).rowsAffected === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_BUFFERING_OBSERVATION_FLAG", "NOT_FOUND"),
				text: `Observational memory record not found: ${id}`,
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isBuffering,
					lastBufferedAtTokens: lastBufferedAtTokens ?? null
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_BUFFERING_OBSERVATION_FLAG", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isBuffering,
					lastBufferedAtTokens: lastBufferedAtTokens ?? null
				}
			}, error);
		}
	}
	async setBufferingReflectionFlag(id, isBuffering) {
		try {
			if ((await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `UPDATE "${OM_TABLE}" SET "isBufferingReflection" = ?, "updatedAt" = ? WHERE id = ?`,
				args: [
					isBuffering,
					(/* @__PURE__ */ new Date()).toISOString(),
					id
				]
			}))).rowsAffected === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_BUFFERING_REFLECTION_FLAG", "NOT_FOUND"),
				text: `Observational memory record not found: ${id}`,
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isBuffering
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_BUFFERING_REFLECTION_FLAG", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					isBuffering
				}
			}, error);
		}
	}
	async clearObservationalMemory(threadId, resourceId) {
		try {
			const lookupKey = this.getOMKey(threadId, resourceId);
			await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `DELETE FROM "${OM_TABLE}" WHERE "lookupKey" = ?`,
				args: [lookupKey]
			}));
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CLEAR_OBSERVATIONAL_MEMORY", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId,
					resourceId
				}
			}, error);
		}
	}
	async setPendingMessageTokens(id, tokenCount) {
		try {
			if ((await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `UPDATE "${OM_TABLE}" SET 
            "pendingMessageTokens" = ?, 
            "updatedAt" = ? 
          WHERE id = ?`,
				args: [
					tokenCount,
					(/* @__PURE__ */ new Date()).toISOString(),
					id
				]
			}))).rowsAffected === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_PENDING_MESSAGE_TOKENS", "NOT_FOUND"),
				text: `Observational memory record not found: ${id}`,
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					tokenCount
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SET_PENDING_MESSAGE_TOKENS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					id,
					tokenCount
				}
			}, error);
		}
	}
	async updateObservationalMemoryConfig(input) {
		try {
			await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const selectResult = await tx.execute({
						sql: `SELECT config FROM "${OM_TABLE}" WHERE id = ?`,
						args: [input.id]
					});
					if (selectResult.rows.length === 0) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_OM_CONFIG", "NOT_FOUND"),
						text: `Observational memory record not found: ${input.id}`,
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
						details: { id: input.id }
					});
					const row = selectResult.rows[0];
					const existing = row.config ? JSON.parse(row.config) : {};
					const merged = this.deepMergeConfig(existing, input.config);
					await tx.execute({
						sql: `UPDATE "${OM_TABLE}" SET config = ?, "updatedAt" = ? WHERE id = ?`,
						args: [
							JSON.stringify(merged),
							(/* @__PURE__ */ new Date()).toISOString(),
							input.id
						]
					});
					await tx.commit();
				} catch (error) {
					if (!tx.closed) await tx.rollback();
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_OM_CONFIG", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.id }
			}, error);
		}
	}
	async updateBufferedObservations(input) {
		try {
			const nowStr = (/* @__PURE__ */ new Date()).toISOString();
			await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const current = await tx.execute({
						sql: `SELECT "bufferedObservationChunks" FROM "${OM_TABLE}" WHERE id = ?`,
						args: [input.id]
					});
					if (!current.rows || current.rows.length === 0) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_BUFFERED_OBSERVATIONS", "NOT_FOUND"),
						text: `Observational memory record not found: ${input.id}`,
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
						details: { id: input.id }
					});
					const row = current.rows[0];
					let existingChunks = [];
					if (row.bufferedObservationChunks) try {
						const parsed = typeof row.bufferedObservationChunks === "string" ? JSON.parse(row.bufferedObservationChunks) : row.bufferedObservationChunks;
						existingChunks = Array.isArray(parsed) ? parsed : [];
					} catch {
						existingChunks = [];
					}
					if (existingChunks.some((existing) => existing.cycleId === input.chunk.cycleId)) {
						await tx.commit();
						return;
					}
					const newChunk = {
						id: `ombuf-${(0, crypto$1.randomUUID)()}`,
						cycleId: input.chunk.cycleId,
						observations: input.chunk.observations,
						tokenCount: input.chunk.tokenCount,
						messageIds: input.chunk.messageIds,
						messageTokens: input.chunk.messageTokens,
						lastObservedAt: input.chunk.lastObservedAt,
						createdAt: /* @__PURE__ */ new Date(),
						suggestedContinuation: input.chunk.suggestedContinuation,
						currentTask: input.chunk.currentTask,
						threadTitle: input.chunk.threadTitle,
						extractedValues: input.chunk.extractedValues,
						extractionFailures: input.chunk.extractionFailures
					};
					const newChunks = [...existingChunks, newChunk];
					const lastBufferedAtTime = input.lastBufferedAtTime ? input.lastBufferedAtTime.toISOString() : null;
					if ((await tx.execute({
						sql: `UPDATE "${OM_TABLE}" SET
              "bufferedObservationChunks" = ?,
              "lastBufferedAtTime" = COALESCE(?, "lastBufferedAtTime"),
              "updatedAt" = ?
            WHERE id = ?`,
						args: [
							JSON.stringify(newChunks),
							lastBufferedAtTime,
							nowStr,
							input.id
						]
					})).rowsAffected === 0) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_BUFFERED_OBSERVATIONS", "NOT_FOUND"),
						text: `Observational memory record not found: ${input.id}`,
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
						details: { id: input.id }
					});
					await tx.commit();
				} catch (error) {
					if (!tx.closed) await tx.rollback();
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_BUFFERED_OBSERVATIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.id }
			}, error);
		}
	}
	async swapBufferedToActive(input) {
		try {
			const nowStr = (/* @__PURE__ */ new Date()).toISOString();
			return await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const current = await tx.execute({
						sql: `SELECT * FROM "${OM_TABLE}" WHERE id = ?`,
						args: [input.id]
					});
					if (!current.rows || current.rows.length === 0) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SWAP_BUFFERED_TO_ACTIVE", "NOT_FOUND"),
						text: `Observational memory record not found: ${input.id}`,
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
						details: { id: input.id }
					});
					const row = current.rows[0];
					let chunks = [];
					if (row.bufferedObservationChunks) try {
						const parsed = typeof row.bufferedObservationChunks === "string" ? JSON.parse(row.bufferedObservationChunks) : row.bufferedObservationChunks;
						chunks = Array.isArray(parsed) ? parsed : [];
					} catch {
						chunks = [];
					}
					if (chunks.length === 0) {
						await tx.commit();
						return {
							chunksActivated: 0,
							messageTokensActivated: 0,
							observationTokensActivated: 0,
							messagesActivated: 0,
							activatedCycleIds: [],
							activatedMessageIds: []
						};
					}
					const retentionFloor = input.messageTokensThreshold * (1 - input.activationRatio);
					const targetMessageTokens = Math.max(0, input.currentPendingTokens - retentionFloor);
					let cumulativeMessageTokens = 0;
					let bestOverBoundary = 0;
					let bestOverTokens = 0;
					let bestUnderBoundary = 0;
					let bestUnderTokens = 0;
					for (let i = 0; i < chunks.length; i++) {
						cumulativeMessageTokens += chunks[i].messageTokens ?? 0;
						const boundary = i + 1;
						if (cumulativeMessageTokens >= targetMessageTokens) {
							if (bestOverBoundary === 0 || cumulativeMessageTokens < bestOverTokens) {
								bestOverBoundary = boundary;
								bestOverTokens = cumulativeMessageTokens;
							}
						} else if (cumulativeMessageTokens > bestUnderTokens) {
							bestUnderBoundary = boundary;
							bestUnderTokens = cumulativeMessageTokens;
						}
					}
					const maxOvershoot = retentionFloor * .95;
					const overshoot = bestOverTokens - targetMessageTokens;
					const remainingAfterOver = input.currentPendingTokens - bestOverTokens;
					const remainingAfterUnder = input.currentPendingTokens - bestUnderTokens;
					const minRemaining = Math.min(1e3, retentionFloor);
					let chunksToActivate;
					if (input.forceMaxActivation && bestOverBoundary > 0 && remainingAfterOver >= minRemaining) chunksToActivate = bestOverBoundary;
					else if (bestOverBoundary > 0 && overshoot <= maxOvershoot && remainingAfterOver >= minRemaining) chunksToActivate = bestOverBoundary;
					else if (bestUnderBoundary > 0 && remainingAfterUnder >= minRemaining) chunksToActivate = bestUnderBoundary;
					else if (bestOverBoundary > 0) chunksToActivate = bestOverBoundary;
					else chunksToActivate = 1;
					const activatedChunks = chunks.slice(0, chunksToActivate);
					const remainingChunks = chunks.slice(chunksToActivate);
					const activatedContent = activatedChunks.map((c) => c.observations).join("\n\n");
					const activatedTokens = activatedChunks.reduce((sum, c) => sum + c.tokenCount, 0);
					const activatedMessageTokens = activatedChunks.reduce((sum, c) => sum + (c.messageTokens ?? 0), 0);
					const activatedMessageCount = activatedChunks.reduce((sum, c) => sum + c.messageIds.length, 0);
					const activatedCycleIds = activatedChunks.map((c) => c.cycleId).filter((id) => !!id);
					const activatedMessageIds = activatedChunks.flatMap((c) => c.messageIds ?? []);
					const latestChunk = activatedChunks[activatedChunks.length - 1];
					const lastObservedAt = input.lastObservedAt ?? (latestChunk?.lastObservedAt ? new Date(latestChunk.lastObservedAt) : /* @__PURE__ */ new Date());
					const lastObservedAtStr = lastObservedAt.toISOString();
					const existingActive = row.activeObservations || "";
					const existingTokenCount = Number(row.observationTokenCount || 0);
					const boundary = `\n\n--- message boundary (${lastObservedAt.toISOString()}) ---\n\n`;
					const newActive = existingActive ? `${existingActive}${boundary}${activatedContent}` : activatedContent;
					const newTokenCount = existingTokenCount + activatedTokens;
					const existingPending = Number(row.pendingMessageTokens || 0);
					const newPending = Math.max(0, existingPending - activatedMessageTokens);
					const updateResult = await tx.execute({
						sql: `UPDATE "${OM_TABLE}" SET
              "activeObservations" = ?,
              "observationTokenCount" = ?,
              "pendingMessageTokens" = ?,
              "bufferedObservationChunks" = ?,
              "lastObservedAt" = ?,
              "updatedAt" = ?
            WHERE id = ?
              AND "bufferedObservationChunks" IS NOT NULL
              AND "bufferedObservationChunks" != '[]'`,
						args: [
							newActive,
							newTokenCount,
							newPending,
							remainingChunks.length > 0 ? JSON.stringify(remainingChunks) : null,
							lastObservedAtStr,
							nowStr,
							input.id
						]
					});
					await tx.commit();
					if (updateResult.rowsAffected === 0) return {
						chunksActivated: 0,
						messageTokensActivated: 0,
						observationTokensActivated: 0,
						messagesActivated: 0,
						activatedCycleIds: [],
						activatedMessageIds: []
					};
					const latestChunkHints = activatedChunks[activatedChunks.length - 1];
					return {
						chunksActivated: activatedChunks.length,
						messageTokensActivated: activatedMessageTokens,
						observationTokensActivated: activatedTokens,
						messagesActivated: activatedMessageCount,
						activatedCycleIds,
						activatedMessageIds,
						observations: activatedContent,
						perChunk: activatedChunks.map((c) => ({
							cycleId: c.cycleId ?? "",
							messageTokens: c.messageTokens ?? 0,
							observationTokens: c.tokenCount,
							messageCount: c.messageIds.length,
							observations: c.observations
						})),
						suggestedContinuation: latestChunkHints?.suggestedContinuation ?? void 0,
						currentTask: latestChunkHints?.currentTask ?? void 0
					};
				} catch (error) {
					if (!tx.closed) await tx.rollback();
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SWAP_BUFFERED_TO_ACTIVE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.id }
			}, error);
		}
	}
	async updateBufferedReflection(input) {
		try {
			const nowStr = (/* @__PURE__ */ new Date()).toISOString();
			if ((await withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `UPDATE "${OM_TABLE}" SET
            "bufferedReflection" = CASE
              WHEN "bufferedReflection" IS NOT NULL AND "bufferedReflection" != ''
              THEN "bufferedReflection" || char(10) || char(10) || ?
              ELSE ?
            END,
            "bufferedReflectionTokens" = COALESCE("bufferedReflectionTokens", 0) + ?,
            "bufferedReflectionInputTokens" = COALESCE("bufferedReflectionInputTokens", 0) + ?,
            "reflectedObservationLineCount" = ?,
            "updatedAt" = ?
          WHERE id = ?`,
				args: [
					input.reflection,
					input.reflection,
					input.tokenCount,
					input.inputTokenCount,
					input.reflectedObservationLineCount,
					nowStr,
					input.id
				]
			}))).rowsAffected === 0) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_BUFFERED_REFLECTION", "NOT_FOUND"),
				text: `Observational memory record not found: ${input.id}`,
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.id }
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_BUFFERED_REFLECTION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.id }
			}, error);
		}
	}
	async swapBufferedReflectionToActive(input) {
		try {
			return await withClientWriteLock(this.#client, async () => {
				const tx = await this.#client.transaction("write");
				try {
					const current = await tx.execute({
						sql: `SELECT * FROM "${OM_TABLE}" WHERE id = ?`,
						args: [input.currentRecord.id]
					});
					if (!current.rows || current.rows.length === 0) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SWAP_BUFFERED_REFLECTION_TO_ACTIVE", "NOT_FOUND"),
						text: `Observational memory record not found: ${input.currentRecord.id}`,
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
						details: { id: input.currentRecord.id }
					});
					const row = current.rows[0];
					const bufferedReflection = row.bufferedReflection || "";
					const reflectedLineCount = Number(row.reflectedObservationLineCount || 0);
					if (!bufferedReflection) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SWAP_BUFFERED_REFLECTION_TO_ACTIVE", "NO_CONTENT"),
						text: "No buffered reflection to swap",
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: { id: input.currentRecord.id }
					});
					const unreflectedContent = (row.activeObservations || "").split("\n").slice(reflectedLineCount).join("\n").trim();
					const newObservations = unreflectedContent ? `${bufferedReflection}\n\n${unreflectedContent}` : bufferedReflection;
					const newRecord = await this.#insertReflectionGeneration(tx, {
						currentRecord: input.currentRecord,
						reflection: newObservations,
						tokenCount: input.tokenCount
					});
					const nowStr = (/* @__PURE__ */ new Date()).toISOString();
					await tx.execute({
						sql: `UPDATE "${OM_TABLE}" SET
              "bufferedReflection" = NULL,
              "bufferedReflectionTokens" = NULL,
              "bufferedReflectionInputTokens" = NULL,
              "reflectedObservationLineCount" = NULL,
              "updatedAt" = ?
            WHERE id = ?`,
						args: [nowStr, input.currentRecord.id]
					});
					await tx.commit();
					return newRecord;
				} catch (error) {
					if (!tx.closed) await tx.rollback();
					throw error;
				}
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SWAP_BUFFERED_REFLECTION_TO_ACTIVE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { id: input.currentRecord.id }
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/notifications/index.ts
/** Ids per UPDATE statement — keeps bind parameters well under SQLite's per-statement limit. */
const BULK_ID_BATCH_SIZE = 500;
const statusTimestamp = (status, now) => {
	if (status === "delivered") return { deliveredAt: now };
	if (status === "seen") return { seenAt: now };
	if (status === "dismissed") return { dismissedAt: now };
	if (status === "archived") return { archivedAt: now };
	if (status === "discarded") return { discardedAt: now };
	return {};
};
function parseJson$2(value) {
	if (value == null) return void 0;
	if (typeof value === "string") try {
		return JSON.parse(value);
	} catch {
		return value;
	}
	return value;
}
function parseDate(value) {
	return value == null ? void 0 : new Date(String(value));
}
const cloneValue = (value) => value === void 0 ? void 0 : structuredClone(value);
function rowToNotification(row) {
	return {
		id: String(row.id),
		threadId: String(row.threadId),
		source: String(row.source),
		kind: String(row.kind),
		priority: String(row.priority),
		status: String(row.status),
		summary: String(row.summary),
		payload: parseJson$2(row.payload),
		resourceId: row.resourceId == null ? void 0 : String(row.resourceId),
		agentId: row.agentId == null ? void 0 : String(row.agentId),
		sourceId: row.sourceId == null ? void 0 : String(row.sourceId),
		dedupeKey: row.dedupeKey == null ? void 0 : String(row.dedupeKey),
		coalesceKey: row.coalesceKey == null ? void 0 : String(row.coalesceKey),
		coalescedCount: Number(row.coalescedCount ?? 1),
		attributes: parseJson$2(row.attributes),
		createdAt: new Date(String(row.createdAt)),
		updatedAt: new Date(String(row.updatedAt)),
		deliveredAt: parseDate(row.deliveredAt),
		seenAt: parseDate(row.seenAt),
		dismissedAt: parseDate(row.dismissedAt),
		archivedAt: parseDate(row.archivedAt),
		discardedAt: parseDate(row.discardedAt),
		deliverAt: parseDate(row.deliverAt),
		summaryAt: parseDate(row.summaryAt),
		deliveryReason: row.deliveryReason == null ? void 0 : String(row.deliveryReason),
		deliveryAttempts: Number(row.deliveryAttempts ?? 0),
		lastDeliveryAttemptAt: parseDate(row.lastDeliveryAttemptAt),
		lastDeliveryError: row.lastDeliveryError == null ? void 0 : String(row.lastDeliveryError),
		deliveredSignalId: row.deliveredSignalId == null ? void 0 : String(row.deliveredSignalId),
		summarySignalId: row.summarySignalId == null ? void 0 : String(row.summarySignalId),
		metadata: parseJson$2(row.metadata)
	};
}
function addArrayFilter(conditions, args, column, value) {
	if (!value) return;
	const values = Array.isArray(value) ? value : [value];
	conditions.push(`"${column}" IN (${values.map(() => "?").join(", ")})`);
	args.push(...values);
}
var NotificationsLibSQL = class NotificationsLibSQL extends _mastra_core_storage.NotificationsStorage {
	/** The notification feed grows unbounded. Single table, anchored on `createdAt`. */
	static retentionTables = { notifications: {
		table: _mastra_core_storage.TABLE_NOTIFICATIONS,
		column: "createdAt",
		indexed: true
	} };
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_NOTIFICATIONS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_NOTIFICATIONS]
		});
		await this.#client.batch([
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_notifications_thread_status_updated ON "${_mastra_core_storage.TABLE_NOTIFICATIONS}" ("threadId", "status", "updatedAt")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_notifications_coalescing ON "${_mastra_core_storage.TABLE_NOTIFICATIONS}" ("threadId", "source", "kind", "status", "agentId", "resourceId", "dedupeKey", "coalesceKey")`,
				args: []
			},
			{
				sql: `CREATE INDEX IF NOT EXISTS idx_notifications_due ON "${_mastra_core_storage.TABLE_NOTIFICATIONS}" ("status", "deliverAt", "summaryAt")`,
				args: []
			}
		], "write");
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_NOTIFICATIONS });
	}
	/** Delete notifications older than the `notifications` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: NotificationsLibSQL.retentionTables,
			order: ["notifications"]
		});
		return runPrune({
			db: this.#db,
			domain: "notifications",
			targets,
			options,
			logger: this.logger
		});
	}
	async createNotification(input) {
		const existing = await this.findCoalescable(input);
		if (existing) {
			const now = /* @__PURE__ */ new Date();
			const attributes = input.attributes ? {
				...cloneValue(existing.attributes),
				...cloneValue(input.attributes)
			} : cloneValue(existing.attributes);
			const metadata = input.metadata ? {
				...cloneValue(existing.metadata),
				...cloneValue(input.metadata)
			} : cloneValue(existing.metadata);
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_NOTIFICATIONS,
				keys: {
					threadId: existing.threadId,
					id: existing.id
				},
				data: {
					summary: input.summary,
					payload: cloneValue(input.payload ?? existing.payload) ?? null,
					priority: input.priority ?? existing.priority,
					attributes: attributes ?? null,
					updatedAt: now,
					deliverAt: input.deliverAt ?? existing.deliverAt ?? null,
					summaryAt: input.summaryAt ?? existing.summaryAt ?? null,
					deliveryReason: input.deliveryReason ?? existing.deliveryReason ?? null,
					coalescedCount: (existing.coalescedCount ?? 1) + 1,
					metadata: metadata ?? null
				}
			});
			const updated = await this.getNotification({
				threadId: existing.threadId,
				id: existing.id
			});
			if (!updated) throw new Error(`Notification ${existing.id} was not found for thread ${existing.threadId}`);
			return updated;
		}
		const now = input.createdAt ?? /* @__PURE__ */ new Date();
		const record = {
			id: input.id ?? (0, crypto$1.randomUUID)(),
			threadId: input.threadId,
			source: input.source,
			kind: input.kind,
			priority: input.priority ?? "medium",
			status: "pending",
			summary: input.summary,
			payload: cloneValue(input.payload),
			resourceId: input.resourceId,
			agentId: input.agentId,
			sourceId: input.sourceId,
			dedupeKey: input.dedupeKey,
			coalesceKey: input.coalesceKey,
			coalescedCount: 1,
			attributes: cloneValue(input.attributes),
			createdAt: now,
			updatedAt: now,
			deliverAt: input.deliverAt,
			summaryAt: input.summaryAt,
			deliveryReason: input.deliveryReason,
			deliveryAttempts: 0,
			metadata: cloneValue(input.metadata)
		};
		await this.#db.insert({
			tableName: _mastra_core_storage.TABLE_NOTIFICATIONS,
			record: {
				...record,
				payload: record.payload ?? null,
				attributes: record.attributes ?? null,
				metadata: record.metadata ?? null,
				resourceId: record.resourceId ?? null,
				agentId: record.agentId ?? null,
				sourceId: record.sourceId ?? null,
				dedupeKey: record.dedupeKey ?? null,
				coalesceKey: record.coalesceKey ?? null,
				deliverAt: record.deliverAt ?? null,
				summaryAt: record.summaryAt ?? null,
				deliveryReason: record.deliveryReason ?? null,
				deliveryAttempts: record.deliveryAttempts ?? 0
			}
		});
		return record;
	}
	async listNotifications(input) {
		const conditions = ["\"threadId\" = ?"];
		const args = [input.threadId];
		addArrayFilter(conditions, args, "status", input.status);
		addArrayFilter(conditions, args, "priority", input.priority);
		if (input.source) {
			conditions.push("\"source\" = ?");
			args.push(input.source);
		}
		if (input.resourceId) {
			conditions.push("\"resourceId\" = ?");
			args.push(input.resourceId);
		}
		if (input.agentId) {
			conditions.push("\"agentId\" = ?");
			args.push(input.agentId);
		}
		if (input.search) {
			conditions.push("(LOWER(\"summary\") LIKE ? OR LOWER(\"kind\") LIKE ?)");
			const search = `%${input.search.toLowerCase()}%`;
			args.push(search, search);
		}
		const limit = input.limit ? " LIMIT ?" : "";
		if (input.limit) args.push(input.limit);
		return ((await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_NOTIFICATIONS)} FROM "${_mastra_core_storage.TABLE_NOTIFICATIONS}" WHERE ${conditions.join(" AND ")} ORDER BY "updatedAt" DESC${limit}`,
			args
		})).rows ?? []).map((row) => rowToNotification(row));
	}
	async listDueNotifications(input) {
		const now = input.now.toISOString();
		const conditions = ["\"status\" = ?", "((\"deliverAt\" IS NOT NULL AND \"deliverAt\" <= ?) OR (\"summaryAt\" IS NOT NULL AND \"summaryAt\" <= ?))"];
		const args = [
			"pending",
			now,
			now
		];
		if (input.agentId) {
			conditions.push("\"agentId\" = ?");
			args.push(input.agentId);
		}
		if (input.resourceId) {
			conditions.push("\"resourceId\" = ?");
			args.push(input.resourceId);
		}
		const limit = input.limit ? " LIMIT ?" : "";
		if (input.limit) args.push(input.limit);
		return ((await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_NOTIFICATIONS)} FROM "${_mastra_core_storage.TABLE_NOTIFICATIONS}" WHERE ${conditions.join(" AND ")} ORDER BY CASE WHEN "deliverAt" IS NULL THEN "summaryAt" WHEN "summaryAt" IS NULL THEN "deliverAt" WHEN "deliverAt" <= "summaryAt" THEN "deliverAt" ELSE "summaryAt" END ASC, "updatedAt" ASC${limit}`,
			args
		})).rows ?? []).map((row) => rowToNotification(row));
	}
	async getNotification(input) {
		const row = (await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_NOTIFICATIONS)} FROM "${_mastra_core_storage.TABLE_NOTIFICATIONS}" WHERE "threadId" = ? AND "id" = ? LIMIT 1`,
			args: [input.threadId, input.id]
		})).rows?.[0];
		return row ? rowToNotification(row) : null;
	}
	async updateNotification(input) {
		if (!await this.getNotification({
			threadId: input.threadId,
			id: input.id
		})) throw new Error(`Notification ${input.id} was not found for thread ${input.threadId}`);
		const now = /* @__PURE__ */ new Date();
		await this.#db.update({
			tableName: _mastra_core_storage.TABLE_NOTIFICATIONS,
			keys: {
				threadId: input.threadId,
				id: input.id
			},
			data: {
				...input.status ? {
					status: input.status,
					...statusTimestamp(input.status, now)
				} : {},
				...input.summary !== void 0 ? { summary: input.summary } : {},
				...input.payload !== void 0 ? { payload: cloneValue(input.payload) } : {},
				...input.attributes !== void 0 ? { attributes: cloneValue(input.attributes) } : {},
				...input.metadata !== void 0 ? { metadata: cloneValue(input.metadata) } : {},
				...input.deliverAt !== void 0 ? { deliverAt: input.deliverAt } : {},
				...input.summaryAt !== void 0 ? { summaryAt: input.summaryAt } : {},
				...input.deliveryReason !== void 0 ? { deliveryReason: input.deliveryReason } : {},
				...input.deliveryAttempts !== void 0 ? { deliveryAttempts: input.deliveryAttempts } : {},
				...input.lastDeliveryAttemptAt !== void 0 ? { lastDeliveryAttemptAt: input.lastDeliveryAttemptAt } : {},
				...input.lastDeliveryError !== void 0 ? { lastDeliveryError: input.lastDeliveryError } : {},
				...input.deliveredSignalId !== void 0 ? { deliveredSignalId: input.deliveredSignalId } : {},
				...input.summarySignalId !== void 0 ? { summarySignalId: input.summarySignalId } : {},
				updatedAt: now
			}
		});
		const updated = await this.getNotification({
			threadId: input.threadId,
			id: input.id
		});
		if (!updated) throw new Error(`Notification ${input.id} was not found for thread ${input.threadId}`);
		return updated;
	}
	async updateNotificationsStatus(input) {
		const ids = Array.from(new Set(input.ids));
		if (ids.length === 0) return [];
		const now = /* @__PURE__ */ new Date();
		const assignments = {
			status: input.status,
			updatedAt: now.toISOString()
		};
		for (const [column, value] of Object.entries(statusTimestamp(input.status, now))) assignments[column] = value.toISOString();
		const setClause = Object.keys(assignments).map((column) => `"${column}" = ?`).join(", ");
		const updated = [];
		for (let offset = 0; offset < ids.length; offset += BULK_ID_BATCH_SIZE) {
			const batch = ids.slice(offset, offset + BULK_ID_BATCH_SIZE);
			const result = await this.#db.executeWriteOperationWithRetry(() => withClientWriteLock(this.#client, () => this.#client.execute({
				sql: `UPDATE "${_mastra_core_storage.TABLE_NOTIFICATIONS}" SET ${setClause} WHERE "threadId" = ? AND "id" IN (${batch.map(() => "?").join(", ")}) RETURNING ${buildSelectColumns(_mastra_core_storage.TABLE_NOTIFICATIONS)}`,
				args: [
					...Object.values(assignments),
					input.threadId,
					...batch
				]
			})), `bulk update notification status in table ${_mastra_core_storage.TABLE_NOTIFICATIONS}`);
			for (const row of result.rows ?? []) updated.push(rowToNotification(row));
		}
		return updated;
	}
	async findCoalescable(input) {
		if (!input.dedupeKey && !input.coalesceKey) return void 0;
		const agentId = input.agentId ?? null;
		const resourceId = input.resourceId ?? null;
		const dedupeKey = input.dedupeKey ?? null;
		const coalesceKey = input.coalesceKey ?? null;
		const row = (await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_NOTIFICATIONS)} FROM "${_mastra_core_storage.TABLE_NOTIFICATIONS}" WHERE "threadId" = ? AND "source" = ? AND "kind" = ? AND "status" = ? AND (("agentId" = ?) OR ("agentId" IS NULL AND ? IS NULL)) AND (("resourceId" = ?) OR ("resourceId" IS NULL AND ? IS NULL)) AND ((? IS NOT NULL AND "dedupeKey" = ?) OR (? IS NOT NULL AND "coalesceKey" = ?)) ORDER BY "updatedAt" DESC LIMIT 1`,
			args: [
				input.threadId,
				input.source,
				input.kind,
				"pending",
				agentId,
				agentId,
				resourceId,
				resourceId,
				dedupeKey,
				dedupeKey,
				coalesceKey,
				coalesceKey
			]
		})).rows?.[0];
		return row ? rowToNotification(row) : void 0;
	}
};
//#endregion
//#region src/storage/domains/observability/index.ts
var ObservabilityLibSQL = class ObservabilityLibSQL extends _mastra_core_storage.ObservabilityStorage {
	/**
	* Spans are the only physical table; traces are derived from them. Spans
	* anchor on `startedAt` (not `createdAt`), stored as an ISO-8601 string.
	*/
	static retentionTables = { spans: {
		table: _mastra_core_storage.TABLE_SPANS,
		column: "startedAt",
		indexed: true
	} };
	#db;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SPANS,
			schema: _mastra_core_storage.SPAN_SCHEMA
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_SPANS,
			schema: _mastra_core_storage.SPAN_SCHEMA,
			ifNotExists: ["requestContext"]
		});
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SPANS });
	}
	/** Delete spans older than the `spans` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: ObservabilityLibSQL.retentionTables,
			order: ["spans"]
		});
		return runPrune({
			db: this.#db,
			domain: "observability",
			targets,
			options,
			logger: this.logger
		});
	}
	/**
	* Manually run the spans migration to deduplicate and add the unique constraint.
	* This is intended to be called from the CLI when duplicates are detected.
	*
	* @returns Migration result with status and details
	*/
	async migrateSpans() {
		return this.#db.migrateSpans();
	}
	/**
	* Check migration status for the spans table.
	* Returns information about whether migration is needed.
	*/
	async checkSpansMigrationStatus() {
		return this.#db.checkSpansMigrationStatus();
	}
	get tracingStrategy() {
		return {
			preferred: "batch-with-updates",
			supported: ["batch-with-updates", "insert-only"]
		};
	}
	async createSpan(args) {
		const { span } = args;
		try {
			const startedAt = span.startedAt instanceof Date ? span.startedAt.toISOString() : span.startedAt;
			const endedAt = span.endedAt instanceof Date ? span.endedAt.toISOString() : span.endedAt;
			const now = (/* @__PURE__ */ new Date()).toISOString();
			const record = {
				...span,
				startedAt,
				endedAt,
				createdAt: now,
				updatedAt: now
			};
			return this.#db.insert({
				tableName: _mastra_core_storage.TABLE_SPANS,
				record
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_SPAN", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: {
					spanId: span.spanId,
					traceId: span.traceId,
					spanType: span.spanType,
					name: span.name
				}
			}, error);
		}
	}
	async getSpan(args) {
		const { traceId, spanId } = args;
		try {
			const rows = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_SPANS,
				whereClause: {
					sql: " WHERE traceId = ? AND spanId = ?",
					args: [traceId, spanId]
				},
				limit: 1
			});
			if (!rows || rows.length === 0) return null;
			return { span: transformFromSqlRow({
				tableName: _mastra_core_storage.TABLE_SPANS,
				sqlRow: rows[0]
			}) };
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SPAN", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: {
					traceId,
					spanId
				}
			}, error);
		}
	}
	async getRootSpan(args) {
		const { traceId } = args;
		try {
			const rows = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_SPANS,
				whereClause: {
					sql: " WHERE traceId = ? AND parentSpanId IS NULL",
					args: [traceId]
				},
				limit: 1
			});
			if (!rows || rows.length === 0) return null;
			return { span: transformFromSqlRow({
				tableName: _mastra_core_storage.TABLE_SPANS,
				sqlRow: rows[0]
			}) };
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_ROOT_SPAN", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { traceId }
			}, error);
		}
	}
	async getTrace(args) {
		const { traceId } = args;
		try {
			const spans = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_SPANS,
				whereClause: {
					sql: " WHERE traceId = ?",
					args: [traceId]
				},
				orderBy: "startedAt ASC"
			});
			if (!spans || spans.length === 0) return null;
			return {
				traceId,
				spans: spans.map((span) => transformFromSqlRow({
					tableName: _mastra_core_storage.TABLE_SPANS,
					sqlRow: span
				}))
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_TRACE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { traceId }
			}, error);
		}
	}
	async getTraceLight(args) {
		const { traceId } = args;
		try {
			const spans = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_SPANS,
				whereClause: {
					sql: " WHERE traceId = ?",
					args: [traceId]
				},
				orderBy: "startedAt ASC"
			});
			if (!spans || spans.length === 0) return null;
			return {
				traceId,
				spans: spans.map((span) => {
					const { input, output, attributes, metadata, tags, links, ...light } = transformFromSqlRow({
						tableName: _mastra_core_storage.TABLE_SPANS,
						sqlRow: span
					});
					return light;
				})
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_TRACE_LIGHT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: { traceId }
			}, error);
		}
	}
	async updateSpan(args) {
		const { traceId, spanId, updates } = args;
		try {
			const data = { ...updates };
			if (data.endedAt instanceof Date) data.endedAt = data.endedAt.toISOString();
			if (data.startedAt instanceof Date) data.startedAt = data.startedAt.toISOString();
			data.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_SPANS,
				keys: {
					spanId,
					traceId
				},
				data
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_SPAN", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: {
					spanId,
					traceId
				}
			}, error);
		}
	}
	async listTraces(args) {
		const { filters, pagination, orderBy } = _mastra_core_storage.listTracesArgsSchema.parse(args);
		const page = pagination?.page ?? 0;
		const perPage = pagination?.perPage ?? 10;
		const tableName = (0, _mastra_core_utils.parseSqlIdentifier)(_mastra_core_storage.TABLE_SPANS, "table name");
		try {
			const conditions = ["parentSpanId IS NULL"];
			const queryArgs = [];
			if (filters) {
				if (filters.startedAt?.start) {
					conditions.push(`startedAt >= ?`);
					queryArgs.push(filters.startedAt.start.toISOString());
				}
				if (filters.startedAt?.end) {
					conditions.push(`startedAt <= ?`);
					queryArgs.push(filters.startedAt.end.toISOString());
				}
				if (filters.endedAt?.start) {
					conditions.push(`endedAt >= ?`);
					queryArgs.push(filters.endedAt.start.toISOString());
				}
				if (filters.endedAt?.end) {
					conditions.push(`endedAt <= ?`);
					queryArgs.push(filters.endedAt.end.toISOString());
				}
				if (filters.spanType !== void 0) {
					conditions.push(`spanType = ?`);
					queryArgs.push(filters.spanType);
				}
				if (filters.entityType !== void 0) {
					conditions.push(`entityType = ?`);
					queryArgs.push(filters.entityType);
				}
				if (filters.entityId !== void 0) {
					conditions.push(`entityId = ?`);
					queryArgs.push(filters.entityId);
				}
				if (filters.entityName !== void 0) {
					conditions.push(`entityName = ?`);
					queryArgs.push(filters.entityName);
				}
				if (filters.userId !== void 0) {
					conditions.push(`userId = ?`);
					queryArgs.push(filters.userId);
				}
				if (filters.organizationId !== void 0) {
					conditions.push(`organizationId = ?`);
					queryArgs.push(filters.organizationId);
				}
				if (filters.resourceId !== void 0) {
					conditions.push(`resourceId = ?`);
					queryArgs.push(filters.resourceId);
				}
				if (filters.runId !== void 0) {
					conditions.push(`runId = ?`);
					queryArgs.push(filters.runId);
				}
				if (filters.sessionId !== void 0) {
					conditions.push(`sessionId = ?`);
					queryArgs.push(filters.sessionId);
				}
				if (filters.threadId !== void 0) {
					conditions.push(`threadId = ?`);
					queryArgs.push(filters.threadId);
				}
				if (filters.requestId !== void 0) {
					conditions.push(`requestId = ?`);
					queryArgs.push(filters.requestId);
				}
				if (filters.environment !== void 0) {
					conditions.push(`environment = ?`);
					queryArgs.push(filters.environment);
				}
				if (filters.source !== void 0) {
					conditions.push(`source = ?`);
					queryArgs.push(filters.source);
				}
				if (filters.serviceName !== void 0) {
					conditions.push(`serviceName = ?`);
					queryArgs.push(filters.serviceName);
				}
				if (filters.scope != null) for (const [key, value] of Object.entries(filters.scope)) {
					if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_TRACES", "INVALID_FILTER_KEY"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: { key }
					});
					conditions.push(`json_extract(scope, '$.${key}') = ?`);
					queryArgs.push(typeof value === "string" ? value : JSON.stringify(value));
				}
				if (filters.metadata != null) for (const [key, value] of Object.entries(filters.metadata)) {
					if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
						id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_TRACES", "INVALID_FILTER_KEY"),
						domain: _mastra_core_error.ErrorDomain.STORAGE,
						category: _mastra_core_error.ErrorCategory.USER,
						details: { key }
					});
					conditions.push(`json_extract(metadata, '$.${key}') = ?`);
					queryArgs.push(typeof value === "string" ? value : JSON.stringify(value));
				}
				if (filters.tags != null && filters.tags.length > 0) for (const tag of filters.tags) {
					conditions.push(`EXISTS (SELECT 1 FROM json_each(${tableName}.tags) WHERE value = ?)`);
					queryArgs.push(tag);
				}
				if (filters.status !== void 0) switch (filters.status) {
					case _mastra_core_storage.TraceStatus.ERROR:
						conditions.push(`error IS NOT NULL`);
						break;
					case _mastra_core_storage.TraceStatus.RUNNING:
						conditions.push(`endedAt IS NULL AND error IS NULL`);
						break;
					case _mastra_core_storage.TraceStatus.SUCCESS:
						conditions.push(`endedAt IS NOT NULL AND error IS NULL`);
						break;
				}
				if (filters.hasChildError !== void 0) if (filters.hasChildError) conditions.push(`EXISTS (
              SELECT 1 FROM ${tableName} c
              WHERE c.traceId = ${tableName}.traceId AND c.error IS NOT NULL
            )`);
				else conditions.push(`NOT EXISTS (
              SELECT 1 FROM ${tableName} c
              WHERE c.traceId = ${tableName}.traceId AND c.error IS NOT NULL
            )`);
			}
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const sortField = orderBy?.field ?? "startedAt";
			const sortDirection = orderBy?.direction ?? "DESC";
			let orderByClause;
			if (sortField === "endedAt") orderByClause = sortDirection === "DESC" ? `CASE WHEN ${sortField} IS NULL THEN 0 ELSE 1 END, ${sortField} DESC` : `CASE WHEN ${sortField} IS NULL THEN 1 ELSE 0 END, ${sortField} ASC`;
			else orderByClause = `${sortField} ${sortDirection}`;
			const count = await this.#db.selectTotalCount({
				tableName: _mastra_core_storage.TABLE_SPANS,
				whereClause: {
					sql: whereClause,
					args: queryArgs
				}
			});
			if (count === 0) return {
				pagination: {
					total: 0,
					page,
					perPage,
					hasMore: false
				},
				spans: []
			};
			const spans = await this.#db.selectMany({
				tableName: _mastra_core_storage.TABLE_SPANS,
				whereClause: {
					sql: whereClause,
					args: queryArgs
				},
				orderBy: orderByClause,
				offset: page * perPage,
				limit: perPage
			});
			return {
				pagination: {
					total: count,
					page,
					perPage,
					hasMore: (page + 1) * perPage < count
				},
				spans: (0, _mastra_core_storage.toTraceSpans)(spans.map((span) => transformFromSqlRow({
					tableName: _mastra_core_storage.TABLE_SPANS,
					sqlRow: span
				})))
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_TRACES", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER
			}, error);
		}
	}
	async batchCreateSpans(args) {
		try {
			const now = (/* @__PURE__ */ new Date()).toISOString();
			const records = args.records.map((record) => {
				const startedAt = record.startedAt instanceof Date ? record.startedAt.toISOString() : record.startedAt;
				const endedAt = record.endedAt instanceof Date ? record.endedAt.toISOString() : record.endedAt;
				return {
					...record,
					startedAt,
					endedAt,
					createdAt: now,
					updatedAt: now
				};
			});
			return this.#db.batchInsert({
				tableName: _mastra_core_storage.TABLE_SPANS,
				records
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BATCH_CREATE_SPANS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER
			}, error);
		}
	}
	async batchUpdateSpans(args) {
		const now = (/* @__PURE__ */ new Date()).toISOString();
		try {
			return this.#db.batchUpdate({
				tableName: _mastra_core_storage.TABLE_SPANS,
				updates: args.records.map((record) => {
					const data = { ...record.updates };
					if (data.endedAt instanceof Date) data.endedAt = data.endedAt.toISOString();
					if (data.startedAt instanceof Date) data.startedAt = data.startedAt.toISOString();
					data.updatedAt = now;
					return {
						keys: {
							spanId: record.spanId,
							traceId: record.traceId
						},
						data
					};
				})
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BATCH_UPDATE_SPANS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER
			}, error);
		}
	}
	async batchDeleteTraces(args) {
		this.assertUnscopedBatchDeleteTraces(args);
		try {
			const keys = args.traceIds.map((traceId) => ({ traceId }));
			return this.#db.batchDelete({
				tableName: _mastra_core_storage.TABLE_SPANS,
				keys
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "BATCH_DELETE_TRACES", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/prompt-blocks/index.ts
var PromptBlocksLibSQL = class extends _mastra_core_storage.PromptBlocksStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_PROMPT_BLOCKS,
			schema: _mastra_core_storage.PROMPT_BLOCKS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS,
			schema: _mastra_core_storage.PROMPT_BLOCK_VERSIONS_SCHEMA
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS,
			schema: _mastra_core_storage.PROMPT_BLOCK_VERSIONS_SCHEMA,
			ifNotExists: ["requestContextSchema"]
		});
		await this.#client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS idx_prompt_block_versions_block_version ON "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" ("blockId", "versionNumber")`);
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_PROMPT_BLOCKS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS });
	}
	async getById(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_PROMPT_BLOCKS)} FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCKS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseBlockRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_PROMPT_BLOCK", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async create(input) {
		const { promptBlock } = input;
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_PROMPT_BLOCKS,
				record: {
					id: promptBlock.id,
					status: "draft",
					activeVersionId: null,
					authorId: promptBlock.authorId ?? null,
					metadata: promptBlock.metadata ?? null,
					createdAt: now.toISOString(),
					updatedAt: now.toISOString()
				}
			});
			const { id: _id, authorId: _authorId, metadata: _metadata, ...snapshotConfig } = promptBlock;
			const versionId = crypto.randomUUID();
			await this.createVersion({
				id: versionId,
				blockId: promptBlock.id,
				versionNumber: 1,
				...snapshotConfig,
				changedFields: Object.keys(snapshotConfig),
				changeMessage: "Initial version"
			});
			return {
				id: promptBlock.id,
				status: "draft",
				activeVersionId: void 0,
				authorId: promptBlock.authorId,
				metadata: promptBlock.metadata,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_PROMPT_BLOCK", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async update(input) {
		const { id, ...updates } = input;
		try {
			const existing = await this.getById(id);
			if (!existing) throw new Error(`Prompt block with id ${id} not found`);
			const { authorId, activeVersionId, metadata, status } = updates;
			const updateData = { updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
			if (authorId !== void 0) updateData.authorId = authorId;
			if (activeVersionId !== void 0) updateData.activeVersionId = activeVersionId;
			if (status !== void 0) updateData.status = status;
			if (metadata !== void 0) updateData.metadata = {
				...existing.metadata,
				...metadata
			};
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_PROMPT_BLOCKS,
				keys: { id },
				data: updateData
			});
			const updated = await this.getById(id);
			if (!updated) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_PROMPT_BLOCK", "NOT_FOUND_AFTER_UPDATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `Prompt block ${id} not found after update`,
				details: { id }
			});
			return updated;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_PROMPT_BLOCK", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async delete(id) {
		try {
			await this.deleteVersionsByParentId(id);
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCKS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_PROMPT_BLOCK", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async list(args) {
		try {
			const { page = 0, perPage: perPageInput, orderBy, authorId, metadata, status } = args || {};
			const { field, direction } = this.parseOrderBy(orderBy);
			const conditions = [];
			const queryParams = [];
			if (status) {
				conditions.push("status = ?");
				queryParams.push(status);
			}
			if (authorId !== void 0) {
				conditions.push("authorId = ?");
				queryParams.push(authorId);
			}
			if (metadata && Object.keys(metadata).length > 0) for (const [key, value] of Object.entries(metadata)) {
				if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_PROMPT_BLOCKS", "INVALID_METADATA_KEY"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `Invalid metadata key: ${key}. Keys must be alphanumeric with underscores.`,
					details: { key }
				});
				conditions.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(typeof value === "string" ? value : JSON.stringify(value));
			}
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCKS}" ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				promptBlocks: [],
				total: 0,
				page,
				perPage: perPageInput ?? 100,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				promptBlocks: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_PROMPT_BLOCKS)} FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCKS}" ${whereClause} ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseBlockRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_PROMPT_BLOCKS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createVersion(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS,
				record: {
					id: input.id,
					blockId: input.blockId,
					versionNumber: input.versionNumber,
					name: input.name,
					description: input.description ?? null,
					content: input.content,
					rules: input.rules ?? null,
					requestContextSchema: input.requestContextSchema ?? null,
					changedFields: input.changedFields ?? null,
					changeMessage: input.changeMessage ?? null,
					createdAt: now.toISOString()
				}
			});
			return {
				...input,
				createdAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_PROMPT_BLOCK_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersion(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS)} FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_PROMPT_BLOCK_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersionByNumber(blockId, versionNumber) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS)} FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE blockId = ? AND versionNumber = ?`,
				args: [blockId, versionNumber]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_PROMPT_BLOCK_VERSION_BY_NUMBER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getLatestVersion(blockId) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS)} FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE blockId = ? ORDER BY versionNumber DESC LIMIT 1`,
				args: [blockId]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_LATEST_PROMPT_BLOCK_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listVersions(input) {
		try {
			const { blockId, page = 0, perPage: perPageInput, orderBy } = input;
			const { field, direction } = this.parseVersionOrderBy(orderBy);
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE blockId = ?`,
				args: [blockId]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				versions: [],
				total: 0,
				page,
				perPage: perPageInput ?? 20,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 20);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				versions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS)} FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE blockId = ? ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						blockId,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseVersionRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_PROMPT_BLOCK_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersion(id) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_PROMPT_BLOCK_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersionsByParentId(entityId) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE "blockId" = ?`,
				args: [entityId]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_PROMPT_BLOCK_VERSIONS_BY_BLOCK", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async countVersions(blockId) {
		try {
			const result = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_PROMPT_BLOCK_VERSIONS}" WHERE blockId = ?`,
				args: [blockId]
			});
			return Number(result.rows?.[0]?.count ?? 0);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "COUNT_PROMPT_BLOCK_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	#parseBlockRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			status: row.status ?? "draft",
			activeVersionId: row.activeVersionId ?? void 0,
			authorId: row.authorId ?? void 0,
			metadata: safeParseJSON(row.metadata),
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	#parseVersionRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			blockId: row.blockId,
			versionNumber: Number(row.versionNumber),
			name: row.name,
			description: row.description ?? void 0,
			content: row.content,
			rules: safeParseJSON(row.rules),
			requestContextSchema: safeParseJSON(row.requestContextSchema),
			changedFields: safeParseJSON(row.changedFields),
			changeMessage: row.changeMessage ?? void 0,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/domains/schedules/index.ts
function parseJson$1(val) {
	if (val == null) return void 0;
	if (typeof val === "string") try {
		return JSON.parse(val);
	} catch {
		return val;
	}
	return val;
}
function toNumber(val) {
	if (typeof val === "bigint") return Number(val);
	return Number(val);
}
function rowToSchedule(row) {
	const target = parseJson$1(row.target);
	if (!target) throw new Error(`Schedule row ${row.id} has invalid target`);
	const schedule = {
		id: String(row.id),
		target: (0, _mastra_core_storage.normalizeScheduleTarget)(target),
		cron: String(row.cron),
		status: String(row.status),
		nextFireAt: toNumber(row.next_fire_at),
		createdAt: toNumber(row.created_at),
		updatedAt: toNumber(row.updated_at)
	};
	if (row.timezone != null) schedule.timezone = String(row.timezone);
	if (row.last_fire_at != null) schedule.lastFireAt = toNumber(row.last_fire_at);
	if (row.last_run_id != null) schedule.lastRunId = String(row.last_run_id);
	const metadata = parseJson$1(row.metadata);
	if (metadata !== void 0) schedule.metadata = metadata;
	if (row.owner_type != null) schedule.ownerType = String(row.owner_type);
	if (row.owner_id != null) schedule.ownerId = String(row.owner_id);
	return schedule;
}
function rowToTrigger(row) {
	const trigger = {
		id: row.id != null ? String(row.id) : void 0,
		scheduleId: String(row.schedule_id),
		runId: row.run_id != null ? String(row.run_id) : null,
		scheduledFireAt: toNumber(row.scheduled_fire_at),
		actualFireAt: toNumber(row.actual_fire_at),
		outcome: String(row.outcome),
		triggerKind: row.trigger_kind != null ? String(row.trigger_kind) : "schedule-fire"
	};
	if (row.error != null) trigger.error = String(row.error);
	if (row.parent_trigger_id != null) trigger.parentTriggerId = String(row.parent_trigger_id);
	const metadata = parseJson$1(row.metadata);
	if (metadata !== void 0) trigger.metadata = metadata;
	return trigger;
}
var SchedulesLibSQL = class SchedulesLibSQL extends _mastra_core_storage.SchedulesStorage {
	/**
	* The fire/run history (`schedule_triggers`, one row per fire) is the growth
	* table; schedule definitions are config and excluded. Anchored on
	* `actual_fire_at`, a bigint epoch-ms column (numeric comparison, not ISO).
	*/
	static retentionTables = { triggers: {
		table: _mastra_core_storage.TABLE_SCHEDULE_TRIGGERS,
		column: "actual_fire_at",
		indexed: true,
		anchorType: "epoch-ms"
	} };
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SCHEDULES,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_SCHEDULES]
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SCHEDULE_TRIGGERS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_SCHEDULE_TRIGGERS]
		});
		await this.#client.batch([{
			sql: `CREATE INDEX IF NOT EXISTS idx_schedules_status_next_fire ON "${_mastra_core_storage.TABLE_SCHEDULES}" ("status", "next_fire_at")`,
			args: []
		}, {
			sql: `CREATE INDEX IF NOT EXISTS idx_schedule_triggers_schedule_fire ON "${_mastra_core_storage.TABLE_SCHEDULE_TRIGGERS}" ("schedule_id", "actual_fire_at")`,
			args: []
		}], "write");
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SCHEDULE_TRIGGERS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SCHEDULES });
	}
	/** Delete schedule fire history older than the `triggers` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: SchedulesLibSQL.retentionTables,
			order: ["triggers"]
		});
		return runPrune({
			db: this.#db,
			domain: "schedules",
			targets,
			options,
			logger: this.logger
		});
	}
	async createSchedule(schedule) {
		if (await this.getSchedule(schedule.id)) throw new Error(`Schedule with id "${schedule.id}" already exists`);
		await this.#db.insert({
			tableName: _mastra_core_storage.TABLE_SCHEDULES,
			record: {
				id: schedule.id,
				target: schedule.target,
				cron: schedule.cron,
				timezone: schedule.timezone ?? null,
				status: schedule.status,
				next_fire_at: schedule.nextFireAt,
				last_fire_at: schedule.lastFireAt ?? null,
				last_run_id: schedule.lastRunId ?? null,
				created_at: schedule.createdAt,
				updated_at: schedule.updatedAt,
				metadata: schedule.metadata ?? null,
				owner_type: schedule.ownerType ?? null,
				owner_id: schedule.ownerId ?? null
			}
		});
		return schedule;
	}
	async getSchedule(id) {
		const row = (await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCHEDULES)} FROM ${_mastra_core_storage.TABLE_SCHEDULES} WHERE id = ?`,
			args: [id]
		})).rows[0];
		return row ? rowToSchedule(row) : null;
	}
	async listSchedules(filter) {
		const conditions = [];
		const params = [];
		if (filter?.status) {
			conditions.push("status = ?");
			params.push(filter.status);
		}
		if (filter?.workflowId) {
			conditions.push("json_extract(target, '$.workflowId') = ?");
			params.push(filter.workflowId);
		}
		if (filter?.ownerType !== void 0) if (filter.ownerType === null) conditions.push("owner_type IS NULL");
		else {
			conditions.push("owner_type = ?");
			params.push(filter.ownerType);
		}
		if (filter?.ownerId !== void 0) if (filter.ownerId === null) conditions.push("owner_id IS NULL");
		else {
			conditions.push("owner_id = ?");
			params.push(filter.ownerId);
		}
		const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
		return (await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCHEDULES)} FROM ${_mastra_core_storage.TABLE_SCHEDULES} ${where} ORDER BY created_at ASC`,
			args: params
		})).rows.map((r) => rowToSchedule(r));
	}
	async listDueSchedules(now, limit) {
		const cap = limit ?? 100;
		return (await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCHEDULES)} FROM ${_mastra_core_storage.TABLE_SCHEDULES}
            WHERE status = ? AND next_fire_at <= ?
            ORDER BY next_fire_at ASC
            LIMIT ?`,
			args: [
				"active",
				now,
				cap
			]
		})).rows.map((r) => rowToSchedule(r));
	}
	async updateSchedule(id, patch) {
		const setClauses = [];
		const params = [];
		if ("cron" in patch && patch.cron !== void 0) {
			setClauses.push("cron = ?");
			params.push(patch.cron);
		}
		if ("timezone" in patch) {
			setClauses.push("timezone = ?");
			params.push(patch.timezone ?? null);
		}
		if ("status" in patch && patch.status !== void 0) {
			setClauses.push("status = ?");
			params.push(patch.status);
		}
		if ("nextFireAt" in patch && patch.nextFireAt !== void 0) {
			setClauses.push("next_fire_at = ?");
			params.push(patch.nextFireAt);
		}
		if ("target" in patch && patch.target !== void 0) {
			setClauses.push("target = jsonb(?)");
			params.push(JSON.stringify(patch.target));
		}
		if ("metadata" in patch) {
			setClauses.push("metadata = jsonb(?)");
			params.push(patch.metadata != null ? JSON.stringify(patch.metadata) : null);
		}
		if ("ownerType" in patch) {
			setClauses.push("owner_type = ?");
			params.push(patch.ownerType ?? null);
		}
		if ("ownerId" in patch) {
			setClauses.push("owner_id = ?");
			params.push(patch.ownerId ?? null);
		}
		setClauses.push("updated_at = ?");
		params.push(Date.now());
		params.push(id);
		if (setClauses.length === 1) {
			const existing = await this.getSchedule(id);
			if (!existing) throw new Error(`Schedule ${id} not found`);
			return existing;
		}
		await this.#client.execute({
			sql: `UPDATE ${_mastra_core_storage.TABLE_SCHEDULES} SET ${setClauses.join(", ")} WHERE id = ?`,
			args: params
		});
		const updated = await this.getSchedule(id);
		if (!updated) throw new Error(`Schedule ${id} not found`);
		return updated;
	}
	async updateScheduleNextFire(id, expectedNextFireAt, newNextFireAt, lastFireAt, lastRunId) {
		return ((await this.#client.execute({
			sql: `UPDATE ${_mastra_core_storage.TABLE_SCHEDULES}
            SET next_fire_at = ?, last_fire_at = ?, last_run_id = ?, updated_at = ?
            WHERE id = ? AND next_fire_at = ? AND status = ?`,
			args: [
				newNextFireAt,
				lastFireAt,
				lastRunId,
				Date.now(),
				id,
				expectedNextFireAt,
				"active"
			]
		})).rowsAffected ?? 0) > 0;
	}
	async deleteSchedule(id) {
		await this.#client.execute({
			sql: `DELETE FROM ${_mastra_core_storage.TABLE_SCHEDULE_TRIGGERS} WHERE schedule_id = ?`,
			args: [id]
		});
		await this.#client.execute({
			sql: `DELETE FROM ${_mastra_core_storage.TABLE_SCHEDULES} WHERE id = ?`,
			args: [id]
		});
	}
	async recordTrigger(trigger) {
		const id = trigger.id ?? crypto.randomUUID();
		await this.#db.insert({
			tableName: _mastra_core_storage.TABLE_SCHEDULE_TRIGGERS,
			record: {
				id,
				schedule_id: trigger.scheduleId,
				run_id: trigger.runId,
				scheduled_fire_at: trigger.scheduledFireAt,
				actual_fire_at: trigger.actualFireAt,
				outcome: trigger.outcome,
				error: trigger.error ?? null,
				trigger_kind: trigger.triggerKind ?? "schedule-fire",
				parent_trigger_id: trigger.parentTriggerId ?? null,
				metadata: trigger.metadata ?? null
			}
		});
	}
	async listTriggers(scheduleId, opts) {
		const conditions = ["schedule_id = ?"];
		const params = [scheduleId];
		if (opts?.fromActualFireAt != null) {
			conditions.push("actual_fire_at >= ?");
			params.push(opts.fromActualFireAt);
		}
		if (opts?.toActualFireAt != null) {
			conditions.push("actual_fire_at < ?");
			params.push(opts.toActualFireAt);
		}
		const limitClause = opts?.limit != null ? `LIMIT ${Math.floor(opts.limit)}` : "";
		return (await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCHEDULE_TRIGGERS)} FROM ${_mastra_core_storage.TABLE_SCHEDULE_TRIGGERS}
            WHERE ${conditions.join(" AND ")}
            ORDER BY actual_fire_at DESC
            ${limitClause}`,
			args: params
		})).rows.map((r) => rowToTrigger(r));
	}
};
//#endregion
//#region src/storage/domains/scorer-definitions/index.ts
var ScorerDefinitionsLibSQL = class extends _mastra_core_storage.ScorerDefinitionsStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SCORER_DEFINITIONS,
			schema: _mastra_core_storage.SCORER_DEFINITIONS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS,
			schema: _mastra_core_storage.SCORER_DEFINITION_VERSIONS_SCHEMA
		});
		await this.#client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS idx_scorer_definition_versions_scorer_version ON "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" ("scorerDefinitionId", "versionNumber")`);
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SCORER_DEFINITIONS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS });
	}
	async getById(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORER_DEFINITIONS)} FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITIONS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseScorerRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SCORER_DEFINITION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async create(input) {
		const { scorerDefinition } = input;
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_SCORER_DEFINITIONS,
				record: {
					id: scorerDefinition.id,
					status: "draft",
					activeVersionId: null,
					authorId: scorerDefinition.authorId ?? null,
					metadata: scorerDefinition.metadata ?? null,
					createdAt: now.toISOString(),
					updatedAt: now.toISOString()
				}
			});
			const { id: _id, authorId: _authorId, metadata: _metadata, ...snapshotConfig } = scorerDefinition;
			const versionId = crypto.randomUUID();
			await this.createVersion({
				id: versionId,
				scorerDefinitionId: scorerDefinition.id,
				versionNumber: 1,
				...snapshotConfig,
				changedFields: Object.keys(snapshotConfig),
				changeMessage: "Initial version"
			});
			return {
				id: scorerDefinition.id,
				status: "draft",
				activeVersionId: void 0,
				authorId: scorerDefinition.authorId,
				metadata: scorerDefinition.metadata,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_SCORER_DEFINITION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async update(input) {
		const { id, ...updates } = input;
		try {
			const existing = await this.getById(id);
			if (!existing) throw new Error(`Scorer definition with id ${id} not found`);
			const { authorId, activeVersionId, metadata, status } = updates;
			const updateData = { updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
			if (authorId !== void 0) updateData.authorId = authorId;
			if (activeVersionId !== void 0) updateData.activeVersionId = activeVersionId;
			if (status !== void 0) updateData.status = status;
			if (metadata !== void 0) updateData.metadata = {
				...existing.metadata,
				...metadata
			};
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_SCORER_DEFINITIONS,
				keys: { id },
				data: updateData
			});
			const updated = await this.getById(id);
			if (!updated) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_SCORER_DEFINITION", "NOT_FOUND_AFTER_UPDATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `Scorer definition ${id} not found after update`,
				details: { id }
			});
			return updated;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_SCORER_DEFINITION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async delete(id) {
		try {
			await this.deleteVersionsByParentId(id);
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITIONS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_SCORER_DEFINITION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async list(args) {
		try {
			const { page = 0, perPage: perPageInput, orderBy, authorId, metadata, status } = args || {};
			const { field, direction } = this.parseOrderBy(orderBy);
			const conditions = [];
			const queryParams = [];
			if (status) {
				conditions.push("status = ?");
				queryParams.push(status);
			}
			if (authorId !== void 0) {
				conditions.push("authorId = ?");
				queryParams.push(authorId);
			}
			if (metadata && Object.keys(metadata).length > 0) for (const [key, value] of Object.entries(metadata)) {
				if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SCORER_DEFINITIONS", "INVALID_METADATA_KEY"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `Invalid metadata key: ${key}. Keys must be alphanumeric with underscores.`,
					details: { key }
				});
				conditions.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(typeof value === "string" ? value : JSON.stringify(value));
			}
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITIONS}" ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				scorerDefinitions: [],
				total: 0,
				page,
				perPage: perPageInput ?? 100,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				scorerDefinitions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORER_DEFINITIONS)} FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITIONS}" ${whereClause} ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseScorerRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SCORER_DEFINITIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createVersion(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS,
				record: {
					id: input.id,
					scorerDefinitionId: input.scorerDefinitionId,
					versionNumber: input.versionNumber,
					name: input.name,
					description: input.description ?? null,
					type: input.type,
					model: input.model ?? null,
					instructions: input.instructions ?? null,
					scoreRange: input.scoreRange ?? null,
					presetConfig: input.presetConfig ?? null,
					defaultSampling: input.defaultSampling ?? null,
					changedFields: input.changedFields ?? null,
					changeMessage: input.changeMessage ?? null,
					createdAt: now.toISOString()
				}
			});
			return {
				...input,
				createdAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_SCORER_DEFINITION_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersion(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SCORER_DEFINITION_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersionByNumber(scorerDefinitionId, versionNumber) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE scorerDefinitionId = ? AND versionNumber = ?`,
				args: [scorerDefinitionId, versionNumber]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SCORER_DEFINITION_VERSION_BY_NUMBER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getLatestVersion(scorerDefinitionId) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE scorerDefinitionId = ? ORDER BY versionNumber DESC LIMIT 1`,
				args: [scorerDefinitionId]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_LATEST_SCORER_DEFINITION_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listVersions(input) {
		try {
			const { scorerDefinitionId, page = 0, perPage: perPageInput, orderBy } = input;
			const { field, direction } = this.parseVersionOrderBy(orderBy);
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE scorerDefinitionId = ?`,
				args: [scorerDefinitionId]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				versions: [],
				total: 0,
				page,
				perPage: perPageInput ?? 20,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 20);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				versions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE scorerDefinitionId = ? ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						scorerDefinitionId,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseVersionRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SCORER_DEFINITION_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersion(id) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_SCORER_DEFINITION_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersionsByParentId(entityId) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE "scorerDefinitionId" = ?`,
				args: [entityId]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_SCORER_DEFINITION_VERSIONS_BY_SCORER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async countVersions(scorerDefinitionId) {
		try {
			const result = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_SCORER_DEFINITION_VERSIONS}" WHERE scorerDefinitionId = ?`,
				args: [scorerDefinitionId]
			});
			return Number(result.rows?.[0]?.count ?? 0);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "COUNT_SCORER_DEFINITION_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	#parseScorerRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			status: row.status ?? "draft",
			activeVersionId: row.activeVersionId ?? void 0,
			authorId: row.authorId ?? void 0,
			metadata: safeParseJSON(row.metadata),
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	#parseVersionRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			scorerDefinitionId: row.scorerDefinitionId,
			versionNumber: Number(row.versionNumber),
			name: row.name,
			description: row.description ?? void 0,
			type: row.type,
			model: safeParseJSON(row.model),
			instructions: row.instructions ?? void 0,
			scoreRange: safeParseJSON(row.scoreRange),
			presetConfig: safeParseJSON(row.presetConfig),
			defaultSampling: safeParseJSON(row.defaultSampling),
			changedFields: safeParseJSON(row.changedFields),
			changeMessage: row.changeMessage ?? void 0,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/domains/scores/index.ts
/** Appends multi-tenant scope conditions (`?` placeholders). Mutates inputs. */
function appendTenancyConditions(conditions, args, filters) {
	if (filters?.organizationId !== void 0) {
		conditions.push(`organizationId = ?`);
		args.push(filters.organizationId);
	}
	if (filters?.projectId !== void 0) {
		conditions.push(`projectId = ?`);
		args.push(filters.projectId);
	}
}
var ScoresLibSQL = class ScoresLibSQL extends _mastra_core_storage.ScoresStorage {
	/**
	* Scorer results accumulate as evals run. Single table, anchored on `createdAt`.
	*/
	static retentionTables = { scorers: {
		table: _mastra_core_storage.TABLE_SCORERS,
		column: "createdAt",
		indexed: true
	} };
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SCORERS,
			schema: _mastra_core_storage.SCORERS_SCHEMA
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_SCORERS,
			schema: _mastra_core_storage.SCORERS_SCHEMA,
			ifNotExists: [
				"spanId",
				"requestContext",
				"organizationId",
				"projectId",
				"batchId",
				"datasetId",
				"datasetItemId"
			]
		});
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SCORERS });
	}
	/** Delete scorer results older than the `scorers` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: ScoresLibSQL.retentionTables,
			order: ["scorers"]
		});
		return runPrune({
			db: this.#db,
			domain: "scores",
			targets,
			options,
			logger: this.logger
		});
	}
	async listScoresByRunId({ runId, pagination, filters }) {
		try {
			const { page, perPage: perPageInput } = pagination;
			const conditions = [`runId = ?`];
			const queryParams = [runId];
			appendTenancyConditions(conditions, queryParams, filters);
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				},
				scores: []
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				scores: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORERS)} FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause} ORDER BY createdAt DESC LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformScoreRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SCORES_BY_RUN_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listScoresByScorerId({ scorerId, entityId, entityType, source, pagination, filters }) {
		try {
			const { page, perPage: perPageInput } = pagination;
			const conditions = [];
			const queryParams = [];
			if (scorerId) {
				conditions.push(`scorerId = ?`);
				queryParams.push(scorerId);
			}
			if (entityId) {
				conditions.push(`entityId = ?`);
				queryParams.push(entityId);
			}
			if (entityType) {
				conditions.push(`entityType = ?`);
				queryParams.push(entityType);
			}
			if (source) {
				conditions.push(`source = ?`);
				queryParams.push(source);
			}
			appendTenancyConditions(conditions, queryParams, filters);
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				},
				scores: []
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				scores: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORERS)} FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause} ORDER BY createdAt DESC LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformScoreRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SCORES_BY_SCORER_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	/**
	* LibSQL-specific score row transformation.
	*/
	transformScoreRow(row) {
		return (0, _mastra_core_storage.transformScoreRow)(row);
	}
	async getScoreById({ id }) {
		const result = await this.#client.execute({
			sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORERS)} FROM ${_mastra_core_storage.TABLE_SCORERS} WHERE id = ?`,
			args: [id]
		});
		return result.rows?.[0] ? this.transformScoreRow(result.rows[0]) : null;
	}
	async saveScore(score) {
		let parsedScore;
		try {
			parsedScore = _mastra_core_evals.saveScorePayloadSchema.parse(score);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SAVE_SCORE", "VALIDATION_FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				details: {
					scorer: typeof score.scorer?.id === "string" ? score.scorer.id : String(score.scorer?.id ?? "unknown"),
					entityId: score.entityId ?? "unknown",
					entityType: score.entityType ?? "unknown",
					traceId: score.traceId ?? "",
					spanId: score.spanId ?? ""
				}
			}, error);
		}
		try {
			const suppliedId = parsedScore.id;
			const id = suppliedId ?? crypto.randomUUID();
			const now = /* @__PURE__ */ new Date();
			if (suppliedId) await this.#client.execute({
				sql: `DELETE FROM ${_mastra_core_storage.TABLE_SCORERS} WHERE id = ?`,
				args: [suppliedId]
			});
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_SCORERS,
				record: {
					...parsedScore,
					id,
					createdAt: now.toISOString(),
					updatedAt: now.toISOString()
				}
			});
			return { score: {
				...parsedScore,
				id,
				createdAt: now,
				updatedAt: now
			} };
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "SAVE_SCORE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listScoresByEntityId({ entityId, entityType, pagination, filters }) {
		try {
			const { page, perPage: perPageInput } = pagination;
			const conditions = [`entityId = ?`, `entityType = ?`];
			const queryParams = [entityId, entityType];
			appendTenancyConditions(conditions, queryParams, filters);
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				pagination: {
					total: 0,
					page,
					perPage: perPageInput,
					hasMore: false
				},
				scores: []
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				scores: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORERS)} FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause} ORDER BY createdAt DESC LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformScoreRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SCORES_BY_ENTITY_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listScoresBySpan({ traceId, spanId, pagination, filters }) {
		try {
			const { page, perPage: perPageInput } = pagination;
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const conditions = [`traceId = ?`, `spanId = ?`];
			const queryParams = [traceId, spanId];
			appendTenancyConditions(conditions, queryParams, filters);
			const whereClause = `WHERE ${conditions.join(" AND ")}`;
			const countSQLResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause}`,
				args: queryParams
			});
			const total = Number(countSQLResult.rows?.[0]?.count ?? 0);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				scores: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SCORERS)} FROM ${_mastra_core_storage.TABLE_SCORERS} ${whereClause} ORDER BY createdAt DESC LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.transformScoreRow(row)) ?? [],
				pagination: {
					total,
					page,
					perPage: perPageForResponse,
					hasMore: end < total
				}
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SCORES_BY_SPAN", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/skills/index.ts
/**
* Config fields that live on version rows (from StorageSkillSnapshotType).
*/
const SNAPSHOT_FIELDS$1 = [
	"name",
	"description",
	"instructions",
	"license",
	"compatibility",
	"source",
	"references",
	"scripts",
	"assets",
	"files",
	"metadata",
	"tree"
];
var SkillsLibSQL = class extends _mastra_core_storage.SkillsStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SKILLS,
			schema: _mastra_core_storage.SKILLS_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_SKILL_VERSIONS,
			schema: _mastra_core_storage.SKILL_VERSIONS_SCHEMA
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_SKILLS,
			schema: _mastra_core_storage.SKILLS_SCHEMA,
			ifNotExists: ["visibility", "favoriteCount"]
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_SKILL_VERSIONS,
			schema: _mastra_core_storage.SKILL_VERSIONS_SCHEMA,
			ifNotExists: ["files"]
		});
		await this.#client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS idx_skill_versions_skill_version ON "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" ("skillId", "versionNumber")`);
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SKILLS });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_SKILL_VERSIONS });
	}
	async getById(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILLS)} FROM "${_mastra_core_storage.TABLE_SKILLS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseSkillRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SKILL", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async create(input) {
		const { skill } = input;
		try {
			const now = /* @__PURE__ */ new Date();
			const visibility = skill.visibility ?? (skill.authorId ? "private" : void 0);
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_SKILLS,
				record: {
					id: skill.id,
					status: "draft",
					activeVersionId: null,
					authorId: skill.authorId ?? null,
					visibility: visibility ?? null,
					favoriteCount: 0,
					createdAt: now.toISOString(),
					updatedAt: now.toISOString()
				}
			});
			const { id: _id, authorId: _authorId, visibility: _visibility, ...snapshotConfig } = skill;
			const versionId = crypto.randomUUID();
			try {
				await this.createVersion({
					id: versionId,
					skillId: skill.id,
					versionNumber: 1,
					...snapshotConfig,
					changedFields: Object.keys(snapshotConfig),
					changeMessage: "Initial version"
				});
			} catch (versionError) {
				await this.#db.delete({
					tableName: _mastra_core_storage.TABLE_SKILLS,
					keys: { id: skill.id }
				});
				throw versionError;
			}
			return {
				id: skill.id,
				status: "draft",
				activeVersionId: void 0,
				authorId: skill.authorId,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_SKILL", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async update(input) {
		const { id, ...updates } = input;
		try {
			if (!await this.getById(id)) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_SKILL", "NOT_FOUND"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				text: `Skill ${id} not found`,
				details: { skillId: id }
			});
			const { authorId, visibility, activeVersionId, status, ...rawConfigFields } = updates;
			const configFields = {};
			for (const [key, value] of Object.entries(rawConfigFields)) if (value !== void 0) configFields[key] = value;
			const configFieldNames = SNAPSHOT_FIELDS$1;
			const hasConfigUpdate = configFieldNames.some((field) => field in configFields);
			const updateData = { updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
			if (authorId !== void 0) updateData.authorId = authorId;
			if (visibility !== void 0) updateData.visibility = visibility;
			if (activeVersionId !== void 0) {
				updateData.activeVersionId = activeVersionId;
				if (status === void 0) updateData.status = "published";
			}
			if (status !== void 0) updateData.status = status;
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_SKILLS,
				keys: { id },
				data: updateData
			});
			if (hasConfigUpdate) {
				const latestVersion = await this.getLatestVersion(id);
				if (!latestVersion) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_SKILL", "NO_VERSIONS"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `No versions found for skill ${id}`,
					details: { skillId: id }
				});
				const { id: _versionId, skillId: _skillId, versionNumber: _versionNumber, changedFields: _changedFields, changeMessage: _changeMessage, createdAt: _createdAt, ...latestConfig } = latestVersion;
				const newConfig = {
					...latestConfig,
					...configFields
				};
				const changedFields = configFieldNames.filter((field) => field in configFields && !(0, _mastra_core_storage_domains_skills.skillSnapshotFieldValuesEqual)(configFields[field], latestConfig[field]));
				if (changedFields.length > 0) {
					const newVersionId = crypto.randomUUID();
					await this.createVersion({
						id: newVersionId,
						skillId: id,
						versionNumber: latestVersion.versionNumber + 1,
						...newConfig,
						changedFields,
						changeMessage: `Updated ${changedFields.join(", ")}`
					});
				}
			}
			const updated = await this.getById(id);
			if (!updated) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_SKILL", "NOT_FOUND_AFTER_UPDATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `Skill ${id} not found after update`,
				details: { id }
			});
			return updated;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_SKILL", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async delete(id) {
		try {
			await this.deleteVersionsByParentId(id);
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_SKILLS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_SKILL", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async list(args) {
		try {
			const { page = 0, perPage: perPageInput, orderBy, authorId, visibility, status, entityIds, pinFavoritedFor, favoritedOnly } = args || {};
			const { field, direction } = this.parseOrderBy(orderBy);
			if (entityIds && entityIds.length === 0) return {
				skills: [],
				total: 0,
				page,
				perPage: perPageInput ?? 100,
				hasMore: false
			};
			const conditions = [];
			const queryParams = [];
			if (authorId !== void 0) {
				conditions.push("s_e.authorId = ?");
				queryParams.push(authorId);
			}
			if (visibility !== void 0) {
				conditions.push("s_e.visibility = ?");
				queryParams.push(visibility);
			}
			if (status !== void 0) {
				conditions.push("s_e.status = ?");
				queryParams.push(status);
			}
			if (entityIds && entityIds.length > 0) {
				const placeholders = entityIds.map(() => "?").join(", ");
				conditions.push(`s_e.id IN (${placeholders})`);
				queryParams.push(...entityIds);
			}
			const joinUserId = pinFavoritedFor;
			const useJoin = Boolean(joinUserId);
			let joinClause = "";
			const joinParams = [];
			if (useJoin && joinUserId) {
				joinClause = `LEFT JOIN "${_mastra_core_storage.TABLE_FAVORITES}" st ON st."entityType" = 'skill' AND st."entityId" = s_e.id AND st."userId" = ?`;
				joinParams.push(joinUserId);
				if (favoritedOnly) conditions.push("st.\"userId\" IS NOT NULL");
			} else if (favoritedOnly) conditions.push("1=0");
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_SKILLS}" s_e ${joinClause} ${whereClause}`,
				args: [...joinParams, ...queryParams]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				skills: [],
				total: 0,
				page,
				perPage: perPageInput ?? 100,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			const orderByParts = [];
			if (useJoin && joinUserId) orderByParts.push(`(st."userId" IS NOT NULL) DESC`);
			orderByParts.push(`s_e."${field}" ${direction}`);
			orderByParts.push(`s_e."id" ASC`);
			const orderByClause = `ORDER BY ${orderByParts.join(", ")}`;
			const selectCols = buildSelectColumnsWithAlias(_mastra_core_storage.TABLE_SKILLS, "s_e");
			return {
				skills: (await this.#client.execute({
					sql: `SELECT ${selectCols} FROM "${_mastra_core_storage.TABLE_SKILLS}" s_e ${joinClause} ${whereClause} ${orderByClause} LIMIT ? OFFSET ?`,
					args: [
						...joinParams,
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseSkillRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SKILLS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createVersion(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#client.execute({
				sql: `INSERT INTO "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" (
          id, "skillId", "versionNumber",
          name, description, instructions, license, compatibility,
          source, "references", scripts, assets, files, metadata, tree,
          "changedFields", "changeMessage", "createdAt"
        ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
				args: [
					input.id,
					input.skillId,
					input.versionNumber,
					input.name,
					input.description ?? null,
					input.instructions ?? null,
					input.license ?? null,
					input.compatibility ? JSON.stringify(input.compatibility) : null,
					input.source ? JSON.stringify(input.source) : null,
					input.references ? JSON.stringify(input.references) : null,
					input.scripts ? JSON.stringify(input.scripts) : null,
					input.assets ? JSON.stringify(input.assets) : null,
					input.files ? JSON.stringify(input.files) : null,
					input.metadata ? JSON.stringify(input.metadata) : null,
					input.tree ? JSON.stringify(input.tree) : null,
					input.changedFields ? JSON.stringify(input.changedFields) : null,
					input.changeMessage ?? null,
					now.toISOString()
				]
			});
			return {
				...input,
				createdAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_SKILL_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersion(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILL_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SKILL_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersions(ids) {
		if (ids.length === 0) return [];
		try {
			const placeholders = ids.map(() => "?").join(", ");
			return ((await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILL_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE id IN (${placeholders})`,
				args: ids
			})).rows ?? []).map((row) => this.#parseVersionRow(row));
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SKILL_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: { count: ids.length }
			}, error);
		}
	}
	async getVersionByNumber(skillId, versionNumber) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILL_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE skillId = ? AND versionNumber = ?`,
				args: [skillId, versionNumber]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_SKILL_VERSION_BY_NUMBER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getLatestVersion(skillId) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILL_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE skillId = ? ORDER BY versionNumber DESC LIMIT 1`,
				args: [skillId]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_LATEST_SKILL_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listVersions(input) {
		try {
			const { skillId, page = 0, perPage: perPageInput, orderBy } = input;
			const { field, direction } = this.parseVersionOrderBy(orderBy);
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE skillId = ?`,
				args: [skillId]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				versions: [],
				total: 0,
				page,
				perPage: perPageInput ?? 20,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 20);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				versions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_SKILL_VERSIONS)} FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE skillId = ? ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						skillId,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseVersionRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_SKILL_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersion(id) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_SKILL_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersionsByParentId(entityId) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE "skillId" = ?`,
				args: [entityId]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_SKILL_VERSIONS_BY_SKILL", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async countVersions(skillId) {
		try {
			const result = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_SKILL_VERSIONS}" WHERE skillId = ?`,
				args: [skillId]
			});
			return Number(result.rows?.[0]?.count ?? 0);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "COUNT_SKILL_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	#parseSkillRow(row) {
		return {
			id: row.id,
			status: row.status ?? "draft",
			activeVersionId: row.activeVersionId ?? void 0,
			authorId: row.authorId ?? void 0,
			visibility: row.visibility ?? void 0,
			favoriteCount: row.favoriteCount === null || row.favoriteCount === void 0 ? 0 : Number(row.favoriteCount),
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	#parseVersionRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			skillId: row.skillId,
			versionNumber: Number(row.versionNumber),
			name: row.name,
			description: row.description ?? void 0,
			instructions: row.instructions ?? void 0,
			license: row.license ?? void 0,
			compatibility: safeParseJSON(row.compatibility),
			source: safeParseJSON(row.source),
			references: safeParseJSON(row.references),
			scripts: safeParseJSON(row.scripts),
			assets: safeParseJSON(row.assets),
			files: safeParseJSON(row.files),
			metadata: safeParseJSON(row.metadata),
			tree: safeParseJSON(row.tree),
			changedFields: safeParseJSON(row.changedFields),
			changeMessage: row.changeMessage ?? void 0,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/domains/thread-state/index.ts
/**
* LibSQL implementation of {@link ThreadStateStorage}.
*
* Stores per-thread, per-type state in `mastra_thread_state`, keyed by the
* composite primary key `(threadId, type)`. The `value` column holds the JSON
* payload (e.g. the task list for `type = 'task'`).
*/
var ThreadStateLibSQL = class ThreadStateLibSQL extends _mastra_core_storage.ThreadStateStorage {
	/**
	* `thread_state` grows as a side effect of thread activity (one row per
	* thread per state type). It anchors on `updatedAt` (last activity), so state
	* for a thread that is still being appended to is not pruned by creation age.
	*/
	static retentionTables = { threadState: {
		table: _mastra_core_storage.TABLE_THREAD_STATE,
		column: "updatedAt",
		indexed: true
	} };
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_THREAD_STATE,
			schema: _mastra_core_storage.THREAD_STATE_SCHEMA,
			compositePrimaryKey: ["threadId", "type"]
		});
	}
	/** Delete thread state older than the `threadState` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: ThreadStateLibSQL.retentionTables,
			order: ["threadState"]
		});
		return runPrune({
			db: this.#db,
			domain: "threadState",
			targets,
			options,
			logger: this.logger
		});
	}
	async dangerouslyClearAll() {
		try {
			await this.#client.execute(`DELETE FROM "${_mastra_core_storage.TABLE_THREAD_STATE}"`);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "THREAD_STATE_CLEAR_ALL", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getState({ threadId, type }) {
		try {
			const raw = (await this.#client.execute({
				sql: `SELECT "value" FROM "${_mastra_core_storage.TABLE_THREAD_STATE}" WHERE "threadId" = ? AND "type" = ? LIMIT 1`,
				args: [threadId, type]
			})).rows?.[0]?.value;
			if (raw === void 0 || raw === null) return void 0;
			return typeof raw === "string" ? JSON.parse(raw) : raw;
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "THREAD_STATE_GET", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId,
					type
				}
			}, error);
		}
	}
	async setState({ threadId, type, value }) {
		const now = (/* @__PURE__ */ new Date()).toISOString();
		const serialized = JSON.stringify(value ?? null);
		try {
			await this.#client.execute({
				sql: `INSERT INTO "${_mastra_core_storage.TABLE_THREAD_STATE}" ("threadId", "type", "value", "createdAt", "updatedAt")
              VALUES (?, ?, ?, ?, ?)
              ON CONFLICT ("threadId", "type")
              DO UPDATE SET "value" = excluded."value", "updatedAt" = excluded."updatedAt"`,
				args: [
					threadId,
					type,
					serialized,
					now,
					now
				]
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "THREAD_STATE_SET", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId,
					type
				}
			}, error);
		}
	}
	async deleteState({ threadId, type }) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_THREAD_STATE}" WHERE "threadId" = ? AND "type" = ?`,
				args: [threadId, type]
			});
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "THREAD_STATE_DELETE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					threadId,
					type
				}
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/tool-provider-connections/index.ts
function normaliseScope(raw) {
	const value = raw == null ? "per-author" : String(raw);
	if (value === "shared") return "shared";
	if (value === "caller-supplied") return "caller-supplied";
	return "per-author";
}
function rowToToolProviderConnection(row) {
	return {
		authorId: String(row.authorId),
		providerId: String(row.providerId),
		toolkit: String(row.toolkit),
		connectionId: String(row.connectionId),
		label: row.label == null ? null : String(row.label),
		scope: normaliseScope(row.scope),
		createdAt: new Date(String(row.createdAt)),
		updatedAt: new Date(String(row.updatedAt))
	};
}
var ToolProviderConnectionsLibSQL = class extends _mastra_core_storage.ToolProviderConnectionsStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS,
			schema: _mastra_core_storage.TOOL_PROVIDER_CONNECTIONS_SCHEMA,
			compositePrimaryKey: [
				"authorId",
				"providerId",
				"connectionId"
			]
		});
		await this.#client.execute(`CREATE INDEX IF NOT EXISTS idx_tool_provider_connections_author ON "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}" ("authorId", "providerId", "toolkit")`);
	}
	async dangerouslyClearAll() {
		await this.#client.execute(`DELETE FROM "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}"`);
	}
	async getConnectionById({ authorId, providerId, connectionId }) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT * FROM "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}" WHERE "authorId" = ? AND "providerId" = ? AND "connectionId" = ? LIMIT 1`,
				args: [
					authorId,
					providerId,
					connectionId
				]
			})).rows?.[0];
			if (!row) return null;
			return rowToToolProviderConnection(row);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "TOOL_PROVIDER_CONNECTION_GET", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					authorId,
					providerId,
					connectionId
				}
			}, error);
		}
	}
	async upsertConnection(input) {
		const { authorId, providerId, toolkit, connectionId, label } = input;
		const now = /* @__PURE__ */ new Date();
		const nowIso = now.toISOString();
		const labelValue = label == null ? null : label;
		try {
			const tx = await this.#client.transaction("write");
			try {
				const existingRow = (await tx.execute({
					sql: `SELECT "createdAt", "scope" FROM "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}" WHERE "authorId" = ? AND "providerId" = ? AND "connectionId" = ? LIMIT 1`,
					args: [
						authorId,
						providerId,
						connectionId
					]
				})).rows?.[0];
				const createdAt = existingRow ? String(existingRow.createdAt) : nowIso;
				const existingScope = existingRow && existingRow.scope != null ? normaliseScope(existingRow.scope) : void 0;
				const scope = input.scope ?? existingScope ?? "per-author";
				if (existingRow) await tx.execute({
					sql: `UPDATE "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}" SET "toolkit" = ?, "label" = ?, "scope" = ?, "updatedAt" = ? WHERE "authorId" = ? AND "providerId" = ? AND "connectionId" = ?`,
					args: [
						toolkit,
						labelValue,
						scope,
						nowIso,
						authorId,
						providerId,
						connectionId
					]
				});
				else await tx.execute({
					sql: `INSERT INTO "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}" ("authorId", "providerId", "toolkit", "connectionId", "label", "scope", "createdAt", "updatedAt") VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
					args: [
						authorId,
						providerId,
						toolkit,
						connectionId,
						labelValue,
						scope,
						createdAt,
						nowIso
					]
				});
				await tx.commit();
				return {
					authorId,
					providerId,
					toolkit,
					connectionId,
					label: labelValue,
					scope,
					createdAt: new Date(createdAt),
					updatedAt: now
				};
			} catch (error) {
				if (!tx.closed) await tx.rollback();
				throw error;
			}
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "TOOL_PROVIDER_CONNECTION_UPSERT", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					authorId,
					providerId,
					connectionId
				}
			}, error);
		}
	}
	async listConnectionsByAuthor({ authorId, providerId, toolkit, scope }) {
		try {
			const clauses = [];
			const args = [];
			if (authorId !== void 0) {
				clauses.push("\"authorId\" = ?");
				args.push(authorId);
			}
			if (providerId) {
				clauses.push("\"providerId\" = ?");
				args.push(providerId);
			}
			if (toolkit) {
				clauses.push("\"toolkit\" = ?");
				args.push(toolkit);
			}
			if (scope) {
				clauses.push("\"scope\" = ?");
				args.push(scope);
			}
			const whereClause = clauses.length > 0 ? ` WHERE ${clauses.join(" AND ")}` : "";
			return ((await this.#client.execute({
				sql: `SELECT * FROM "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}"${whereClause}`,
				args
			})).rows ?? []).map((row) => rowToToolProviderConnection(row));
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "TOOL_PROVIDER_CONNECTION_LIST", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					authorId: authorId ?? "",
					providerId: providerId ?? "",
					toolkit: toolkit ?? ""
				}
			}, error);
		}
	}
	async deleteConnection({ authorId, providerId, connectionId }) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_TOOL_PROVIDER_CONNECTIONS}" WHERE "authorId" = ? AND "providerId" = ? AND "connectionId" = ?`,
				args: [
					authorId,
					providerId,
					connectionId
				]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "TOOL_PROVIDER_CONNECTION_DELETE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
				details: {
					authorId,
					providerId,
					connectionId
				}
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/workflow-definitions/index.ts
function parseJson(val, column, rowId) {
	if (val == null) return void 0;
	if (typeof val === "string") try {
		return JSON.parse(val);
	} catch {
		throw new Error(`Workflow definition row "${String(rowId)}" has malformed JSON in column "${column}".`);
	}
	return val;
}
function workflowDefinitionSelectColumns() {
	return buildSelectColumns(_mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS).replace("json(\"schedule\") as \"schedule\"", "\"schedule\"");
}
function rowToDefinition(row) {
	const inputSchema = parseJson(row.inputSchema, "inputSchema", row.id);
	const outputSchema = parseJson(row.outputSchema, "outputSchema", row.id);
	const graph = parseJson(row.graph, "graph", row.id);
	if (inputSchema === void 0 || outputSchema === void 0 || graph === void 0) throw new Error(`Workflow definition row "${row.id}" is missing required JSON columns.`);
	const def = {
		id: String(row.id),
		inputSchema,
		outputSchema,
		graph,
		status: row.status,
		source: row.source,
		createdAt: new Date(row.createdAt),
		updatedAt: new Date(row.updatedAt)
	};
	if (row.description != null) def.description = String(row.description);
	const metadata = parseJson(row.metadata, "metadata", row.id);
	if (metadata !== void 0) def.metadata = metadata;
	const stateSchema = parseJson(row.stateSchema, "stateSchema", row.id);
	if (stateSchema !== void 0) def.stateSchema = stateSchema;
	const requestContextSchema = parseJson(row.requestContextSchema, "requestContextSchema", row.id);
	if (requestContextSchema !== void 0) def.requestContextSchema = requestContextSchema;
	try {
		const schedule = parseJson(row.schedule, "schedule", row.id);
		if (schedule != null) def.schedule = schedule;
	} catch {
		def.schedule = row.schedule;
	}
	if (row.authorId != null) def.authorId = String(row.authorId);
	return def;
}
var WorkflowDefinitionsLibSQL = class extends _mastra_core_storage.WorkflowDefinitionsStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS]
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS,
			schema: _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS],
			ifNotExists: ["schedule"]
		});
		await this.#client.execute({
			sql: `CREATE INDEX IF NOT EXISTS idx_workflow_definitions_status ON "${_mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS}" ("status")`,
			args: []
		});
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS });
	}
	async upsert(input) {
		const now = /* @__PURE__ */ new Date();
		if (!await this.get(input.id)) {
			if (!("inputSchema" in input) || !input.inputSchema) throw new Error(`Cannot create workflow definition "${input.id}": inputSchema is required.`);
			if (!("outputSchema" in input) || !input.outputSchema) throw new Error(`Cannot create workflow definition "${input.id}": outputSchema is required.`);
			if (!("graph" in input) || !input.graph) throw new Error(`Cannot create workflow definition "${input.id}": graph is required.`);
			const record = {
				id: input.id,
				description: input.description ?? null,
				metadata: input.metadata ?? null,
				inputSchema: input.inputSchema,
				outputSchema: input.outputSchema,
				stateSchema: input.stateSchema ?? null,
				requestContextSchema: input.requestContextSchema ?? null,
				graph: input.graph,
				schedule: "schedule" in input ? input.schedule ?? null : null,
				status: "active",
				source: "storage",
				authorId: "authorId" in input ? input.authorId ?? null : null,
				createdAt: now,
				updatedAt: now
			};
			try {
				await this.#db.insertOnly({
					tableName: _mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS,
					record
				});
			} catch (error) {
				if (!await this.get(input.id)) throw error;
				return this.#applyUpdate(input, now);
			}
			const created = await this.get(input.id);
			if (!created) throw new Error(`Failed to persist workflow definition "${input.id}".`);
			return created;
		}
		return this.#applyUpdate(input, now);
	}
	async #applyUpdate(input, now) {
		const data = { updatedAt: now };
		if ("description" in input && input.description !== void 0) data.description = input.description;
		if ("metadata" in input && input.metadata !== void 0) data.metadata = input.metadata;
		if ("inputSchema" in input && input.inputSchema !== void 0) data.inputSchema = input.inputSchema;
		if ("outputSchema" in input && input.outputSchema !== void 0) data.outputSchema = input.outputSchema;
		if ("stateSchema" in input && input.stateSchema !== void 0) data.stateSchema = input.stateSchema;
		if ("requestContextSchema" in input && input.requestContextSchema !== void 0) data.requestContextSchema = input.requestContextSchema;
		if ("graph" in input && input.graph !== void 0) data.graph = input.graph;
		if ("schedule" in input && input.schedule !== void 0) data.schedule = input.schedule;
		if ("status" in input && input.status !== void 0) data.status = input.status;
		if ("authorId" in input && input.authorId !== void 0) data.authorId = input.authorId;
		await this.#db.update({
			tableName: _mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS,
			keys: { id: input.id },
			data
		});
		const updated = await this.get(input.id);
		if (!updated) throw new Error(`Failed to update workflow definition "${input.id}".`);
		return updated;
	}
	async get(id) {
		const row = (await this.#client.execute({
			sql: `SELECT ${workflowDefinitionSelectColumns()} FROM "${_mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS}" WHERE id = ?`,
			args: [id]
		})).rows[0];
		return row ? rowToDefinition(row) : null;
	}
	async list(args) {
		const conditions = [];
		const params = [];
		if (args?.status) {
			conditions.push("status = ?");
			params.push(args.status);
		}
		if (args?.authorId !== void 0) {
			conditions.push("authorId = ?");
			params.push(args.authorId);
		}
		const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
		const definitions = (await this.#client.execute({
			sql: `SELECT ${workflowDefinitionSelectColumns()} FROM "${_mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS}" ${where} ORDER BY updatedAt DESC`,
			args: params
		})).rows.map((row) => rowToDefinition(row));
		return {
			definitions,
			total: definitions.length
		};
	}
	async delete(id) {
		await this.#client.execute({
			sql: `DELETE FROM "${_mastra_core_storage.TABLE_WORKFLOW_DEFINITIONS}" WHERE id = ?`,
			args: [id]
		});
	}
};
//#endregion
//#region src/storage/domains/workflows/index.ts
var WorkflowsLibSQL = class WorkflowsLibSQL extends _mastra_core_storage.WorkflowsStorage {
	/**
	* Workflow run snapshots accumulate as runs execute. Anchored on `updatedAt`
	* (last activity) so suspended/long-running runs are not pruned by start age.
	*/
	static retentionTables = { workflowSnapshot: {
		table: _mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT,
		column: "updatedAt",
		indexed: true
	} };
	#db;
	#client;
	executeWithRetry;
	constructor(config) {
		super();
		const client = resolveClient(config);
		const maxRetries = config.maxRetries ?? 5;
		const initialBackoffMs = config.initialBackoffMs ?? 500;
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries,
			initialBackoffMs
		});
		this.executeWithRetry = createExecuteWriteOperationWithRetry({
			logger: this.logger,
			maxRetries,
			initialBackoffMs
		});
		this.setupPragmaSettings().catch((err) => this.logger.warn("LibSQL Workflows: Failed to setup PRAGMA settings.", err));
	}
	supportsConcurrentUpdates() {
		return true;
	}
	parseWorkflowRun(row) {
		let parsedSnapshot = row.snapshot;
		if (typeof parsedSnapshot === "string") try {
			parsedSnapshot = JSON.parse(row.snapshot);
		} catch (e) {
			this.logger.warn(`Failed to parse snapshot for workflow ${row.workflow_name}: ${e}`);
		}
		return {
			workflowName: row.workflow_name,
			runId: row.run_id,
			snapshot: parsedSnapshot,
			resourceId: row.resourceId,
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	async init() {
		const schema = _mastra_core_storage.TABLE_SCHEMAS[_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT];
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT,
			schema
		});
		await this.#db.alterTable({
			tableName: _mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT,
			schema,
			ifNotExists: ["resourceId"]
		});
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT });
	}
	/** Delete workflow snapshots older than the `workflowSnapshot` policy's `maxAge`, batched. */
	async prune(policies, options) {
		const targets = resolveTargets({
			policies,
			descriptor: WorkflowsLibSQL.retentionTables,
			order: ["workflowSnapshot"]
		});
		return runPrune({
			db: this.#db,
			domain: "workflows",
			targets,
			options,
			logger: this.logger
		});
	}
	async setupPragmaSettings() {
		try {
			await this.#client.execute("PRAGMA busy_timeout = 10000;");
			this.logger.debug("LibSQL Workflows: PRAGMA busy_timeout=10000 set.");
			try {
				await this.#client.execute("PRAGMA journal_mode = WAL;");
				this.logger.debug("LibSQL Workflows: PRAGMA journal_mode=WAL set.");
			} catch {
				this.logger.debug("LibSQL Workflows: WAL mode not supported, using default journal mode.");
			}
			try {
				await this.#client.execute("PRAGMA synchronous = NORMAL;");
				this.logger.debug("LibSQL Workflows: PRAGMA synchronous=NORMAL set.");
			} catch {
				this.logger.debug("LibSQL Workflows: Failed to set synchronous mode.");
			}
		} catch (err) {
			this.logger.warn("LibSQL Workflows: Failed to set PRAGMA settings.", err);
		}
	}
	async updateWorkflowResults({ workflowName, runId, stepId, result, requestContext }) {
		return this.executeWithRetry(() => withClientWriteLock(this.#client, async () => {
			const tx = await this.#client.transaction("write");
			try {
				const existingSnapshotResult = await tx.execute({
					sql: `SELECT json(snapshot) as snapshot FROM ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} WHERE workflow_name = ? AND run_id = ?`,
					args: [workflowName, runId]
				});
				let snapshot;
				if (!existingSnapshotResult.rows?.[0]) snapshot = {
					context: {},
					activePaths: [],
					timestamp: Date.now(),
					suspendedPaths: {},
					activeStepsPath: {},
					resumeLabels: {},
					serializedStepGraph: [],
					status: "pending",
					value: {},
					waitingPaths: {},
					runId,
					requestContext: {}
				};
				else {
					const existingSnapshot = existingSnapshotResult.rows[0].snapshot;
					snapshot = typeof existingSnapshot === "string" ? JSON.parse(existingSnapshot) : existingSnapshot;
				}
				(0, _mastra_core_storage.mergeWorkflowStepResult)({
					snapshot,
					stepId,
					result,
					requestContext
				});
				const now = (/* @__PURE__ */ new Date()).toISOString();
				await tx.execute({
					sql: `INSERT INTO ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} (workflow_name, run_id, snapshot, createdAt, updatedAt)
                VALUES (?, ?, jsonb(?), ?, ?)
                ON CONFLICT(workflow_name, run_id)
                DO UPDATE SET snapshot = excluded.snapshot, updatedAt = excluded.updatedAt`,
					args: [
						workflowName,
						runId,
						safeStringify(snapshot),
						now,
						now
					]
				});
				await tx.commit();
				return snapshot.context;
			} catch (error) {
				if (!tx.closed) await tx.rollback();
				throw error;
			}
		}), "updateWorkflowResults");
	}
	async updateWorkflowState({ workflowName, runId, opts }) {
		return this.executeWithRetry(() => withClientWriteLock(this.#client, async () => {
			const tx = await this.#client.transaction("write");
			try {
				const existingSnapshotResult = await tx.execute({
					sql: `SELECT json(snapshot) as snapshot FROM ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} WHERE workflow_name = ? AND run_id = ?`,
					args: [workflowName, runId]
				});
				if (!existingSnapshotResult.rows?.[0]) {
					await tx.rollback();
					return;
				}
				const existingSnapshot = existingSnapshotResult.rows[0].snapshot;
				const snapshot = typeof existingSnapshot === "string" ? JSON.parse(existingSnapshot) : existingSnapshot;
				if (!snapshot || !snapshot?.context) {
					await tx.rollback();
					throw new Error(`Snapshot not found for runId ${runId}`);
				}
				const { expectedStatus, ...state } = opts;
				if (!(0, _mastra_core_storage.matchesExpectedWorkflowStatus)(snapshot.status, expectedStatus)) {
					await tx.rollback();
					return;
				}
				const updatedSnapshot = {
					...snapshot,
					...state
				};
				await tx.execute({
					sql: `UPDATE ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} SET snapshot = jsonb(?) WHERE workflow_name = ? AND run_id = ?`,
					args: [
						safeStringify(updatedSnapshot),
						workflowName,
						runId
					]
				});
				await tx.commit();
				return updatedSnapshot;
			} catch (error) {
				if (!tx.closed) await tx.rollback();
				throw error;
			}
		}), "updateWorkflowState");
	}
	async persistWorkflowSnapshot({ workflowName, runId, resourceId, snapshot, createdAt, updatedAt }) {
		const now = /* @__PURE__ */ new Date();
		const createdAtValue = (createdAt ?? now).toISOString();
		const updatedAtValue = (updatedAt ?? now).toISOString();
		this.logger.debug("Persisting workflow snapshot", {
			workflowName,
			runId
		});
		await this.executeWithRetry(() => withClientWriteLock(this.#client, () => this.#client.execute({
			sql: `INSERT INTO ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} (workflow_name, run_id, resourceId, snapshot, createdAt, updatedAt)
                VALUES (?, ?, ?, jsonb(?), ?, ?)
                ON CONFLICT(workflow_name, run_id)
                DO UPDATE SET resourceId = COALESCE(excluded.resourceId, ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT}.resourceId), snapshot = excluded.snapshot, updatedAt = excluded.updatedAt`,
			args: [
				workflowName,
				runId,
				resourceId ?? null,
				safeStringify(snapshot),
				createdAtValue,
				updatedAtValue
			]
		})), "persistWorkflowSnapshot");
	}
	async loadWorkflowSnapshot({ workflowName, runId }) {
		this.logger.debug("Loading workflow snapshot", {
			workflowName,
			runId
		});
		const d = await this.#db.select({
			tableName: _mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT,
			keys: {
				workflow_name: workflowName,
				run_id: runId
			}
		});
		return d ? d.snapshot : null;
	}
	async getWorkflowRunById({ runId, workflowName }) {
		const conditions = [];
		const args = [];
		if (runId) {
			conditions.push("run_id = ?");
			args.push(runId);
		}
		if (workflowName) {
			conditions.push("workflow_name = ?");
			args.push(workflowName);
		}
		const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
		try {
			const result = await this.#client.execute({
				sql: `SELECT workflow_name, run_id, resourceId, json(snapshot) as snapshot, createdAt, updatedAt FROM ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} ${whereClause} ORDER BY createdAt DESC LIMIT 1`,
				args
			});
			if (!result.rows?.[0]) return null;
			return this.parseWorkflowRun(result.rows[0]);
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_WORKFLOW_RUN_BY_ID", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteWorkflowRunById({ runId, workflowName }) {
		return this.executeWithRetry(async () => {
			try {
				await this.#client.execute({
					sql: `DELETE FROM ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} WHERE workflow_name = ? AND run_id = ?`,
					args: [workflowName, runId]
				});
			} catch (error) {
				throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_WORKFLOW_RUN_BY_ID", "FAILED"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.THIRD_PARTY,
					details: {
						runId,
						workflowName
					}
				}, error);
			}
		}, "deleteWorkflowRunById");
	}
	async listWorkflowRuns({ workflowName, fromDate, toDate, page, perPage, resourceId, status } = {}) {
		try {
			const conditions = [];
			const args = [];
			if (workflowName) {
				conditions.push("workflow_name = ?");
				args.push(workflowName);
			}
			if (status) {
				conditions.push("json_extract(snapshot, '$.status') = ?");
				args.push(status);
			}
			if (fromDate) {
				conditions.push("createdAt >= ?");
				args.push(fromDate.toISOString());
			}
			if (toDate) {
				conditions.push("createdAt <= ?");
				args.push(toDate.toISOString());
			}
			if (resourceId) if (await this.#db.hasColumn(_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT, "resourceId")) {
				conditions.push("resourceId = ?");
				args.push(resourceId);
			} else this.logger.warn(`[${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT}] resourceId column not found. Skipping resourceId filter.`);
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			let total = 0;
			const usePagination = typeof perPage === "number" && typeof page === "number";
			if (usePagination) {
				const countResult = await this.#client.execute({
					sql: `SELECT COUNT(*) as count FROM ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} ${whereClause}`,
					args
				});
				total = Number(countResult.rows?.[0]?.count ?? 0);
			}
			const normalizedPerPage = usePagination ? (0, _mastra_core_storage.normalizePerPage)(perPage, Number.MAX_SAFE_INTEGER) : 0;
			const offset = usePagination ? page * normalizedPerPage : 0;
			const runs = ((await this.#client.execute({
				sql: `SELECT workflow_name, run_id, resourceId, json(snapshot) as snapshot, createdAt, updatedAt FROM ${_mastra_core_storage.TABLE_WORKFLOW_SNAPSHOT} ${whereClause} ORDER BY createdAt DESC${usePagination ? ` LIMIT ? OFFSET ?` : ""}`,
				args: usePagination ? [
					...args,
					normalizedPerPage,
					offset
				] : args
			})).rows || []).map((row) => this.parseWorkflowRun(row));
			return {
				runs,
				total: total || runs.length
			};
		} catch (error) {
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_WORKFLOW_RUNS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
};
//#endregion
//#region src/storage/domains/workspaces/index.ts
/**
* Config fields that live on version rows (from StorageWorkspaceSnapshotType).
*/
const SNAPSHOT_FIELDS = [
	"name",
	"description",
	"filesystem",
	"sandbox",
	"mounts",
	"search",
	"skills",
	"tools",
	"autoSync",
	"operationTimeout"
];
var WorkspacesLibSQL = class extends _mastra_core_storage.WorkspacesStorage {
	#db;
	#client;
	constructor(config) {
		super();
		const client = resolveClient(config);
		this.#client = client;
		this.#db = new LibSQLDB({
			client,
			maxRetries: config.maxRetries,
			initialBackoffMs: config.initialBackoffMs
		});
	}
	async init() {
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_WORKSPACES,
			schema: _mastra_core_storage.WORKSPACES_SCHEMA
		});
		await this.#db.createTable({
			tableName: _mastra_core_storage.TABLE_WORKSPACE_VERSIONS,
			schema: _mastra_core_storage.WORKSPACE_VERSIONS_SCHEMA
		});
		await this.#client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS idx_workspace_versions_workspace_version ON "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" ("workspaceId", "versionNumber")`);
	}
	async dangerouslyClearAll() {
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_WORKSPACES });
		await this.#db.deleteData({ tableName: _mastra_core_storage.TABLE_WORKSPACE_VERSIONS });
	}
	async getById(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_WORKSPACES)} FROM "${_mastra_core_storage.TABLE_WORKSPACES}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseWorkspaceRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_WORKSPACE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async create(input) {
		const { workspace } = input;
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#db.insert({
				tableName: _mastra_core_storage.TABLE_WORKSPACES,
				record: {
					id: workspace.id,
					status: "draft",
					activeVersionId: null,
					authorId: workspace.authorId ?? null,
					metadata: workspace.metadata ?? null,
					createdAt: now.toISOString(),
					updatedAt: now.toISOString()
				}
			});
			const { id: _id, authorId: _authorId, metadata: _metadata, ...snapshotConfig } = workspace;
			const versionId = crypto.randomUUID();
			try {
				await this.createVersion({
					id: versionId,
					workspaceId: workspace.id,
					versionNumber: 1,
					...snapshotConfig,
					changedFields: Object.keys(snapshotConfig),
					changeMessage: "Initial version"
				});
			} catch (versionError) {
				await this.#db.delete({
					tableName: _mastra_core_storage.TABLE_WORKSPACES,
					keys: { id: workspace.id }
				});
				throw versionError;
			}
			return {
				id: workspace.id,
				status: "draft",
				activeVersionId: void 0,
				authorId: workspace.authorId,
				metadata: workspace.metadata,
				createdAt: now,
				updatedAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_WORKSPACE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async update(input) {
		const { id, ...updates } = input;
		try {
			const existing = await this.getById(id);
			if (!existing) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_WORKSPACE", "NOT_FOUND"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.USER,
				text: `Workspace ${id} not found`,
				details: { workspaceId: id }
			});
			const { authorId, activeVersionId, metadata, status, ...rawConfigFields } = updates;
			const configFields = {};
			for (const [key, value] of Object.entries(rawConfigFields)) if (value !== void 0) configFields[key] = value;
			const configFieldNames = SNAPSHOT_FIELDS;
			const hasConfigUpdate = configFieldNames.some((field) => field in configFields);
			const updateData = { updatedAt: (/* @__PURE__ */ new Date()).toISOString() };
			if (authorId !== void 0) updateData.authorId = authorId;
			if (activeVersionId !== void 0) {
				updateData.activeVersionId = activeVersionId;
				if (status === void 0) updateData.status = "published";
			}
			if (status !== void 0) updateData.status = status;
			if (metadata !== void 0) updateData.metadata = {
				...existing.metadata || {},
				...metadata
			};
			await this.#db.update({
				tableName: _mastra_core_storage.TABLE_WORKSPACES,
				keys: { id },
				data: updateData
			});
			if (hasConfigUpdate) {
				const latestVersion = await this.getLatestVersion(id);
				if (!latestVersion) throw new Error(`No versions found for workspace ${id}`);
				const { id: _versionId, workspaceId: _workspaceId, versionNumber: _versionNumber, changedFields: _changedFields, changeMessage: _changeMessage, createdAt: _createdAt, ...latestConfig } = latestVersion;
				const newConfig = {
					...latestConfig,
					...configFields
				};
				const changedFields = configFieldNames.filter((field) => field in configFields && JSON.stringify(configFields[field]) !== JSON.stringify(latestConfig[field]));
				if (changedFields.length > 0) {
					const newVersionId = crypto.randomUUID();
					await this.createVersion({
						id: newVersionId,
						workspaceId: id,
						versionNumber: latestVersion.versionNumber + 1,
						...newConfig,
						changedFields,
						changeMessage: `Updated ${changedFields.join(", ")}`
					});
				}
			}
			const updated = await this.getById(id);
			if (!updated) throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_WORKSPACE", "NOT_FOUND_AFTER_UPDATE"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.SYSTEM,
				text: `Workspace ${id} not found after update`,
				details: { id }
			});
			return updated;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "UPDATE_WORKSPACE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async delete(id) {
		try {
			await this.deleteVersionsByParentId(id);
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_WORKSPACES}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_WORKSPACE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async list(args) {
		try {
			const { page = 0, perPage: perPageInput, orderBy, authorId, metadata } = args || {};
			const { field, direction } = this.parseOrderBy(orderBy);
			const conditions = [];
			const queryParams = [];
			if (authorId !== void 0) {
				conditions.push("authorId = ?");
				queryParams.push(authorId);
			}
			if (metadata && Object.keys(metadata).length > 0) for (const [key, value] of Object.entries(metadata)) {
				if (!/^[a-zA-Z_][a-zA-Z0-9_]*$/.test(key)) throw new _mastra_core_error.MastraError({
					id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_WORKSPACES", "INVALID_METADATA_KEY"),
					domain: _mastra_core_error.ErrorDomain.STORAGE,
					category: _mastra_core_error.ErrorCategory.USER,
					text: `Invalid metadata key: ${key}. Keys must be alphanumeric with underscores.`,
					details: { key }
				});
				conditions.push(`json_extract(metadata, '$.${key}') = ?`);
				queryParams.push(typeof value === "string" ? value : JSON.stringify(value));
			}
			const whereClause = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_WORKSPACES}" ${whereClause}`,
				args: queryParams
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				workspaces: [],
				total: 0,
				page,
				perPage: perPageInput ?? 100,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 100);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				workspaces: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_WORKSPACES)} FROM "${_mastra_core_storage.TABLE_WORKSPACES}" ${whereClause} ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						...queryParams,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseWorkspaceRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_WORKSPACES", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async createVersion(input) {
		try {
			const now = /* @__PURE__ */ new Date();
			await this.#client.execute({
				sql: `INSERT INTO "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" (
  id, "workspaceId", "versionNumber",
  name, description, filesystem, sandbox, mounts, search, skills, tools,
  "autoSync", "operationTimeout",
  "changedFields", "changeMessage", "createdAt"
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
				args: [
					input.id,
					input.workspaceId,
					input.versionNumber,
					input.name,
					input.description ?? null,
					input.filesystem ? JSON.stringify(input.filesystem) : null,
					input.sandbox ? JSON.stringify(input.sandbox) : null,
					input.mounts ? JSON.stringify(input.mounts) : null,
					input.search ? JSON.stringify(input.search) : null,
					input.skills ? JSON.stringify(input.skills) : null,
					input.tools ? JSON.stringify(input.tools) : null,
					input.autoSync ? 1 : 0,
					input.operationTimeout ?? null,
					input.changedFields ? JSON.stringify(input.changedFields) : null,
					input.changeMessage ?? null,
					now.toISOString()
				]
			});
			return {
				...input,
				createdAt: now
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "CREATE_WORKSPACE_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersion(id) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_WORKSPACE_VERSIONS)} FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE id = ?`,
				args: [id]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_WORKSPACE_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getVersionByNumber(workspaceId, versionNumber) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_WORKSPACE_VERSIONS)} FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE "workspaceId" = ? AND "versionNumber" = ?`,
				args: [workspaceId, versionNumber]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_WORKSPACE_VERSION_BY_NUMBER", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async getLatestVersion(workspaceId) {
		try {
			const row = (await this.#client.execute({
				sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_WORKSPACE_VERSIONS)} FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE "workspaceId" = ? ORDER BY "versionNumber" DESC LIMIT 1`,
				args: [workspaceId]
			})).rows?.[0];
			return row ? this.#parseVersionRow(row) : null;
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "GET_LATEST_WORKSPACE_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async listVersions(input) {
		try {
			const { workspaceId, page = 0, perPage: perPageInput, orderBy } = input;
			const { field, direction } = this.parseVersionOrderBy(orderBy);
			const countResult = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE "workspaceId" = ?`,
				args: [workspaceId]
			});
			const total = Number(countResult.rows?.[0]?.count ?? 0);
			if (total === 0) return {
				versions: [],
				total: 0,
				page,
				perPage: perPageInput ?? 20,
				hasMore: false
			};
			const perPage = (0, _mastra_core_storage.normalizePerPage)(perPageInput, 20);
			const { offset: start, perPage: perPageForResponse } = (0, _mastra_core_storage.calculatePagination)(page, perPageInput, perPage);
			const limitValue = perPageInput === false ? total : perPage;
			const end = perPageInput === false ? total : start + perPage;
			return {
				versions: (await this.#client.execute({
					sql: `SELECT ${buildSelectColumns(_mastra_core_storage.TABLE_WORKSPACE_VERSIONS)} FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE "workspaceId" = ? ORDER BY ${field} ${direction} LIMIT ? OFFSET ?`,
					args: [
						workspaceId,
						limitValue,
						start
					]
				})).rows?.map((row) => this.#parseVersionRow(row)) ?? [],
				total,
				page,
				perPage: perPageForResponse,
				hasMore: end < total
			};
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "LIST_WORKSPACE_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersion(id) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE "id" = ?`,
				args: [id]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_WORKSPACE_VERSION", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async deleteVersionsByParentId(entityId) {
		try {
			await this.#client.execute({
				sql: `DELETE FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE "workspaceId" = ?`,
				args: [entityId]
			});
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "DELETE_WORKSPACE_VERSIONS_BY_WORKSPACE", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	async countVersions(workspaceId) {
		try {
			const result = await this.#client.execute({
				sql: `SELECT COUNT(*) as count FROM "${_mastra_core_storage.TABLE_WORKSPACE_VERSIONS}" WHERE "workspaceId" = ?`,
				args: [workspaceId]
			});
			return Number(result.rows?.[0]?.count ?? 0);
		} catch (error) {
			if (error instanceof _mastra_core_error.MastraError) throw error;
			throw new _mastra_core_error.MastraError({
				id: (0, _mastra_core_storage.createStorageErrorId)("LIBSQL", "COUNT_WORKSPACE_VERSIONS", "FAILED"),
				domain: _mastra_core_error.ErrorDomain.STORAGE,
				category: _mastra_core_error.ErrorCategory.THIRD_PARTY
			}, error);
		}
	}
	#parseWorkspaceRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			status: row.status ?? "draft",
			activeVersionId: row.activeVersionId ?? void 0,
			authorId: row.authorId ?? void 0,
			metadata: safeParseJSON(row.metadata),
			createdAt: new Date(row.createdAt),
			updatedAt: new Date(row.updatedAt)
		};
	}
	#parseVersionRow(row) {
		const safeParseJSON = (val) => {
			if (val === null || val === void 0) return void 0;
			if (typeof val === "string") try {
				return JSON.parse(val);
			} catch {
				return val;
			}
			return val;
		};
		return {
			id: row.id,
			workspaceId: row.workspaceId,
			versionNumber: Number(row.versionNumber),
			name: row.name,
			description: row.description ?? void 0,
			filesystem: safeParseJSON(row.filesystem),
			sandbox: safeParseJSON(row.sandbox),
			mounts: safeParseJSON(row.mounts),
			search: safeParseJSON(row.search),
			skills: safeParseJSON(row.skills),
			tools: safeParseJSON(row.tools),
			autoSync: Boolean(row.autoSync),
			operationTimeout: row.operationTimeout != null ? Number(row.operationTimeout) : void 0,
			changedFields: safeParseJSON(row.changedFields),
			changeMessage: row.changeMessage ?? void 0,
			createdAt: new Date(row.createdAt)
		};
	}
};
//#endregion
//#region src/storage/factory-storage.ts
const IDENTIFIER_RE = /^[A-Za-z_][A-Za-z0-9_]*$/;
const COLUMN_DDL = {
	text: "TEXT",
	bigint: "INTEGER",
	integer: "INTEGER",
	boolean: "INTEGER",
	json: "TEXT",
	timestamp: "TEXT",
	"uuid-pk": "TEXT"
};
function assertIdentifier(kind, name) {
	if (!IDENTIFIER_RE.test(name)) throw new Error(`LibSQLFactoryStorage: invalid ${kind} identifier '${name}'`);
}
function isUniqueViolation(error) {
	const code = typeof error === "object" && error !== null ? error.code : void 0;
	if (code === "SQLITE_CONSTRAINT_UNIQUE" || code === "SQLITE_CONSTRAINT_PRIMARYKEY") return true;
	const message = error instanceof Error ? error.message : String(error);
	return message.includes("UNIQUE constraint failed") || message.includes("SQLITE_CONSTRAINT_UNIQUE") || message.includes("SQLITE_CONSTRAINT_PRIMARYKEY");
}
function primaryKeyOf(schema) {
	const pks = Object.entries(schema.columns).filter(([, spec]) => spec.type === "uuid-pk" || spec.primaryKey);
	if (pks.length !== 1) throw new Error(`LibSQLFactoryStorage: collection '${schema.name}' must declare exactly one primary key (found ${pks.length})`);
	return pks[0][0];
}
function assertUpsertConflict(schema, conflictKeys, row) {
	const keys = new Set(conflictKeys);
	if (keys.size !== conflictKeys.length || conflictKeys.some((key) => row[key] === void 0)) throw new Error(`LibSQLFactoryStorage: upsert conflict keys for '${schema.name}' must be unique and present in the row`);
	if (![{ columns: [primaryKeyOf(schema)] }, ...schema.uniqueIndexes ?? []].some((candidate) => {
		if (candidate.columns.length !== keys.size || candidate.columns.some((column) => !keys.has(column))) return false;
		if ("whereNotNull" in candidate && candidate.whereNotNull && row[candidate.whereNotNull] == null) return false;
		if ("whereNull" in candidate && candidate.whereNull && row[candidate.whereNull] != null) return false;
		return true;
	})) throw new Error(`LibSQLFactoryStorage: upsert conflict keys [${conflictKeys.join(", ")}] do not match an applicable primary key or unique index on '${schema.name}'`);
}
function serializeDefault(value) {
	if (typeof value === "string") return `'${value.replace(/'/g, "''")}'`;
	if (typeof value === "boolean") return value ? "1" : "0";
	return String(value);
}
const runWithoutWriteLock = (fn) => fn();
var LibSQLFactoryStorageOps = class {
	#client;
	#schemas;
	#withWriteLock;
	constructor(client, schemas, withWriteLock) {
		this.#client = client;
		this.#schemas = schemas;
		this.#withWriteLock = withWriteLock;
	}
	#schema(collection) {
		const schema = this.#schemas.get(collection);
		if (!schema) throw new Error(`LibSQLFactoryStorage: unknown collection '${collection}' — register it via ensureCollections() first`);
		return schema;
	}
	#column(schema, column) {
		const spec = schema.columns[column];
		if (!spec) throw new Error(`LibSQLFactoryStorage: unknown column '${column}' on collection '${schema.name}'`);
		return spec;
	}
	#serialize(spec, value) {
		if (value === null || value === void 0) return null;
		switch (spec.type) {
			case "timestamp": return value instanceof Date ? value.toISOString() : String(value);
			case "boolean": return value ? 1 : 0;
			case "json": return JSON.stringify(value);
			case "bigint":
			case "integer": return Number(value);
			default: return String(value);
		}
	}
	#deserializeRow(schema, raw) {
		const row = {};
		for (const [name, spec] of Object.entries(schema.columns)) {
			const value = raw[name];
			if (value === null || value === void 0) {
				row[name] = null;
				continue;
			}
			switch (spec.type) {
				case "timestamp":
					row[name] = new Date(String(value));
					break;
				case "boolean":
					row[name] = Number(value) === 1;
					break;
				case "json":
					row[name] = JSON.parse(String(value));
					break;
				case "bigint":
				case "integer":
					row[name] = Number(value);
					break;
				default: row[name] = String(value);
			}
		}
		return row;
	}
	/** Builds `WHERE` SQL (without the keyword). `{}` yields a match-all clause. */
	#buildWhere(schema, where) {
		const clauses = [];
		const args = [];
		for (const [column, condition] of Object.entries(where)) {
			const spec = this.#column(schema, column);
			if (condition !== null && typeof condition === "object" && !(condition instanceof Date) && "in" in condition) {
				if (condition.in.length === 0) {
					clauses.push("1 = 0");
					continue;
				}
				const nonNull = condition.in.filter((value) => value !== null);
				const includesNull = nonNull.length !== condition.in.length;
				const inClause = nonNull.length > 0 ? `"${column}" IN (${nonNull.map(() => "?").join(", ")})` : void 0;
				clauses.push(inClause && includesNull ? `(${inClause} OR "${column}" IS NULL)` : inClause ?? `"${column}" IS NULL`);
				args.push(...nonNull.map((value) => this.#serialize(spec, value)));
			} else if (condition === null) clauses.push(`"${column}" IS NULL`);
			else {
				clauses.push(`"${column}" = ?`);
				args.push(this.#serialize(spec, condition));
			}
		}
		return {
			sql: clauses.length > 0 ? clauses.join(" AND ") : "1 = 1",
			args
		};
	}
	/** Keyset condition: rows strictly after `cursor.values` in the `orderBy` order. */
	#buildCursor(schema, orderBy, values) {
		if (values.length !== orderBy.length) throw new Error(`LibSQLFactoryStorage: cursor has ${values.length} values but orderBy has ${orderBy.length} columns`);
		const branches = [];
		const args = [];
		for (let i = 0; i < orderBy.length; i++) {
			const parts = [];
			for (let j = 0; j < i; j++) {
				const [column] = orderBy[j];
				parts.push(`"${column}" = ?`);
				args.push(this.#serialize(this.#column(schema, column), values[j]));
			}
			const [column, dir] = orderBy[i];
			parts.push(`"${column}" ${dir === "desc" ? "<" : ">"} ?`);
			args.push(this.#serialize(this.#column(schema, column), values[i]));
			branches.push(`(${parts.join(" AND ")})`);
		}
		return {
			sql: `(${branches.join(" OR ")})`,
			args
		};
	}
	async #select(collection, where, opts) {
		const schema = this.#schema(collection);
		const filter = this.#buildWhere(schema, where);
		let sql = `SELECT * FROM "${schema.name}" WHERE ${filter.sql}`;
		const args = [...filter.args];
		if (opts?.cursor) {
			if (!opts.orderBy || opts.orderBy.length === 0) throw new Error("LibSQLFactoryStorage: cursor pagination requires orderBy");
			const cursor = this.#buildCursor(schema, opts.orderBy, opts.cursor.values);
			sql += ` AND ${cursor.sql}`;
			args.push(...cursor.args);
		}
		if (opts?.orderBy && opts.orderBy.length > 0) {
			const order = opts.orderBy.map(([column, dir]) => {
				this.#column(schema, column);
				return `"${column}" ${dir === "desc" ? "DESC" : "ASC"}`;
			}).join(", ");
			sql += ` ORDER BY ${order}`;
		}
		if (opts?.limit !== void 0) {
			sql += ` LIMIT ?`;
			args.push(opts.limit);
		}
		return (await this.#client.execute({
			sql,
			args
		})).rows.map((raw) => this.#deserializeRow(schema, raw));
	}
	async findOne(collection, where) {
		return (await this.#select(collection, where, { limit: 1 }))[0] ?? null;
	}
	async findMany(collection, where, opts) {
		return this.#select(collection, where, opts);
	}
	async count(collection, where) {
		const schema = this.#schema(collection);
		const filter = this.#buildWhere(schema, where);
		const result = await this.#client.execute({
			sql: `SELECT COUNT(*) AS count FROM "${schema.name}" WHERE ${filter.sql}`,
			args: filter.args
		});
		return Number(result.rows[0]?.count ?? 0);
	}
	async #insertOne(collection, row) {
		const schema = this.#schema(collection);
		const pk = primaryKeyOf(schema);
		const values = { ...row };
		if (schema.columns[pk].type === "uuid-pk" && values[pk] === void 0) values[pk] = (0, crypto$1.randomUUID)();
		const columns = Object.keys(values).filter((column) => values[column] !== void 0);
		for (const column of columns) this.#column(schema, column);
		const sql = `INSERT INTO "${schema.name}" (${columns.map((c) => `"${c}"`).join(", ")}) VALUES (${columns.map(() => "?").join(", ")})`;
		const args = columns.map((column) => this.#serialize(this.#column(schema, column), values[column]));
		try {
			await this.#client.execute({
				sql,
				args
			});
		} catch (error) {
			if (isUniqueViolation(error)) throw new _mastra_core_storage.UniqueViolationError(collection, { cause: error });
			throw error;
		}
		const inserted = await this.findOne(collection, { [pk]: values[pk] });
		if (!inserted) throw new Error(`LibSQLFactoryStorage: failed to read back inserted row from '${collection}'`);
		return inserted;
	}
	async insertOne(collection, row) {
		return this.#withWriteLock(() => this.#insertOne(collection, row));
	}
	async #upsertOne(collection, conflictKeys, row) {
		const schema = this.#schema(collection);
		const pk = primaryKeyOf(schema);
		assertUpsertConflict(schema, conflictKeys, row);
		const keyWhere = Object.fromEntries(conflictKeys.map((key) => [key, row[key]]));
		let lastError;
		for (let attempt = 0; attempt < 2; attempt++) {
			const existing = await this.findOne(collection, keyWhere);
			if (existing) {
				const set = Object.fromEntries(Object.entries(row).filter(([column, value]) => value !== void 0 && column !== pk && !conflictKeys.includes(column)));
				if (Object.keys(set).length > 0) await this.#updateMany(collection, { [pk]: existing[pk] }, set);
				const updated = await this.findOne(collection, { [pk]: existing[pk] });
				if (!updated) continue;
				return updated;
			}
			try {
				return await this.#insertOne(collection, row);
			} catch (error) {
				if (!(error instanceof _mastra_core_storage.UniqueViolationError)) throw error;
				lastError = error;
			}
		}
		throw lastError ?? /* @__PURE__ */ new Error(`LibSQLFactoryStorage: upsert into '${collection}' did not converge`);
	}
	async upsertOne(collection, conflictKeys, row) {
		return this.#withWriteLock(() => this.#upsertOne(collection, conflictKeys, row));
	}
	async #updateMany(collection, where, set) {
		const schema = this.#schema(collection);
		const columns = Object.keys(set).filter((column) => set[column] !== void 0);
		if (columns.length === 0) return 0;
		const filter = this.#buildWhere(schema, where);
		const sql = `UPDATE "${schema.name}" SET ${columns.map((c) => `"${c}" = ?`).join(", ")} WHERE ${filter.sql}`;
		const args = [...columns.map((column) => this.#serialize(this.#column(schema, column), set[column])), ...filter.args];
		return (await this.#client.execute({
			sql,
			args
		})).rowsAffected;
	}
	async updateMany(collection, where, set) {
		return this.#withWriteLock(() => this.#updateMany(collection, where, set));
	}
	async #deleteMany(collection, where) {
		const schema = this.#schema(collection);
		const filter = this.#buildWhere(schema, where);
		return (await this.#client.execute({
			sql: `DELETE FROM "${schema.name}" WHERE ${filter.sql}`,
			args: filter.args
		})).rowsAffected;
	}
	async deleteMany(collection, where) {
		return this.#withWriteLock(() => this.#deleteMany(collection, where));
	}
	async updateAtomic(collection, where, fn) {
		return this.#withWriteLock(async () => {
			const pk = primaryKeyOf(this.#schema(collection));
			const row = await this.findOne(collection, where);
			if (!row) return null;
			const patch = await fn(row);
			if (patch === null) return row;
			const pkWhere = { [pk]: row[pk] };
			await this.#updateMany(collection, pkWhere, patch);
			return this.findOne(collection, pkWhere);
		});
	}
};
/**
* LibSQL/Turso {@link FactoryStorage} backend: one libsql database powering
* agent state (via a wrapped {@link LibSQLStore}) and app-owned collections
* (via {@link FactoryStorageOps}), sharing a single client.
*/
var LibSQLFactoryStorage = class extends _mastra_core_storage.FactoryStorage {
	ops;
	#client;
	#config;
	#schemas = /* @__PURE__ */ new Map();
	#mastraStorage;
	constructor(config) {
		super();
		this.#config = config;
		const isLocalDb = config.url.startsWith("file:") || config.url.includes(":memory:");
		const client = (0, _libsql_client.createClient)({
			url: config.url,
			...config.authToken ? { authToken: config.authToken } : {},
			...isLocalDb ? { timeout: DEFAULT_CONNECTION_TIMEOUT_MS } : {}
		});
		this.#client = isSingleConnectionDatabase(config) ? gateSingleConnectionClient(client) : client;
		this.ops = new LibSQLFactoryStorageOps(this.#client, this.#schemas, (fn) => withClientWriteLock(this.#client, fn));
	}
	getMastraStorage() {
		this.#mastraStorage ??= new LibSQLStore({
			id: this.#config.id ?? "libsql-factory",
			client: this.#client,
			...this.#config.retention ? { retention: this.#config.retention } : {}
		});
		return this.#mastraStorage;
	}
	async initStorage() {
		await this.#client.execute("SELECT 1");
	}
	async withTransaction(fn) {
		return withClientWriteLock(this.#client, async () => {
			if (this.#config.url.includes(":memory:")) return fn(new LibSQLFactoryStorageOps(this.#client, this.#schemas, runWithoutWriteLock));
			const transaction = await this.#client.transaction("write");
			try {
				const result = await fn(new LibSQLFactoryStorageOps(transaction, this.#schemas, runWithoutWriteLock));
				await transaction.commit();
				return result;
			} catch (error) {
				await transaction.rollback();
				throw error;
			} finally {
				transaction.close();
			}
		});
	}
	async ensureCollections(schemas) {
		await withClientWriteLock(this.#client, async () => {
			for (const schema of schemas) {
				await this.#ensureCollection(schema);
				this.#schemas.set(schema.name, schema);
			}
		});
	}
	async close() {
		await this.#client.close();
	}
	authDatabase() {
		return {
			dialect: "libsql",
			client: this.#client
		};
	}
	#columnDdl(name, spec) {
		assertIdentifier("column", name);
		let ddl = `"${name}" ${COLUMN_DDL[spec.type]}`;
		if (spec.type === "uuid-pk" || spec.primaryKey) ddl += " PRIMARY KEY";
		if (!spec.nullable && spec.type !== "uuid-pk" && !spec.primaryKey) ddl += " NOT NULL";
		if (spec.default !== void 0) ddl += ` DEFAULT ${serializeDefault(spec.default)}`;
		return ddl;
	}
	/**
	* A table created by an older schema may still say NOT NULL on a column the
	* current schema declares nullable — inserts of null then fail on databases
	* that predate the change. SQLite has no `ALTER COLUMN DROP NOT NULL`, so
	* relaxation swaps in a table rebuilt from the current schema (create shadow
	* → copy → drop → rename) atomically.
	*/
	async #relaxDriftedNotNulls(schema) {
		if (!(await this.#client.execute(`PRAGMA table_info("${schema.name}")`)).rows.some((row) => {
			const spec = schema.columns[String(row.name)];
			if (!spec || spec.type === "uuid-pk" || spec.primaryKey) return false;
			return spec.nullable === true && Number(row.notnull) === 1;
		})) return;
		const ddl = Object.entries(schema.columns).map(([name, spec]) => this.#columnDdl(name, spec));
		const shadow = `${schema.name}__nullable_rebuild`;
		const cols = Object.keys(schema.columns).map((name) => `"${name}"`).join(", ");
		await this.#client.batch([
			`DROP TABLE IF EXISTS "${shadow}"`,
			`CREATE TABLE "${shadow}" (${ddl.join(", ")})`,
			`INSERT INTO "${shadow}" (${cols}) SELECT ${cols} FROM "${schema.name}"`,
			`DROP TABLE "${schema.name}"`,
			`ALTER TABLE "${shadow}" RENAME TO "${schema.name}"`
		], "write");
	}
	async #ensureCollection(schema) {
		assertIdentifier("collection", schema.name);
		primaryKeyOf(schema);
		const columns = Object.entries(schema.columns).map(([name, spec]) => this.#columnDdl(name, spec));
		await this.#client.execute(`CREATE TABLE IF NOT EXISTS "${schema.name}" (${columns.join(", ")})`);
		const info = await this.#client.execute(`PRAGMA table_info("${schema.name}")`);
		const existing = new Set(info.rows.map((row) => String(row.name)));
		for (const [name, spec] of Object.entries(schema.columns)) {
			if (existing.has(name)) continue;
			await this.#client.execute(`ALTER TABLE "${schema.name}" ADD COLUMN ${this.#columnDdl(name, spec)}`);
		}
		await this.#relaxDriftedNotNulls(schema);
		for (const index of schema.uniqueIndexes ?? []) {
			assertIdentifier("index", index.name);
			index.columns.forEach((column) => assertIdentifier("column", column));
			let where = "";
			if (index.whereNotNull) {
				assertIdentifier("column", index.whereNotNull);
				where = ` WHERE "${index.whereNotNull}" IS NOT NULL`;
			} else if (index.whereNull) {
				assertIdentifier("column", index.whereNull);
				where = ` WHERE "${index.whereNull}" IS NULL`;
			}
			await this.#client.execute(`CREATE UNIQUE INDEX IF NOT EXISTS "${index.name}" ON "${schema.name}" (${index.columns.map((c) => `"${c}"`).join(", ")})${where}`);
		}
		for (const index of schema.indexes ?? []) {
			assertIdentifier("index", index.name);
			index.columns.forEach((column) => assertIdentifier("column", column));
			await this.#client.execute(`CREATE INDEX IF NOT EXISTS "${index.name}" ON "${schema.name}" (${index.columns.map((c) => `"${c}"`).join(", ")})`);
		}
	}
};
//#endregion
//#region src/storage/index.ts
const DEFAULT_LOCAL_CACHE_SIZE = -16e3;
const DEFAULT_LOCAL_MMAP_SIZE = 134217728;
/**
* LibSQL/Turso storage adapter for Mastra.
*
* Access domain-specific storage via `getStore()`:
*
* @example
* ```typescript
* const storage = new LibSQLStore({ id: 'my-store', url: 'file:./dev.db' });
*
* // Access memory domain
* const memory = await storage.getStore('memory');
* await memory?.saveThread({ thread });
*
* // Access workflows domain
* const workflows = await storage.getStore('workflows');
* await workflows?.persistWorkflowSnapshot({ workflowName, runId, snapshot });
* ```
*/
var LibSQLStore = class extends _mastra_core_storage.MastraCompositeStore {
	client;
	maxRetries;
	initialBackoffMs;
	connectionTimeoutMs;
	pragmasReady;
	isLocalDb;
	localPragmas;
	stores;
	constructor(config) {
		if (!config.id || typeof config.id !== "string" || config.id.trim() === "") throw new Error("LibSQLStore: id must be provided and cannot be empty.");
		super({
			id: config.id,
			name: `LibSQLStore`,
			disableInit: config.disableInit,
			retention: config.retention
		});
		this.maxRetries = config.maxRetries ?? 5;
		this.initialBackoffMs = config.initialBackoffMs ?? 100;
		this.connectionTimeoutMs = config.connectionTimeoutMs ?? 5e3;
		this.localPragmas = {
			cacheSize: config.localPragmas?.cacheSize ?? DEFAULT_LOCAL_CACHE_SIZE,
			mmapSize: config.localPragmas?.mmapSize ?? DEFAULT_LOCAL_MMAP_SIZE
		};
		if ("url" in config) {
			this.isLocalDb = (config.url.startsWith("file:") || config.url.includes(":memory:")) && !config.syncUrl;
			const client = (0, _libsql_client.createClient)({
				url: config.url,
				...config.authToken ? { authToken: config.authToken } : {},
				...config.syncUrl ? { syncUrl: config.syncUrl } : {},
				...config.syncInterval !== void 0 ? { syncInterval: config.syncInterval } : {},
				...this.isLocalDb ? { timeout: this.connectionTimeoutMs } : {}
			});
			this.client = isSingleConnectionDatabase(config) ? gateSingleConnectionClient(client) : client;
			this.pragmasReady = this.isLocalDb ? this.applyLocalPragmas() : Promise.resolve();
		} else {
			this.client = config.client;
			this.isLocalDb = false;
			this.pragmasReady = Promise.resolve();
		}
		const domainConfig = {
			client: this.client,
			maxRetries: this.maxRetries,
			initialBackoffMs: this.initialBackoffMs
		};
		const scores = new ScoresLibSQL(domainConfig);
		const workflows = new WorkflowsLibSQL(domainConfig);
		const workflowDefinitions = new WorkflowDefinitionsLibSQL(domainConfig);
		const memory = new MemoryLibSQL(domainConfig);
		const knowledge = new KnowledgeLibSQL(domainConfig);
		const observability = new ObservabilityLibSQL(domainConfig);
		const agents = new AgentsLibSQL(domainConfig);
		const channels = new ChannelsLibSQL(domainConfig);
		const datasets = new DatasetsLibSQL(domainConfig);
		const experiments = new ExperimentsLibSQL(domainConfig);
		const promptBlocks = new PromptBlocksLibSQL(domainConfig);
		const scorerDefinitions = new ScorerDefinitionsLibSQL(domainConfig);
		const mcpClients = new MCPClientsLibSQL(domainConfig);
		const mcpServers = new MCPServersLibSQL(domainConfig);
		const workspaces = new WorkspacesLibSQL(domainConfig);
		const skills = new SkillsLibSQL(domainConfig);
		const favorites = new FavoritesLibSQL(domainConfig);
		const blobs = new BlobsLibSQL(domainConfig);
		const backgroundTasks = new BackgroundTasksLibSQL(domainConfig);
		const schedules = new SchedulesLibSQL(domainConfig);
		const harness = new HarnessLibSQL(domainConfig);
		const toolProviderConnections = new ToolProviderConnectionsLibSQL(domainConfig);
		const notifications = new NotificationsLibSQL(domainConfig);
		const threadState = new ThreadStateLibSQL(domainConfig);
		this.stores = {
			scores,
			workflows,
			workflowDefinitions,
			memory,
			knowledge,
			observability,
			agents,
			channels,
			datasets,
			experiments,
			promptBlocks,
			scorerDefinitions,
			mcpClients,
			mcpServers,
			workspaces,
			skills,
			favorites,
			blobs,
			backgroundTasks,
			schedules,
			harness,
			toolProviderConnections,
			notifications,
			threadState
		};
	}
	async applyLocalPragmas() {
		const pragmas = [
			["journal_mode=WAL", "PRAGMA journal_mode=WAL;"],
			[`busy_timeout=${this.connectionTimeoutMs}`, `PRAGMA busy_timeout=${this.connectionTimeoutMs};`],
			["synchronous=NORMAL", "PRAGMA synchronous=NORMAL;"],
			["temp_store=MEMORY", "PRAGMA temp_store=MEMORY;"],
			[`cache_size=${this.localPragmas.cacheSize}`, `PRAGMA cache_size=${this.localPragmas.cacheSize};`],
			[`mmap_size=${this.localPragmas.mmapSize}`, `PRAGMA mmap_size=${this.localPragmas.mmapSize};`]
		];
		for (const [label, sql] of pragmas) try {
			await this.client.execute(sql);
			this.logger.debug(`LibSQLStore: PRAGMA ${label} set.`);
		} catch (err) {
			this.logger.warn(`LibSQLStore: Failed to set PRAGMA ${label}.`, err);
		}
	}
	getStoresToInit() {
		return Object.values(this.stores).filter(Boolean);
	}
	async initDomainsSequentially() {
		for (const store of this.getStoresToInit()) await store.init();
		return true;
	}
	async initDomainsInParallel() {
		await Promise.all(this.getStoresToInit().map((store) => store.init()));
		return true;
	}
	async init() {
		await this.pragmasReady;
		if (!this.isLocalDb) {
			if (this.shouldCacheInit) {
				if (this.hasInitialized) {
					await this.hasInitialized;
					return;
				}
				this.hasInitialized = this.initDomainsInParallel();
				await this.hasInitialized;
				return;
			}
			await this.initDomainsInParallel();
			return;
		}
		if (this.shouldCacheInit) {
			if (this.hasInitialized) {
				await this.hasInitialized;
				return;
			}
			this.hasInitialized = this.initDomainsSequentially();
			await this.hasInitialized;
			return;
		}
		await this.initDomainsSequentially();
	}
	/**
	* Closes the underlying libsql client, releasing this store's OS file handles.
	*
	* Safe to call more than once; subsequent calls are no-ops.
	*/
	async close() {
		if (!this.client.closed) await this.client.close();
	}
};
//#endregion
//#region src/vector/prompt.ts
/**
* Vector store specific prompt that details supported operators and examples.
* This prompt helps users construct valid filters for LibSQL Vector.
*/
const LIBSQL_PROMPT = `When querying LibSQL Vector, you can ONLY use the operators listed below. Any other operators will be rejected.
Important: Don't explain how to construct the filter - use the specified operators and fields to search the content and return relevant results.
If a user tries to give an explicit operator that is not supported, reject the filter entirely and let them know that the operator is not supported.

Basic Comparison Operators:
- $eq: Exact match (default when using field: value)
  Example: { "category": "electronics" }
- $ne: Not equal
  Example: { "category": { "$ne": "electronics" } }
- $gt: Greater than
  Example: { "price": { "$gt": 100 } }
- $gte: Greater than or equal
  Example: { "price": { "$gte": 100 } }
- $lt: Less than
  Example: { "price": { "$lt": 100 } }
- $lte: Less than or equal
  Example: { "price": { "$lte": 100 } }

Array Operators:
- $in: Match any value in array
  Example: { "category": { "$in": ["electronics", "books"] } }
- $nin: Does not match any value in array
  Example: { "category": { "$nin": ["electronics", "books"] } }
- $all: Match all values in array
  Example: { "tags": { "$all": ["premium", "sale"] } }
- $elemMatch: Match array elements that meet all specified conditions
  Example: { "items": { "$elemMatch": { "price": { "$gt": 100 } } } }
- $contains: Check if array contains value
  Example: { "tags": { "$contains": "premium" } }

Logical Operators:
- $and: Logical AND (implicit when using multiple conditions)
  Example: { "$and": [{ "price": { "$gt": 100 } }, { "category": "electronics" }] }
- $or: Logical OR
  Example: { "$or": [{ "price": { "$lt": 50 } }, { "category": "books" }] }
- $not: Logical NOT
  Example: { "$not": { "category": "electronics" } }
- $nor: Logical NOR
  Example: { "$nor": [{ "price": { "$lt": 50 } }, { "category": "books" }] }

Element Operators:
- $exists: Check if field exists
  Example: { "rating": { "$exists": true } }

Special Operators:
- $size: Array length check
  Example: { "tags": { "$size": 2 } }

Restrictions:
- Regex patterns are not supported
- Direct RegExp patterns will throw an error
- Nested fields are supported using dot notation
- Multiple conditions on the same field are supported with both implicit and explicit $and
- Array operations work on array fields only
- Basic operators handle array values as JSON strings
- Empty arrays in conditions are handled gracefully
- Only logical operators ($and, $or, $not, $nor) can be used at the top level
- All other operators must be used within a field condition
  Valid: { "field": { "$gt": 100 } }
  Valid: { "$and": [...] }
  Invalid: { "$gt": 100 }
  Invalid: { "$contains": "value" }
- Logical operators must contain field conditions, not direct operators
  Valid: { "$and": [{ "field": { "$gt": 100 } }] }
  Invalid: { "$and": [{ "$gt": 100 }] }
- $not operator:
  - Must be an object
  - Cannot be empty
  - Can be used at field level or top level
  - Valid: { "$not": { "field": "value" } }
  - Valid: { "field": { "$not": { "$eq": "value" } } }
- Other logical operators ($and, $or, $nor):
  - Can only be used at top level or nested within other logical operators
  - Can not be used on a field level, or be nested inside a field
  - Can not be used inside an operator
  - Valid: { "$and": [{ "field": { "$gt": 100 } }] }
  - Valid: { "$or": [{ "$and": [{ "field": { "$gt": 100 } }] }] }
  - Invalid: { "field": { "$and": [{ "$gt": 100 }] } }
  - Invalid: { "field": { "$or": [{ "$gt": 100 }] } }
  - Invalid: { "field": { "$gt": { "$and": [{...}] } } }
- $elemMatch requires an object with conditions
  Valid: { "array": { "$elemMatch": { "field": "value" } } }
  Invalid: { "array": { "$elemMatch": "value" } }

Example Complex Query:
{
  "$and": [
    { "category": { "$in": ["electronics", "computers"] } },
    { "price": { "$gte": 100, "$lte": 1000 } },
    { "tags": { "$all": ["premium", "sale"] } },
    { "items": { "$elemMatch": { "price": { "$gt": 50 }, "inStock": true } } },
    { "$or": [
      { "stock": { "$gt": 0 } },
      { "preorder": true }
    ]}
  ]
}`;
//#endregion
exports.AgentsLibSQL = AgentsLibSQL;
exports.BackgroundTasksLibSQL = BackgroundTasksLibSQL;
exports.BlobsLibSQL = BlobsLibSQL;
exports.ChannelsLibSQL = ChannelsLibSQL;
exports.DatasetsLibSQL = DatasetsLibSQL;
exports.DefaultStorage = LibSQLStore;
exports.LibSQLStore = LibSQLStore;
exports.ExperimentsLibSQL = ExperimentsLibSQL;
exports.FavoritesLibSQL = FavoritesLibSQL;
exports.HarnessLibSQL = HarnessLibSQL;
exports.KnowledgeLibSQL = KnowledgeLibSQL;
exports.LIBSQL_PROMPT = LIBSQL_PROMPT;
exports.LibSQLFactoryStorage = LibSQLFactoryStorage;
exports.LibSQLVector = LibSQLVector;
exports.MCPClientsLibSQL = MCPClientsLibSQL;
exports.MCPServersLibSQL = MCPServersLibSQL;
exports.MemoryLibSQL = MemoryLibSQL;
exports.NotificationsLibSQL = NotificationsLibSQL;
exports.ObservabilityLibSQL = ObservabilityLibSQL;
exports.PromptBlocksLibSQL = PromptBlocksLibSQL;
exports.SchedulesLibSQL = SchedulesLibSQL;
exports.ScorerDefinitionsLibSQL = ScorerDefinitionsLibSQL;
exports.ScoresLibSQL = ScoresLibSQL;
exports.SkillsLibSQL = SkillsLibSQL;
exports.ThreadStateLibSQL = ThreadStateLibSQL;
exports.ToolProviderConnectionsLibSQL = ToolProviderConnectionsLibSQL;
exports.WorkflowDefinitionsLibSQL = WorkflowDefinitionsLibSQL;
exports.WorkflowsLibSQL = WorkflowsLibSQL;
exports.WorkspacesLibSQL = WorkspacesLibSQL;

//# sourceMappingURL=index.cjs.map