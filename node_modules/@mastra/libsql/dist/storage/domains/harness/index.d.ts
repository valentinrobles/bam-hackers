import { HarnessStorage } from '@mastra/core/storage';
import type { SessionRecord, PruneOptions, PruneResult, RetentionTablesDescriptor, TableRetentionPolicy } from '@mastra/core/storage';
import type { LibSQLDomainConfig } from '../../db/index.js';
export declare class HarnessLibSQL extends HarnessStorage {
    #private;
    /** Session records accumulate over time. Single table, anchored on `createdAt`. */
    static readonly retentionTables: RetentionTablesDescriptor;
    constructor(config: LibSQLDomainConfig);
    init(): Promise<void>;
    dangerouslyClearAll(): Promise<void>;
    /** Delete harness sessions older than the `sessions` policy's `maxAge`, batched. */
    prune(policies: Record<string, TableRetentionPolicy>, options?: PruneOptions): Promise<PruneResult[]>;
    loadSession(sessionId: string): Promise<SessionRecord | null>;
    saveSession(record: SessionRecord): Promise<void>;
    listSessions(): Promise<SessionRecord[]>;
}
//# sourceMappingURL=index.d.ts.map