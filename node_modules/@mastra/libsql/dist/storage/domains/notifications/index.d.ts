import { NotificationsStorage } from '@mastra/core/storage';
import type { CreateNotificationInput, ListDueNotificationsInput, ListNotificationsInput, NotificationRecord, NotificationStatus, UpdateNotificationInput, PruneOptions, PruneResult, RetentionTablesDescriptor, TableRetentionPolicy } from '@mastra/core/storage';
import type { LibSQLDomainConfig } from '../../db/index.js';
export declare class NotificationsLibSQL extends NotificationsStorage {
    #private;
    /** The notification feed grows unbounded. Single table, anchored on `createdAt`. */
    static readonly retentionTables: RetentionTablesDescriptor;
    constructor(config: LibSQLDomainConfig);
    init(): Promise<void>;
    dangerouslyClearAll(): Promise<void>;
    /** Delete notifications older than the `notifications` policy's `maxAge`, batched. */
    prune(policies: Record<string, TableRetentionPolicy>, options?: PruneOptions): Promise<PruneResult[]>;
    createNotification(input: CreateNotificationInput): Promise<NotificationRecord>;
    listNotifications(input: ListNotificationsInput): Promise<NotificationRecord[]>;
    listDueNotifications(input: ListDueNotificationsInput): Promise<NotificationRecord[]>;
    getNotification(input: {
        threadId: string;
        id: string;
    }): Promise<NotificationRecord | null>;
    updateNotification(input: UpdateNotificationInput): Promise<NotificationRecord>;
    updateNotificationsStatus(input: {
        threadId: string;
        ids: string[];
        status: NotificationStatus;
    }): Promise<NotificationRecord[]>;
    private findCoalescable;
}
//# sourceMappingURL=index.d.ts.map