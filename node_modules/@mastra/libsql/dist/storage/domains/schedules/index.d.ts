import type { Schedule, ScheduleFilter, ScheduleTrigger, ScheduleTriggerListOptions, ScheduleUpdate, PruneOptions, PruneResult, RetentionTablesDescriptor, TableRetentionPolicy } from '@mastra/core/storage';
import { SchedulesStorage } from '@mastra/core/storage';
import type { LibSQLDomainConfig } from '../../db/index.js';
export declare class SchedulesLibSQL extends SchedulesStorage {
    #private;
    /**
     * The fire/run history (`schedule_triggers`, one row per fire) is the growth
     * table; schedule definitions are config and excluded. Anchored on
     * `actual_fire_at`, a bigint epoch-ms column (numeric comparison, not ISO).
     */
    static readonly retentionTables: RetentionTablesDescriptor;
    constructor(config: LibSQLDomainConfig);
    init(): Promise<void>;
    dangerouslyClearAll(): Promise<void>;
    /** Delete schedule fire history older than the `triggers` policy's `maxAge`, batched. */
    prune(policies: Record<string, TableRetentionPolicy>, options?: PruneOptions): Promise<PruneResult[]>;
    createSchedule(schedule: Schedule): Promise<Schedule>;
    getSchedule(id: string): Promise<Schedule | null>;
    listSchedules(filter?: ScheduleFilter): Promise<Schedule[]>;
    listDueSchedules(now: number, limit?: number): Promise<Schedule[]>;
    updateSchedule(id: string, patch: ScheduleUpdate): Promise<Schedule>;
    updateScheduleNextFire(id: string, expectedNextFireAt: number, newNextFireAt: number, lastFireAt: number, lastRunId: string): Promise<boolean>;
    deleteSchedule(id: string): Promise<void>;
    recordTrigger(trigger: ScheduleTrigger): Promise<void>;
    listTriggers(scheduleId: string, opts?: ScheduleTriggerListOptions): Promise<ScheduleTrigger[]>;
}
//# sourceMappingURL=index.d.ts.map