import { Extractor } from './extractor.js';
import type { ContinuationHintsConfig, ObservationConfig, ReflectionConfig, ResolvedObservationConfig } from './types.js';
export declare function createCurrentTaskExtractor(): Extractor<string>;
export declare function createSuggestedResponseExtractor(): Extractor<string>;
export declare function createThreadTitleExtractor(): Extractor<string>;
export interface ResolvedContinuationHints {
    currentTask: boolean;
    suggestedResponse: boolean;
}
/**
 * Normalize the user-facing `continuationHints` config into explicit per-section flags.
 * Both sections stay enabled unless the caller opts out, so omitting the config is a no-op.
 */
export declare function resolveContinuationHints(config: ContinuationHintsConfig | undefined): ResolvedContinuationHints;
interface ComposeExtractorOptions {
    continuationHints?: ContinuationHintsConfig;
    includeThreadTitle?: boolean;
    userExtractors?: readonly Extractor<any>[];
}
export declare function composeExtractors(options: ComposeExtractorOptions): Extractor<any>[];
export declare function composeObservationExtractors(config: Pick<ResolvedObservationConfig, 'threadTitle'> & Pick<ObservationConfig, 'extract' | 'continuationHints'>): Extractor[];
export declare function composeReflectionExtractors(config: Pick<ReflectionConfig, 'extract' | 'continuationHints'>): Extractor[];
export {};
//# sourceMappingURL=built-in-extractors.d.ts.map