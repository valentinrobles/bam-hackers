import type { Agent, AgentMemoryOption } from '@mastra/core/agent';
import type { ObservabilityContext } from '@mastra/core/observability';
import type { RequestContext } from '@mastra/core/request-context';
import type { Extractor, ExtractorSource } from './extractor.js';
export interface StructuredExtractionResult {
    values: Record<string, unknown>;
    failures: Array<{
        slug: string;
        error: string;
    }>;
}
export declare function extractStructuredValues(opts: {
    agent: Agent<any, any, any, any>;
    source: ExtractorSource;
    extractors?: readonly Extractor<any>[];
    memory?: AgentMemoryOption;
    priorExtractedValues?: Record<string, unknown>;
    requestContext?: RequestContext;
    observabilityContext?: ObservabilityContext;
    abortSignal?: AbortSignal;
}): Promise<StructuredExtractionResult>;
//# sourceMappingURL=extraction-runner.d.ts.map