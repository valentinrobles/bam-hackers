import type { Agent, MastraDBMessage } from '@mastra/core/agent';
import type { ProcessOutputResultArgs, ProcessOutputStepArgs, Processor } from '@mastra/core/processors';
export declare class RemindContinuationProcessor implements Processor<'remind-continuation'> {
    private readonly options;
    readonly id = "remind-continuation";
    readonly name = "Reminder Continuation Processor";
    private readonly deliveredTerminalReplies;
    constructor(options: {
        threadId: string;
        resourceId: string;
        parentThreadId: string;
        parentAgent: Agent;
        maxSteps: number;
        getReminderAgent(): Agent;
    });
    processOutputStep(args: ProcessOutputStepArgs): Promise<MastraDBMessage[]>;
    processOutputResult(args: ProcessOutputResultArgs): Promise<MastraDBMessage[]>;
    private dispatchContinuation;
    private deliverUnableToAnswer;
}
//# sourceMappingURL=remind-continuation.d.ts.map