import type { Agent } from '@mastra/core/agent';
import type { ToolAction } from '@mastra/core/tools';
import type { Memory } from '../../../index.js';
import type { ObservationalMemoryModel } from '../types.js';
import type { ResolvedSubconsciousAgent } from './types.js';
export type AskMemoryResult = {
    accepted: true;
    replyId: string;
    status: 'pending';
} | {
    accepted: false;
    replyId?: string;
    status: 'rejected';
    error: string;
};
export declare function createAskMemoryTool(options: {
    memory: Memory;
    config: ResolvedSubconsciousAgent;
    omModel?: ObservationalMemoryModel;
    getParentAgent(agentId: string): Agent | undefined;
}): ToolAction<any, any, any>;
export declare function createReplyToMemoryQuestionTool(options: {
    parentAgent: Agent;
    parentThreadId: string;
    resourceId: string;
}): ToolAction<any, any, any>;
//# sourceMappingURL=remind-questions.d.ts.map