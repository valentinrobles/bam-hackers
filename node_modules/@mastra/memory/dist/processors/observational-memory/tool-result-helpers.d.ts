export declare const DEFAULT_OBSERVER_TOOL_RESULT_MAX_TOKENS = 10000;
/**
 * Serializes a tool result without truncating it.
 *
 * Token accounting must see the full result: the truncation applied by
 * {@link formatToolResultForObserver} exists to bound what the Observer LLM reads,
 * not to describe what the agent's provider context actually holds.
 */
export declare function serializeToolResultForTokenCounting(value: unknown): string;
export declare function resolveToolResultValue(part: {
    providerMetadata?: Record<string, any>;
} | undefined, invocationResult: unknown): {
    value: unknown;
    usingStoredModelOutput: boolean;
};
export declare function truncateStringByTokens(text: string, maxTokens: number): string;
export declare function formatToolResultForObserver(value: unknown, options?: {
    maxTokens?: number;
}): string;
//# sourceMappingURL=tool-result-helpers.d.ts.map