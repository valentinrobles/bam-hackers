let zod_v4 = require("zod/v4");
//#region src/server/schemas/common.ts
/**
* Brands a permissive runtime schema with a concrete compile-time type.
*
* Some request-body fields carry values the server cannot fully validate with
* zod: functions (stop conditions, client tools), Zod schemas (structured
* output), or complex core types that would drift if re-declared here. For
* those fields runtime validation stays permissive and the receiving
* agent/workflow API performs the real validation, while handlers and
* generated route types still get the concrete type.
*
* Every call site is an intentional, documented gap between runtime
* validation and the declared type. Prefer a real zod schema whenever the
* shape can reasonably be expressed — only reach for this when it cannot.
*/
function typedPermissive(schema) {
	return schema;
}
const runIdSchema = zod_v4.z.object({ runId: zod_v4.z.string().describe("Unique identifier for the run") });
/**
* Query parameter schema for runId (optional)
* Used by create-run route where runId is optional
*/
const optionalRunIdSchema = zod_v4.z.object({ runId: zod_v4.z.string().optional() });
/**
* Pagination response info
* Used across all paginated endpoints
*/
const paginationInfoSchema = zod_v4.z.object({
	total: zod_v4.z.number(),
	page: zod_v4.z.number(),
	perPage: zod_v4.z.union([zod_v4.z.number(), zod_v4.z.literal(false)]),
	hasMore: zod_v4.z.boolean()
});
/**
* Pagination values are non-negative integers. Constraining them here keeps
* malformed input a 400 at the request boundary: without `.int().min(0)`,
* `z.coerce.number()` only rejects values that coerce to `NaN`, so `page=-1`
* and `perPage=2.5` reach the storage layer, which rejects them with a plain
* `Error` that `handleError` can only report as a 500.
*
* The lower bound is 0 rather than 1 because `perPage: 0` is a supported
* storage contract (the include-only fast path).
*/
const paginationNumber = () => zod_v4.z.coerce.number().int().min(0);
/**
* Factory function for page/perPage pagination query params
* @param defaultPerPage - Default value for perPage (omit for no default)
*/
const createPagePaginationSchema = (defaultPerPage) => {
	const baseSchema = { page: paginationNumber().optional().default(0) };
	if (defaultPerPage !== void 0) return zod_v4.z.object({
		...baseSchema,
		perPage: paginationNumber().optional().default(defaultPerPage)
	});
	else return zod_v4.z.object({
		...baseSchema,
		perPage: paginationNumber().optional()
	});
};
/**
* Factory function for pagination that supports both page/perPage and limit/offset
* Use this when you need backwards compatibility with older clients using limit/offset
*/
const createCombinedPaginationSchema = () => {
	return zod_v4.z.object({
		page: paginationNumber().optional(),
		perPage: paginationNumber().optional(),
		/**
		* @deprecated Use page and perPage instead
		*/
		offset: paginationNumber().optional(),
		/**
		* @deprecated Use page and perPage instead
		*/
		limit: paginationNumber().optional()
	});
};
/**
* Tracing options for observability
* Used by agents and workflows
*/
const tracingOptionsSchema = zod_v4.z.object({
	metadata: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional(),
	requestContextKeys: zod_v4.z.array(zod_v4.z.string()).optional(),
	traceId: zod_v4.z.string().optional(),
	parentSpanId: zod_v4.z.string().optional(),
	tags: zod_v4.z.array(zod_v4.z.string()).optional(),
	hideInput: zod_v4.z.boolean().optional(),
	hideOutput: zod_v4.z.boolean().optional()
});
/**
* Core message schema from AI SDK
* Represents messages exchanged with AI models
* Content can be string, array of content parts, or object (for complex message types)
*/
const coreMessageSchema = typedPermissive(zod_v4.z.unknown());
/**
* Standard success response schema
* Used by operations that return only a success boolean
*/
const successResponseSchema = zod_v4.z.object({ success: zod_v4.z.boolean() });
/**
* Standard message response schema
* Used by operations that return only a message string
*/
const messageResponseSchema = zod_v4.z.object({ message: zod_v4.z.string() });
/**
* Partial data query parameter schema
* Used by list endpoints to return minimal data without schemas
*/
const partialQuerySchema = zod_v4.z.object({ partial: zod_v4.z.string().optional() });
/**
* Status filter for get-by-id endpoints.
* Controls which version is resolved:
* - 'published' (default) — resolve with the active (published) version.
* - 'draft' — resolve with the latest version (which may be ahead of the published one).
*/
const statusQuerySchema = zod_v4.z.object({ status: zod_v4.z.enum([
	"draft",
	"published",
	"archived"
]).optional().default("published").describe("Which version to resolve: published (active version) or draft (latest version)") });
/**
* Base log message schema
*/
const baseLogMessageSchema = zod_v4.z.object({
	level: zod_v4.z.enum([
		"debug",
		"info",
		"warn",
		"error",
		"silent"
	]),
	msg: zod_v4.z.string(),
	time: zod_v4.z.date(),
	context: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional(),
	runId: zod_v4.z.string().optional(),
	pid: zod_v4.z.number(),
	hostname: zod_v4.z.string(),
	name: zod_v4.z.string()
});
//#endregion
Object.defineProperty(exports, "baseLogMessageSchema", {
	enumerable: true,
	get: function() {
		return baseLogMessageSchema;
	}
});
Object.defineProperty(exports, "coreMessageSchema", {
	enumerable: true,
	get: function() {
		return coreMessageSchema;
	}
});
Object.defineProperty(exports, "createCombinedPaginationSchema", {
	enumerable: true,
	get: function() {
		return createCombinedPaginationSchema;
	}
});
Object.defineProperty(exports, "createPagePaginationSchema", {
	enumerable: true,
	get: function() {
		return createPagePaginationSchema;
	}
});
Object.defineProperty(exports, "messageResponseSchema", {
	enumerable: true,
	get: function() {
		return messageResponseSchema;
	}
});
Object.defineProperty(exports, "optionalRunIdSchema", {
	enumerable: true,
	get: function() {
		return optionalRunIdSchema;
	}
});
Object.defineProperty(exports, "paginationInfoSchema", {
	enumerable: true,
	get: function() {
		return paginationInfoSchema;
	}
});
Object.defineProperty(exports, "paginationNumber", {
	enumerable: true,
	get: function() {
		return paginationNumber;
	}
});
Object.defineProperty(exports, "partialQuerySchema", {
	enumerable: true,
	get: function() {
		return partialQuerySchema;
	}
});
Object.defineProperty(exports, "runIdSchema", {
	enumerable: true,
	get: function() {
		return runIdSchema;
	}
});
Object.defineProperty(exports, "statusQuerySchema", {
	enumerable: true,
	get: function() {
		return statusQuerySchema;
	}
});
Object.defineProperty(exports, "successResponseSchema", {
	enumerable: true,
	get: function() {
		return successResponseSchema;
	}
});
Object.defineProperty(exports, "tracingOptionsSchema", {
	enumerable: true,
	get: function() {
		return tracingOptionsSchema;
	}
});
Object.defineProperty(exports, "typedPermissive", {
	enumerable: true,
	get: function() {
		return typedPermissive;
	}
});

//# sourceMappingURL=common-BPjna_b6.cjs.map