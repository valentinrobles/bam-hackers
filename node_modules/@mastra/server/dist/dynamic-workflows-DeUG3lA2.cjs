const require_rolldown_runtime = require("./rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("./http-exception-CKDHupKw.cjs");
const require_route_builder = require("./route-builder-DupY8fkI.cjs");
const require_server_handlers_error = require("./server/handlers/error.cjs");
let zod_v4 = require("zod/v4");
//#region src/server/schemas/dynamic-workflows.ts
const stepOptionsSchema = zod_v4.z.object({
	retries: zod_v4.z.number().int().nonnegative().optional(),
	metadata: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional()
}).optional();
const entryDisplayFields = {
	description: zod_v4.z.string().optional(),
	metadata: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional()
};
const containerIdentityFields = {
	id: zod_v4.z.string().optional(),
	...entryDisplayFields
};
const literalScalar = zod_v4.z.union([
	zod_v4.z.string(),
	zod_v4.z.number(),
	zod_v4.z.boolean(),
	zod_v4.z.null()
]);
const pathOrLiteral = zod_v4.z.union([zod_v4.z.object({ path: zod_v4.z.string().min(1) }).strict(), zod_v4.z.object({ literal: literalScalar }).strict()]);
const predicateSchema = zod_v4.z.lazy(() => zod_v4.z.union([
	zod_v4.z.object({
		op: zod_v4.z.enum([
			"eq",
			"ne",
			"lt",
			"lte",
			"gt",
			"gte"
		]),
		left: pathOrLiteral,
		right: pathOrLiteral
	}).strict(),
	zod_v4.z.object({
		op: zod_v4.z.enum(["in", "notIn"]),
		value: pathOrLiteral,
		set: zod_v4.z.array(literalScalar).min(1)
	}).strict(),
	zod_v4.z.object({
		op: zod_v4.z.enum(["exists", "notExists"]),
		path: zod_v4.z.string().min(1)
	}).strict(),
	zod_v4.z.object({
		op: zod_v4.z.enum(["truthy", "falsy"]),
		value: pathOrLiteral
	}).strict(),
	zod_v4.z.object({
		op: zod_v4.z.enum(["and", "or"]),
		args: zod_v4.z.array(predicateSchema).min(1)
	}).strict(),
	zod_v4.z.object({
		op: zod_v4.z.literal("not"),
		arg: predicateSchema
	}).strict()
]));
const agentEntrySchema = zod_v4.z.object({
	type: zod_v4.z.literal("agent"),
	id: zod_v4.z.string(),
	agentId: zod_v4.z.string(),
	description: zod_v4.z.string().optional(),
	outputSchema: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional(),
	options: stepOptionsSchema
});
const toolEntrySchema = zod_v4.z.object({
	type: zod_v4.z.literal("tool"),
	id: zod_v4.z.string(),
	toolId: zod_v4.z.string(),
	description: zod_v4.z.string().optional(),
	options: stepOptionsSchema
});
const mappingEntrySchema = zod_v4.z.object({
	type: zod_v4.z.literal("mapping"),
	id: zod_v4.z.string(),
	...entryDisplayFields,
	mapConfig: zod_v4.z.string()
});
const workflowEntrySchema = zod_v4.z.object({
	type: zod_v4.z.literal("workflow"),
	id: zod_v4.z.string(),
	workflowId: zod_v4.z.string(),
	description: zod_v4.z.string().optional()
});
const singleStepEntrySchema = zod_v4.z.discriminatedUnion("type", [
	agentEntrySchema,
	toolEntrySchema,
	mappingEntrySchema,
	workflowEntrySchema
]);
const foreachInnerStepSchema = zod_v4.z.discriminatedUnion("type", [
	agentEntrySchema,
	toolEntrySchema,
	workflowEntrySchema
]);
const graphEntrySchema = zod_v4.z.discriminatedUnion("type", [
	agentEntrySchema,
	toolEntrySchema,
	mappingEntrySchema,
	workflowEntrySchema,
	zod_v4.z.object({
		type: zod_v4.z.literal("parallel"),
		...containerIdentityFields,
		steps: zod_v4.z.array(singleStepEntrySchema)
	}),
	zod_v4.z.object({
		type: zod_v4.z.literal("foreach"),
		...containerIdentityFields,
		step: foreachInnerStepSchema,
		opts: zod_v4.z.object({ concurrency: zod_v4.z.number().int().positive() }).optional()
	}),
	zod_v4.z.object({
		type: zod_v4.z.literal("sleep"),
		id: zod_v4.z.string(),
		...entryDisplayFields,
		duration: zod_v4.z.number()
	}),
	zod_v4.z.object({
		type: zod_v4.z.literal("sleepUntil"),
		id: zod_v4.z.string(),
		...entryDisplayFields,
		date: zod_v4.z.string()
	}),
	zod_v4.z.object({
		type: zod_v4.z.literal("conditional"),
		...containerIdentityFields,
		steps: zod_v4.z.array(singleStepEntrySchema),
		predicates: zod_v4.z.array(predicateSchema)
	}).refine((entry) => entry.predicates.length === entry.steps.length, {
		message: "conditional entries must have exactly one predicate per branch (`predicates` and `steps` must be the same length)",
		path: ["predicates"]
	}),
	zod_v4.z.object({
		type: zod_v4.z.literal("loop"),
		...containerIdentityFields,
		step: singleStepEntrySchema,
		loopType: zod_v4.z.enum(["dowhile", "dountil"]),
		predicate: predicateSchema
	})
]);
const dynamicWorkflowIdPathParams = zod_v4.z.object({ dynamicWorkflowId: zod_v4.z.string().describe("Unique identifier for the dynamic workflow definition") });
const listDynamicWorkflowsQuerySchema = zod_v4.z.object({
	status: zod_v4.z.enum(["active", "archived"]).optional().describe("Filter dynamic workflows by status (defaults to active when omitted by the handler)"),
	authorId: zod_v4.z.string().optional().describe("Filter dynamic workflows by author identifier")
});
/**
* One static workflow definition on the wire. Matches the input shape of
* `mastra.addDynamicWorkflow()`.
*/
const dynamicWorkflowDefinitionBodySchema = zod_v4.z.object({
	id: zod_v4.z.string().describe("Workflow id — kebab-case, descriptive"),
	description: zod_v4.z.string().optional(),
	metadata: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional(),
	inputSchema: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).describe("JSON Schema (Draft 2020-12) for the workflow input"),
	outputSchema: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).describe("JSON Schema (Draft 2020-12) for the workflow output"),
	stateSchema: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional(),
	requestContextSchema: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional(),
	graph: zod_v4.z.array(graphEntrySchema).describe("Static workflow graph — ordered array of serialized step entries with all refs as ids.")
});
/**
* Body for `POST /stored/workflows` — upsert a static workflow definition,
* optionally together with the helper workflows it nests.
*/
const upsertDynamicWorkflowBodySchema = dynamicWorkflowDefinitionBodySchema.extend({ dependencies: zod_v4.z.array(dynamicWorkflowDefinitionBodySchema).optional().describe("Helper workflow definitions this workflow nests. Saved with it as one unit — the whole set is validated together, hydrated in derived dependency order, and rejected together, so a failed save never leaves orphaned helpers behind. Each helper becomes an ordinary dynamic workflow in its own right.") });
/**
* Shape returned for any single dynamic workflow row.
*/
const dynamicWorkflowResponseSchema = zod_v4.z.object({
	id: zod_v4.z.string(),
	description: zod_v4.z.string().optional(),
	metadata: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional(),
	inputSchema: zod_v4.z.unknown(),
	outputSchema: zod_v4.z.unknown(),
	stateSchema: zod_v4.z.unknown().optional(),
	requestContextSchema: zod_v4.z.unknown().optional(),
	graph: zod_v4.z.array(zod_v4.z.unknown()),
	status: zod_v4.z.enum(["active", "archived"]),
	source: zod_v4.z.literal("storage"),
	authorId: zod_v4.z.string().optional(),
	createdAt: zod_v4.z.union([zod_v4.z.date(), zod_v4.z.string()]),
	updatedAt: zod_v4.z.union([zod_v4.z.date(), zod_v4.z.string()])
});
const listDynamicWorkflowsResponseSchema = zod_v4.z.object({
	workflows: zod_v4.z.array(dynamicWorkflowResponseSchema),
	total: zod_v4.z.number()
});
const getDynamicWorkflowResponseSchema = dynamicWorkflowResponseSchema;
const upsertDynamicWorkflowResponseSchema = zod_v4.z.object({
	ok: zod_v4.z.literal(true),
	id: zod_v4.z.string(),
	dependencyIds: zod_v4.z.array(zod_v4.z.string()).optional().describe("Ids of the helper workflows saved alongside this one. Present only when dependencies were supplied.")
});
const deleteDynamicWorkflowResponseSchema = zod_v4.z.object({
	success: zod_v4.z.literal(true),
	message: zod_v4.z.string()
});
//#endregion
//#region src/server/handlers/dynamic-workflows.ts
var dynamic_workflows_exports = /* @__PURE__ */ require_rolldown_runtime.__exportAll({
	DELETE_DYNAMIC_WORKFLOW_ROUTE: () => DELETE_DYNAMIC_WORKFLOW_ROUTE,
	GET_DYNAMIC_WORKFLOW_ROUTE: () => GET_DYNAMIC_WORKFLOW_ROUTE,
	LIST_DYNAMIC_WORKFLOWS_ROUTE: () => LIST_DYNAMIC_WORKFLOWS_ROUTE,
	UPSERT_DYNAMIC_WORKFLOW_ROUTE: () => UPSERT_DYNAMIC_WORKFLOW_ROUTE
});
/**
* GET /stored/workflows — list stored static workflow definitions.
*
* Mirrors `LIST_STORED_AGENTS_ROUTE` but without favorites/visibility/authorship
* scoping (which the workflow-definitions domain doesn't carry in v1).
*/
const LIST_DYNAMIC_WORKFLOWS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/workflows",
	responseType: "json",
	queryParamSchema: listDynamicWorkflowsQuerySchema,
	responseSchema: listDynamicWorkflowsResponseSchema,
	summary: "List dynamic workflow definitions",
	description: "Returns workflow definitions persisted to storage. Filterable by status and authorId.",
	tags: ["Dynamic Workflows"],
	requiresAuth: true,
	requiresPermission: "stored-workflows:read",
	handler: async ({ mastra, status, authorId }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const store = await storage.getStore("workflowDefinitions");
			if (!store) throw new require_http_exception.HTTPException(500, { message: "workflowDefinitions storage domain is not available" });
			const result = await store.list({
				status: status ?? "active",
				authorId
			});
			return {
				workflows: result.definitions,
				total: result.total
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing dynamic workflows");
		}
	}
});
/**
* GET /stored/workflows/:dynamicWorkflowId — get one dynamic workflow.
*/
const GET_DYNAMIC_WORKFLOW_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/workflows/:dynamicWorkflowId",
	responseType: "json",
	pathParamSchema: dynamicWorkflowIdPathParams,
	responseSchema: getDynamicWorkflowResponseSchema,
	summary: "Get a dynamic workflow definition by id",
	description: "Returns a single workflow definition persisted to storage.",
	tags: ["Dynamic Workflows"],
	requiresAuth: true,
	requiresPermission: "stored-workflows:read",
	handler: async ({ mastra, dynamicWorkflowId }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const store = await storage.getStore("workflowDefinitions");
			if (!store) throw new require_http_exception.HTTPException(500, { message: "workflowDefinitions storage domain is not available" });
			const def = await store.get(dynamicWorkflowId);
			if (!def) throw new require_http_exception.HTTPException(404, { message: `Dynamic workflow "${dynamicWorkflowId}" not found` });
			return def;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting dynamic workflow");
		}
	}
});
/**
* POST /stored/workflows — upsert a static workflow definition and live-register
* it on the Mastra instance.
*
* Calls `mastra.addDynamicWorkflow(def)` which persists the row + rehydrates a
* runnable workflow + registers it via `mastra.addWorkflow(workflow, id)`.
* After this returns, `GET /workflows/:id` and `POST /workflows/:id/run` work
* immediately — no server restart needed.
*/
const UPSERT_DYNAMIC_WORKFLOW_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/workflows",
	responseType: "json",
	bodySchema: upsertDynamicWorkflowBodySchema,
	responseSchema: upsertDynamicWorkflowResponseSchema,
	summary: "Upsert a dynamic workflow definition and live-register it",
	description: "Persists a static workflow definition and live-registers it on the running Mastra instance. Idempotent — same id updates in place.",
	tags: ["Dynamic Workflows"],
	requiresAuth: true,
	requiresPermission: "stored-workflows:write",
	handler: async ({ mastra, id, description, metadata, inputSchema, outputSchema, stateSchema, requestContextSchema, graph, dependencies }) => {
		try {
			const def = {
				id,
				description,
				metadata,
				inputSchema,
				outputSchema,
				stateSchema,
				requestContextSchema,
				graph
			};
			const bundle = [...dependencies ?? [], def];
			await mastra.addDynamicWorkflows(bundle);
			return {
				ok: true,
				id: def.id,
				...dependencies?.length ? { dependencyIds: dependencies.map((dependency) => dependency.id) } : {}
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error upserting dynamic workflow");
		}
	}
});
/**
* DELETE /stored/workflows/:dynamicWorkflowId — delete a dynamic workflow.
*
* Removes the row from storage AND un-registers the live Workflow instance so
* a subsequent POST with the same id starts from a clean slate. Idempotent.
*/
const DELETE_DYNAMIC_WORKFLOW_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/workflows/:dynamicWorkflowId",
	responseType: "json",
	pathParamSchema: dynamicWorkflowIdPathParams,
	responseSchema: deleteDynamicWorkflowResponseSchema,
	summary: "Delete a dynamic workflow definition",
	description: "Removes a dynamic workflow definition and unregisters the live workflow instance. Idempotent.",
	tags: ["Dynamic Workflows"],
	requiresAuth: true,
	requiresPermission: "stored-workflows:write",
	handler: async ({ mastra, dynamicWorkflowId }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const store = await storage.getStore("workflowDefinitions");
			if (!store) throw new require_http_exception.HTTPException(500, { message: "workflowDefinitions storage domain is not available" });
			await store.delete(dynamicWorkflowId);
			mastra.removeWorkflow(dynamicWorkflowId);
			return {
				success: true,
				message: `Workflow ${dynamicWorkflowId} deleted`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting dynamic workflow");
		}
	}
});
//#endregion
Object.defineProperty(exports, "DELETE_DYNAMIC_WORKFLOW_ROUTE", {
	enumerable: true,
	get: function() {
		return DELETE_DYNAMIC_WORKFLOW_ROUTE;
	}
});
Object.defineProperty(exports, "GET_DYNAMIC_WORKFLOW_ROUTE", {
	enumerable: true,
	get: function() {
		return GET_DYNAMIC_WORKFLOW_ROUTE;
	}
});
Object.defineProperty(exports, "LIST_DYNAMIC_WORKFLOWS_ROUTE", {
	enumerable: true,
	get: function() {
		return LIST_DYNAMIC_WORKFLOWS_ROUTE;
	}
});
Object.defineProperty(exports, "UPSERT_DYNAMIC_WORKFLOW_ROUTE", {
	enumerable: true,
	get: function() {
		return UPSERT_DYNAMIC_WORKFLOW_ROUTE;
	}
});
Object.defineProperty(exports, "dynamic_workflows_exports", {
	enumerable: true,
	get: function() {
		return dynamic_workflows_exports;
	}
});

//# sourceMappingURL=dynamic-workflows-DeUG3lA2.cjs.map