const require_agents = require("./agents-CZE_MSQE.cjs");
let zod_v4 = require("zod/v4");
//#region src/server/schemas/editor-builder.ts
/**
* Agent feature flags for the builder.
*
* Wire format: each key is an optional boolean. The server normalizes admin
* input via `resolveAgentFeatures` (default-on semantics): any omitted key
* resolves to `true`; admins opt out by setting a key to `false`. The
* `GET /editor/builder/settings` response always carries a fully-resolved
* object — clients should still use strict `=== true` checks.
*
* Special cases:
* - `browser`: only resolves to `true` when `configuration.agent.browser` is
*   provided. Omitted with no config ⇒ silently `false` (no warning).
*   Explicit `true` with no config ⇒ warns and downgrades to `false`.
*/
const agentFeaturesSchema = zod_v4.z.object({
	tools: zod_v4.z.boolean().optional(),
	agents: zod_v4.z.boolean().optional(),
	workflows: zod_v4.z.boolean().optional(),
	scorers: zod_v4.z.boolean().optional(),
	skills: zod_v4.z.boolean().optional(),
	memory: zod_v4.z.boolean().optional(),
	variables: zod_v4.z.boolean().optional(),
	favorites: zod_v4.z.boolean().optional(),
	avatarUpload: zod_v4.z.boolean().optional(),
	browser: zod_v4.z.boolean().optional(),
	/**
	* Whether the model picker is visible in the Agent Builder.
	* Omitted ⇒ picker visible (default-on). Explicit `false` ⇒ picker hidden
	* (locked mode); `models.default` is required and applied.
	*/
	model: zod_v4.z.boolean().optional()
});
/**
* Allowlist + default-model entries for {@link agentModelsSchema}.
*
* Four standalone schemas (known × custom × entry × default) joined with `z.union`.
* The schema does NOT validate `provider` against the runtime registry — invalid strings
* surface as warnings during Phase 4 config validation.
*
* NOTE: `z.union(...).extend()` does not exist; that's why these are separate schemas.
*/
const knownProviderEntrySchema = zod_v4.z.object({
	provider: zod_v4.z.string().min(1),
	modelId: zod_v4.z.string().min(1).optional()
}).strict();
const customProviderEntrySchema = zod_v4.z.object({
	kind: zod_v4.z.literal("custom"),
	provider: zod_v4.z.string().min(1),
	modelId: zod_v4.z.string().min(1).optional()
}).strict();
const knownDefaultModelEntrySchema = zod_v4.z.object({
	provider: zod_v4.z.string().min(1),
	modelId: zod_v4.z.string().min(1)
}).strict();
const customDefaultModelEntrySchema = zod_v4.z.object({
	kind: zod_v4.z.literal("custom"),
	provider: zod_v4.z.string().min(1),
	modelId: zod_v4.z.string().min(1)
}).strict();
const providerModelEntrySchema = zod_v4.z.union([customProviderEntrySchema, knownProviderEntrySchema]);
const defaultModelEntrySchema = zod_v4.z.union([customDefaultModelEntrySchema, knownDefaultModelEntrySchema]);
/**
* Admin-controlled model allowlist + default for the Agent Builder.
*/
const agentModelsSchema = zod_v4.z.object({
	allowed: zod_v4.z.array(providerModelEntrySchema).optional(),
	default: defaultModelEntrySchema.optional()
});
/**
* Admin-controlled allowlist for one of the builder pickers (tools/agents/workflows).
*
* Semantics:
* - omitted ⇒ unrestricted (all registered IDs shown in the picker).
* - `allowed: []` ⇒ empty picker (explicit lockdown).
* - `allowed: [...ids]` ⇒ only the listed IDs are shown.
*/
const pickerAllowlistSchema = zod_v4.z.object({ allowed: zod_v4.z.array(zod_v4.z.string()).optional() }).strict();
/**
* Agent configuration (pinned, non-overridable settings).
*
* Known structured field: `models` (Phase 1 contracts).
* Other keys flow through unchanged for forward compatibility.
*/
const agentConfigurationSchema = zod_v4.z.object({
	models: agentModelsSchema.optional(),
	tools: pickerAllowlistSchema.optional(),
	agents: pickerAllowlistSchema.optional(),
	workflows: pickerAllowlistSchema.optional()
}).catchall(zod_v4.z.unknown());
/**
* Resolved picker visibility returned in `BuilderSettingsResponse`.
*
* Per kind:
* - `null` ⇒ unrestricted (show all registered entries).
* - `string[]` ⇒ explicit allowlist (may be empty to show none).
*/
const builderPickerSchema = zod_v4.z.object({
	visibleTools: zod_v4.z.array(zod_v4.z.string()).nullable(),
	visibleAgents: zod_v4.z.array(zod_v4.z.string()).nullable(),
	visibleWorkflows: zod_v4.z.array(zod_v4.z.string()).nullable()
});
/**
* Derived `BuilderModelPolicy`. Server-owned shape so the playground hook is a
* thin selector and the UI never re-derives policy from `features` / `configuration`.
*
* Mirrors `BuilderModelPolicy` from `@mastra/core/agent-builder/ee`:
* - `active: false` ⇒ all other fields ignored.
* - `active: true` + `pickerVisible: false` (locked) ⇒ `default` set in valid configs.
* - `allowed`/`default` are passed through verbatim when present.
*/
const builderModelPolicySchema = zod_v4.z.object({
	active: zod_v4.z.boolean(),
	pickerVisible: zod_v4.z.boolean().optional(),
	allowed: zod_v4.z.array(providerModelEntrySchema).optional(),
	default: defaultModelEntrySchema.optional()
});
/**
* Response schema for GET /editor/builder/settings
*/
const builderSettingsResponseSchema = zod_v4.z.object({
	enabled: zod_v4.z.boolean(),
	features: zod_v4.z.object({ agent: agentFeaturesSchema.optional() }).optional(),
	configuration: zod_v4.z.object({ agent: agentConfigurationSchema.optional() }).optional(),
	modelPolicy: builderModelPolicySchema.optional(),
	/**
	* Resolved picker visibility for tools/agents/workflows. Always present when
	* the builder is enabled. Omitted when the builder is disabled.
	*/
	picker: builderPickerSchema.optional(),
	/**
	* Non-fatal warnings produced by `EditorAgentBuilder`'s constructor-time
	* validation (e.g. allowlist entries with unknown provider strings, or
	* picker allowlist entries that don't match a registered ID). UI surfaces
	* these as a banner in the Builder admin view.
	*/
	modelPolicyWarnings: zod_v4.z.array(zod_v4.z.string()).optional()
});
/**
* Infrastructure status response for Agent Builder admin diagnostics.
*
* Reports the Agent Builder-specific primitive configuration plus lightweight
* runtime resolution state where useful.
*/
const infrastructureStatusResponseSchema = zod_v4.z.object({
	channels: zod_v4.z.object({ providers: zod_v4.z.array(zod_v4.z.object({
		id: zod_v4.z.string(),
		name: zod_v4.z.string(),
		isConfigured: zod_v4.z.boolean(),
		routeCount: zod_v4.z.number()
	})) }),
	browser: zod_v4.z.object({
		type: zod_v4.z.string().nullable(),
		provider: zod_v4.z.string().nullable(),
		env: zod_v4.z.string().nullable(),
		registered: zod_v4.z.boolean(),
		availableProviders: zod_v4.z.array(zod_v4.z.string()),
		config: zod_v4.z.array(zod_v4.z.object({
			key: zod_v4.z.string(),
			value: zod_v4.z.string()
		}))
	}),
	workspace: zod_v4.z.object({
		type: zod_v4.z.string().nullable(),
		workspaceId: zod_v4.z.string().nullable(),
		name: zod_v4.z.string().nullable(),
		source: zod_v4.z.string().nullable(),
		registered: zod_v4.z.boolean(),
		hasFilesystem: zod_v4.z.boolean(),
		hasSandbox: zod_v4.z.boolean(),
		filesystemProvider: zod_v4.z.string().nullable(),
		sandboxProvider: zod_v4.z.string().nullable(),
		config: zod_v4.z.array(zod_v4.z.object({
			key: zod_v4.z.string(),
			value: zod_v4.z.string()
		}))
	}),
	registries: zod_v4.z.object({ skillsSh: zod_v4.z.object({ enabled: zod_v4.z.boolean() }) })
});
/**
* Response schema for GET /editor/builder/models/available.
*
* Same provider shape as GET /agents/providers, but each provider's `models`
* list is already filtered by the active builder model policy (server applies
* the EE allowlist). Providers with no allowed models are omitted entirely so
* the Studio model picker can render the response verbatim.
*/
const builderAvailableModelsResponseSchema = zod_v4.z.object({ providers: zod_v4.z.array(require_agents.providerSchema) });
//#endregion
Object.defineProperty(exports, "agentConfigurationSchema", {
	enumerable: true,
	get: function() {
		return agentConfigurationSchema;
	}
});
Object.defineProperty(exports, "agentFeaturesSchema", {
	enumerable: true,
	get: function() {
		return agentFeaturesSchema;
	}
});
Object.defineProperty(exports, "agentModelsSchema", {
	enumerable: true,
	get: function() {
		return agentModelsSchema;
	}
});
Object.defineProperty(exports, "builderAvailableModelsResponseSchema", {
	enumerable: true,
	get: function() {
		return builderAvailableModelsResponseSchema;
	}
});
Object.defineProperty(exports, "builderModelPolicySchema", {
	enumerable: true,
	get: function() {
		return builderModelPolicySchema;
	}
});
Object.defineProperty(exports, "builderPickerSchema", {
	enumerable: true,
	get: function() {
		return builderPickerSchema;
	}
});
Object.defineProperty(exports, "builderSettingsResponseSchema", {
	enumerable: true,
	get: function() {
		return builderSettingsResponseSchema;
	}
});
Object.defineProperty(exports, "defaultModelEntrySchema", {
	enumerable: true,
	get: function() {
		return defaultModelEntrySchema;
	}
});
Object.defineProperty(exports, "infrastructureStatusResponseSchema", {
	enumerable: true,
	get: function() {
		return infrastructureStatusResponseSchema;
	}
});
Object.defineProperty(exports, "pickerAllowlistSchema", {
	enumerable: true,
	get: function() {
		return pickerAllowlistSchema;
	}
});
Object.defineProperty(exports, "providerModelEntrySchema", {
	enumerable: true,
	get: function() {
		return providerModelEntrySchema;
	}
});

//# sourceMappingURL=editor-builder-BHf-g7q9.cjs.map