import z from "zod";
//#region src/server/schemas/responses.ts
const responseIdPathParams = z.object({ responseId: z.string().describe("Unique identifier for the stored response") });
const responseInputTextPartSchema = z.object({
	type: z.enum([
		"input_text",
		"text",
		"output_text"
	]),
	text: z.string()
});
const responseInputMessageSchema = z.object({
	role: z.enum([
		"system",
		"developer",
		"user",
		"assistant"
	]),
	content: z.union([z.string(), z.array(responseInputTextPartSchema)])
});
const providerOptionValuesSchema = z.record(z.string(), z.unknown());
const providerOptionsSchema = z.object({
	openai: providerOptionValuesSchema.optional().describe("OpenAI provider options such as previousResponseId, conversation, or responseId"),
	azure: providerOptionValuesSchema.optional().describe("Azure OpenAI provider options such as previousResponseId, store, or itemId")
}).passthrough();
const responseTextFormatJsonObjectSchema = z.object({ type: z.literal("json_object").describe("Requests JSON object output compatibility for the response") });
const responseTextFormatJsonSchemaSchema = z.object({
	type: z.literal("json_schema"),
	name: z.string(),
	description: z.string().optional(),
	schema: z.record(z.string(), z.unknown()),
	strict: z.boolean().optional()
});
const responseTextFormatSchema = z.union([responseTextFormatJsonObjectSchema, responseTextFormatJsonSchemaSchema]);
const responseTextSchema = z.object({ format: responseTextFormatSchema });
const createResponseBodySchema = z.object({
	model: z.string().optional().describe("Optional model identifier override, such as openai/gpt-5. When omitted, the agent default model is used."),
	agent_id: z.string().min(1).optional().describe("Mastra agent ID for the request. Required unless previous_response_id is provided."),
	input: z.union([z.string(), z.array(responseInputMessageSchema)]),
	instructions: z.string().optional(),
	text: responseTextSchema.optional().describe("Optional text output format. Supports `json_object` for JSON mode and `json_schema` for schema-constrained structured output."),
	conversation_id: z.string().optional().describe("Optional conversation ID. In Mastra this is the raw threadId."),
	providerOptions: providerOptionsSchema.optional().describe("Optional provider-specific options passed through to the underlying model call"),
	stream: z.boolean().optional().default(false),
	store: z.boolean().optional().default(false),
	previous_response_id: z.string().min(1).optional()
}).passthrough().refine((data) => data.agent_id || data.previous_response_id, {
	message: "agent_id is required unless previous_response_id is provided",
	path: []
});
const responseOutputTextSchema = z.object({
	type: z.literal("output_text"),
	text: z.string(),
	annotations: z.array(z.unknown()).optional(),
	logprobs: z.array(z.unknown()).optional()
});
const responseInputTextContentSchema = z.object({
	type: z.literal("input_text"),
	text: z.string()
});
const conversationMessageContentSchema = z.union([responseInputTextContentSchema, responseOutputTextSchema]);
const responseOutputMessageSchema = z.object({
	id: z.string(),
	type: z.literal("message"),
	role: z.literal("assistant"),
	status: z.enum([
		"in_progress",
		"completed",
		"incomplete"
	]),
	content: z.array(responseOutputTextSchema)
});
const responseOutputFunctionCallSchema = z.object({
	id: z.string(),
	type: z.literal("function_call"),
	call_id: z.string(),
	name: z.string(),
	arguments: z.string(),
	status: z.enum([
		"in_progress",
		"completed",
		"incomplete"
	]).optional()
});
const responseOutputFunctionCallOutputSchema = z.object({
	id: z.string(),
	type: z.literal("function_call_output"),
	call_id: z.string(),
	output: z.string()
});
const responseUsageSchema = z.object({
	input_tokens: z.number(),
	output_tokens: z.number(),
	total_tokens: z.number(),
	input_tokens_details: z.object({ cached_tokens: z.number() }).optional(),
	output_tokens_details: z.object({ reasoning_tokens: z.number() }).optional()
});
const responseToolSchema = z.object({
	type: z.literal("function"),
	name: z.string(),
	description: z.string().optional(),
	parameters: z.unknown().optional()
});
const responseOutputItemSchema = z.union([
	responseOutputMessageSchema,
	responseOutputFunctionCallSchema,
	responseOutputFunctionCallOutputSchema
]);
const conversationItemMessageSchema = z.object({
	id: z.string(),
	type: z.literal("message"),
	role: z.enum([
		"system",
		"user",
		"assistant"
	]),
	status: z.literal("completed"),
	content: z.array(conversationMessageContentSchema)
});
const conversationItemSchema = z.union([
	conversationItemMessageSchema,
	responseOutputFunctionCallSchema,
	responseOutputFunctionCallOutputSchema
]);
const responseObjectSchema = z.object({
	id: z.string(),
	object: z.literal("response"),
	created_at: z.number(),
	completed_at: z.number().nullable(),
	model: z.string(),
	status: z.enum([
		"in_progress",
		"completed",
		"incomplete"
	]),
	output: z.array(responseOutputItemSchema),
	usage: responseUsageSchema.nullable(),
	error: z.null().optional(),
	incomplete_details: z.null().optional(),
	instructions: z.string().nullable().optional(),
	text: responseTextSchema.nullable().optional(),
	previous_response_id: z.string().nullable().optional(),
	conversation_id: z.string().nullable().optional(),
	providerOptions: providerOptionsSchema.optional(),
	tools: z.array(responseToolSchema).optional(),
	store: z.boolean().optional()
});
const deleteResponseSchema = z.object({
	id: z.string(),
	object: z.literal("response"),
	deleted: z.literal(true)
});
//#endregion
export { responseToolSchema as _, deleteResponseSchema as a, responseInputTextContentSchema as c, responseOutputFunctionCallOutputSchema as d, responseOutputFunctionCallSchema as f, responseTextFormatSchema as g, responseOutputTextSchema as h, createResponseBodySchema as i, responseInputTextPartSchema as l, responseOutputMessageSchema as m, conversationItemSchema as n, responseIdPathParams as o, responseOutputItemSchema as p, conversationMessageContentSchema as r, responseInputMessageSchema as s, conversationItemMessageSchema as t, responseObjectSchema as u, responseUsageSchema as v };

//# sourceMappingURL=responses-DJNJgCTg.js.map