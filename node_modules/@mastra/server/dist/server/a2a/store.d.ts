import type { Task } from '@mastra/core/a2a';
export declare class TaskStoreVersionConflictError extends Error {
    constructor(expectedVersion: number, currentVersion: number);
}
export declare class InMemoryTaskStore {
    private store;
    private versions;
    private listeners;
    private abortControllers;
    activeCancellations: Set<string>;
    private getKey;
    load({ agentId, taskId }: {
        agentId: string;
        taskId: string;
    }): Promise<Task | null>;
    loadWithVersion({ agentId, taskId }: {
        agentId: string;
        taskId: string;
    }): {
        task: Task;
        version: number;
    } | null;
    save({ agentId, data, expectedVersion, skipIfCanceled, }: {
        agentId: string;
        data: Task;
        expectedVersion?: number;
        skipIfCanceled?: boolean;
    }): Promise<Task>;
    getVersion({ agentId, taskId }: {
        agentId: string;
        taskId: string;
    }): number;
    list({ agentId }: {
        agentId: string;
    }): Task[];
    registerAbortController({ agentId, taskId, controller, }: {
        agentId: string;
        taskId: string;
        controller: AbortController;
    }): () => void;
    abortTask({ agentId, taskId, reason }: {
        agentId: string;
        taskId: string;
        reason?: unknown;
    }): void;
    waitForNextUpdate({ agentId, taskId, afterVersion, signal, }: {
        agentId: string;
        taskId: string;
        afterVersion: number;
        signal?: AbortSignal;
    }): Promise<{
        task: Task;
        version: number;
    }>;
}
//# sourceMappingURL=store.d.ts.map