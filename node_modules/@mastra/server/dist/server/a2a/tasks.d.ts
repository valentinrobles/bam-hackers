import type { Message, Task, TaskStatus, TaskContext, TaskArtifactUpdateEvent } from '@mastra/core/a2a';
import type { IMastraLogger } from '@mastra/core/logger';
import { type RequestContext } from '@mastra/core/request-context';
import { type InMemoryTaskStore } from './store.js';
export declare function applyUpdateToTask(current: Task, update: Omit<TaskStatus, 'timestamp'> | TaskArtifactUpdateEvent): Task;
export declare function resolveTaskMemory({ task, agentId, requestContext, metadata, message, }: {
    task?: Task;
    agentId: string;
    requestContext?: RequestContext;
    metadata?: Record<string, unknown>;
    message?: Message;
}): {
    thread: string | undefined;
    resource: string;
};
export declare function loadOrCreateTask({ agentId, taskId, taskStore, message, contextId, metadata, logger, requestContext, }: {
    agentId: string;
    taskId: string;
    taskStore: InMemoryTaskStore;
    message: Message;
    contextId?: string;
    metadata?: Record<string, unknown>;
    logger?: IMastraLogger;
    requestContext?: RequestContext;
}): Promise<Task>;
export declare function createTaskContext({ task, userMessage, history, activeCancellations, }: {
    task: Task;
    userMessage: Message;
    history: Message[];
    activeCancellations: Set<string>;
}): TaskContext;
//# sourceMappingURL=tasks.d.ts.map