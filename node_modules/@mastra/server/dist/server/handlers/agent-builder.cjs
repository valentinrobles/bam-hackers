Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_rolldown_runtime = require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_common = require("../../common-BPjna_b6.cjs");
const require_workflows = require("../../workflows-CvYeyvIc.cjs");
const require_agent_builder = require("../../agent-builder-k72O4OB9.cjs");
const require_agents = require("../../agents-CZE_MSQE.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_server_handlers_workflows = require("./workflows.cjs");
//#region src/server/handlers/agent-builder.ts
var agent_builder_exports = /* @__PURE__ */ require_rolldown_runtime.__exportAll({
	CANCEL_AGENT_BUILDER_ACTION_RUN_ROUTE: () => CANCEL_AGENT_BUILDER_ACTION_RUN_ROUTE,
	CREATE_AGENT_BUILDER_ACTION_RUN_ROUTE: () => CREATE_AGENT_BUILDER_ACTION_RUN_ROUTE,
	GET_AGENT_BUILDER_ACTION_BY_ID_ROUTE: () => GET_AGENT_BUILDER_ACTION_BY_ID_ROUTE,
	GET_AGENT_BUILDER_ACTION_RUN_BY_ID_ROUTE: () => GET_AGENT_BUILDER_ACTION_RUN_BY_ID_ROUTE,
	LIST_AGENT_BUILDER_ACTIONS_ROUTE: () => LIST_AGENT_BUILDER_ACTIONS_ROUTE,
	LIST_AGENT_BUILDER_ACTION_RUNS_ROUTE: () => LIST_AGENT_BUILDER_ACTION_RUNS_ROUTE,
	OBSERVE_STREAM_AGENT_BUILDER_ACTION_ROUTE: () => OBSERVE_STREAM_AGENT_BUILDER_ACTION_ROUTE,
	OBSERVE_STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE: () => OBSERVE_STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE,
	RESUME_AGENT_BUILDER_ACTION_ROUTE: () => RESUME_AGENT_BUILDER_ACTION_ROUTE,
	RESUME_ASYNC_AGENT_BUILDER_ACTION_ROUTE: () => RESUME_ASYNC_AGENT_BUILDER_ACTION_ROUTE,
	RESUME_NO_WAIT_AGENT_BUILDER_ACTION_ROUTE: () => RESUME_NO_WAIT_AGENT_BUILDER_ACTION_ROUTE,
	RESUME_STREAM_AGENT_BUILDER_ACTION_ROUTE: () => RESUME_STREAM_AGENT_BUILDER_ACTION_ROUTE,
	START_AGENT_BUILDER_ACTION_RUN_ROUTE: () => START_AGENT_BUILDER_ACTION_RUN_ROUTE,
	START_ASYNC_AGENT_BUILDER_ACTION_ROUTE: () => START_ASYNC_AGENT_BUILDER_ACTION_ROUTE,
	STREAM_AGENT_BUILDER_ACTION_ROUTE: () => STREAM_AGENT_BUILDER_ACTION_ROUTE,
	STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE: () => STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE
});
let agentBuilderWorkflowsPromise;
async function loadAgentBuilderWorkflows() {
	agentBuilderWorkflowsPromise ??= Promise.resolve().then(() => require("../../dist-BKxbtiJd.cjs")).then((mod) => mod.agentBuilderWorkflows);
	return agentBuilderWorkflowsPromise;
}
async function registerAgentBuilderWorkflows(mastra) {
	const agentBuilderWorkflows = await loadAgentBuilderWorkflows();
	require_utils.WorkflowRegistry.registerTemporaryWorkflows(agentBuilderWorkflows, mastra);
	return agentBuilderWorkflows;
}
const LIST_AGENT_BUILDER_ACTIONS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/agent-builder",
	responseType: "json",
	responseSchema: require_workflows.listWorkflowsResponseSchema,
	summary: "List agent-builder actions",
	description: "Returns a list of all available agent-builder actions",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			logger.info("Listing agent builder actions");
			return await require_server_handlers_workflows.LIST_WORKFLOWS_ROUTE.handler(ctx);
		} catch (error) {
			logger.error("Error listing agent builder actions", { error });
			return require_server_handlers_error.handleError(error, "Error getting agent builder workflows");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const GET_AGENT_BUILDER_ACTION_BY_ID_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/agent-builder/:actionId",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	responseSchema: require_workflows.workflowInfoSchema,
	summary: "Get action by ID",
	description: "Returns details for a specific agent-builder action",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId } = ctx;
		const logger = mastra.getLogger();
		try {
			const agentBuilderWorkflows = await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}. Valid actions are: ${Object.keys(agentBuilderWorkflows).join(", ")}` });
			logger.info("Getting agent builder action by ID", { actionId });
			return await require_server_handlers_workflows.GET_WORKFLOW_BY_ID_ROUTE.handler({
				...ctx,
				workflowId: actionId
			});
		} catch (error) {
			logger.error("Error getting agent builder action by ID", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error getting agent builder action");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const LIST_AGENT_BUILDER_ACTION_RUNS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/agent-builder/:actionId/runs",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_workflows.listWorkflowRunsQuerySchema,
	responseSchema: require_workflows.workflowRunsResponseSchema,
	summary: "List action runs",
	description: "Returns a paginated list of execution runs for the specified action",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Listing agent builder action runs", { actionId });
			return await require_server_handlers_workflows.LIST_WORKFLOW_RUNS_ROUTE.handler({
				...ctx,
				workflowId: actionId
			});
		} catch (error) {
			logger.error("Error listing agent builder action runs", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error getting agent builder action runs");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const GET_AGENT_BUILDER_ACTION_RUN_BY_ID_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/agent-builder/:actionId/runs/:runId",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionRunPathParams,
	queryParamSchema: require_workflows.workflowRunResultQuerySchema,
	responseSchema: require_workflows.workflowRunResultSchema,
	summary: "Get action run by ID",
	description: "Returns details for a specific action run with metadata and processed execution state. Use the fields query parameter to reduce payload size.",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Getting agent builder action run by ID", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.GET_WORKFLOW_RUN_BY_ID_ROUTE.handler({
				...ctx,
				workflowId: actionId
			});
		} catch (error) {
			logger.error("Error getting agent builder action run", {
				error,
				actionId,
				runId
			});
			return require_server_handlers_error.handleError(error, "Error getting agent builder action run");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const CREATE_AGENT_BUILDER_ACTION_RUN_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/create-run",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.optionalRunIdSchema,
	responseSchema: require_workflows.createWorkflowRunResponseSchema,
	summary: "Create action run",
	description: "Creates a new action execution instance with an optional custom run ID",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Creating agent builder action run", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.CREATE_WORKFLOW_RUN_ROUTE.handler({
				...ctx,
				workflowId: actionId
			});
		} catch (error) {
			logger.error("Error creating agent builder action run", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error creating agent builder action run");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const STREAM_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/stream",
	responseType: "stream",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	bodySchema: require_agent_builder.streamAgentBuilderBodySchema,
	responseSchema: require_agents.streamResponseSchema,
	summary: "Stream action execution",
	description: "Executes an action and streams the results in real-time",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Streaming agent builder action", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.STREAM_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error streaming agent builder action", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error streaming agent builder action");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const START_ASYNC_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/start-async",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.optionalRunIdSchema,
	bodySchema: require_agent_builder.startAsyncAgentBuilderBodySchema,
	responseSchema: require_workflows.workflowExecutionResultSchema,
	summary: "Start action asynchronously",
	description: "Starts an action execution asynchronously without streaming results",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Starting agent builder action asynchronously", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.START_ASYNC_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error starting agent builder action asynchronously", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error starting agent builder action");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const START_AGENT_BUILDER_ACTION_RUN_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/start",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	bodySchema: require_agent_builder.startAsyncAgentBuilderBodySchema,
	responseSchema: require_workflows.workflowControlResponseSchema,
	summary: "Start specific action run",
	description: "Starts execution of a specific action run by ID",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Starting specific agent builder action run", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.START_WORKFLOW_RUN_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error starting agent builder action run", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error starting agent builder action run");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const OBSERVE_STREAM_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/observe",
	responseType: "stream",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	responseSchema: require_agents.streamResponseSchema,
	summary: "Observe action stream",
	description: "Observes and streams updates from an already running action execution",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Observing agent builder action stream", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.OBSERVE_STREAM_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId
			});
		} catch (error) {
			logger.error("Error observing agent builder action stream", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error observing agent builder action stream");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const RESUME_ASYNC_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/resume-async",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	bodySchema: require_agent_builder.resumeAgentBuilderBodySchema,
	responseSchema: require_workflows.workflowExecutionResultSchema,
	summary: "Resume action asynchronously",
	description: "Resumes a suspended action execution asynchronously without streaming",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, step, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Resuming agent builder action asynchronously", {
				actionId,
				runId,
				step
			});
			return await require_server_handlers_workflows.RESUME_ASYNC_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error resuming agent builder action asynchronously", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error resuming agent builder action");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
/**
* Fire-and-forget resume for agent-builder actions: returns immediately with the runId without
* waiting for completion. Delegates to the workflows `resume-no-wait` route.
*
* TODO(v2): fold this behavior into the `resume-async` route in Mastra v2 and remove this route.
* Kept separate in v1 to avoid breaking the existing `resume-async` response contract.
*/
const RESUME_NO_WAIT_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/resume-no-wait",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	bodySchema: require_agent_builder.resumeAgentBuilderBodySchema,
	responseSchema: require_workflows.createWorkflowRunResponseSchema,
	summary: "Resume action without waiting",
	description: "Resumes a suspended action execution without waiting (fire-and-forget) and returns immediately with the runId. The action continues executing in the background.",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, step, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Resuming agent builder action without waiting", {
				actionId,
				runId,
				step
			});
			return await require_server_handlers_workflows.RESUME_NO_WAIT_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error resuming agent builder action without waiting", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error resuming agent builder action");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const RESUME_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/resume",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	bodySchema: require_agent_builder.resumeAgentBuilderBodySchema,
	responseSchema: require_workflows.workflowControlResponseSchema,
	summary: "Resume action",
	description: "Resumes a suspended action execution from a specific step",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, step, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Resuming agent builder action", {
				actionId,
				runId,
				step
			});
			return await require_server_handlers_workflows.RESUME_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error resuming agent builder action", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error resuming agent builder action");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const RESUME_STREAM_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/resume-stream",
	responseType: "stream",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	bodySchema: require_agent_builder.resumeAgentBuilderBodySchema,
	responseSchema: require_agents.streamResponseSchema,
	summary: "Resume action stream",
	description: "Resumes a suspended action execution and continues streaming results",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, step, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Resuming agent builder action stream", {
				actionId,
				runId,
				step
			});
			return await require_server_handlers_workflows.RESUME_STREAM_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error resuming agent builder action stream", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error resuming agent builder action stream");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const CANCEL_AGENT_BUILDER_ACTION_RUN_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/runs/:runId/cancel",
	responseType: "json",
	pathParamSchema: require_agent_builder.actionRunPathParams,
	responseSchema: require_workflows.workflowControlResponseSchema,
	summary: "Cancel action run",
	description: "Cancels an in-progress action execution",
	tags: ["Agent Builder"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Cancelling agent builder action run", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.CANCEL_WORKFLOW_RUN_ROUTE.handler({
				...ctx,
				workflowId: actionId
			});
		} catch (error) {
			logger.error("Error cancelling agent builder action run", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error cancelling agent builder action run");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/stream-legacy",
	responseType: "stream",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	bodySchema: require_agent_builder.streamLegacyAgentBuilderBodySchema,
	responseSchema: require_agents.streamResponseSchema,
	summary: "[DEPRECATED] Stream agent-builder action with legacy format",
	description: "Legacy endpoint for streaming agent-builder action execution. Use /agent-builder/:actionId/stream instead.",
	tags: ["Agent Builder", "Legacy"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId, requestContext } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Streaming agent builder action (legacy)", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.STREAM_LEGACY_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId,
				requestContext
			});
		} catch (error) {
			logger.error("Error streaming agent builder action (legacy)", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error streaming agent builder action");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
const OBSERVE_STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/agent-builder/:actionId/observe-stream-legacy",
	responseType: "stream",
	pathParamSchema: require_agent_builder.actionIdPathParams,
	queryParamSchema: require_common.runIdSchema,
	responseSchema: require_agents.streamResponseSchema,
	summary: "[DEPRECATED] Observe agent-builder action stream with legacy format",
	description: "Legacy endpoint for observing agent-builder action stream. Use /agent-builder/:actionId/observe instead.",
	tags: ["Agent Builder", "Legacy"],
	requiresAuth: true,
	handler: async (ctx) => {
		const { mastra, actionId, runId } = ctx;
		const logger = mastra.getLogger();
		try {
			await registerAgentBuilderWorkflows(mastra);
			if (actionId && !require_utils.WorkflowRegistry.isAgentBuilderWorkflow(actionId)) throw new require_http_exception.HTTPException(400, { message: `Invalid agent-builder action: ${actionId}` });
			logger.info("Observing agent builder action stream (legacy)", {
				actionId,
				runId
			});
			return await require_server_handlers_workflows.OBSERVE_STREAM_LEGACY_WORKFLOW_ROUTE.handler({
				...ctx,
				workflowId: actionId
			});
		} catch (error) {
			logger.error("Error observing agent builder action stream (legacy)", {
				error,
				actionId
			});
			return require_server_handlers_error.handleError(error, "Error observing agent builder action stream");
		} finally {
			require_utils.WorkflowRegistry.cleanup();
		}
	}
});
//#endregion
exports.CANCEL_AGENT_BUILDER_ACTION_RUN_ROUTE = CANCEL_AGENT_BUILDER_ACTION_RUN_ROUTE;
exports.CREATE_AGENT_BUILDER_ACTION_RUN_ROUTE = CREATE_AGENT_BUILDER_ACTION_RUN_ROUTE;
exports.GET_AGENT_BUILDER_ACTION_BY_ID_ROUTE = GET_AGENT_BUILDER_ACTION_BY_ID_ROUTE;
exports.GET_AGENT_BUILDER_ACTION_RUN_BY_ID_ROUTE = GET_AGENT_BUILDER_ACTION_RUN_BY_ID_ROUTE;
exports.LIST_AGENT_BUILDER_ACTIONS_ROUTE = LIST_AGENT_BUILDER_ACTIONS_ROUTE;
exports.LIST_AGENT_BUILDER_ACTION_RUNS_ROUTE = LIST_AGENT_BUILDER_ACTION_RUNS_ROUTE;
exports.OBSERVE_STREAM_AGENT_BUILDER_ACTION_ROUTE = OBSERVE_STREAM_AGENT_BUILDER_ACTION_ROUTE;
exports.OBSERVE_STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE = OBSERVE_STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE;
exports.RESUME_AGENT_BUILDER_ACTION_ROUTE = RESUME_AGENT_BUILDER_ACTION_ROUTE;
exports.RESUME_ASYNC_AGENT_BUILDER_ACTION_ROUTE = RESUME_ASYNC_AGENT_BUILDER_ACTION_ROUTE;
exports.RESUME_NO_WAIT_AGENT_BUILDER_ACTION_ROUTE = RESUME_NO_WAIT_AGENT_BUILDER_ACTION_ROUTE;
exports.RESUME_STREAM_AGENT_BUILDER_ACTION_ROUTE = RESUME_STREAM_AGENT_BUILDER_ACTION_ROUTE;
exports.START_AGENT_BUILDER_ACTION_RUN_ROUTE = START_AGENT_BUILDER_ACTION_RUN_ROUTE;
exports.START_ASYNC_AGENT_BUILDER_ACTION_ROUTE = START_ASYNC_AGENT_BUILDER_ACTION_ROUTE;
exports.STREAM_AGENT_BUILDER_ACTION_ROUTE = STREAM_AGENT_BUILDER_ACTION_ROUTE;
exports.STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE = STREAM_LEGACY_AGENT_BUILDER_ACTION_ROUTE;
Object.defineProperty(exports, "agent_builder_exports", {
	enumerable: true,
	get: function() {
		return agent_builder_exports;
	}
});

//# sourceMappingURL=agent-builder.cjs.map