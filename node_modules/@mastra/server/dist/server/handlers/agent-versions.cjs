Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_version_common = require("../../version-common-CxQdBbPm.cjs");
const require_agent_versions = require("../../agent-versions-D99q4Gos.cjs");
const require_server_handlers_validate_agent_instructions = require("./validate-agent-instructions.cjs");
const require_server_handlers_version_helpers = require("./version-helpers.cjs");
//#region src/server/handlers/agent-versions.ts
/**
* The config field names that live on version rows (StorageAgentSnapshotType fields).
* Used to extract config from a version record for comparison and restoration.
*/
const SNAPSHOT_CONFIG_FIELDS = [
	"name",
	"description",
	"instructions",
	"model",
	"tools",
	"defaultOptions",
	"workflows",
	"agents",
	"integrationTools",
	"inputProcessors",
	"outputProcessors",
	"memory",
	"scorers",
	"requestContextSchema",
	"mcpClients"
];
/**
* GET /stored/agents/:agentId/versions - List all versions for an agent
*/
const LIST_AGENT_VERSIONS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/agents/:agentId/versions",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_agent_versions.agentVersionPathParams,
	queryParamSchema: require_version_common.listVersionsQuerySchema,
	responseSchema: require_agent_versions.listVersionsResponseSchema,
	summary: "List agent versions",
	description: "Returns a paginated list of all versions for a stored agent",
	tags: ["Agent Versions"],
	handler: async ({ mastra, agentId, page, perPage, orderBy, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const agentsStore = await storage.getStore("agents");
			if (!agentsStore) throw new require_http_exception.HTTPException(500, { message: "Agents storage domain is not available" });
			const storedAgent = await agentsStore.getById(agentId);
			let codeAgentExists = false;
			try {
				mastra.getAgentById(agentId);
				codeAgentExists = true;
			} catch {}
			if (!storedAgent && !codeAgentExists) throw new require_http_exception.HTTPException(404, { message: `Agent with id ${agentId} not found` });
			require_utils.assertStoredResourceScope(storedAgent, await require_utils.getStoredResourceScope(mastra, requestContext));
			return await agentsStore.listVersions({
				agentId,
				page,
				perPage,
				orderBy
			});
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing agent versions");
		}
	}
});
/**
* POST /stored/agents/:agentId/versions - Create a new version snapshot
*/
const CREATE_AGENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/agents/:agentId/versions",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_agent_versions.agentVersionPathParams,
	bodySchema: require_version_common.createVersionBodySchema,
	responseSchema: require_agent_versions.createVersionResponseSchema,
	summary: "Create agent version",
	description: "Creates a new version snapshot of the current agent configuration",
	tags: ["Agent Versions"],
	handler: async ({ mastra, agentId, changeMessage, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const agentsStore = await storage.getStore("agents");
			if (!agentsStore) throw new require_http_exception.HTTPException(500, { message: "Agents storage domain is not available" });
			const agent = await agentsStore.getById(agentId);
			if (!agent) throw new require_http_exception.HTTPException(404, { message: `Agent with id ${agentId} not found` });
			require_utils.assertStoredResourceScope(agent, await require_utils.getStoredResourceScope(mastra, requestContext));
			let currentConfig = {};
			if (agent.activeVersionId) {
				const activeVersion = await agentsStore.getVersion(agent.activeVersionId);
				if (activeVersion) currentConfig = require_server_handlers_version_helpers.extractConfigFromVersion(activeVersion, SNAPSHOT_CONFIG_FIELDS);
			}
			const latestVersion = await agentsStore.getLatestVersion(agentId);
			if (!agent.activeVersionId && latestVersion) currentConfig = require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, SNAPSHOT_CONFIG_FIELDS);
			const changedFields = require_server_handlers_version_helpers.calculateChangedFields(latestVersion ? require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, SNAPSHOT_CONFIG_FIELDS) : null, currentConfig);
			const { versionId } = await require_server_handlers_version_helpers.createVersionWithRetry(agentsStore, agentId, "agentId", currentConfig, changedFields, { changeMessage });
			const version = await agentsStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(500, { message: "Failed to retrieve created version" });
			await require_server_handlers_version_helpers.enforceRetentionLimit(agentsStore, agentId, "agentId", agent.activeVersionId);
			return version;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error creating agent version");
		}
	}
});
/**
* GET /stored/agents/:agentId/versions/:versionId - Get a specific version
*/
const GET_AGENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/agents/:agentId/versions/:versionId",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_agent_versions.versionIdPathParams,
	responseSchema: require_agent_versions.getVersionResponseSchema,
	summary: "Get agent version",
	description: "Returns a specific version of an agent by its version ID",
	tags: ["Agent Versions"],
	handler: async ({ mastra, agentId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const agentsStore = await storage.getStore("agents");
			if (!agentsStore) throw new require_http_exception.HTTPException(500, { message: "Agents storage domain is not available" });
			const version = await agentsStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.agentId !== agentId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for agent ${agentId}` });
			require_utils.assertStoredResourceScope(await agentsStore.getById(agentId), await require_utils.getStoredResourceScope(mastra, requestContext));
			return version;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting agent version");
		}
	}
});
/**
* POST /stored/agents/:agentId/versions/:versionId/activate - Set a version as active
*/
const ACTIVATE_AGENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/agents/:agentId/versions/:versionId/activate",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_agent_versions.versionIdPathParams,
	responseSchema: require_version_common.activateVersionResponseSchema,
	summary: "Activate agent version",
	description: "Sets a specific version as the active version for the agent",
	tags: ["Agent Versions"],
	handler: async ({ mastra, agentId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const agentsStore = await storage.getStore("agents");
			if (!agentsStore) throw new require_http_exception.HTTPException(500, { message: "Agents storage domain is not available" });
			const agent = await agentsStore.getById(agentId);
			if (!agent) throw new require_http_exception.HTTPException(404, { message: `Agent with id ${agentId} not found` });
			require_utils.assertStoredResourceScope(agent, await require_utils.getStoredResourceScope(mastra, requestContext));
			const version = await agentsStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.agentId !== agentId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for agent ${agentId}` });
			await require_server_handlers_validate_agent_instructions.validateAgentInstructionReferences({
				instructions: version.instructions,
				mastra,
				requestContext
			});
			await agentsStore.update({
				id: agentId,
				activeVersionId: versionId,
				status: "published"
			});
			mastra.getEditor()?.agent.clearCache(agentId);
			return {
				success: true,
				message: `Version ${version.versionNumber} is now active`,
				activeVersionId: versionId
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error activating agent version");
		}
	}
});
/**
* POST /stored/agents/:agentId/versions/:versionId/restore - Restore agent to a version
*/
const RESTORE_AGENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/agents/:agentId/versions/:versionId/restore",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_agent_versions.versionIdPathParams,
	responseSchema: require_agent_versions.restoreVersionResponseSchema,
	summary: "Restore agent version",
	description: "Restores the agent configuration from a version, creating a new version",
	tags: ["Agent Versions"],
	handler: async ({ mastra, agentId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const agentsStore = await storage.getStore("agents");
			if (!agentsStore) throw new require_http_exception.HTTPException(500, { message: "Agents storage domain is not available" });
			const agent = await agentsStore.getById(agentId);
			if (!agent) throw new require_http_exception.HTTPException(404, { message: `Agent with id ${agentId} not found` });
			require_utils.assertStoredResourceScope(agent, await require_utils.getStoredResourceScope(mastra, requestContext));
			const versionToRestore = await agentsStore.getVersion(versionId);
			if (!versionToRestore) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (versionToRestore.agentId !== agentId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for agent ${agentId}` });
			const restoredConfig = require_server_handlers_version_helpers.extractConfigFromVersion(versionToRestore, SNAPSHOT_CONFIG_FIELDS);
			await agentsStore.update({
				id: agentId,
				...restoredConfig
			});
			const latestVersion = await agentsStore.getLatestVersion(agentId);
			const { versionId: newVersionId } = await require_server_handlers_version_helpers.createVersionWithRetry(agentsStore, agentId, "agentId", restoredConfig, require_server_handlers_version_helpers.calculateChangedFields(latestVersion ? require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, SNAPSHOT_CONFIG_FIELDS) : null, restoredConfig), { changeMessage: `Restored from version ${versionToRestore.versionNumber}` });
			const newVersion = await agentsStore.getVersion(newVersionId);
			if (!newVersion) throw new require_http_exception.HTTPException(500, { message: "Failed to retrieve created version" });
			await require_server_handlers_version_helpers.enforceRetentionLimit(agentsStore, agentId, "agentId", agent.activeVersionId);
			mastra.getEditor()?.agent.clearCache(agentId);
			return newVersion;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error restoring agent version");
		}
	}
});
/**
* DELETE /stored/agents/:agentId/versions/:versionId - Delete a version
*/
const DELETE_AGENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/agents/:agentId/versions/:versionId",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_agent_versions.versionIdPathParams,
	responseSchema: require_version_common.deleteVersionResponseSchema,
	summary: "Delete agent version",
	description: "Deletes a specific version (cannot delete the active version)",
	tags: ["Agent Versions"],
	handler: async ({ mastra, agentId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const agentsStore = await storage.getStore("agents");
			if (!agentsStore) throw new require_http_exception.HTTPException(500, { message: "Agents storage domain is not available" });
			const agent = await agentsStore.getById(agentId);
			if (!agent) throw new require_http_exception.HTTPException(404, { message: `Agent with id ${agentId} not found` });
			require_utils.assertStoredResourceScope(agent, await require_utils.getStoredResourceScope(mastra, requestContext));
			const version = await agentsStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.agentId !== agentId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for agent ${agentId}` });
			if (agent.activeVersionId === versionId) throw new require_http_exception.HTTPException(400, { message: "Cannot delete the active version. Activate a different version first." });
			await agentsStore.deleteVersion(versionId);
			mastra.getEditor()?.agent.clearCache(agentId);
			return {
				success: true,
				message: `Version ${version.versionNumber} deleted successfully`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting agent version");
		}
	}
});
/**
* GET /stored/agents/:agentId/versions/compare - Compare two versions
*/
const COMPARE_AGENT_VERSIONS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/agents/:agentId/versions/compare",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_agent_versions.agentVersionPathParams,
	queryParamSchema: require_version_common.compareVersionsQuerySchema,
	responseSchema: require_agent_versions.compareVersionsResponseSchema,
	summary: "Compare agent versions",
	description: "Compares two versions and returns the differences between them",
	tags: ["Agent Versions"],
	handler: async ({ mastra, agentId, from, to, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const agentsStore = await storage.getStore("agents");
			if (!agentsStore) throw new require_http_exception.HTTPException(500, { message: "Agents storage domain is not available" });
			require_utils.assertStoredResourceScope(await agentsStore.getById(agentId), await require_utils.getStoredResourceScope(mastra, requestContext));
			const fromVersion = await agentsStore.getVersion(from);
			if (!fromVersion) throw new require_http_exception.HTTPException(404, { message: `Version with id ${from} not found` });
			if (fromVersion.agentId !== agentId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${from} not found for agent ${agentId}` });
			const toVersion = await agentsStore.getVersion(to);
			if (!toVersion) throw new require_http_exception.HTTPException(404, { message: `Version with id ${to} not found` });
			if (toVersion.agentId !== agentId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${to} not found for agent ${agentId}` });
			return {
				diffs: require_server_handlers_version_helpers.computeVersionDiffs(require_server_handlers_version_helpers.extractConfigFromVersion(fromVersion, SNAPSHOT_CONFIG_FIELDS), require_server_handlers_version_helpers.extractConfigFromVersion(toVersion, SNAPSHOT_CONFIG_FIELDS)),
				fromVersion,
				toVersion
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error comparing agent versions");
		}
	}
});
//#endregion
exports.ACTIVATE_AGENT_VERSION_ROUTE = ACTIVATE_AGENT_VERSION_ROUTE;
exports.COMPARE_AGENT_VERSIONS_ROUTE = COMPARE_AGENT_VERSIONS_ROUTE;
exports.CREATE_AGENT_VERSION_ROUTE = CREATE_AGENT_VERSION_ROUTE;
exports.DELETE_AGENT_VERSION_ROUTE = DELETE_AGENT_VERSION_ROUTE;
exports.GET_AGENT_VERSION_ROUTE = GET_AGENT_VERSION_ROUTE;
exports.LIST_AGENT_VERSIONS_ROUTE = LIST_AGENT_VERSIONS_ROUTE;
exports.RESTORE_AGENT_VERSION_ROUTE = RESTORE_AGENT_VERSION_ROUTE;

//# sourceMappingURL=agent-versions.cjs.map