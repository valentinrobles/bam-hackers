import { t as HTTPException } from "../../http-exception-CyfxZB6W.js";
import { n as createRoute, t as createPublicRoute } from "../../route-builder-BukcT_Kp.js";
import { a as MASTRA_PUBLIC_HOST_HEADER, m as isStudioClientTypeHeader } from "../../constants-0jFCWoex.js";
import { handleError } from "./error.js";
import { p as supportsSessionRefresh } from "../../helpers-610L0lwQ.js";
import { _ as ssoLoginQuerySchema, c as currentUserResponseSchema, d as permissionPatternsResponseSchema, m as ssoCallbackQuerySchema, o as credentialsSignInBodySchema, p as refreshResponseSchema, r as capabilitiesResponseSchema, s as credentialsSignUpBodySchema } from "../../auth-CZFXpwba.js";
import { z } from "zod/v4";
//#region src/server/handlers/auth.ts
let _buildCapabilitiesPromise;
function loadBuildCapabilities() {
	if (!_buildCapabilitiesPromise) _buildCapabilitiesPromise = import("@mastra/core/auth/ee").then((m) => m.buildCapabilities).catch(() => {
		console.error("[@mastra/server] EE auth features require @mastra/core >= 1.6.0. Please upgrade: npm install @mastra/core@latest");
	});
	return _buildCapabilitiesPromise;
}
let _permissionPatternsPromise;
function loadPermissionPatterns() {
	if (!_permissionPatternsPromise) _permissionPatternsPromise = import("@mastra/core/auth/ee").then((m) => m.PERMISSION_PATTERNS).catch(() => {
		console.error("[@mastra/server] EE auth features require @mastra/core >= 1.6.0. Please upgrade: npm install @mastra/core@latest");
	});
	return _permissionPatternsPromise;
}
/**
* Helper to get auth provider from Mastra instance.
*
* Dual auth is OPT-IN: if studio.auth is explicitly configured, Studio requests
* use it exclusively. Otherwise, Studio requests fall back to server.auth for
* backward compatibility.
*/
function getAuthProvider(mastra, isStudio) {
	const studioConfig = mastra.getStudio?.();
	const hasStudioAuth = studioConfig?.auth && typeof studioConfig.auth.authenticateToken === "function";
	if (isStudio && hasStudioAuth) return studioConfig.auth;
	const serverConfig = mastra.getServer?.();
	if (!serverConfig?.auth) return null;
	if (typeof serverConfig.auth.authenticateToken === "function") return serverConfig.auth;
	return null;
}
/**
* Check if the request is from Studio (via x-mastra-client-type header).
*/
function isStudioRequest(request) {
	return isStudioClientTypeHeader(request.headers.get("x-mastra-client-type") ?? void 0);
}
/**
* Get the public-facing origin from a request, respecting reverse proxy headers.
* Behind a proxy (e.g. edge router), request.url contains the internal hostname,
* so we rely on forwarded headers to reconstruct the real public origin.
*
* Assumes the server is behind a trusted proxy (or running locally). When
* exposed directly to untrusted clients, the Host header is attacker-controlled
* and must be validated upstream.
*
* Priority:
* 1. X-Mastra-Public-Host (proxy chain that rewrites X-Forwarded-Host) → always
*    HTTPS. Some platform gateways — Railway's among them — overwrite
*    X-Forwarded-Host with their own internal domain, which would otherwise
*    leave the app resolving an internal hostname it must never hand to an
*    OAuth provider. An edge that knows the browser-facing host sets this
*    header so it survives that rewrite.
* 2. X-Forwarded-Host (traditional reverse proxy) → always HTTPS. Knative's
*    queue-proxy overwrites X-Forwarded-Proto based on the internal HTTP
*    connection, so X-Forwarded-Proto is ignored here.
* 3. Host header with X-Forwarded-Proto (AWS ALB, some proxies) → respect proto.
* 4. Host header alone → use the scheme from request.url (covers both direct
*    HTTP access and proxies that preserve Host but don't set a proto header).
* 5. No Host header → fall back to request.url.origin (local dev / direct access).
*/
function getPublicOrigin(request) {
	const publicHost = request.headers.get(MASTRA_PUBLIC_HOST_HEADER)?.split(",")[0]?.trim();
	if (publicHost) return `https://${publicHost}`;
	const forwardedHost = request.headers.get("x-forwarded-host")?.split(",")[0]?.trim();
	if (forwardedHost) return `https://${forwardedHost}`;
	const host = request.headers.get("host");
	if (host) return `${request.headers.get("x-forwarded-proto")?.split(",")[0]?.trim() || new URL(request.url).protocol.replace(":", "")}://${host}`;
	return new URL(request.url).origin;
}
/**
* Helper to get RBAC provider from Mastra config.
* Checks studio config first when isStudio is true.
*/
function getRBACProvider(mastra, isStudio) {
	if (isStudio) {
		const studioConfig = mastra.getStudio?.();
		if (studioConfig?.rbac) return studioConfig.rbac;
	}
	return (mastra.getServer?.())?.rbac;
}
/**
* Helper to get FGA provider from Mastra config.
* Checks studio config first when isStudio is true.
*/
function getFGAProvider(mastra, isStudio) {
	if (isStudio) {
		const studioConfig = mastra.getStudio?.();
		if (studioConfig?.fga) return studioConfig.fga;
	}
	return (mastra.getServer?.())?.fga;
}
/**
* Type guard to check if auth provider implements an interface.
*/
function implementsInterface(auth, method) {
	return auth !== null && typeof auth === "object" && typeof auth[method] === "function";
}
const GET_AUTH_CAPABILITIES_ROUTE = createPublicRoute({
	method: "GET",
	path: "/auth/capabilities",
	responseType: "json",
	responseSchema: capabilitiesResponseSchema,
	summary: "Get auth capabilities",
	description: "Returns authentication capabilities and current user info. Used by Studio to determine available features and user state.",
	tags: ["Auth"],
	handler: async (ctx) => {
		try {
			const { mastra, request, routePrefix } = ctx;
			const isStudio = isStudioRequest(request);
			const auth = getAuthProvider(mastra, isStudio);
			if (!auth) return {
				enabled: false,
				login: null
			};
			const rbac = getRBACProvider(mastra, isStudio);
			const fga = getFGAProvider(mastra, isStudio);
			const buildCapabilities = await loadBuildCapabilities();
			if (!buildCapabilities) return {
				enabled: false,
				login: null
			};
			const capabilities = await buildCapabilities(auth, request, {
				rbac,
				fga,
				apiPrefix: routePrefix
			});
			if (!("user" in capabilities) && supportsSessionRefresh(auth)) try {
				const sessionId = await auth.getSessionIdFromRequest(request);
				if (sessionId) {
					const refreshedSession = await auth.refreshSession(sessionId);
					if (refreshedSession) {
						const sessionHeaders = await auth.getSessionHeaders(refreshedSession);
						const cookieValue = extractCookieFromHeaders(sessionHeaders);
						if (cookieValue) {
							const refreshedRequest = new Request(request.url, {
								method: request.method,
								headers: new Headers(request.headers)
							});
							refreshedRequest.headers.set("Cookie", cookieValue);
							const refreshedCapabilities = await buildCapabilities(auth, refreshedRequest, {
								rbac,
								apiPrefix: routePrefix
							});
							if ("user" in refreshedCapabilities) refreshedCapabilities.__refreshHeaders = sessionHeaders;
							return refreshedCapabilities;
						}
					}
				}
			} catch {}
			return capabilities;
		} catch (error) {
			return handleError(error, "Error getting auth capabilities");
		}
	}
});
/**
* Extract a full cookie string from session headers (e.g. Set-Cookie → Cookie).
*/
function extractCookieFromHeaders(headers) {
	const setCookie = headers["Set-Cookie"] || headers["set-cookie"];
	if (!setCookie) return null;
	const match = setCookie.match(/^([^;]+)/);
	return match ? match[1] ?? null : null;
}
const GET_CURRENT_USER_ROUTE = createPublicRoute({
	method: "GET",
	path: "/auth/me",
	responseType: "json",
	responseSchema: currentUserResponseSchema,
	summary: "Get current user",
	description: "Returns the currently authenticated user, or null if not authenticated.",
	tags: ["Auth"],
	handler: async (ctx) => {
		try {
			const { mastra, request } = ctx;
			const isStudio = isStudioRequest(request);
			const auth = getAuthProvider(mastra, isStudio);
			const rbac = getRBACProvider(mastra, isStudio);
			if (!auth || !implementsInterface(auth, "getCurrentUser")) return null;
			const user = await auth.getCurrentUser(request);
			if (!user) return null;
			let roles;
			let permissions;
			if (rbac) try {
				roles = await rbac.getRoles(user);
				permissions = await rbac.getPermissions(user);
			} catch {}
			return {
				id: user.id,
				email: user.email,
				name: user.name,
				avatarUrl: user.avatarUrl,
				roles,
				permissions
			};
		} catch (error) {
			return handleError(error, "Error getting current user");
		}
	}
});
const GET_SSO_LOGIN_ROUTE = createPublicRoute({
	method: "GET",
	path: "/auth/sso/login",
	responseType: "datastream-response",
	queryParamSchema: ssoLoginQuerySchema,
	summary: "Initiate SSO login",
	description: "Returns the SSO login URL and sets PKCE cookies if needed.",
	tags: ["Auth"],
	handler: async (ctx) => {
		try {
			const { mastra, redirect_uri, request, routePrefix } = ctx;
			const auth = getAuthProvider(mastra, isStudioRequest(request));
			if (!auth || !implementsInterface(auth, "getLoginUrl")) throw new HTTPException(404, { message: "SSO not configured" });
			const origin = getPublicOrigin(request);
			const raw = (routePrefix || "/api").trim();
			const withSlash = raw.startsWith("/") ? raw : `/${raw}`;
			const oauthCallbackUri = `${origin}${withSlash.endsWith("/") ? withSlash.slice(0, -1) : withSlash}/auth/sso/callback`;
			let postLoginRedirect = "/";
			if (redirect_uri) if (!redirect_uri.startsWith("http")) postLoginRedirect = redirect_uri;
			else try {
				const redirectUrl = new URL(redirect_uri);
				const requestOrigin = new URL(origin);
				const isHttps = redirectUrl.protocol === "http:" || redirectUrl.protocol === "https:";
				const isSameOrigin = redirectUrl.origin === requestOrigin.origin;
				const isLocalhost = redirectUrl.hostname === "localhost" || redirectUrl.hostname === "127.0.0.1" || redirectUrl.hostname === "[::1]";
				if (isHttps && (isSameOrigin || isLocalhost)) postLoginRedirect = redirect_uri;
			} catch {}
			const state = `${crypto.randomUUID()}|${encodeURIComponent(postLoginRedirect)}`;
			const loginUrl = await Promise.resolve(auth.getLoginUrl(oauthCallbackUri, state));
			const headers = new Headers({ "Content-Type": "application/json" });
			if (implementsInterface(auth, "getLoginCookies") && auth.getLoginCookies) {
				const cookies = auth.getLoginCookies(oauthCallbackUri, state);
				if (cookies?.length) for (const cookie of cookies) headers.append("Set-Cookie", cookie);
			}
			return new Response(JSON.stringify({ url: loginUrl }), {
				status: 200,
				headers
			});
		} catch (error) {
			return handleError(error, "Error initiating SSO login");
		}
	}
});
const GET_SSO_CALLBACK_ROUTE = createPublicRoute({
	method: "GET",
	path: "/auth/sso/callback",
	responseType: "datastream-response",
	queryParamSchema: ssoCallbackQuerySchema,
	summary: "Handle SSO callback",
	description: "Handles the OAuth callback, exchanges code for session, and redirects to the app.",
	tags: ["Auth"],
	handler: async (ctx) => {
		const { mastra, code, state, request } = ctx;
		isStudioRequest(request);
		const baseUrl = getPublicOrigin(request);
		let redirectTo = "/";
		let stateId = state || "";
		if (state && state.includes("|")) {
			const [id, encodedRedirect] = state.split("|", 2);
			stateId = id;
			try {
				redirectTo = decodeURIComponent(encodedRedirect);
			} catch {
				redirectTo = "/";
			}
		}
		let absoluteRedirect;
		if (redirectTo.startsWith("http")) try {
			const parsed = new URL(redirectTo);
			const baseOrigin = new URL(baseUrl);
			const isHttps = parsed.protocol === "http:" || parsed.protocol === "https:";
			const isSameOrigin = parsed.origin === baseOrigin.origin;
			const isLocalhost = parsed.hostname === "localhost" || parsed.hostname === "127.0.0.1" || parsed.hostname === "[::1]";
			absoluteRedirect = isHttps && (isSameOrigin || isLocalhost) ? redirectTo : `${baseUrl}/`;
		} catch {
			absoluteRedirect = `${baseUrl}/`;
		}
		else absoluteRedirect = `${baseUrl}${redirectTo}`;
		try {
			let auth = getAuthProvider(mastra, true);
			if (!auth || !implementsInterface(auth, "handleCallback")) auth = getAuthProvider(mastra, false);
			if (!auth || !implementsInterface(auth, "handleCallback")) return Response.redirect(`${absoluteRedirect}?error=sso_not_configured`, 302);
			const reqCookieHeader = request.headers.get("cookie");
			if (typeof auth.setCallbackCookieHeader === "function") auth.setCallbackCookieHeader(reqCookieHeader);
			const result = await auth.handleCallback(code, stateId);
			const user = result.user;
			const headers = new Headers();
			headers.set("Location", absoluteRedirect);
			if (result.cookies?.length) for (const cookie of result.cookies) headers.append("Set-Cookie", cookie);
			else if (implementsInterface(auth, "createSession") && result.tokens) {
				const session = await auth.createSession(user.id, {
					accessToken: result.tokens.accessToken,
					refreshToken: result.tokens.refreshToken,
					expiresAt: result.tokens.expiresAt,
					organizationId: user.organizationId
				});
				const sessionHeaders = auth.getSessionHeaders(session);
				for (const [key, value] of Object.entries(sessionHeaders)) headers.append(key, value);
			}
			return new Response(null, {
				status: 302,
				headers
			});
		} catch (error) {
			const errorMessage = encodeURIComponent(error instanceof Error ? error.message : "Unknown error");
			return Response.redirect(`${absoluteRedirect}?error=${errorMessage}`, 302);
		}
	}
});
const POST_LOGOUT_ROUTE = createPublicRoute({
	method: "POST",
	path: "/auth/logout",
	responseType: "datastream-response",
	summary: "Logout",
	description: "Destroys the current session and returns logout redirect URL if available.",
	tags: ["Auth"],
	handler: async (ctx) => {
		const { mastra, request } = ctx;
		const isStudio = isStudioRequest(request);
		try {
			const auth = getAuthProvider(mastra, isStudio);
			if (!auth) return new Response(JSON.stringify({ success: true }), {
				status: 200,
				headers: { "Content-Type": "application/json" }
			});
			if (implementsInterface(auth, "getSessionIdFromRequest")) {
				const sessionId = auth.getSessionIdFromRequest(request);
				if (sessionId && implementsInterface(auth, "destroySession")) await auth.destroySession(sessionId);
			}
			let redirectTo;
			if (implementsInterface(auth, "getLogoutUrl") && auth.getLogoutUrl) {
				const origin = getPublicOrigin(request);
				redirectTo = await auth.getLogoutUrl(origin, request) ?? void 0;
			}
			const headers = new Headers({ "Content-Type": "application/json" });
			if (implementsInterface(auth, "getClearSessionHeaders")) {
				const clearHeaders = auth.getClearSessionHeaders();
				for (const [key, value] of Object.entries(clearHeaders)) headers.append(key, value);
			}
			return new Response(JSON.stringify({
				success: true,
				redirectTo
			}), {
				status: 200,
				headers
			});
		} catch (error) {
			return handleError(error, "Error logging out");
		}
	}
});
const POST_REFRESH_ROUTE = createPublicRoute({
	method: "POST",
	path: "/auth/refresh",
	responseType: "datastream-response",
	responseSchema: refreshResponseSchema,
	summary: "Refresh session",
	description: "Refreshes the current session, extending its expiry. Sets a new session cookie on success.",
	tags: ["Auth"],
	handler: async (ctx) => {
		const { mastra, request } = ctx;
		const isStudio = isStudioRequest(request);
		try {
			const auth = getAuthProvider(mastra, isStudio);
			if (!auth || !implementsInterface(auth, "refreshSession") || !implementsInterface(auth, "getSessionIdFromRequest")) throw new HTTPException(404, { message: "Session refresh not configured" });
			const sessionId = auth.getSessionIdFromRequest(request);
			if (!sessionId) throw new HTTPException(401, { message: "No session" });
			const newSession = await auth.refreshSession(sessionId);
			if (!newSession) throw new HTTPException(401, { message: "Session expired" });
			const headers = new Headers({ "Content-Type": "application/json" });
			if (implementsInterface(auth, "getSessionHeaders")) {
				const sessionHeaders = auth.getSessionHeaders(newSession);
				for (const [key, value] of Object.entries(sessionHeaders)) headers.append(key, value);
			}
			return new Response(JSON.stringify({ success: true }), {
				status: 200,
				headers
			});
		} catch (error) {
			if (error instanceof HTTPException) throw error;
			return handleError(error, "Error refreshing session");
		}
	}
});
const POST_CREDENTIALS_SIGN_IN_ROUTE = createPublicRoute({
	method: "POST",
	path: "/auth/credentials/sign-in",
	responseType: "datastream-response",
	bodySchema: credentialsSignInBodySchema,
	summary: "Sign in with credentials",
	description: "Authenticates a user with email and password.",
	tags: ["Auth"],
	handler: async (ctx) => {
		const { mastra, request, email, password } = ctx;
		const isStudio = isStudioRequest(request);
		try {
			const auth = getAuthProvider(mastra, isStudio);
			if (!auth || !implementsInterface(auth, "signIn")) throw new HTTPException(404, { message: "Credentials authentication not configured" });
			const result = await auth.signIn(email, password, request);
			const user = result.user;
			const responseBody = JSON.stringify({
				user: {
					id: user.id,
					email: user.email,
					name: user.name,
					avatarUrl: user.avatarUrl
				},
				token: result.token
			});
			const headers = new Headers({ "Content-Type": "application/json" });
			if (result.cookies?.length) for (const cookie of result.cookies) headers.append("Set-Cookie", cookie);
			return new Response(responseBody, {
				status: 200,
				headers
			});
		} catch (error) {
			if (error instanceof HTTPException) throw error;
			throw new HTTPException(401, { message: "Invalid email or password" });
		}
	}
});
const POST_CREDENTIALS_SIGN_UP_ROUTE = createPublicRoute({
	method: "POST",
	path: "/auth/credentials/sign-up",
	responseType: "datastream-response",
	bodySchema: credentialsSignUpBodySchema,
	summary: "Sign up with credentials",
	description: "Creates a new user account with email and password.",
	tags: ["Auth"],
	handler: async (ctx) => {
		const { mastra, request, email, password, name } = ctx;
		const isStudio = isStudioRequest(request);
		try {
			const auth = getAuthProvider(mastra, isStudio);
			if (!auth || !implementsInterface(auth, "signUp")) throw new HTTPException(404, { message: "Credentials authentication not configured" });
			const result = await auth.signUp(email, password, name, request);
			const user = result.user;
			const responseBody = JSON.stringify({
				user: {
					id: user.id,
					email: user.email,
					name: user.name,
					avatarUrl: user.avatarUrl
				},
				token: result.token
			});
			const headers = new Headers({ "Content-Type": "application/json" });
			if (result.cookies?.length) for (const cookie of result.cookies) headers.append("Set-Cookie", cookie);
			return new Response(responseBody, {
				status: 200,
				headers
			});
		} catch (error) {
			if (error instanceof HTTPException) throw error;
			ctx.mastra?.getLogger?.()?.error("Sign-up error", { error: error instanceof Error ? {
				message: error.message,
				stack: error.stack
			} : error });
			throw new HTTPException(400, { message: "Failed to create account" });
		}
	}
});
const GET_ROLE_PERMISSIONS_ROUTE = createRoute({
	method: "GET",
	path: "/auth/roles/:roleId/permissions",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: z.object({ roleId: z.string() }),
	responseSchema: z.object({
		roleId: z.string(),
		permissions: z.array(z.string())
	}),
	summary: "Get permissions for a role",
	description: "Returns the resolved permissions for a specific role. Only accessible by admin users. Used by the \"View as role\" feature.",
	tags: ["Auth"],
	handler: async (ctx) => {
		try {
			const { mastra, requestContext, roleId } = ctx;
			if (!(requestContext?.get("mastra__userPermissions") ?? []).some((p) => p === "*" || p === "*:*")) throw new HTTPException(403, { message: "Admin access required" });
			const rbac = getRBACProvider(mastra);
			if (!rbac?.getPermissionsForRole) throw new HTTPException(404, { message: "RBAC provider does not support role permission resolution" });
			return {
				roleId,
				permissions: await rbac.getPermissionsForRole(roleId)
			};
		} catch (error) {
			if (error instanceof HTTPException) throw error;
			return handleError(error, "Error getting role permissions");
		}
	}
});
const GET_PERMISSION_PATTERNS_ROUTE = createRoute({
	method: "GET",
	path: "/auth/permission-patterns",
	requiresAuth: true,
	responseType: "json",
	responseSchema: permissionPatternsResponseSchema,
	summary: "List valid permission patterns",
	description: "Returns the authoritative list of valid permission-pattern strings. Used by Studio to validate the route→permission literals it ships and to gate the sidebar.",
	tags: ["Auth"],
	handler: async () => {
		const patterns = await loadPermissionPatterns();
		return { patterns: Object.keys(patterns ?? {}) };
	}
});
const AUTH_ROUTES = [
	GET_AUTH_CAPABILITIES_ROUTE,
	GET_CURRENT_USER_ROUTE,
	GET_SSO_LOGIN_ROUTE,
	GET_SSO_CALLBACK_ROUTE,
	POST_LOGOUT_ROUTE,
	POST_REFRESH_ROUTE,
	POST_CREDENTIALS_SIGN_IN_ROUTE,
	POST_CREDENTIALS_SIGN_UP_ROUTE,
	GET_ROLE_PERMISSIONS_ROUTE,
	GET_PERMISSION_PATTERNS_ROUTE
];
//#endregion
export { AUTH_ROUTES, GET_AUTH_CAPABILITIES_ROUTE, GET_CURRENT_USER_ROUTE, GET_PERMISSION_PATTERNS_ROUTE, GET_ROLE_PERMISSIONS_ROUTE, GET_SSO_CALLBACK_ROUTE, GET_SSO_LOGIN_ROUTE, POST_CREDENTIALS_SIGN_IN_ROUTE, POST_CREDENTIALS_SIGN_UP_ROUTE, POST_LOGOUT_ROUTE, POST_REFRESH_ROUTE, getPublicOrigin };

//# sourceMappingURL=auth.js.map