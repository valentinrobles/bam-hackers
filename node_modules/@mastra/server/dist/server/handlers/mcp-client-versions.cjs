Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_server_handlers_version_helpers = require("./version-helpers.cjs");
const require_mcp_client_versions = require("../../mcp-client-versions-DoGlpOEW.cjs");
//#region src/server/handlers/mcp-client-versions.ts
/**
* GET /stored/mcp-clients/:mcpClientId/versions - List all versions for an MCP client
*/
const LIST_MCP_CLIENT_VERSIONS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/mcp-clients/:mcpClientId/versions",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_mcp_client_versions.mcpClientVersionPathParams,
	queryParamSchema: require_mcp_client_versions.listMCPClientVersionsQuerySchema,
	responseSchema: require_mcp_client_versions.listMCPClientVersionsResponseSchema,
	summary: "List MCP client versions",
	description: "Returns a paginated list of all versions for a stored MCP client",
	tags: ["MCP Client Versions"],
	handler: async ({ mastra, mcpClientId, page, perPage, orderBy, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const mcpClient = await mcpClientStore.getById(mcpClientId);
			if (!mcpClient) throw new require_http_exception.HTTPException(404, { message: `MCP client with id ${mcpClientId} not found` });
			require_utils.assertStoredResourceScope(mcpClient, await require_utils.getStoredResourceScope(mastra, requestContext));
			return await mcpClientStore.listVersions({
				mcpClientId,
				page,
				perPage,
				orderBy
			});
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing MCP client versions");
		}
	}
});
/**
* POST /stored/mcp-clients/:mcpClientId/versions - Create a new version snapshot
*/
const CREATE_MCP_CLIENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/mcp-clients/:mcpClientId/versions",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_mcp_client_versions.mcpClientVersionPathParams,
	bodySchema: require_mcp_client_versions.createMCPClientVersionBodySchema,
	responseSchema: require_mcp_client_versions.createMCPClientVersionResponseSchema,
	summary: "Create MCP client version",
	description: "Creates a new version snapshot of the current MCP client configuration",
	tags: ["MCP Client Versions"],
	handler: async ({ mastra, mcpClientId, changeMessage, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const mcpClient = await mcpClientStore.getById(mcpClientId);
			if (!mcpClient) throw new require_http_exception.HTTPException(404, { message: `MCP client with id ${mcpClientId} not found` });
			require_utils.assertStoredResourceScope(mcpClient, await require_utils.getStoredResourceScope(mastra, requestContext));
			let currentConfig = {};
			if (mcpClient.activeVersionId) {
				const activeVersion = await mcpClientStore.getVersion(mcpClient.activeVersionId);
				if (activeVersion) currentConfig = require_server_handlers_version_helpers.extractConfigFromVersion(activeVersion, require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS);
			}
			const latestVersion = await mcpClientStore.getLatestVersion(mcpClientId);
			if (!mcpClient.activeVersionId && latestVersion) currentConfig = require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS);
			const changedFields = require_server_handlers_version_helpers.calculateChangedFields(latestVersion ? require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS) : null, currentConfig);
			const { versionId } = await require_server_handlers_version_helpers.createVersionWithRetry(mcpClientStore, mcpClientId, "mcpClientId", currentConfig, changedFields, { changeMessage });
			const version = await mcpClientStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(500, { message: "Failed to retrieve created version" });
			await require_server_handlers_version_helpers.enforceRetentionLimit(mcpClientStore, mcpClientId, "mcpClientId", mcpClient.activeVersionId);
			return version;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error creating MCP client version");
		}
	}
});
/**
* GET /stored/mcp-clients/:mcpClientId/versions/:versionId - Get a specific version
*/
const GET_MCP_CLIENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/mcp-clients/:mcpClientId/versions/:versionId",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_mcp_client_versions.mcpClientVersionIdPathParams,
	responseSchema: require_mcp_client_versions.getMCPClientVersionResponseSchema,
	summary: "Get MCP client version",
	description: "Returns a specific version of an MCP client by its version ID",
	tags: ["MCP Client Versions"],
	handler: async ({ mastra, mcpClientId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const version = await mcpClientStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.mcpClientId !== mcpClientId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for MCP client ${mcpClientId}` });
			require_utils.assertStoredResourceScope(await mcpClientStore.getById(mcpClientId), await require_utils.getStoredResourceScope(mastra, requestContext));
			return version;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting MCP client version");
		}
	}
});
/**
* POST /stored/mcp-clients/:mcpClientId/versions/:versionId/activate - Set a version as active
*/
const ACTIVATE_MCP_CLIENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/mcp-clients/:mcpClientId/versions/:versionId/activate",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_mcp_client_versions.mcpClientVersionIdPathParams,
	responseSchema: require_mcp_client_versions.activateMCPClientVersionResponseSchema,
	summary: "Activate MCP client version",
	description: "Sets a specific version as the active version for the MCP client",
	tags: ["MCP Client Versions"],
	handler: async ({ mastra, mcpClientId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const mcpClient = await mcpClientStore.getById(mcpClientId);
			if (!mcpClient) throw new require_http_exception.HTTPException(404, { message: `MCP client with id ${mcpClientId} not found` });
			require_utils.assertStoredResourceScope(mcpClient, await require_utils.getStoredResourceScope(mastra, requestContext));
			const version = await mcpClientStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.mcpClientId !== mcpClientId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for MCP client ${mcpClientId}` });
			await mcpClientStore.update({
				id: mcpClientId,
				activeVersionId: versionId,
				status: "published"
			});
			mastra.getEditor()?.mcp.clearCache(mcpClientId);
			return {
				success: true,
				message: `Version ${version.versionNumber} is now active`,
				activeVersionId: versionId
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error activating MCP client version");
		}
	}
});
/**
* POST /stored/mcp-clients/:mcpClientId/versions/:versionId/restore - Restore MCP client to a version
*/
const RESTORE_MCP_CLIENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/mcp-clients/:mcpClientId/versions/:versionId/restore",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_mcp_client_versions.mcpClientVersionIdPathParams,
	responseSchema: require_mcp_client_versions.restoreMCPClientVersionResponseSchema,
	summary: "Restore MCP client version",
	description: "Restores the MCP client configuration from a version, creating a new version",
	tags: ["MCP Client Versions"],
	handler: async ({ mastra, mcpClientId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const mcpClient = await mcpClientStore.getById(mcpClientId);
			if (!mcpClient) throw new require_http_exception.HTTPException(404, { message: `MCP client with id ${mcpClientId} not found` });
			require_utils.assertStoredResourceScope(mcpClient, await require_utils.getStoredResourceScope(mastra, requestContext));
			const versionToRestore = await mcpClientStore.getVersion(versionId);
			if (!versionToRestore) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (versionToRestore.mcpClientId !== mcpClientId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for MCP client ${mcpClientId}` });
			const restoredConfig = require_server_handlers_version_helpers.extractConfigFromVersion(versionToRestore, require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS);
			await mcpClientStore.update({
				id: mcpClientId,
				...restoredConfig
			});
			const latestVersion = await mcpClientStore.getLatestVersion(mcpClientId);
			const { versionId: newVersionId } = await require_server_handlers_version_helpers.createVersionWithRetry(mcpClientStore, mcpClientId, "mcpClientId", restoredConfig, require_server_handlers_version_helpers.calculateChangedFields(latestVersion ? require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS) : null, restoredConfig), { changeMessage: `Restored from version ${versionToRestore.versionNumber}` });
			const newVersion = await mcpClientStore.getVersion(newVersionId);
			if (!newVersion) throw new require_http_exception.HTTPException(500, { message: "Failed to retrieve created version" });
			await require_server_handlers_version_helpers.enforceRetentionLimit(mcpClientStore, mcpClientId, "mcpClientId", mcpClient.activeVersionId);
			mastra.getEditor()?.mcp.clearCache(mcpClientId);
			return newVersion;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error restoring MCP client version");
		}
	}
});
/**
* DELETE /stored/mcp-clients/:mcpClientId/versions/:versionId - Delete a version
*/
const DELETE_MCP_CLIENT_VERSION_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/mcp-clients/:mcpClientId/versions/:versionId",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_mcp_client_versions.mcpClientVersionIdPathParams,
	responseSchema: require_mcp_client_versions.deleteMCPClientVersionResponseSchema,
	summary: "Delete MCP client version",
	description: "Deletes a specific version (cannot delete the active version)",
	tags: ["MCP Client Versions"],
	handler: async ({ mastra, mcpClientId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const mcpClient = await mcpClientStore.getById(mcpClientId);
			if (!mcpClient) throw new require_http_exception.HTTPException(404, { message: `MCP client with id ${mcpClientId} not found` });
			require_utils.assertStoredResourceScope(mcpClient, await require_utils.getStoredResourceScope(mastra, requestContext));
			const version = await mcpClientStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.mcpClientId !== mcpClientId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for MCP client ${mcpClientId}` });
			if (mcpClient.activeVersionId === versionId) throw new require_http_exception.HTTPException(400, { message: "Cannot delete the active version. Activate a different version first." });
			await mcpClientStore.deleteVersion(versionId);
			mastra.getEditor()?.mcp.clearCache(mcpClientId);
			return {
				success: true,
				message: `Version ${version.versionNumber} deleted successfully`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting MCP client version");
		}
	}
});
/**
* GET /stored/mcp-clients/:mcpClientId/versions/compare - Compare two versions
*/
const COMPARE_MCP_CLIENT_VERSIONS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/mcp-clients/:mcpClientId/versions/compare",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_mcp_client_versions.mcpClientVersionPathParams,
	queryParamSchema: require_mcp_client_versions.compareMCPClientVersionsQuerySchema,
	responseSchema: require_mcp_client_versions.compareMCPClientVersionsResponseSchema,
	summary: "Compare MCP client versions",
	description: "Compares two versions and returns the differences between them",
	tags: ["MCP Client Versions"],
	handler: async ({ mastra, mcpClientId, from, to, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			require_utils.assertStoredResourceScope(await mcpClientStore.getById(mcpClientId), await require_utils.getStoredResourceScope(mastra, requestContext));
			const fromVersion = await mcpClientStore.getVersion(from);
			if (!fromVersion) throw new require_http_exception.HTTPException(404, { message: `Version with id ${from} not found` });
			if (fromVersion.mcpClientId !== mcpClientId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${from} not found for MCP client ${mcpClientId}` });
			const toVersion = await mcpClientStore.getVersion(to);
			if (!toVersion) throw new require_http_exception.HTTPException(404, { message: `Version with id ${to} not found` });
			if (toVersion.mcpClientId !== mcpClientId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${to} not found for MCP client ${mcpClientId}` });
			return {
				diffs: require_server_handlers_version_helpers.computeVersionDiffs(require_server_handlers_version_helpers.extractConfigFromVersion(fromVersion, require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS), require_server_handlers_version_helpers.extractConfigFromVersion(toVersion, require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS)),
				fromVersion,
				toVersion
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error comparing MCP client versions");
		}
	}
});
//#endregion
exports.ACTIVATE_MCP_CLIENT_VERSION_ROUTE = ACTIVATE_MCP_CLIENT_VERSION_ROUTE;
exports.COMPARE_MCP_CLIENT_VERSIONS_ROUTE = COMPARE_MCP_CLIENT_VERSIONS_ROUTE;
exports.CREATE_MCP_CLIENT_VERSION_ROUTE = CREATE_MCP_CLIENT_VERSION_ROUTE;
exports.DELETE_MCP_CLIENT_VERSION_ROUTE = DELETE_MCP_CLIENT_VERSION_ROUTE;
exports.GET_MCP_CLIENT_VERSION_ROUTE = GET_MCP_CLIENT_VERSION_ROUTE;
exports.LIST_MCP_CLIENT_VERSIONS_ROUTE = LIST_MCP_CLIENT_VERSIONS_ROUTE;
exports.RESTORE_MCP_CLIENT_VERSION_ROUTE = RESTORE_MCP_CLIENT_VERSION_ROUTE;

//# sourceMappingURL=mcp-client-versions.cjs.map