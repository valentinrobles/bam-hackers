Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
let zod_v4 = require("zod/v4");
//#region src/server/handlers/observability-list-query-schemas.ts
/**
* Server-local route-layer query schemas for observability list endpoints.
*
* Why these live in @mastra/server instead of importing the newer shared core
* exports directly:
* - `mode`, `after`, and `limit` were added after older @mastra/core versions
*   that @mastra/server still needs to tolerate at runtime.
* - Direct named imports of newly-added schema exports create module-link-time
*   failures when a newer server is paired with an older core.
* - These are route parsing concerns, so mirroring the current core schema
*   semantics here keeps the server backward-compatible without forcing a peer
*   dependency lockstep upgrade.
*
* TODO(Mastra 2.0): remove this shim and import the shared observability list
* query schemas directly once server/core no longer need to tolerate older
* mixed-version pairings.
*/
const paginationArgsSchema = zod_v4.z.object({
	page: zod_v4.z.coerce.number().int().min(0).optional().default(0).describe("Zero-indexed page number"),
	perPage: zod_v4.z.coerce.number().int().min(1).max(100).optional().default(10).describe("Number of items per page")
}).describe("Pagination options for list queries");
const deltaCursorSchema = zod_v4.z.string().min(1).describe("Opaque cursor value for incremental polling");
const listModeSchema = zod_v4.z.enum(["page", "delta"]).describe("List mode: 'page' | 'delta', defaults to 'page' when omitted.");
const deltaLimitSchema = zod_v4.z.coerce.number().int().min(1).max(100).optional().describe("Maximum number of updates to return in one delta poll");
//#endregion
exports.deltaCursorSchema = deltaCursorSchema;
exports.deltaLimitSchema = deltaLimitSchema;
exports.listModeSchema = listModeSchema;
exports.paginationArgsSchema = paginationArgsSchema;

//# sourceMappingURL=observability-list-query-schemas.cjs.map