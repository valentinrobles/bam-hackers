Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_rolldown_runtime = require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_agents = require("../../agents-CZE_MSQE.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_utils = require("../../utils-aIu3gkhJ.cjs");
const require_server_handlers_agents = require("./agents.cjs");
let _mastra_core_tools = require("@mastra/core/tools");
//#region src/server/handlers/plans.ts
var plans_exports = /* @__PURE__ */ require_rolldown_runtime.__exportAll({ READ_AGENT_PLAN_ROUTE: () => READ_AGENT_PLAN_ROUTE });
const PLANS_DIRECTORY = ".mastracode/plans/";
function isPlanPath(path) {
	if (!path.startsWith(PLANS_DIRECTORY)) return false;
	if (!path.endsWith(".md")) return false;
	if (path.includes("\\") || path.includes("\0")) return false;
	return path.slice(18).split("/").every((segment) => segment.length > 0 && segment !== "." && segment !== "..");
}
const READ_AGENT_PLAN_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/agents/:agentId/plans/file",
	responseType: "json",
	pathParamSchema: require_agents.agentIdPathParams,
	queryParamSchema: require_agents.agentPlanQuerySchema,
	responseSchema: require_agents.agentPlanResponseSchema,
	summary: "Read a submitted agent plan",
	description: "Returns a markdown plan when the agent exposes the core submit_plan capability and the path is under .mastracode/plans/.",
	tags: ["Agents", "Tools"],
	requiresAuth: true,
	requiresPermission: require_utils.MastraFGAPermissions.AGENTS_READ,
	handler: async ({ agentId, mastra, path, requestContext, status, versionId }) => {
		try {
			const agent = await require_server_handlers_agents.getAgentFromSystem({
				mastra,
				agentId,
				versionOptions: versionId ? { versionId } : status ? { status } : void 0,
				requestContext
			});
			const tools = await agent.listTools({ requestContext });
			if (!Object.values(tools).some((tool) => typeof tool === "object" && tool !== null && "id" in tool && tool.id === _mastra_core_tools.submitPlanTool.id)) throw new require_http_exception.HTTPException(404, { message: "Plan capability not found" });
			if (!isPlanPath(path)) throw new require_http_exception.HTTPException(400, { message: "Invalid plan path" });
			const filesystem = await (await agent.getWorkspace({ requestContext }))?.resolveFilesystem({ requestContext });
			if (!filesystem) throw new require_http_exception.HTTPException(404, { message: "No workspace filesystem configured" });
			if (!await filesystem.exists(path)) throw new require_http_exception.HTTPException(404, { message: `Plan file "${path}" not found` });
			const content = await filesystem.readFile(path, { encoding: "utf-8" });
			return {
				path,
				content: typeof content === "string" ? content : content.toString("utf-8")
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error reading submitted plan");
		}
	}
});
//#endregion
exports.READ_AGENT_PLAN_ROUTE = READ_AGENT_PLAN_ROUTE;
Object.defineProperty(exports, "plans_exports", {
	enumerable: true,
	get: function() {
		return plans_exports;
	}
});

//# sourceMappingURL=plans.cjs.map