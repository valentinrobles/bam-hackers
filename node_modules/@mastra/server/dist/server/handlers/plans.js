import { r as __exportAll } from "../../rolldown-runtime-maW3aOIk.js";
import { t as HTTPException } from "../../http-exception-CyfxZB6W.js";
import { a as agentIdPathParams, o as agentPlanQuerySchema, s as agentPlanResponseSchema } from "../../agents-ssoMemNs.js";
import { n as createRoute } from "../../route-builder-BukcT_Kp.js";
import { handleError } from "./error.js";
import { p as MastraFGAPermissions } from "../../utils-KsERfbhc.js";
import { getAgentFromSystem } from "./agents.js";
import { submitPlanTool } from "@mastra/core/tools";
//#region src/server/handlers/plans.ts
var plans_exports = /* @__PURE__ */ __exportAll({ READ_AGENT_PLAN_ROUTE: () => READ_AGENT_PLAN_ROUTE });
const PLANS_DIRECTORY = ".mastracode/plans/";
function isPlanPath(path) {
	if (!path.startsWith(PLANS_DIRECTORY)) return false;
	if (!path.endsWith(".md")) return false;
	if (path.includes("\\") || path.includes("\0")) return false;
	return path.slice(18).split("/").every((segment) => segment.length > 0 && segment !== "." && segment !== "..");
}
const READ_AGENT_PLAN_ROUTE = createRoute({
	method: "GET",
	path: "/agents/:agentId/plans/file",
	responseType: "json",
	pathParamSchema: agentIdPathParams,
	queryParamSchema: agentPlanQuerySchema,
	responseSchema: agentPlanResponseSchema,
	summary: "Read a submitted agent plan",
	description: "Returns a markdown plan when the agent exposes the core submit_plan capability and the path is under .mastracode/plans/.",
	tags: ["Agents", "Tools"],
	requiresAuth: true,
	requiresPermission: MastraFGAPermissions.AGENTS_READ,
	handler: async ({ agentId, mastra, path, requestContext, status, versionId }) => {
		try {
			const agent = await getAgentFromSystem({
				mastra,
				agentId,
				versionOptions: versionId ? { versionId } : status ? { status } : void 0,
				requestContext
			});
			const tools = await agent.listTools({ requestContext });
			if (!Object.values(tools).some((tool) => typeof tool === "object" && tool !== null && "id" in tool && tool.id === submitPlanTool.id)) throw new HTTPException(404, { message: "Plan capability not found" });
			if (!isPlanPath(path)) throw new HTTPException(400, { message: "Invalid plan path" });
			const filesystem = await (await agent.getWorkspace({ requestContext }))?.resolveFilesystem({ requestContext });
			if (!filesystem) throw new HTTPException(404, { message: "No workspace filesystem configured" });
			if (!await filesystem.exists(path)) throw new HTTPException(404, { message: `Plan file "${path}" not found` });
			const content = await filesystem.readFile(path, { encoding: "utf-8" });
			return {
				path,
				content: typeof content === "string" ? content : content.toString("utf-8")
			};
		} catch (error) {
			return handleError(error, "Error reading submitted plan");
		}
	}
});
//#endregion
export { READ_AGENT_PLAN_ROUTE, plans_exports as t };

//# sourceMappingURL=plans.js.map