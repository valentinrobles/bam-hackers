Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_processor_providers = require("../../processor-providers-DHvTA9AL.cjs");
let _mastra_core_utils_zod_to_json = require("@mastra/core/utils/zod-to-json");
//#region src/server/handlers/processor-providers.ts
/**
* GET /processor-providers - List all registered processor providers
*/
const LIST_PROCESSOR_PROVIDERS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/processor-providers",
	responseType: "json",
	responseSchema: require_processor_providers.getProcessorProvidersResponseSchema,
	summary: "List processor providers",
	description: "Returns a list of all registered processor providers with their info and available phases",
	tags: ["Processor Providers"],
	requiresAuth: true,
	handler: async ({ mastra }) => {
		try {
			const editor = mastra.getEditor();
			if (!editor) throw new require_http_exception.HTTPException(500, { message: "Editor is not configured" });
			const providers = editor.getProcessorProviders();
			return { providers: Object.values(providers).map((provider) => ({
				...provider.info,
				availablePhases: provider.availablePhases
			})) };
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing processor providers");
		}
	}
});
/**
* GET /processor-providers/:providerId - Get a specific processor provider with config schema
*/
const GET_PROCESSOR_PROVIDER_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/processor-providers/:providerId",
	responseType: "json",
	pathParamSchema: require_processor_providers.processorProviderIdPathParams,
	responseSchema: require_processor_providers.getProcessorProviderResponseSchema,
	summary: "Get processor provider details",
	description: "Returns details about a specific processor provider, including its configuration schema",
	tags: ["Processor Providers"],
	requiresAuth: true,
	handler: async ({ mastra, providerId }) => {
		try {
			const editor = mastra.getEditor();
			if (!editor) throw new require_http_exception.HTTPException(500, { message: "Editor is not configured" });
			const provider = editor.getProcessorProvider(providerId);
			if (!provider) throw new require_http_exception.HTTPException(404, { message: `Processor provider with id ${providerId} not found` });
			return {
				...provider.info,
				availablePhases: provider.availablePhases,
				configSchema: (0, _mastra_core_utils_zod_to_json.zodToJsonSchema)(provider.configSchema)
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting processor provider");
		}
	}
});
//#endregion
exports.GET_PROCESSOR_PROVIDER_ROUTE = GET_PROCESSOR_PROVIDER_ROUTE;
exports.LIST_PROCESSOR_PROVIDERS_ROUTE = LIST_PROCESSOR_PROVIDERS_ROUTE;

//# sourceMappingURL=processor-providers.cjs.map