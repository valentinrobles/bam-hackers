Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_server_handlers_version_helpers = require("./version-helpers.cjs");
const require_prompt_block_versions = require("../../prompt-block-versions-ChvK-DMW.cjs");
//#region src/server/handlers/prompt-block-versions.ts
const SNAPSHOT_CONFIG_FIELDS = [
	"name",
	"description",
	"content",
	"rules"
];
/**
* GET /stored/prompt-blocks/:promptBlockId/versions - List all versions for a prompt block
*/
const LIST_PROMPT_BLOCK_VERSIONS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/prompt-blocks/:promptBlockId/versions",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_prompt_block_versions.promptBlockVersionPathParams,
	queryParamSchema: require_prompt_block_versions.listPromptBlockVersionsQuerySchema,
	responseSchema: require_prompt_block_versions.listPromptBlockVersionsResponseSchema,
	summary: "List prompt block versions",
	description: "Returns a paginated list of all versions for a stored prompt block",
	tags: ["Prompt Block Versions"],
	handler: async ({ mastra, promptBlockId, page, perPage, orderBy, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const promptBlock = await promptBlockStore.getById(promptBlockId);
			if (!promptBlock) throw new require_http_exception.HTTPException(404, { message: `Prompt block with id ${promptBlockId} not found` });
			require_utils.assertStoredResourceScope(promptBlock, await require_utils.getStoredResourceScope(mastra, requestContext));
			return await promptBlockStore.listVersions({
				blockId: promptBlockId,
				page,
				perPage,
				orderBy
			});
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing prompt block versions");
		}
	}
});
/**
* POST /stored/prompt-blocks/:promptBlockId/versions - Create a new version snapshot
*/
const CREATE_PROMPT_BLOCK_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/prompt-blocks/:promptBlockId/versions",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_prompt_block_versions.promptBlockVersionPathParams,
	bodySchema: require_prompt_block_versions.createPromptBlockVersionBodySchema,
	responseSchema: require_prompt_block_versions.createPromptBlockVersionResponseSchema,
	summary: "Create prompt block version",
	description: "Creates a new version snapshot of the current prompt block configuration",
	tags: ["Prompt Block Versions"],
	handler: async ({ mastra, promptBlockId, changeMessage, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const promptBlock = await promptBlockStore.getById(promptBlockId);
			if (!promptBlock) throw new require_http_exception.HTTPException(404, { message: `Prompt block with id ${promptBlockId} not found` });
			require_utils.assertStoredResourceScope(promptBlock, await require_utils.getStoredResourceScope(mastra, requestContext));
			let currentConfig = {};
			if (promptBlock.activeVersionId) {
				const activeVersion = await promptBlockStore.getVersion(promptBlock.activeVersionId);
				if (activeVersion) currentConfig = require_server_handlers_version_helpers.extractConfigFromVersion(activeVersion, SNAPSHOT_CONFIG_FIELDS);
			}
			const latestVersion = await promptBlockStore.getLatestVersion(promptBlockId);
			if (!promptBlock.activeVersionId && latestVersion) currentConfig = require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, SNAPSHOT_CONFIG_FIELDS);
			const changedFields = require_server_handlers_version_helpers.calculateChangedFields(latestVersion ? require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, SNAPSHOT_CONFIG_FIELDS) : null, currentConfig);
			const { versionId } = await require_server_handlers_version_helpers.createVersionWithRetry(promptBlockStore, promptBlockId, "blockId", currentConfig, changedFields, { changeMessage });
			const version = await promptBlockStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(500, { message: "Failed to retrieve created version" });
			await require_server_handlers_version_helpers.enforceRetentionLimit(promptBlockStore, promptBlockId, "blockId", promptBlock.activeVersionId);
			return version;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error creating prompt block version");
		}
	}
});
/**
* GET /stored/prompt-blocks/:promptBlockId/versions/:versionId - Get a specific version
*/
const GET_PROMPT_BLOCK_VERSION_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/prompt-blocks/:promptBlockId/versions/:versionId",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_prompt_block_versions.promptBlockVersionIdPathParams,
	responseSchema: require_prompt_block_versions.getPromptBlockVersionResponseSchema,
	summary: "Get prompt block version",
	description: "Returns a specific version of a prompt block by its version ID",
	tags: ["Prompt Block Versions"],
	handler: async ({ mastra, promptBlockId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const version = await promptBlockStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.blockId !== promptBlockId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for prompt block ${promptBlockId}` });
			require_utils.assertStoredResourceScope(await promptBlockStore.getById(promptBlockId), await require_utils.getStoredResourceScope(mastra, requestContext));
			return version;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting prompt block version");
		}
	}
});
/**
* POST /stored/prompt-blocks/:promptBlockId/versions/:versionId/activate - Set a version as active
*/
const ACTIVATE_PROMPT_BLOCK_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/prompt-blocks/:promptBlockId/versions/:versionId/activate",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_prompt_block_versions.promptBlockVersionIdPathParams,
	responseSchema: require_prompt_block_versions.activatePromptBlockVersionResponseSchema,
	summary: "Activate prompt block version",
	description: "Sets a specific version as the active version for the prompt block",
	tags: ["Prompt Block Versions"],
	handler: async ({ mastra, promptBlockId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const promptBlock = await promptBlockStore.getById(promptBlockId);
			if (!promptBlock) throw new require_http_exception.HTTPException(404, { message: `Prompt block with id ${promptBlockId} not found` });
			require_utils.assertStoredResourceScope(promptBlock, await require_utils.getStoredResourceScope(mastra, requestContext));
			const version = await promptBlockStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.blockId !== promptBlockId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for prompt block ${promptBlockId}` });
			await promptBlockStore.update({
				id: promptBlockId,
				activeVersionId: versionId,
				status: "published"
			});
			mastra.getEditor()?.prompt.clearCache(promptBlockId);
			return {
				success: true,
				message: `Version ${version.versionNumber} is now active`,
				activeVersionId: versionId
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error activating prompt block version");
		}
	}
});
/**
* POST /stored/prompt-blocks/:promptBlockId/versions/:versionId/restore - Restore prompt block to a version
*/
const RESTORE_PROMPT_BLOCK_VERSION_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/prompt-blocks/:promptBlockId/versions/:versionId/restore",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_prompt_block_versions.promptBlockVersionIdPathParams,
	responseSchema: require_prompt_block_versions.restorePromptBlockVersionResponseSchema,
	summary: "Restore prompt block version",
	description: "Restores the prompt block configuration from a version, creating a new version",
	tags: ["Prompt Block Versions"],
	handler: async ({ mastra, promptBlockId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const promptBlock = await promptBlockStore.getById(promptBlockId);
			if (!promptBlock) throw new require_http_exception.HTTPException(404, { message: `Prompt block with id ${promptBlockId} not found` });
			require_utils.assertStoredResourceScope(promptBlock, await require_utils.getStoredResourceScope(mastra, requestContext));
			const versionToRestore = await promptBlockStore.getVersion(versionId);
			if (!versionToRestore) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (versionToRestore.blockId !== promptBlockId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for prompt block ${promptBlockId}` });
			const restoredConfig = require_server_handlers_version_helpers.extractConfigFromVersion(versionToRestore, SNAPSHOT_CONFIG_FIELDS);
			await promptBlockStore.update({
				id: promptBlockId,
				...restoredConfig
			});
			const latestVersion = await promptBlockStore.getLatestVersion(promptBlockId);
			const { versionId: newVersionId } = await require_server_handlers_version_helpers.createVersionWithRetry(promptBlockStore, promptBlockId, "blockId", restoredConfig, require_server_handlers_version_helpers.calculateChangedFields(latestVersion ? require_server_handlers_version_helpers.extractConfigFromVersion(latestVersion, SNAPSHOT_CONFIG_FIELDS) : null, restoredConfig), { changeMessage: `Restored from version ${versionToRestore.versionNumber}` });
			const newVersion = await promptBlockStore.getVersion(newVersionId);
			if (!newVersion) throw new require_http_exception.HTTPException(500, { message: "Failed to retrieve created version" });
			await require_server_handlers_version_helpers.enforceRetentionLimit(promptBlockStore, promptBlockId, "blockId", promptBlock.activeVersionId);
			mastra.getEditor()?.prompt.clearCache(promptBlockId);
			return newVersion;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error restoring prompt block version");
		}
	}
});
/**
* DELETE /stored/prompt-blocks/:promptBlockId/versions/:versionId - Delete a version
*/
const DELETE_PROMPT_BLOCK_VERSION_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/prompt-blocks/:promptBlockId/versions/:versionId",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_prompt_block_versions.promptBlockVersionIdPathParams,
	responseSchema: require_prompt_block_versions.deletePromptBlockVersionResponseSchema,
	summary: "Delete prompt block version",
	description: "Deletes a specific version (cannot delete the active version)",
	tags: ["Prompt Block Versions"],
	handler: async ({ mastra, promptBlockId, versionId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const promptBlock = await promptBlockStore.getById(promptBlockId);
			if (!promptBlock) throw new require_http_exception.HTTPException(404, { message: `Prompt block with id ${promptBlockId} not found` });
			require_utils.assertStoredResourceScope(promptBlock, await require_utils.getStoredResourceScope(mastra, requestContext));
			const version = await promptBlockStore.getVersion(versionId);
			if (!version) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found` });
			if (version.blockId !== promptBlockId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${versionId} not found for prompt block ${promptBlockId}` });
			if (promptBlock.activeVersionId === versionId) throw new require_http_exception.HTTPException(400, { message: "Cannot delete the active version. Activate a different version first." });
			await promptBlockStore.deleteVersion(versionId);
			mastra.getEditor()?.prompt.clearCache(promptBlockId);
			return {
				success: true,
				message: `Version ${version.versionNumber} deleted successfully`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting prompt block version");
		}
	}
});
/**
* GET /stored/prompt-blocks/:promptBlockId/versions/compare - Compare two versions
*/
const COMPARE_PROMPT_BLOCK_VERSIONS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/prompt-blocks/:promptBlockId/versions/compare",
	requiresAuth: true,
	responseType: "json",
	pathParamSchema: require_prompt_block_versions.promptBlockVersionPathParams,
	queryParamSchema: require_prompt_block_versions.comparePromptBlockVersionsQuerySchema,
	responseSchema: require_prompt_block_versions.comparePromptBlockVersionsResponseSchema,
	summary: "Compare prompt block versions",
	description: "Compares two versions and returns the differences between them",
	tags: ["Prompt Block Versions"],
	handler: async ({ mastra, promptBlockId, from, to, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			require_utils.assertStoredResourceScope(await promptBlockStore.getById(promptBlockId), await require_utils.getStoredResourceScope(mastra, requestContext));
			const fromVersion = await promptBlockStore.getVersion(from);
			if (!fromVersion) throw new require_http_exception.HTTPException(404, { message: `Version with id ${from} not found` });
			if (fromVersion.blockId !== promptBlockId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${from} not found for prompt block ${promptBlockId}` });
			const toVersion = await promptBlockStore.getVersion(to);
			if (!toVersion) throw new require_http_exception.HTTPException(404, { message: `Version with id ${to} not found` });
			if (toVersion.blockId !== promptBlockId) throw new require_http_exception.HTTPException(404, { message: `Version with id ${to} not found for prompt block ${promptBlockId}` });
			return {
				diffs: require_server_handlers_version_helpers.computeVersionDiffs(require_server_handlers_version_helpers.extractConfigFromVersion(fromVersion, SNAPSHOT_CONFIG_FIELDS), require_server_handlers_version_helpers.extractConfigFromVersion(toVersion, SNAPSHOT_CONFIG_FIELDS)),
				fromVersion,
				toVersion
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error comparing prompt block versions");
		}
	}
});
//#endregion
exports.ACTIVATE_PROMPT_BLOCK_VERSION_ROUTE = ACTIVATE_PROMPT_BLOCK_VERSION_ROUTE;
exports.COMPARE_PROMPT_BLOCK_VERSIONS_ROUTE = COMPARE_PROMPT_BLOCK_VERSIONS_ROUTE;
exports.CREATE_PROMPT_BLOCK_VERSION_ROUTE = CREATE_PROMPT_BLOCK_VERSION_ROUTE;
exports.DELETE_PROMPT_BLOCK_VERSION_ROUTE = DELETE_PROMPT_BLOCK_VERSION_ROUTE;
exports.GET_PROMPT_BLOCK_VERSION_ROUTE = GET_PROMPT_BLOCK_VERSION_ROUTE;
exports.LIST_PROMPT_BLOCK_VERSIONS_ROUTE = LIST_PROMPT_BLOCK_VERSIONS_ROUTE;
exports.RESTORE_PROMPT_BLOCK_VERSION_ROUTE = RESTORE_PROMPT_BLOCK_VERSION_ROUTE;

//# sourceMappingURL=prompt-block-versions.cjs.map