Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_common = require("../../common-BPjna_b6.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_server_handlers_version_helpers = require("./version-helpers.cjs");
const require_stored_mcp_clients = require("../../stored-mcp-clients-Bh7nQkqD.cjs");
//#region src/server/handlers/stored-mcp-clients.ts
/**
* GET /stored/mcp-clients - List all stored MCP clients
*/
const LIST_STORED_MCP_CLIENTS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/mcp-clients",
	responseType: "json",
	queryParamSchema: require_stored_mcp_clients.listStoredMCPClientsQuerySchema,
	responseSchema: require_stored_mcp_clients.listStoredMCPClientsResponseSchema,
	summary: "List stored MCP clients",
	description: "Returns a paginated list of all MCP client configurations stored in the database",
	tags: ["Stored MCP Clients"],
	requiresAuth: true,
	handler: async ({ mastra, page, perPage, orderBy, status, authorId, metadata, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			return await mcpClientStore.listResolved({
				page,
				perPage,
				orderBy,
				status,
				authorId,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, scope)
			});
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing stored MCP clients");
		}
	}
});
/**
* GET /stored/mcp-clients/:storedMCPClientId - Get a stored MCP client by ID
*/
const GET_STORED_MCP_CLIENT_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/mcp-clients/:storedMCPClientId",
	responseType: "json",
	pathParamSchema: require_stored_mcp_clients.storedMCPClientIdPathParams,
	queryParamSchema: require_common.statusQuerySchema,
	responseSchema: require_stored_mcp_clients.getStoredMCPClientResponseSchema,
	summary: "Get stored MCP client by ID",
	description: "Returns a specific MCP client from storage by its unique identifier. Use ?status=draft to resolve with the latest (draft) version, or ?status=published (default) for the active published version.",
	tags: ["Stored MCP Clients"],
	requiresAuth: true,
	handler: async ({ mastra, storedMCPClientId, status, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const mcpClient = await mcpClientStore.getByIdResolved(storedMCPClientId, { status });
			if (!mcpClient) throw new require_http_exception.HTTPException(404, { message: `Stored MCP client with id ${storedMCPClientId} not found` });
			require_utils.assertStoredResourceScope(mcpClient, await require_utils.getStoredResourceScope(mastra, requestContext));
			return mcpClient;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting stored MCP client");
		}
	}
});
/**
* POST /stored/mcp-clients - Create a new stored MCP client
*/
const CREATE_STORED_MCP_CLIENT_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/mcp-clients",
	responseType: "json",
	bodySchema: require_stored_mcp_clients.createStoredMCPClientBodySchema,
	responseSchema: require_stored_mcp_clients.createStoredMCPClientResponseSchema,
	summary: "Create stored MCP client",
	description: "Creates a new MCP client configuration in storage with the provided servers",
	tags: ["Stored MCP Clients"],
	requiresAuth: true,
	handler: async ({ mastra, id: providedId, authorId, metadata, name, description, servers, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const id = providedId || require_utils.toSlug(name);
			if (!id) throw new require_http_exception.HTTPException(400, { message: "Could not derive MCP client ID from name. Please provide an explicit id." });
			if (await mcpClientStore.getById(id)) throw new require_http_exception.HTTPException(409, { message: `MCP client with id ${id} already exists` });
			await mcpClientStore.create({ mcpClient: {
				id,
				authorId,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, await require_utils.getStoredResourceScope(mastra, requestContext)),
				name,
				description,
				servers
			} });
			const resolved = await mcpClientStore.getByIdResolved(id, { status: "draft" });
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve created MCP client" });
			return resolved;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error creating stored MCP client");
		}
	}
});
/**
* PATCH /stored/mcp-clients/:storedMCPClientId - Update a stored MCP client
*/
const UPDATE_STORED_MCP_CLIENT_ROUTE = require_route_builder.createRoute({
	method: "PATCH",
	path: "/stored/mcp-clients/:storedMCPClientId",
	responseType: "json",
	pathParamSchema: require_stored_mcp_clients.storedMCPClientIdPathParams,
	bodySchema: require_stored_mcp_clients.updateStoredMCPClientBodySchema,
	responseSchema: require_stored_mcp_clients.updateStoredMCPClientResponseSchema,
	summary: "Update stored MCP client",
	description: "Updates an existing MCP client in storage with the provided fields",
	tags: ["Stored MCP Clients"],
	requiresAuth: true,
	handler: async ({ mastra, storedMCPClientId, authorId, metadata, requestContext, name, description, servers }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const existing = await mcpClientStore.getById(storedMCPClientId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored MCP client with id ${storedMCPClientId} not found` });
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			require_utils.assertStoredResourceScope(existing, scope);
			const scopedMetadata = metadata !== void 0 ? require_utils.scopeStoredResourceMetadata({
				...existing.metadata ?? {},
				...metadata
			}, scope) : void 0;
			await require_server_handlers_version_helpers.handleAutoVersioning(mcpClientStore, storedMCPClientId, "mcpClientId", require_server_handlers_version_helpers.MCP_CLIENT_SNAPSHOT_CONFIG_FIELDS, existing, await mcpClientStore.update({
				id: storedMCPClientId,
				authorId,
				...scopedMetadata !== void 0 ? { metadata: scopedMetadata } : {},
				name,
				description,
				servers
			}), Object.fromEntries(Object.entries({
				name,
				description,
				servers
			}).filter(([_, v]) => v !== void 0)));
			const resolved = await mcpClientStore.getByIdResolved(storedMCPClientId, { status: "draft" });
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve updated MCP client" });
			return resolved;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error updating stored MCP client");
		}
	}
});
/**
* DELETE /stored/mcp-clients/:storedMCPClientId - Delete a stored MCP client
*/
const DELETE_STORED_MCP_CLIENT_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/mcp-clients/:storedMCPClientId",
	responseType: "json",
	pathParamSchema: require_stored_mcp_clients.storedMCPClientIdPathParams,
	responseSchema: require_stored_mcp_clients.deleteStoredMCPClientResponseSchema,
	summary: "Delete stored MCP client",
	description: "Deletes an MCP client from storage by its unique identifier",
	tags: ["Stored MCP Clients"],
	requiresAuth: true,
	handler: async ({ mastra, storedMCPClientId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const mcpClientStore = await storage.getStore("mcpClients");
			if (!mcpClientStore) throw new require_http_exception.HTTPException(500, { message: "MCP clients storage domain is not available" });
			const existing = await mcpClientStore.getById(storedMCPClientId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored MCP client with id ${storedMCPClientId} not found` });
			require_utils.assertStoredResourceScope(existing, await require_utils.getStoredResourceScope(mastra, requestContext));
			await mcpClientStore.delete(storedMCPClientId);
			return {
				success: true,
				message: `MCP client ${storedMCPClientId} deleted successfully`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting stored MCP client");
		}
	}
});
//#endregion
exports.CREATE_STORED_MCP_CLIENT_ROUTE = CREATE_STORED_MCP_CLIENT_ROUTE;
exports.DELETE_STORED_MCP_CLIENT_ROUTE = DELETE_STORED_MCP_CLIENT_ROUTE;
exports.GET_STORED_MCP_CLIENT_ROUTE = GET_STORED_MCP_CLIENT_ROUTE;
exports.LIST_STORED_MCP_CLIENTS_ROUTE = LIST_STORED_MCP_CLIENTS_ROUTE;
exports.UPDATE_STORED_MCP_CLIENT_ROUTE = UPDATE_STORED_MCP_CLIENT_ROUTE;

//# sourceMappingURL=stored-mcp-clients.cjs.map