Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_common = require("../../common-BPjna_b6.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_server_handlers_version_helpers = require("./version-helpers.cjs");
const require_stored_prompt_blocks = require("../../stored-prompt-blocks-CyL4SKVR.cjs");
//#region src/server/handlers/stored-prompt-blocks.ts
const PROMPT_BLOCK_SNAPSHOT_CONFIG_FIELDS = [
	"name",
	"description",
	"content",
	"rules",
	"requestContextSchema"
];
/** Computes whether a prompt block has an unpublished draft version. */
function computeHasDraft(latestVersion, activeVersionId) {
	return !!(latestVersion && (!activeVersionId || latestVersion.id !== activeVersionId));
}
/**
* GET /stored/prompt-blocks - List all stored prompt blocks
*/
const LIST_STORED_PROMPT_BLOCKS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/prompt-blocks",
	responseType: "json",
	queryParamSchema: require_stored_prompt_blocks.listStoredPromptBlocksQuerySchema,
	responseSchema: require_stored_prompt_blocks.listStoredPromptBlocksResponseSchema,
	summary: "List stored prompt blocks",
	description: "Returns a paginated list of all prompt blocks stored in the database",
	tags: ["Stored Prompt Blocks"],
	requiresAuth: true,
	handler: async ({ mastra, page, perPage, orderBy, status, authorId, metadata, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			const result = await promptBlockStore.listResolved({
				page,
				perPage,
				orderBy,
				status,
				authorId,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, scope)
			});
			const promptBlocks = await Promise.all(result.promptBlocks.map(async (block) => {
				const latestVersion = await promptBlockStore.getLatestVersion(block.id);
				return {
					...block,
					hasDraft: computeHasDraft(latestVersion, block.activeVersionId)
				};
			}));
			return {
				...result,
				promptBlocks
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing stored prompt blocks");
		}
	}
});
/**
* GET /stored/prompt-blocks/:storedPromptBlockId - Get a stored prompt block by ID
*/
const GET_STORED_PROMPT_BLOCK_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/prompt-blocks/:storedPromptBlockId",
	responseType: "json",
	pathParamSchema: require_stored_prompt_blocks.storedPromptBlockIdPathParams,
	queryParamSchema: require_common.statusQuerySchema,
	responseSchema: require_stored_prompt_blocks.getStoredPromptBlockResponseSchema,
	summary: "Get stored prompt block by ID",
	description: "Returns a specific prompt block from storage by its unique identifier. Use ?status=draft to resolve with the latest (draft) version, or ?status=published (default) for the active published version.",
	tags: ["Stored Prompt Blocks"],
	requiresAuth: true,
	handler: async ({ mastra, storedPromptBlockId, status, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const promptBlock = await promptBlockStore.getByIdResolved(storedPromptBlockId, { status });
			if (!promptBlock) throw new require_http_exception.HTTPException(404, { message: `Stored prompt block with id ${storedPromptBlockId} not found` });
			require_utils.assertStoredResourceScope(promptBlock, await require_utils.getStoredResourceScope(mastra, requestContext));
			const latestVersion = await promptBlockStore.getLatestVersion(storedPromptBlockId);
			return {
				...promptBlock,
				hasDraft: computeHasDraft(latestVersion, promptBlock.activeVersionId)
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting stored prompt block");
		}
	}
});
/**
* POST /stored/prompt-blocks - Create a new stored prompt block
*/
const CREATE_STORED_PROMPT_BLOCK_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/prompt-blocks",
	responseType: "json",
	bodySchema: require_stored_prompt_blocks.createStoredPromptBlockBodySchema,
	responseSchema: require_stored_prompt_blocks.createStoredPromptBlockResponseSchema,
	summary: "Create stored prompt block",
	description: "Creates a new prompt block in storage with the provided configuration",
	tags: ["Stored Prompt Blocks"],
	requiresAuth: true,
	handler: async ({ mastra, id: providedId, authorId, metadata, name, description, content, rules, requestContextSchema, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const id = providedId || require_utils.toSlug(name);
			if (!id) throw new require_http_exception.HTTPException(400, { message: "Could not derive prompt block ID from name. Please provide an explicit id." });
			if (await promptBlockStore.getById(id)) throw new require_http_exception.HTTPException(409, { message: `Prompt block with id ${id} already exists` });
			await promptBlockStore.create({ promptBlock: {
				id,
				authorId,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, await require_utils.getStoredResourceScope(mastra, requestContext)),
				name,
				description,
				content,
				rules,
				requestContextSchema
			} });
			const resolved = await promptBlockStore.getByIdResolved(id, { status: "draft" });
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve created prompt block" });
			const latestVersion = await promptBlockStore.getLatestVersion(id);
			const hasDraft = !!(latestVersion && (!resolved.activeVersionId || latestVersion.id !== resolved.activeVersionId));
			return {
				...resolved,
				hasDraft
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error creating stored prompt block");
		}
	}
});
/**
* PATCH /stored/prompt-blocks/:storedPromptBlockId - Update a stored prompt block
*/
const UPDATE_STORED_PROMPT_BLOCK_ROUTE = require_route_builder.createRoute({
	method: "PATCH",
	path: "/stored/prompt-blocks/:storedPromptBlockId",
	responseType: "json",
	pathParamSchema: require_stored_prompt_blocks.storedPromptBlockIdPathParams,
	bodySchema: require_stored_prompt_blocks.updateStoredPromptBlockBodySchema,
	responseSchema: require_stored_prompt_blocks.updateStoredPromptBlockResponseSchema,
	summary: "Update stored prompt block",
	description: "Updates an existing prompt block in storage with the provided fields",
	tags: ["Stored Prompt Blocks"],
	requiresAuth: true,
	handler: async ({ mastra, storedPromptBlockId, authorId, metadata, name, description, content, rules, requestContextSchema, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const existing = await promptBlockStore.getById(storedPromptBlockId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored prompt block with id ${storedPromptBlockId} not found` });
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			require_utils.assertStoredResourceScope(existing, scope);
			const scopedMetadata = metadata !== void 0 ? require_utils.scopeStoredResourceMetadata({
				...existing.metadata ?? {},
				...metadata
			}, scope) : void 0;
			const updatedPromptBlock = await promptBlockStore.update({
				id: storedPromptBlockId,
				authorId,
				...scopedMetadata !== void 0 ? { metadata: scopedMetadata } : {},
				name,
				description,
				content,
				rules,
				requestContextSchema
			});
			const providedConfigFields = Object.fromEntries(Object.entries({
				name,
				description,
				content,
				rules,
				requestContextSchema
			}).filter(([_, v]) => v !== void 0));
			await require_server_handlers_version_helpers.handleAutoVersioning(promptBlockStore, storedPromptBlockId, "blockId", PROMPT_BLOCK_SNAPSHOT_CONFIG_FIELDS, existing, updatedPromptBlock, providedConfigFields);
			const resolved = await promptBlockStore.getByIdResolved(storedPromptBlockId, { status: "draft" });
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve updated prompt block" });
			const latestVersion = await promptBlockStore.getLatestVersion(storedPromptBlockId);
			const hasDraft = !!(latestVersion && (!resolved.activeVersionId || latestVersion.id !== resolved.activeVersionId));
			return {
				...resolved,
				hasDraft
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error updating stored prompt block");
		}
	}
});
/**
* DELETE /stored/prompt-blocks/:storedPromptBlockId - Delete a stored prompt block
*/
const DELETE_STORED_PROMPT_BLOCK_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/prompt-blocks/:storedPromptBlockId",
	responseType: "json",
	pathParamSchema: require_stored_prompt_blocks.storedPromptBlockIdPathParams,
	responseSchema: require_stored_prompt_blocks.deleteStoredPromptBlockResponseSchema,
	summary: "Delete stored prompt block",
	description: "Deletes a prompt block from storage by its unique identifier",
	tags: ["Stored Prompt Blocks"],
	requiresAuth: true,
	handler: async ({ mastra, storedPromptBlockId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const promptBlockStore = await storage.getStore("promptBlocks");
			if (!promptBlockStore) throw new require_http_exception.HTTPException(500, { message: "Prompt blocks storage domain is not available" });
			const existing = await promptBlockStore.getById(storedPromptBlockId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored prompt block with id ${storedPromptBlockId} not found` });
			require_utils.assertStoredResourceScope(existing, await require_utils.getStoredResourceScope(mastra, requestContext));
			await promptBlockStore.delete(storedPromptBlockId);
			return {
				success: true,
				message: `Prompt block ${storedPromptBlockId} deleted successfully`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting stored prompt block");
		}
	}
});
//#endregion
exports.CREATE_STORED_PROMPT_BLOCK_ROUTE = CREATE_STORED_PROMPT_BLOCK_ROUTE;
exports.DELETE_STORED_PROMPT_BLOCK_ROUTE = DELETE_STORED_PROMPT_BLOCK_ROUTE;
exports.GET_STORED_PROMPT_BLOCK_ROUTE = GET_STORED_PROMPT_BLOCK_ROUTE;
exports.LIST_STORED_PROMPT_BLOCKS_ROUTE = LIST_STORED_PROMPT_BLOCKS_ROUTE;
exports.UPDATE_STORED_PROMPT_BLOCK_ROUTE = UPDATE_STORED_PROMPT_BLOCK_ROUTE;

//# sourceMappingURL=stored-prompt-blocks.cjs.map