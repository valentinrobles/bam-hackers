Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_common = require("../../common-BPjna_b6.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_server_handlers_version_helpers = require("./version-helpers.cjs");
const require_stored_scorers = require("../../stored-scorers-CWxW1_mY.cjs");
//#region src/server/handlers/stored-scorers.ts
const SCORER_SNAPSHOT_CONFIG_FIELDS = [
	"name",
	"description",
	"type",
	"model",
	"instructions",
	"scoreRange",
	"presetConfig",
	"defaultSampling"
];
/**
* GET /stored/scorers - List all stored scorer definitions
*/
const LIST_STORED_SCORERS_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/scorers",
	responseType: "json",
	queryParamSchema: require_stored_scorers.listStoredScorersQuerySchema,
	responseSchema: require_stored_scorers.listStoredScorersResponseSchema,
	summary: "List stored scorer definitions",
	description: "Returns a paginated list of all scorer definitions stored in the database",
	tags: ["Stored Scorers"],
	requiresAuth: true,
	handler: async ({ mastra, page, perPage, orderBy, status, authorId, metadata, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const scorerStore = await storage.getStore("scorerDefinitions");
			if (!scorerStore) throw new require_http_exception.HTTPException(500, { message: "Scorer definitions storage domain is not available" });
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			return await scorerStore.listResolved({
				page,
				perPage,
				orderBy,
				status,
				authorId,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, scope)
			});
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing stored scorer definitions");
		}
	}
});
/**
* GET /stored/scorers/:storedScorerId - Get a stored scorer definition by ID
*/
const GET_STORED_SCORER_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/scorers/:storedScorerId",
	responseType: "json",
	pathParamSchema: require_stored_scorers.storedScorerIdPathParams,
	queryParamSchema: require_common.statusQuerySchema,
	responseSchema: require_stored_scorers.getStoredScorerResponseSchema,
	summary: "Get stored scorer definition by ID",
	description: "Returns a specific scorer definition from storage by its unique identifier. Use ?status=draft to resolve with the latest (draft) version, or ?status=published (default) for the active published version.",
	tags: ["Stored Scorers"],
	requiresAuth: true,
	handler: async ({ mastra, storedScorerId, status, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const scorerStore = await storage.getStore("scorerDefinitions");
			if (!scorerStore) throw new require_http_exception.HTTPException(500, { message: "Scorer definitions storage domain is not available" });
			const scorer = await scorerStore.getByIdResolved(storedScorerId, { status });
			if (!scorer) throw new require_http_exception.HTTPException(404, { message: `Stored scorer definition with id ${storedScorerId} not found` });
			require_utils.assertStoredResourceScope(scorer, await require_utils.getStoredResourceScope(mastra, requestContext));
			return scorer;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting stored scorer definition");
		}
	}
});
/**
* POST /stored/scorers - Create a new stored scorer definition
*/
const CREATE_STORED_SCORER_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/scorers",
	responseType: "json",
	bodySchema: require_stored_scorers.createStoredScorerBodySchema,
	responseSchema: require_stored_scorers.createStoredScorerResponseSchema,
	summary: "Create stored scorer definition",
	description: "Creates a new scorer definition in storage with the provided configuration",
	tags: ["Stored Scorers"],
	requiresAuth: true,
	handler: async ({ mastra, id: providedId, authorId, metadata, name, description, type, model, instructions, scoreRange, presetConfig, defaultSampling, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const scorerStore = await storage.getStore("scorerDefinitions");
			if (!scorerStore) throw new require_http_exception.HTTPException(500, { message: "Scorer definitions storage domain is not available" });
			const id = providedId || require_utils.toSlug(name);
			if (!id) throw new require_http_exception.HTTPException(400, { message: "Could not derive scorer definition ID from name. Please provide an explicit id." });
			if (await scorerStore.getById(id)) throw new require_http_exception.HTTPException(409, { message: `Scorer definition with id ${id} already exists` });
			await scorerStore.create({ scorerDefinition: {
				id,
				authorId,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, await require_utils.getStoredResourceScope(mastra, requestContext)),
				name,
				description,
				type,
				model,
				instructions,
				scoreRange,
				presetConfig,
				defaultSampling
			} });
			const resolved = await scorerStore.getByIdResolved(id, { status: "draft" });
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve created scorer definition" });
			return resolved;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error creating stored scorer definition");
		}
	}
});
/**
* PATCH /stored/scorers/:storedScorerId - Update a stored scorer definition
*/
const UPDATE_STORED_SCORER_ROUTE = require_route_builder.createRoute({
	method: "PATCH",
	path: "/stored/scorers/:storedScorerId",
	responseType: "json",
	pathParamSchema: require_stored_scorers.storedScorerIdPathParams,
	bodySchema: require_stored_scorers.updateStoredScorerBodySchema,
	responseSchema: require_stored_scorers.updateStoredScorerResponseSchema,
	summary: "Update stored scorer definition",
	description: "Updates an existing scorer definition in storage with the provided fields",
	tags: ["Stored Scorers"],
	requiresAuth: true,
	handler: async ({ mastra, storedScorerId, authorId, metadata, name, description, type, model, instructions, scoreRange, presetConfig, defaultSampling, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const scorerStore = await storage.getStore("scorerDefinitions");
			if (!scorerStore) throw new require_http_exception.HTTPException(500, { message: "Scorer definitions storage domain is not available" });
			const existing = await scorerStore.getById(storedScorerId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored scorer definition with id ${storedScorerId} not found` });
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			require_utils.assertStoredResourceScope(existing, scope);
			const scopedMetadata = metadata !== void 0 ? require_utils.scopeStoredResourceMetadata({
				...existing.metadata ?? {},
				...metadata
			}, scope) : void 0;
			const updatedScorer = await scorerStore.update({
				id: storedScorerId,
				authorId,
				...scopedMetadata !== void 0 ? { metadata: scopedMetadata } : {},
				name,
				description,
				type,
				model,
				instructions,
				scoreRange,
				presetConfig,
				defaultSampling
			});
			const providedConfigFields = Object.fromEntries(Object.entries({
				name,
				description,
				type,
				model,
				instructions,
				scoreRange,
				presetConfig,
				defaultSampling
			}).filter(([_, v]) => v !== void 0));
			await require_server_handlers_version_helpers.handleAutoVersioning(scorerStore, storedScorerId, "scorerDefinitionId", SCORER_SNAPSHOT_CONFIG_FIELDS, existing, updatedScorer, providedConfigFields);
			const editor = mastra.getEditor();
			if (editor) editor.scorer.clearCache(storedScorerId);
			const resolved = await scorerStore.getByIdResolved(storedScorerId, { status: "draft" });
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve updated scorer definition" });
			return resolved;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error updating stored scorer definition");
		}
	}
});
/**
* DELETE /stored/scorers/:storedScorerId - Delete a stored scorer definition
*/
const DELETE_STORED_SCORER_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/scorers/:storedScorerId",
	responseType: "json",
	pathParamSchema: require_stored_scorers.storedScorerIdPathParams,
	responseSchema: require_stored_scorers.deleteStoredScorerResponseSchema,
	summary: "Delete stored scorer definition",
	description: "Deletes a scorer definition from storage by its unique identifier",
	tags: ["Stored Scorers"],
	requiresAuth: true,
	handler: async ({ mastra, storedScorerId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const scorerStore = await storage.getStore("scorerDefinitions");
			if (!scorerStore) throw new require_http_exception.HTTPException(500, { message: "Scorer definitions storage domain is not available" });
			const existing = await scorerStore.getById(storedScorerId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored scorer definition with id ${storedScorerId} not found` });
			require_utils.assertStoredResourceScope(existing, await require_utils.getStoredResourceScope(mastra, requestContext));
			await scorerStore.delete(storedScorerId);
			mastra.getEditor()?.scorer.clearCache(storedScorerId);
			return {
				success: true,
				message: `Scorer definition ${storedScorerId} deleted successfully`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting stored scorer definition");
		}
	}
});
//#endregion
exports.CREATE_STORED_SCORER_ROUTE = CREATE_STORED_SCORER_ROUTE;
exports.DELETE_STORED_SCORER_ROUTE = DELETE_STORED_SCORER_ROUTE;
exports.GET_STORED_SCORER_ROUTE = GET_STORED_SCORER_ROUTE;
exports.LIST_STORED_SCORERS_ROUTE = LIST_STORED_SCORERS_ROUTE;
exports.UPDATE_STORED_SCORER_ROUTE = UPDATE_STORED_SCORER_ROUTE;

//# sourceMappingURL=stored-scorers.cjs.map