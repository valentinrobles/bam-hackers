Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
const require_route_builder = require("../../route-builder-DupY8fkI.cjs");
const require_utils = require("../../utils-WcR0QSUB.cjs");
const require_server_handlers_error = require("./error.cjs");
const require_stored_workspaces = require("../../stored-workspaces-epewrppK.cjs");
const require_server_handlers_authorship = require("./authorship.cjs");
//#region src/server/handlers/stored-workspaces.ts
/**
* GET /stored/workspaces - List all stored workspaces
*/
const LIST_STORED_WORKSPACES_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/workspaces",
	responseType: "json",
	queryParamSchema: require_stored_workspaces.listStoredWorkspacesQuerySchema,
	responseSchema: require_stored_workspaces.listStoredWorkspacesResponseSchema,
	summary: "List stored workspaces",
	description: "Returns a paginated list of all workspace configurations stored in the database",
	tags: ["Stored Workspaces"],
	requiresAuth: true,
	handler: async ({ mastra, page, perPage, orderBy, authorId, metadata, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const workspaceStore = await storage.getStore("workspaces");
			if (!workspaceStore) throw new require_http_exception.HTTPException(500, { message: "Workspaces storage domain is not available" });
			const filter = require_server_handlers_authorship.resolveAuthorFilter({
				requestContext,
				resource: "stored-workspaces",
				queryAuthorId: authorId
			});
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			const result = await workspaceStore.listResolved({
				page,
				perPage,
				orderBy,
				authorId: filter.kind === "exact" ? filter.authorId : void 0,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, scope)
			});
			const visibleWorkspaces = result.workspaces.filter((record) => require_server_handlers_authorship.matchesAuthorFilter(record, filter));
			const runtimeWorkspaces = mastra.listWorkspaces();
			const workspaces = visibleWorkspaces.map((ws) => ({
				...ws,
				runtimeRegistered: ws.id in runtimeWorkspaces
			}));
			return {
				...result,
				workspaces
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error listing stored workspaces");
		}
	}
});
/**
* GET /stored/workspaces/:storedWorkspaceId - Get a stored workspace by ID
*/
const GET_STORED_WORKSPACE_ROUTE = require_route_builder.createRoute({
	method: "GET",
	path: "/stored/workspaces/:storedWorkspaceId",
	responseType: "json",
	pathParamSchema: require_stored_workspaces.storedWorkspaceIdPathParams,
	responseSchema: require_stored_workspaces.getStoredWorkspaceResponseSchema,
	summary: "Get stored workspace by ID",
	description: "Returns a specific workspace from storage by its unique identifier (resolved with active version config)",
	tags: ["Stored Workspaces"],
	requiresAuth: true,
	handler: async ({ mastra, storedWorkspaceId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const workspaceStore = await storage.getStore("workspaces");
			if (!workspaceStore) throw new require_http_exception.HTTPException(500, { message: "Workspaces storage domain is not available" });
			const workspace = await workspaceStore.getByIdResolved(storedWorkspaceId);
			if (!workspace) throw new require_http_exception.HTTPException(404, { message: `Stored workspace with id ${storedWorkspaceId} not found` });
			require_utils.assertStoredResourceScope(workspace, await require_utils.getStoredResourceScope(mastra, requestContext));
			require_server_handlers_authorship.assertReadAccess({
				requestContext,
				resource: "stored-workspaces",
				resourceId: storedWorkspaceId,
				record: workspace
			});
			return workspace;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error getting stored workspace");
		}
	}
});
/**
* POST /stored/workspaces - Create a new stored workspace
*/
const CREATE_STORED_WORKSPACE_ROUTE = require_route_builder.createRoute({
	method: "POST",
	path: "/stored/workspaces",
	responseType: "json",
	bodySchema: require_stored_workspaces.createStoredWorkspaceBodySchema,
	responseSchema: require_stored_workspaces.createStoredWorkspaceResponseSchema,
	summary: "Create stored workspace",
	description: "Creates a new workspace configuration in storage with the provided settings",
	tags: ["Stored Workspaces"],
	requiresAuth: true,
	handler: async ({ mastra, id: providedId, metadata, name, description, filesystem, sandbox, mounts, search, skills, tools, autoSync, operationTimeout, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const workspaceStore = await storage.getStore("workspaces");
			if (!workspaceStore) throw new require_http_exception.HTTPException(500, { message: "Workspaces storage domain is not available" });
			const id = providedId || require_utils.toSlug(name);
			if (!id) throw new require_http_exception.HTTPException(400, { message: "Could not derive workspace ID from name. Please provide an explicit id." });
			if (await workspaceStore.getById(id)) throw new require_http_exception.HTTPException(409, { message: `Workspace with id ${id} already exists` });
			const authorId = require_server_handlers_authorship.getCallerAuthorId(requestContext) ?? void 0;
			await workspaceStore.create({ workspace: {
				id,
				authorId,
				metadata: require_utils.scopeStoredResourceMetadata(metadata, await require_utils.getStoredResourceScope(mastra, requestContext)),
				name,
				description,
				filesystem,
				sandbox,
				mounts,
				search,
				skills,
				tools,
				autoSync,
				operationTimeout
			} });
			const resolved = await workspaceStore.getByIdResolved(id);
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve created workspace" });
			return resolved;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error creating stored workspace");
		}
	}
});
/**
* PATCH /stored/workspaces/:storedWorkspaceId - Update a stored workspace
*/
const UPDATE_STORED_WORKSPACE_ROUTE = require_route_builder.createRoute({
	method: "PATCH",
	path: "/stored/workspaces/:storedWorkspaceId",
	responseType: "json",
	pathParamSchema: require_stored_workspaces.storedWorkspaceIdPathParams,
	bodySchema: require_stored_workspaces.updateStoredWorkspaceBodySchema,
	responseSchema: require_stored_workspaces.updateStoredWorkspaceResponseSchema,
	summary: "Update stored workspace",
	description: "Updates an existing workspace in storage with the provided fields",
	tags: ["Stored Workspaces"],
	requiresAuth: true,
	handler: async ({ mastra, storedWorkspaceId, metadata, name, description, filesystem, sandbox, mounts, search, skills, tools, autoSync, operationTimeout, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const workspaceStore = await storage.getStore("workspaces");
			if (!workspaceStore) throw new require_http_exception.HTTPException(500, { message: "Workspaces storage domain is not available" });
			const existing = await workspaceStore.getById(storedWorkspaceId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored workspace with id ${storedWorkspaceId} not found` });
			const scope = await require_utils.getStoredResourceScope(mastra, requestContext);
			require_utils.assertStoredResourceScope(existing, scope);
			require_server_handlers_authorship.assertWriteAccess({
				requestContext,
				resource: "stored-workspaces",
				resourceId: storedWorkspaceId,
				action: "edit",
				record: existing
			});
			const scopedMetadata = metadata !== void 0 ? require_utils.scopeStoredResourceMetadata({
				...existing.metadata ?? {},
				...metadata
			}, scope) : void 0;
			const updateInput = { id: storedWorkspaceId };
			const candidate = {
				...scopedMetadata !== void 0 ? { metadata: scopedMetadata } : {},
				name,
				description,
				filesystem,
				sandbox,
				mounts,
				search,
				skills,
				tools,
				autoSync,
				operationTimeout
			};
			for (const [key, value] of Object.entries(candidate)) if (value !== void 0) updateInput[key] = value;
			await workspaceStore.update(updateInput);
			const resolved = await workspaceStore.getByIdResolved(storedWorkspaceId);
			if (!resolved) throw new require_http_exception.HTTPException(500, { message: "Failed to resolve updated workspace" });
			return resolved;
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error updating stored workspace");
		}
	}
});
/**
* DELETE /stored/workspaces/:storedWorkspaceId - Delete a stored workspace
*/
const DELETE_STORED_WORKSPACE_ROUTE = require_route_builder.createRoute({
	method: "DELETE",
	path: "/stored/workspaces/:storedWorkspaceId",
	responseType: "json",
	pathParamSchema: require_stored_workspaces.storedWorkspaceIdPathParams,
	responseSchema: require_stored_workspaces.deleteStoredWorkspaceResponseSchema,
	summary: "Delete stored workspace",
	description: "Deletes a workspace from storage by its unique identifier",
	tags: ["Stored Workspaces"],
	requiresAuth: true,
	handler: async ({ mastra, storedWorkspaceId, requestContext }) => {
		try {
			const storage = mastra.getStorage();
			if (!storage) throw new require_http_exception.HTTPException(500, { message: "Storage is not configured" });
			const workspaceStore = await storage.getStore("workspaces");
			if (!workspaceStore) throw new require_http_exception.HTTPException(500, { message: "Workspaces storage domain is not available" });
			const existing = await workspaceStore.getById(storedWorkspaceId);
			if (!existing) throw new require_http_exception.HTTPException(404, { message: `Stored workspace with id ${storedWorkspaceId} not found` });
			require_utils.assertStoredResourceScope(existing, await require_utils.getStoredResourceScope(mastra, requestContext));
			require_server_handlers_authorship.assertWriteAccess({
				requestContext,
				resource: "stored-workspaces",
				resourceId: storedWorkspaceId,
				action: "delete",
				record: existing
			});
			await workspaceStore.delete(storedWorkspaceId);
			return {
				success: true,
				message: `Workspace ${storedWorkspaceId} deleted successfully`
			};
		} catch (error) {
			return require_server_handlers_error.handleError(error, "Error deleting stored workspace");
		}
	}
});
//#endregion
exports.CREATE_STORED_WORKSPACE_ROUTE = CREATE_STORED_WORKSPACE_ROUTE;
exports.DELETE_STORED_WORKSPACE_ROUTE = DELETE_STORED_WORKSPACE_ROUTE;
exports.GET_STORED_WORKSPACE_ROUTE = GET_STORED_WORKSPACE_ROUTE;
exports.LIST_STORED_WORKSPACES_ROUTE = LIST_STORED_WORKSPACES_ROUTE;
exports.UPDATE_STORED_WORKSPACE_ROUTE = UPDATE_STORED_WORKSPACE_ROUTE;

//# sourceMappingURL=stored-workspaces.cjs.map