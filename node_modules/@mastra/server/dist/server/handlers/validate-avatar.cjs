Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../../rolldown-runtime-Bd6kNlEP.cjs");
const require_http_exception = require("../../http-exception-CKDHupKw.cjs");
//#region src/server/handlers/validate-avatar.ts
const AVATAR_MAX_BYTES = 512 * 1024;
/**
* Validates `metadata.avatarUrl` if present.
* Ensures it's a well-formed data URL and the decoded payload is ≤ 512 KB.
* No-ops when metadata is absent or doesn't contain avatarUrl.
*/
function validateMetadataAvatarUrl(metadata) {
	if (!metadata || !("avatarUrl" in metadata) || metadata.avatarUrl === null || metadata.avatarUrl === void 0) return;
	if (typeof metadata.avatarUrl !== "string") throw new require_http_exception.HTTPException(400, { message: "metadata.avatarUrl must be a string" });
	const match = metadata.avatarUrl.match(/^data:([^;]+);base64,(.+)$/);
	if (!match) throw new require_http_exception.HTTPException(400, { message: "metadata.avatarUrl must be a valid data URL (data:<mime>;base64,<data>)" });
	const base64Payload = match[2];
	if (!(base64Payload.length > 0 && base64Payload.length % 4 === 0 && /^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/.test(base64Payload))) throw new require_http_exception.HTTPException(400, { message: "metadata.avatarUrl contains invalid base64" });
	const byteLength = Buffer.from(base64Payload, "base64").byteLength;
	if (byteLength === 0) throw new require_http_exception.HTTPException(400, { message: "metadata.avatarUrl is empty" });
	if (byteLength > AVATAR_MAX_BYTES) throw new require_http_exception.HTTPException(413, { message: `metadata.avatarUrl exceeds ${AVATAR_MAX_BYTES}-byte limit (got ${byteLength})` });
}
//#endregion
exports.validateMetadataAvatarUrl = validateMetadataAvatarUrl;

//# sourceMappingURL=validate-avatar.cjs.map