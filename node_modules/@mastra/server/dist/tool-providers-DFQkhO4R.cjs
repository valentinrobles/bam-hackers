let zod_v4 = require("zod/v4");
//#region src/server/schemas/tool-providers.ts
/**
* Zod schemas for the Agent Builder / CMS tool providers storage shape.
*
* Rules:
*  - **Additive-only.** No version field. Future fields are introduced as
*    optional and existing fields are never removed in v1.x.
*  - `label` is optional when there is exactly one connection for a
*    `toolkit`. Once two or more connections share a `toolkit`, every
*    connection must carry a non-empty, ≤ 32 char, `[A-Za-z0-9 _-]+`
*    label that is case-insensitively unique within that toolkit.
*  - `kind` accepts all three values for forward-compat; v1 only writes
*    `'author'`.
*/
const labelSchema = zod_v4.z.string().min(1, "Connection label is required").max(32, "Connection label must be ≤ 32 characters").regex(/^[A-Za-z0-9 _-]+$/, "Connection label may only contain letters, digits, spaces, _ and -");
/**
* Per-pin identity bucketing.
*
* - `'per-author'` (default) — bucketed under the caller's resolved authorId.
* - `'shared'` — bucketed under SHARED_BUCKET_ID; visible to every caller.
* - `'caller-supplied'` — bucketed under `requestContext[MASTRA_RESOURCE_ID_KEY]`.
*
* Optional for back-compat with pre-scope stored pins.
*/
const connectionScopeSchema = zod_v4.z.enum([
	"shared",
	"per-author",
	"caller-supplied"
]);
const connectionSchema = zod_v4.z.object({
	kind: zod_v4.z.enum([
		"author",
		"invoker",
		"platform"
	]),
	toolkit: zod_v4.z.string().min(1),
	connectionId: zod_v4.z.string(),
	label: labelSchema.optional(),
	scope: connectionScopeSchema.optional()
});
const toolMetaSchema = zod_v4.z.object({
	toolkit: zod_v4.z.string().min(1).optional(),
	description: zod_v4.z.string().optional()
});
/**
* Stored shape for one provider's configuration on one agent.
*
* `superRefine` enforces case-insensitive uniqueness of `label` within
* each `connections[toolkit]` array.
*/
const toolProviderConfigSchema = zod_v4.z.object({
	tools: zod_v4.z.record(zod_v4.z.string(), toolMetaSchema),
	connections: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.array(connectionSchema))
}).superRefine((value, ctx) => {
	for (const [toolkit, connections] of Object.entries(value.connections)) {
		if (connections.length < 2) continue;
		const seen = /* @__PURE__ */ new Map();
		for (let i = 0; i < connections.length; i++) {
			const conn = connections[i];
			const trimmed = conn.label?.trim() ?? "";
			if (trimmed.length === 0) {
				ctx.addIssue({
					code: "custom",
					path: [
						"connections",
						toolkit,
						i,
						"label"
					],
					message: `Connection label is required on toolkit "${toolkit}" once it has two or more connections`
				});
				continue;
			}
			const key = trimmed.toLocaleLowerCase();
			if (seen.get(key) !== void 0) ctx.addIssue({
				code: "custom",
				path: [
					"connections",
					toolkit,
					i,
					"label"
				],
				message: `Duplicate connection label "${conn.label}" on toolkit "${toolkit}" (labels must be unique case-insensitively)`
			});
			else seen.set(key, i);
		}
	}
});
/**
* Full v1 tool providers payload: keyed by provider id.
*/
const toolProvidersSchema = zod_v4.z.record(zod_v4.z.string(), toolProviderConfigSchema);
const toolProviderIdPathParams = zod_v4.z.object({ providerId: zod_v4.z.string().describe("Unique identifier for the tool provider") });
const toolSlugPathParams = toolProviderIdPathParams.extend({ toolSlug: zod_v4.z.string().describe("Slug identifier for the tool") });
const toolProviderAuthStatusPathParams = toolProviderIdPathParams.extend({ authId: zod_v4.z.string().describe("Opaque auth handle returned by authorize") });
const toolProviderConnectionPathParams = toolProviderIdPathParams.extend({ connectionId: zod_v4.z.string().describe("Adapter-native connection id (e.g. Composio ca_...)") });
const listToolProviderToolsQuerySchema = zod_v4.z.object({
	toolkit: zod_v4.z.string().optional().describe("Filter tools by toolkit slug"),
	search: zod_v4.z.string().optional().describe("Search tools by name or description"),
	page: zod_v4.z.coerce.number().optional().describe("Page number for pagination (1-indexed)"),
	perPage: zod_v4.z.coerce.number().optional().describe("Number of items per page")
});
const listConnectionFieldsQuerySchema = zod_v4.z.object({ toolkit: zod_v4.z.string().describe("Toolkit slug whose connection field schema to list") });
const listConnectionsQuerySchema = zod_v4.z.object({
	toolkit: zod_v4.z.string().describe("Toolkit slug whose connections to list"),
	authorId: zod_v4.z.string().optional().describe("Admin-only: restrict listing to a specific author. Silently ignored for non-admin callers."),
	scope: connectionScopeSchema.optional().describe("Filter results by scope. Omit to include shared + per-author pins for the caller."),
	page: zod_v4.z.coerce.number().int().positive().optional().describe("Page number for pagination (1-indexed)"),
	perPage: zod_v4.z.coerce.number().int().positive().max(200).optional().describe("Number of items per page (default 50, max 200)")
});
const disconnectConnectionQuerySchema = zod_v4.z.object({
	force: zod_v4.z.union([zod_v4.z.boolean(), zod_v4.z.enum(["true", "false"])]).optional().describe("When true, revoke at the provider and drop the row even if pinned by agents"),
	toolkit: zod_v4.z.string().optional().describe("Toolkit slug for the connection (used when the row was upserted with one)")
});
const connectionUsageQuerySchema = zod_v4.z.object({ toolkit: zod_v4.z.string().optional().describe("Optional toolkit slug to scope the usage scan") });
const authorizeToolProviderBodySchema = zod_v4.z.object({
	toolkit: zod_v4.z.string().describe("Toolkit slug being authorized"),
	connectionId: zod_v4.z.string().optional().describe("Existing connection bucket id when re-authorizing; omit for a brand-new connection"),
	toolName: zod_v4.z.string().optional().describe("Optional tool slug for tool-scoped authorization"),
	config: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional().describe("Provider-specific user-supplied connection fields (e.g. subdomain)"),
	label: zod_v4.z.string().min(1, "Connection label is required").max(32, "Connection label must be ≤ 32 characters").regex(/^[A-Za-z0-9 _-]+$/, "Connection label may only contain letters, digits, spaces, _ and -").nullish().describe("Optional human label to persist on the resulting tool_provider_connections row. Must match the stored connection label rules (≤ 32 chars, [A-Za-z0-9 _-]+)."),
	scope: connectionScopeSchema.optional().describe("Identity bucket. \"shared\" pins under SHARED_BUCKET_ID. \"caller-supplied\" pins under the request-context resourceId (returns 400 when missing). Defaults to \"per-author\".")
});
const connectionStatusToolProviderBodySchema = zod_v4.z.object({ items: zod_v4.z.array(zod_v4.z.object({
	connectionId: zod_v4.z.string(),
	toolkit: zod_v4.z.string()
})).describe("Connection tuples to batch-check") });
const paginationSchema = zod_v4.z.object({
	total: zod_v4.z.number().optional(),
	page: zod_v4.z.number().optional(),
	perPage: zod_v4.z.number().optional(),
	hasMore: zod_v4.z.boolean()
}).optional();
const capabilitiesSchema = zod_v4.z.object({
	multipleConnectionsPerToolkit: zod_v4.z.boolean(),
	batchConnectionStatus: zod_v4.z.boolean(),
	reauthorizeReusesConnectionId: zod_v4.z.boolean(),
	supportsRevoke: zod_v4.z.boolean().optional()
});
const listToolProvidersResponseSchema = zod_v4.z.object({ providers: zod_v4.z.array(zod_v4.z.object({
	id: zod_v4.z.string(),
	name: zod_v4.z.string(),
	description: zod_v4.z.string().optional(),
	displayName: zod_v4.z.string().optional(),
	capabilities: capabilitiesSchema.optional()
})) });
const listToolProviderToolkitsResponseSchema = zod_v4.z.object({
	data: zod_v4.z.array(zod_v4.z.object({
		slug: zod_v4.z.string(),
		name: zod_v4.z.string(),
		description: zod_v4.z.string().optional(),
		icon: zod_v4.z.string().optional()
	})),
	pagination: paginationSchema
});
const listToolProviderToolsResponseSchema = zod_v4.z.object({
	data: zod_v4.z.array(zod_v4.z.object({
		slug: zod_v4.z.string(),
		name: zod_v4.z.string(),
		description: zod_v4.z.string().optional(),
		toolkit: zod_v4.z.string().optional()
	})),
	pagination: paginationSchema
});
const getToolProviderToolSchemaResponseSchema = zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown());
const authorizeToolProviderResponseSchema = zod_v4.z.object({
	url: zod_v4.z.string(),
	authId: zod_v4.z.string()
});
const authStatusToolProviderResponseSchema = zod_v4.z.object({ status: zod_v4.z.enum([
	"pending",
	"completed",
	"failed"
]) });
const connectionStatusToolProviderResponseSchema = zod_v4.z.object({ items: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.object({ connected: zod_v4.z.boolean() })) });
const listConnectionsResponseSchema = zod_v4.z.object({
	items: zod_v4.z.array(zod_v4.z.object({
		connectionId: zod_v4.z.string(),
		status: zod_v4.z.enum([
			"active",
			"pending",
			"failed",
			"inactive"
		]),
		createdAt: zod_v4.z.string().optional(),
		label: zod_v4.z.string().nullish().describe("Persisted display label from tool_provider_connections, if any"),
		authorId: zod_v4.z.string().optional().describe("Owner of the connection (when known)"),
		scope: connectionScopeSchema.optional().describe("Persisted scope from tool_provider_connections. Missing for rows that predate the scope field.")
	})),
	pagination: paginationSchema
});
const listConnectionFieldsResponseSchema = zod_v4.z.object({ fields: zod_v4.z.array(zod_v4.z.object({
	name: zod_v4.z.string(),
	displayName: zod_v4.z.string().optional(),
	description: zod_v4.z.string().optional(),
	type: zod_v4.z.enum([
		"string",
		"number",
		"boolean"
	]),
	required: zod_v4.z.boolean(),
	default: zod_v4.z.unknown().optional()
})) });
const disconnectConnectionResponseSchema = zod_v4.z.object({
	ok: zod_v4.z.literal(true),
	revoked: zod_v4.z.boolean().describe("Whether the provider-side connection was revoked")
});
const updateConnectionBodySchema = zod_v4.z.object({ label: zod_v4.z.union([
	labelSchema,
	zod_v4.z.literal(""),
	zod_v4.z.null()
]).describe("New display label for the connection. Pass null (or empty string) to clear the existing label.") });
const updateConnectionResponseSchema = zod_v4.z.object({
	ok: zod_v4.z.literal(true),
	label: zod_v4.z.string().nullable().describe("The persisted label after the update (null when cleared)")
});
const connectionUsageResponseSchema = zod_v4.z.object({ agents: zod_v4.z.array(zod_v4.z.object({
	id: zod_v4.z.string(),
	name: zod_v4.z.string()
})) });
const toolProviderHealthResponseSchema = zod_v4.z.object({
	ok: zod_v4.z.boolean(),
	message: zod_v4.z.string().optional(),
	details: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.unknown()).optional()
});
//#endregion
Object.defineProperty(exports, "authStatusToolProviderResponseSchema", {
	enumerable: true,
	get: function() {
		return authStatusToolProviderResponseSchema;
	}
});
Object.defineProperty(exports, "authorizeToolProviderBodySchema", {
	enumerable: true,
	get: function() {
		return authorizeToolProviderBodySchema;
	}
});
Object.defineProperty(exports, "authorizeToolProviderResponseSchema", {
	enumerable: true,
	get: function() {
		return authorizeToolProviderResponseSchema;
	}
});
Object.defineProperty(exports, "connectionSchema", {
	enumerable: true,
	get: function() {
		return connectionSchema;
	}
});
Object.defineProperty(exports, "connectionScopeSchema", {
	enumerable: true,
	get: function() {
		return connectionScopeSchema;
	}
});
Object.defineProperty(exports, "connectionStatusToolProviderBodySchema", {
	enumerable: true,
	get: function() {
		return connectionStatusToolProviderBodySchema;
	}
});
Object.defineProperty(exports, "connectionStatusToolProviderResponseSchema", {
	enumerable: true,
	get: function() {
		return connectionStatusToolProviderResponseSchema;
	}
});
Object.defineProperty(exports, "connectionUsageQuerySchema", {
	enumerable: true,
	get: function() {
		return connectionUsageQuerySchema;
	}
});
Object.defineProperty(exports, "connectionUsageResponseSchema", {
	enumerable: true,
	get: function() {
		return connectionUsageResponseSchema;
	}
});
Object.defineProperty(exports, "disconnectConnectionQuerySchema", {
	enumerable: true,
	get: function() {
		return disconnectConnectionQuerySchema;
	}
});
Object.defineProperty(exports, "disconnectConnectionResponseSchema", {
	enumerable: true,
	get: function() {
		return disconnectConnectionResponseSchema;
	}
});
Object.defineProperty(exports, "getToolProviderToolSchemaResponseSchema", {
	enumerable: true,
	get: function() {
		return getToolProviderToolSchemaResponseSchema;
	}
});
Object.defineProperty(exports, "listConnectionFieldsQuerySchema", {
	enumerable: true,
	get: function() {
		return listConnectionFieldsQuerySchema;
	}
});
Object.defineProperty(exports, "listConnectionFieldsResponseSchema", {
	enumerable: true,
	get: function() {
		return listConnectionFieldsResponseSchema;
	}
});
Object.defineProperty(exports, "listConnectionsQuerySchema", {
	enumerable: true,
	get: function() {
		return listConnectionsQuerySchema;
	}
});
Object.defineProperty(exports, "listConnectionsResponseSchema", {
	enumerable: true,
	get: function() {
		return listConnectionsResponseSchema;
	}
});
Object.defineProperty(exports, "listToolProviderToolkitsResponseSchema", {
	enumerable: true,
	get: function() {
		return listToolProviderToolkitsResponseSchema;
	}
});
Object.defineProperty(exports, "listToolProviderToolsQuerySchema", {
	enumerable: true,
	get: function() {
		return listToolProviderToolsQuerySchema;
	}
});
Object.defineProperty(exports, "listToolProviderToolsResponseSchema", {
	enumerable: true,
	get: function() {
		return listToolProviderToolsResponseSchema;
	}
});
Object.defineProperty(exports, "listToolProvidersResponseSchema", {
	enumerable: true,
	get: function() {
		return listToolProvidersResponseSchema;
	}
});
Object.defineProperty(exports, "toolProviderAuthStatusPathParams", {
	enumerable: true,
	get: function() {
		return toolProviderAuthStatusPathParams;
	}
});
Object.defineProperty(exports, "toolProviderConfigSchema", {
	enumerable: true,
	get: function() {
		return toolProviderConfigSchema;
	}
});
Object.defineProperty(exports, "toolProviderConnectionPathParams", {
	enumerable: true,
	get: function() {
		return toolProviderConnectionPathParams;
	}
});
Object.defineProperty(exports, "toolProviderHealthResponseSchema", {
	enumerable: true,
	get: function() {
		return toolProviderHealthResponseSchema;
	}
});
Object.defineProperty(exports, "toolProviderIdPathParams", {
	enumerable: true,
	get: function() {
		return toolProviderIdPathParams;
	}
});
Object.defineProperty(exports, "toolProvidersSchema", {
	enumerable: true,
	get: function() {
		return toolProvidersSchema;
	}
});
Object.defineProperty(exports, "toolSlugPathParams", {
	enumerable: true,
	get: function() {
		return toolSlugPathParams;
	}
});
Object.defineProperty(exports, "updateConnectionBodySchema", {
	enumerable: true,
	get: function() {
		return updateConnectionBodySchema;
	}
});
Object.defineProperty(exports, "updateConnectionResponseSchema", {
	enumerable: true,
	get: function() {
		return updateConnectionResponseSchema;
	}
});

//# sourceMappingURL=tool-providers-DFQkhO4R.cjs.map