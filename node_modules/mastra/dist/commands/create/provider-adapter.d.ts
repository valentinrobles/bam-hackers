import type { PackageManager } from '../../utils/package-manager.js';
import type { CreateLLMProvider } from './command.js';
export interface ManagedProviderConfig {
    displayName: string;
    primaryModel?: string;
    observationalModel?: string;
    apiKeyEnv: string;
}
export declare const MANAGED_PROVIDER_CONFIGS: Record<CreateLLMProvider, ManagedProviderConfig>;
interface ManagedTemplateAdaptationResult extends ManagedProviderConfig {
    apiKeyWritten: boolean;
    adaptationFailed: boolean;
}
export declare function adaptDefaultTemplate({ projectPath, projectName, packageManager, provider, apiKey, versionTag, }: {
    projectPath: string;
    projectName: string;
    packageManager: PackageManager;
    provider: CreateLLMProvider;
    apiKey?: string;
    versionTag: string;
}): Promise<ManagedTemplateAdaptationResult>;
export {};
//# sourceMappingURL=provider-adapter.d.ts.map