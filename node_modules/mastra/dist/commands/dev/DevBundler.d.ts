import { createWatcher } from '@mastra/deployer/build';
import type { PrepareFsAgentsEntryResult } from '@mastra/deployer/build';
import { Bundler } from '@mastra/deployer/bundler';
interface FsRoutingWatchOptions {
    mastraDir: string;
    userEntryFile: string | undefined;
    outputDirectory: string;
    preparedEntry: PrepareFsAgentsEntryResult;
}
export declare class DevBundler extends Bundler {
    private customEnvFile?;
    private factory;
    constructor(customEnvFile?: string, factory?: boolean);
    getEnvFiles(): Promise<string[]>;
    prepare(outputDirectory: string): Promise<void>;
    watch(entryFile: string, outputDirectory: string, toolsPaths: (string | string[])[], fsRoutingWatchOptions?: FsRoutingWatchOptions): ReturnType<typeof createWatcher>;
    bundle(): Promise<void>;
}
export {};
//# sourceMappingURL=DevBundler.d.ts.map