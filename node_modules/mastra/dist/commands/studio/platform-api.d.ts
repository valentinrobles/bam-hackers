import type { LogCollector } from '../../utils/deploy-log-format.js';
import type { DeployDiagnosisLookup } from '../deploy-suggestions.js';
export interface Project {
    id: string;
    name: string;
    slug: string | null;
    organizationId: string;
    /** Present on the studio list endpoint; set at creation and never changed by deploys on the unified path. */
    factoryEnabled?: boolean;
    latestDeployId: string | null;
    latestDeployStatus: string | null;
    latestDeployCreatedAt?: string | null;
    instanceUrl: string | null;
    createdAt: string | null;
    updatedAt: string | null;
}
export interface DeployStatus {
    id: string;
    status: string;
    instanceUrl: string | null;
    error: string | null;
}
export declare function fetchProjects(token: string, orgId: string): Promise<Project[]>;
export declare function createProject(token: string, orgId: string, name: string): Promise<Project>;
export interface DeployInfo {
    id: string;
    status: string;
    instanceUrl: string | null;
    error: string | null;
    projectName?: string | null;
    createdAt?: string | null;
}
export declare function fetchDeployStatus(deployId: string, token: string, orgId?: string): Promise<DeployInfo>;
export declare function fetchDeployDiagnosis(deployId: string, token: string, orgId?: string): Promise<DeployDiagnosisLookup>;
export declare function startDeployDiagnosis(deployId: string, token: string, orgId?: string): Promise<void>;
export declare function uploadDeploy(token: string, orgId: string, projectId: string, zipBuffer: Buffer, meta?: {
    gitBranch?: string;
    projectName?: string;
    envVars?: Record<string, string>;
    mastraVersion?: string;
    disablePlatformObservability?: boolean;
}): Promise<{
    id: string;
    status: string;
}>;
export interface PollDeployOptions {
    /** Print every log line instead of the rolling tail shown on a TTY. */
    showAllLogs?: boolean;
    /** Receives every raw log entry, so a failure excerpt can be printed later. */
    collectLogs?: LogCollector;
}
export declare function pollDeploy(deployId: string, token: string, orgId: string, maxWaitMs?: number, options?: PollDeployOptions): Promise<DeployStatus>;
//# sourceMappingURL=platform-api.d.ts.map