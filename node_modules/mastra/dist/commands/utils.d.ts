import type { PackageManager } from '../utils/package-manager.js';
export declare function getPackageManager(): PackageManager;
/**
 * Wrap an async commander action so failures print a clean error message
 * (never a stack trace) and exit non-zero.
 */
export declare function wrapAction(fn: (...args: any[]) => Promise<void>): (...args: any[]) => void;
export declare function parseMcp(value: string): "antigravity" | "cursor" | "cursor-global" | "vscode" | "windsurf";
export declare function parseSkills(value: string): string[];
export declare function parseComponents(value: string): ("agents" | "scorers" | "tools" | "workflows")[];
export declare function parseLlmProvider(value: string): "anthropic" | "cerebras" | "google" | "groq" | "mistral" | "openai";
export declare function shouldSkipDotenvLoading(): boolean;
/**
 * Get the version tag (e.g., 'beta', 'latest') for the currently running mastra CLI.
 * Create passes its known version to avoid resolving package metadata from an installed layout.
 * Init omits it and preserves the existing best-effort undefined fallback.
 */
export declare function getVersionTag(version?: string): Promise<string | undefined>;
/**
 * Check if the current directory already has git initialized.
 */
export declare function isGitInitialized({ cwd }: {
    cwd: string;
}): Promise<boolean>;
/**
 * Initialize a git repository in the specified directory.
 */
export declare function gitInit({ cwd }: {
    cwd: string;
}): Promise<void>;
//# sourceMappingURL=utils.d.ts.map