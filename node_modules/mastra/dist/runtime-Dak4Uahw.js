import { createHash, randomUUID } from "crypto";
import { once } from "events";
//#region src/commands/experiment/runtime.ts
const EXPERIMENT_WORKER_PROTOCOL_VERSION = "1";
const EXPERIMENT_DATASET_CANONICALIZATION_VERSION = "1";
const EXPERIMENT_WORKER_MAX_FRAME_BYTES = 1024 * 1024;
const EXPERIMENT_WORKER_MAX_PENDING_OUTPUT_BYTES = 4 * EXPERIMENT_WORKER_MAX_FRAME_BYTES;
const MAX_TIMER_DELAY_MS = 2147483647;
const EXPERIMENT_WORKER_EXIT_CODES = {
	completed: 0,
	completedWithErrors: 10,
	fatal: 20,
	retryable: 21,
	cancelled: 30,
	timedOut: 31,
	protocol: 70
};
var ProtocolOutputError = class extends Error {
	frameAccepted;
	constructor(message, frameAccepted) {
		super(message);
		this.frameAccepted = frameAccepted;
	}
};
function isRecord(value) {
	return value !== null && typeof value === "object" && !Array.isArray(value);
}
function isStringArray(value) {
	return Array.isArray(value) && value.every((item) => typeof item === "string");
}
function isPositiveInteger(value) {
	return Number.isInteger(value) && Number(value) > 0;
}
function canonicalize(value) {
	if (value === null || typeof value !== "object") return JSON.stringify(value);
	if (Array.isArray(value)) return `[${value.map(canonicalize).join(",")}]`;
	return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonicalize(value[key])}`).join(",")}}`;
}
function datasetDigest(items) {
	return createHash("sha256").update(canonicalize(items)).digest("hex");
}
function isValidFinishedEvent(event) {
	return event.type === "experiment.run.finished" && [
		"completed",
		"failed",
		"cancelled"
	].includes(event.outcome ?? "") && typeof event.completedWithErrors === "boolean";
}
function validateRunRequest(value, build) {
	if (!isRecord(value) || value.type !== "run") return "expected run request";
	const request = value;
	if (request.protocolVersion !== build.protocolVersion || request.packet?.protocolVersion !== build.protocolVersion || !isStringArray(request.supportedProtocolVersions) || !request.supportedProtocolVersions.includes(build.protocolVersion)) return "unsupported protocol version";
	if (typeof request.experimentId !== "string" || request.experimentId !== request.packet?.experimentId || typeof request.jobId !== "string" || !isPositiveInteger(request.attempt) || typeof request.idempotencyKey !== "string") return "invalid correlation";
	if (typeof request.deadlineAt !== "string" || !Number.isFinite(Date.parse(request.deadlineAt))) return "invalid deadline";
	const packet = request.packet;
	const dataset = packet?.dataset;
	if (!isRecord(packet) || !isRecord(dataset) || !Array.isArray(dataset.items) || !dataset.items.every((item) => isRecord(item) && typeof item.id === "string" && "input" in item && (item.metadata === void 0 || isRecord(item.metadata)) && (item.requestContext === void 0 || isRecord(item.requestContext)) && (item.toolMocks === void 0 || Array.isArray(item.toolMocks) && item.toolMocks.every((mock) => isRecord(mock) && typeof mock.toolId === "string" && isRecord(mock.args) && "output" in mock && (mock.matchArgs === void 0 || mock.matchArgs === "strict" || mock.matchArgs === "ignore")))) || dataset.itemCount !== dataset.items.length) return "invalid dataset";
	if (dataset.canonicalizationVersion !== build.datasetCanonicalizationVersion || request.datasetAttestation?.canonicalizationVersion !== build.datasetCanonicalizationVersion) return "unsupported dataset canonicalization version";
	const digest = datasetDigest(dataset.items);
	if (dataset.digest !== digest || request.datasetAttestation?.digest !== digest || request.datasetAttestation?.itemCount !== dataset.items.length) return "dataset attestation mismatch";
	if (!isRecord(packet.artifacts) || packet.artifacts.buildId !== build.buildId) return "worker build identity mismatch";
	if (!isRecord(packet.tenant) || !isRecord(packet.environment)) return "invalid artifact provenance";
	if (!isRecord(packet.target) || !["agent", "workflow"].includes(packet.target.type) || typeof packet.target.id !== "string") return "invalid target";
	if (!Array.isArray(packet.scorers) || !packet.scorers.every((scorer) => isRecord(scorer) && typeof scorer.id === "string" && typeof scorer.version === "string") || !isPositiveInteger(packet.limits?.concurrency) || !isPositiveInteger(packet.limits?.timeoutMs)) return "invalid experiment configuration";
	if (!isStringArray(packet.policies?.allowedToolIds) || !isStringArray(packet.policies?.allowedNetworkHosts) || !Array.isArray(packet.secretReferences)) return "invalid execution policy";
	if (packet.policies.allowedNetworkHosts.length > 0 || packet.secretReferences.length > 0) return "unsupported network or secret policy";
	const mockedToolIds = new Set(dataset.items.flatMap((item) => Array.isArray(item.toolMocks) ? item.toolMocks.map((mock) => mock.toolId) : []));
	const allowedToolIds = new Set(packet.policies.allowedToolIds);
	if (packet.target.type !== "agent" && (mockedToolIds.size > 0 || allowedToolIds.size > 0)) return "tool policies are supported only for agent targets";
	if (packet.policies.allowedToolIds.some((toolId) => !mockedToolIds.has(toolId)) || [...mockedToolIds].some((toolId) => !allowedToolIds.has(toolId))) return "allowed tools and deterministic mocks must match";
}
function validateCancelRequest(value, correlation) {
	if (!isRecord(value)) return false;
	return value.type === "cancel" && value.protocolVersion === "1" && value.experimentId === correlation.experimentId && value.jobId === correlation.jobId && value.attempt === correlation.attempt && value.idempotencyKey === correlation.idempotencyKey && typeof value.requestedAt === "string" && Number.isFinite(Date.parse(value.requestedAt)) && typeof value.reason === "string";
}
async function runExperimentWorker({ mastra, runExperiment, build, stdin = process.stdin, stdout = process.stdout, stderr = process.stderr, workerId = randomUUID(), createEventId = randomUUID, now = () => /* @__PURE__ */ new Date() }) {
	let correlation;
	let controller;
	let runPromise;
	let completion;
	let activeRequest;
	let protocolFailure;
	let terminal = false;
	let finishing = false;
	let sequence = 0;
	let heartbeat;
	let clearDeadlineTimer;
	let deadlineAt = Number.POSITIVE_INFINITY;
	let writeTail = Promise.resolve();
	let pendingOutputBytes = 0;
	let heartbeatQueued = false;
	const report = (message) => stderr.write(`[mastra experiment worker] ${message}\n`);
	const scheduleAtDeadline = (callback) => {
		let timer;
		let cleared = false;
		let fired = false;
		const schedule = () => {
			if (cleared || fired) return;
			const remaining = deadlineAt - Date.now();
			timer = setTimeout(remaining <= 0 ? () => {
				if (cleared || fired) return;
				fired = true;
				callback();
			} : schedule, remaining <= 0 ? 0 : Math.min(remaining, MAX_TIMER_DELAY_MS));
		};
		schedule();
		return () => {
			cleared = true;
			clearTimeout(timer);
		};
	};
	const abortForProtocolFailure = (message) => {
		if (protocolFailure || terminal) return;
		protocolFailure = new Error(message);
		report(message);
		controller?.abort(protocolFailure);
	};
	const waitForDrain = async () => {
		const drainController = new AbortController();
		let deadlineExceeded = false;
		const clearTimer = scheduleAtDeadline(() => {
			deadlineExceeded = true;
			drainController.abort();
		});
		try {
			await once(stdout, "drain", { signal: drainController.signal });
		} catch (error) {
			if (deadlineExceeded) throw new Error("stdout backpressure exceeded the experiment deadline");
			throw error;
		} finally {
			clearTimer();
			drainController.abort();
		}
	};
	const writeEvent = (type, payload) => {
		if (!correlation || terminal || finishing && type !== "terminal" || type === "heartbeat" && heartbeatQueued) return writeTail;
		const event = {
			...correlation,
			eventId: createEventId(),
			sequence,
			emittedAt: now().toISOString(),
			type,
			payload
		};
		const frame = `${JSON.stringify(event)}\n`;
		const frameBytes = Buffer.byteLength(frame);
		if (frameBytes > 1048576) return Promise.reject(/* @__PURE__ */ new Error("output frame exceeds maximum size"));
		if (pendingOutputBytes + frameBytes > 4194304) return Promise.reject(/* @__PURE__ */ new Error("pending protocol output exceeds maximum size"));
		sequence += 1;
		pendingOutputBytes += frameBytes;
		if (type === "heartbeat") heartbeatQueued = true;
		const queuedWrite = writeTail.then(async () => {
			try {
				if (!stdout.write(frame)) try {
					await waitForDrain();
				} catch (error) {
					throw new ProtocolOutputError(error instanceof Error ? error.message : String(error), true);
				}
			} finally {
				pendingOutputBytes -= frameBytes;
				if (type === "heartbeat") heartbeatQueued = false;
			}
		});
		writeTail = queuedWrite.catch((error) => {
			if (type === "heartbeat") abortForProtocolFailure(error instanceof Error ? error.message : String(error));
		});
		return queuedWrite;
	};
	const finish = async (status, semanticEvent, exitCode, retryable = false) => {
		if (terminal || finishing) return;
		finishing = true;
		clearInterval(heartbeat);
		try {
			await writeTail;
			if (protocolFailure) throw protocolFailure;
			await writeEvent("terminal", {
				status,
				...status === "failed" ? { retryable } : {},
				semanticEvent
			});
			terminal = true;
			return exitCode;
		} finally {
			clearInterval(heartbeat);
			clearDeadlineTimer?.();
		}
	};
	const emitCompletion = async (value) => {
		try {
			return await finish(value.status, value.semanticEvent, value.exitCode, value.retryable) ?? value.exitCode;
		} catch (error) {
			clearInterval(heartbeat);
			clearDeadlineTimer?.();
			const failure = error instanceof Error ? error : new Error(String(error));
			report(`terminal protocol output failed: ${failure.message}`);
			if (failure instanceof ProtocolOutputError && failure.frameAccepted) {
				terminal = true;
				return value.exitCode;
			}
			try {
				await writeEvent("terminal", {
					status: "failed",
					retryable: false,
					semanticEvent: {
						type: "experiment.run.finished",
						version: 1,
						experimentId: correlation?.experimentId ?? value.semanticEvent.experimentId,
						sequence: 0,
						timestamp: now().toISOString(),
						target: value.semanticEvent.target,
						outcome: "failed",
						completedWithErrors: false,
						error: {
							name: failure.name,
							message: failure.message
						}
					}
				});
			} catch (fallbackError) {
				report(`fallback terminal protocol output failed: ${fallbackError instanceof Error ? fallbackError.message : String(fallbackError)}`);
			}
			terminal = true;
			return EXPERIMENT_WORKER_EXIT_CODES.protocol;
		}
	};
	const failedSemanticEvent = (request, error, outcome = "failed") => ({
		type: "experiment.run.finished",
		version: 1,
		experimentId: request.experimentId,
		sequence: 0,
		timestamp: now().toISOString(),
		target: request.packet.target,
		outcome,
		completedWithErrors: false,
		error: {
			name: error.name,
			message: error.message
		}
	});
	const executeRun = async (request) => {
		const packet = request.packet;
		let finishedEvent;
		let runError;
		let runErrorRetryable = false;
		await writeEvent("accepted", {
			workerId,
			negotiatedProtocolVersion: request.protocolVersion
		});
		heartbeat = setInterval(() => {
			writeEvent("heartbeat", {}).catch((error) => abortForProtocolFailure(String(error)));
		}, 5e3);
		let rejectDeadline;
		const deadlinePromise = new Promise((_, reject) => {
			rejectDeadline = reject;
		});
		clearDeadlineTimer = scheduleAtDeadline(() => {
			const error = /* @__PURE__ */ new Error("Experiment deadline exceeded");
			controller?.abort(error);
			rejectDeadline?.(error);
		});
		try {
			await Promise.race([runExperiment(mastra, {
				data: packet.dataset.items.map((item) => ({
					...item,
					metadata: {
						...item.metadata,
						...item.expectedTrajectory !== void 0 ? { expectedTrajectory: item.expectedTrajectory } : {},
						...item.source !== void 0 ? { source: item.source } : {}
					},
					...item.toolMocks ? { toolMocks: item.toolMocks.map((mock) => ({
						toolName: mock.toolId,
						args: mock.args,
						output: mock.output,
						...mock.matchArgs ? { matchArgs: mock.matchArgs } : {}
					})) } : {}
				})),
				targetType: packet.target.type,
				targetId: packet.target.id,
				scorers: packet.scorers.map((scorer) => scorer.id),
				maxConcurrency: packet.limits.concurrency,
				itemTimeout: packet.limits.timeoutMs,
				signal: controller.signal,
				unmockedToolPolicy: "deny",
				requestContext: packet.requestContext,
				metadata: {
					...packet.metadata,
					tenant: packet.tenant,
					environment: packet.environment,
					artifacts: packet.artifacts,
					scorerVersions: Object.fromEntries(packet.scorers.map((scorer) => [scorer.id, scorer.version])),
					policies: packet.policies,
					requestedAt: packet.requestedAt
				},
				persistence: {
					experiments: "none",
					scores: "none"
				},
				experimentId: request.experimentId,
				onEvent: async (event) => {
					if (event.type === "experiment.run.started") await writeEvent("run-started", { semanticEvent: event });
					else if (event.type === "experiment.item.completed") await writeEvent("item-completed", {
						itemId: event.itemId,
						itemIndex: event.itemIndex,
						semanticEvent: event
					});
					else if (event.type === "experiment.run.finished") {
						if (finishedEvent) throw new Error("Experiment emitted multiple terminal semantic events");
						finishedEvent = event;
					}
				}
			}), deadlinePromise]);
		} catch (error) {
			runErrorRetryable = isRecord(error) && error.retryable === true;
			runError = error instanceof Error ? error : new Error(String(error));
		} finally {
			clearDeadlineTimer?.();
			clearDeadlineTimer = void 0;
		}
		try {
			let clearTimer;
			await Promise.race([mastra.shutdown(), new Promise((_, reject) => {
				clearTimer = scheduleAtDeadline(() => reject(/* @__PURE__ */ new Error("Mastra shutdown exceeded the experiment deadline")));
			})]).finally(() => clearTimer?.());
		} catch (error) {
			runError = error instanceof Error ? error : new Error(String(error));
		}
		if (protocolFailure) {
			await writeEvent("process-failure", { error: {
				name: protocolFailure.name,
				message: protocolFailure.message
			} });
			return {
				status: "failed",
				semanticEvent: failedSemanticEvent(request, protocolFailure),
				exitCode: EXPERIMENT_WORKER_EXIT_CODES.protocol,
				retryable: false
			};
		}
		if (runError || !finishedEvent || !isValidFinishedEvent(finishedEvent)) {
			const error = runError ?? /* @__PURE__ */ new Error(finishedEvent ? "Experiment emitted an invalid terminal semantic event" : "Experiment completed without a terminal semantic event");
			const timedOut = Date.now() >= deadlineAt;
			const cancelled = controller?.signal.aborted && !timedOut;
			return {
				status: timedOut ? "timed-out" : cancelled ? "cancelled" : "failed",
				semanticEvent: failedSemanticEvent(request, error, cancelled ? "cancelled" : "failed"),
				exitCode: timedOut ? EXPERIMENT_WORKER_EXIT_CODES.timedOut : cancelled ? EXPERIMENT_WORKER_EXIT_CODES.cancelled : runErrorRetryable ? EXPERIMENT_WORKER_EXIT_CODES.retryable : EXPERIMENT_WORKER_EXIT_CODES.fatal,
				retryable: !timedOut && !cancelled && runErrorRetryable
			};
		}
		const status = finishedEvent.outcome === "cancelled" ? "cancelled" : finishedEvent.completedWithErrors ? "completed-with-errors" : finishedEvent.outcome === "failed" ? "failed" : "completed";
		return {
			status,
			semanticEvent: finishedEvent,
			exitCode: status === "cancelled" ? EXPERIMENT_WORKER_EXIT_CODES.cancelled : status === "completed-with-errors" ? EXPERIMENT_WORKER_EXIT_CODES.completedWithErrors : status === "failed" ? EXPERIMENT_WORKER_EXIT_CODES.fatal : EXPERIMENT_WORKER_EXIT_CODES.completed
		};
	};
	const handleFrame = async (frame) => {
		let text;
		try {
			text = new TextDecoder("utf-8", { fatal: true }).decode(frame);
		} catch {
			abortForProtocolFailure("frame is not valid UTF-8");
			return;
		}
		let value;
		try {
			value = JSON.parse(text);
		} catch {
			abortForProtocolFailure("malformed JSON frame");
			return;
		}
		if (!correlation) {
			const validationError = validateRunRequest(value, build);
			if (validationError) {
				abortForProtocolFailure(validationError);
				return;
			}
			const request = value;
			correlation = {
				protocolVersion: request.protocolVersion,
				experimentId: request.experimentId,
				jobId: request.jobId,
				attempt: request.attempt,
				idempotencyKey: request.idempotencyKey
			};
			controller = new AbortController();
			activeRequest = request;
			deadlineAt = Date.parse(request.deadlineAt);
			runPromise = executeRun(request).then((result) => {
				completion = result;
			}).catch((error) => {
				const failure = error instanceof Error ? error : new Error(String(error));
				report(`protocol output failed: ${failure.message}`);
				completion = {
					status: "failed",
					semanticEvent: failedSemanticEvent(request, failure),
					exitCode: EXPERIMENT_WORKER_EXIT_CODES.protocol,
					retryable: false
				};
			});
			return;
		}
		if (!validateCancelRequest(value, correlation)) {
			abortForProtocolFailure("unexpected or invalid request");
			return;
		}
		controller?.abort(new Error(value.reason));
	};
	let pending = Buffer.alloc(0);
	const iterator = stdin[Symbol.asyncIterator]();
	while (!terminal && !protocolFailure) {
		let nextChunk;
		let result;
		try {
			nextChunk = iterator.next();
			result = runPromise ? await Promise.race([nextChunk.then((input) => ({
				type: "input",
				input
			})), runPromise.then(() => ({ type: "run-complete" }))]) : {
				type: "input",
				input: await nextChunk
			};
		} catch (error) {
			abortForProtocolFailure(`stdin read failed: ${error instanceof Error ? error.message : String(error)}`);
			break;
		}
		if (result.type === "run-complete") {
			nextChunk.catch(() => void 0);
			stdin.destroy?.();
			if (pending.byteLength > 0 && activeRequest) {
				abortForProtocolFailure("truncated frame: final newline is required");
				const error = protocolFailure ?? /* @__PURE__ */ new Error("truncated frame: final newline is required");
				await writeEvent("process-failure", { error: {
					name: error.name,
					message: error.message
				} });
				completion = {
					status: "failed",
					semanticEvent: failedSemanticEvent(activeRequest, error),
					exitCode: EXPERIMENT_WORKER_EXIT_CODES.protocol,
					retryable: false
				};
			}
			if (!completion) return EXPERIMENT_WORKER_EXIT_CODES.fatal;
			return emitCompletion(completion);
		}
		if (result.input.done) break;
		const chunk = result.input.value;
		pending = Buffer.concat([pending, Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk)]);
		while (!protocolFailure) {
			const newline = pending.indexOf(10);
			if (newline === -1) break;
			if (newline > 1048576) abortForProtocolFailure("frame exceeds maximum size");
			else await handleFrame(pending.subarray(0, newline));
			pending = pending.subarray(newline + 1);
		}
		if (pending.byteLength > 1048576) abortForProtocolFailure("frame exceeds maximum size");
	}
	const hasTruncatedFrame = pending.byteLength > 0;
	if (!protocolFailure && hasTruncatedFrame) abortForProtocolFailure("truncated frame: final newline is required");
	if (!correlation) {
		report(protocolFailure?.message ?? "stdin closed before run request");
		return EXPERIMENT_WORKER_EXIT_CODES.protocol;
	}
	await runPromise;
	if (hasTruncatedFrame && activeRequest) {
		const error = protocolFailure ?? /* @__PURE__ */ new Error("truncated frame: final newline is required");
		await writeEvent("process-failure", { error: {
			name: error.name,
			message: error.message
		} });
		completion = {
			status: "failed",
			semanticEvent: failedSemanticEvent(activeRequest, error),
			exitCode: EXPERIMENT_WORKER_EXIT_CODES.protocol,
			retryable: false
		};
	}
	if (!completion) return EXPERIMENT_WORKER_EXIT_CODES.fatal;
	return emitCompletion(completion);
}
//#endregion
export { EXPERIMENT_WORKER_PROTOCOL_VERSION as a, EXPERIMENT_WORKER_MAX_PENDING_OUTPUT_BYTES as i, EXPERIMENT_WORKER_EXIT_CODES as n, runExperimentWorker as o, EXPERIMENT_WORKER_MAX_FRAME_BYTES as r, EXPERIMENT_DATASET_CANONICALIZATION_VERSION as t };

//# sourceMappingURL=runtime-Dak4Uahw.js.map